# Dolphin — Client 1 Step-by-Step Roadmap

`DOLPHIN_PROJECT_HANDOFF.md` تنها منبع وضعیت زنده و شواهد است؛ این فایل فقط نقشه فازبندی‌شده و پیش‌نیاز/gate هر فاز را نگه می‌دارد. فقط یک فاز در هر زمان اجرا شود مگر صریحا «موازی‌پذیر» علامت خورده باشد؛ فاز غیرموازی بعدی تا سبز شدن gate فاز قبل شروع نشود. نسخه ۲۰۲۶/۰۸/۱۴ (ممیزی P0) ساختار فازبندی قدیمی (`V0-V6`، `C1-0..C1-9`، `R0-R16`) را با یک ترتیب P0-P15 واحد جایگزین کرد. **این نسخه (فاز اصلاحی `P0R`، همان تاریخ) آن ترتیب را در چند نقطه اصلاح می‌کند**: کشف زیرساخت و PostgreSQL را زودتر می‌آورد، طراحی deployment-profile را از پیاده‌سازی زودهنگام جدا می‌کند، وابستگی‌های مالی اجباری فرضی را با gate تصمیم واقعی جایگزین می‌کند، سخت‌سازی build-context را زودتر از بسته‌بندی نهایی release می‌آورد، و پرسونای UAT را دقیق می‌کند. سابقه فازهای قدیمی‌تر در `git log` این فایل قابل بازیابی است.

## خلاصه وضعیت ورودی این نسخه

کار قبلی (قابل تایید در کد، نه فقط ادعا) هسته زیر را واقعا ساخته و به backend واقعی وصل کرده است: احراز هویت/نقش ثابت، User، Customer/Phone، Lead/Assignment/Interaction، ProductCategory/Product، Sale، SalesDocument/Postal داخلی، After-Sales (workstream ایزوله)، InboundSMS داخلی (بدون آداپتور زنده)، گزارش عملکرد کاربر با XLSX، ActivityLog — این «رده A» است، جزئیات در `DOLPHIN_PROJECT_HANDOFF.md` بخش ۳/۴/۵. هیچ ماژول مالی/موجودی/سند حسابداری/فایل/PDF/یکپارچگی بیرونی وجود ندارد (تایید شد، همان‌جا) — این «رده B» است. رده C فهرست backlog کم‌اولویت/کاندید است که تا تصمیم مستقیم تازه هدف تحویل Client 1 محسوب نمی‌شود. یک شکاف پیاده‌سازی شناخته‌شده روی «مدیریت کاربر» باز است (بخش P1.7).

## نقشه فاز

| فاز | هدف | ممکن است رفتار تولید را تغییر دهد؟ | موازی‌پذیر با |
|---|---|---:|---|
| P0 | ممیزی کامل مخزن و تطبیق حقیقت مستندات | خیر | — |
| P0R | اصلاح مستندات پیش از پیاده‌سازی (همین فاز) | خیر | — |
| P0R.1 | survey زودهنگام زیرساخت/سرور/شبکه هدف | خیر (survey است) | P1 |
| P0R.2 | ✅ راه‌اندازی PostgreSQL محلی/staging زودهنگام (انجام‌شده) | فقط رانتایم محلی | P1 |
| P0R.3 | مقایسه و انتخاب طراحی deployment-profile | خیر (فقط طراحی) | P1 |
| P0R.4 | سخت‌سازی زودهنگام build-context ایمیج | بله، محدود به build | P1 |
| P1 | بستن تصمیم‌های کسب‌وکار/دامنه باز | خیر | P0R.1–P0R.4 |
| P1.7 | ✅ هم‌راستاسازی Authorization مدیریت کاربر با سیاست Client 1 (انجام‌شده) | بله، محدود | — |
| P2 | ✅ اتصال UI فعال و سخت‌سازی کنترل مرده (انجام‌شده) | بله، محدود | P1 |
| P3 | ✅ پیاده‌سازی deployment profile (Option C، انجام‌شده) | بله | — |
| P4 | ✅ موجودی و حرکت انبار (انجام‌شده) | بله | — |
| P5 | ✅ Order و Quotation (انجام‌شده) | بله | — |
| P6 | ✅ Invoice، مالیات پیکربندی‌پذیر، تخفیف، شماره‌گذاری (انجام‌شده) | بله | — |
| P7 | ✅ Payment، چک، قسط، دفتر حساب مشتری (انجام‌شده) | بله | — |
| P8 | ✅ مطالبات، سود ناخالص، ارزش موجودی (انجام‌شده) | بله | — |
| P9 | ✅ چاپ و PDF سمت سرور برای فاکتور و پیش‌فاکتور (انجام‌شده) | بله | — |
| P10 | فایل/سند عملیاتی امن | بله | — |
| P11 | یکپارچگی‌های بیرونی تاییدشده | بله/بیرونی | — |
| P12 | بسته‌بندی نهایی release: امضا، SBOM، signing، reproducible build | بله | — |
| P13 | proof رانتایم PostgreSQL/Compose/Nginx شبه‌تولید کامل | فقط رانتایم | — |
| P14 | اجرای واقعی شبکه/TLS/VPN سایت (بر پایه واقعیت‌های P0R.1) | بیرونی | — |
| P15 | استقرار هدف، UAT، آموزش، rollback، sign-off | بیرونی/تولید | — |

### اصلاح ترتیب وابستگی مالی (P0R.7.4)

**اصلاح:** نسخه قبلی این نقشه ادعا می‌کرد Order/Quotation «بدون موجودی معتبر معنی مالی ندارد»، Invoice «بدون Order/Quotation منبع ندارد»، Payment «بدون Invoice تخصیص ندارد»، و سود/زیان «بدون بهای تمام‌شده و بدون Invoice فرمول ندارد» — این‌ها ادعای وابستگی اجباری بودند که هیچ تصمیم مستقیمی پشتشان نبود. این جمله‌ها حذف شدند. ترتیب فازهای P4→P8 فقط یک **ترتیب پیش‌فرض معقول** برای کاهش ریسک است، نه یک وابستگی داده‌ای اثبات‌شده در کد؛ تغییر آن فقط با gate تصمیم مصوب مجاز است، نه با استدلال «چون منطقی به‌نظر می‌رسد».

چرخه‌های ممکن — این‌ها **فقط نمونه‌اند، نه تصویب‌شده**، و باید در P1 برای هرکدام تصمیم گرفته شود:

```text
Lead → Sale → Invoice
Lead → Quotation → Order → Invoice
Customer → Invoice مستقیم
Payment روی حساب → تخصیص بعدی
Invoice → Payment تخصیص‌یافته
```

`BACKEND_SPEC.md` §2.6 صراحتا منع می‌کند که منبع Invoice، تبدیل اجباری Order→Invoice یا Quotation→Order، رزرو اجباری Inventory، تخصیص اجباری Payment→Invoice، مبنای حسابداری سود/زیان، قواعد مالیات، یا قرارداد بدهکار/بستانکار به‌خاطر مرسوم‌بودن فنی آن‌ها به‌طور خودکار تایید تلقی شوند.

---

## وضعیت واقعی در برابر نقشه، و گام‌های بعدی — ۲۰۲۶/۰۹/۰۱ (نسخهٔ `1.6.12`)

این بخش نقشهٔ بالا را با آنچه واقعاً اتفاق افتاده آشتی می‌دهد. جزئیات کامل هر
مورد در `DOLPHIN_PROJECT_HANDOFF.md` است؛ این‌جا فقط جمع‌بندی و اثر آن روی
ترتیب فازهاست.

### چه چیزی از زمان آخرین آپدیت این نقشه واقعاً ساخته و تحویل شده

- سامانهٔ مجوز در سطح کاربر (فازهای ۱.۶.۳ تا ۱.۶.۵): نقش پیش‌فرض می‌دهد، مدیر
  پلتفرم می‌تواند مجوز **یک کاربر مشخص** را جدا تنظیم کند، و این واقعاً در
  بک‌اند اجرا می‌شود — نه فقط یک flag تزئینی در دیتابیس.
- رفع کامل نام‌گذاری داخلی به Dolphin (فازهای ۱.۶.۶ تا ۱.۶.۱۱): لوگوها، همهٔ
  شناسه‌های داخلی (`Kariz`/`ForooshBin` → `Dolphin` به‌جز چند متغیر محیطی
  استقرار که عمداً دست‌نخورده ماندند)، و یکپارچه‌سازی مستندات پراکنده (این
  فایل شامل نیست).
- **یک استقرار واقعی مشتری اتفاق افتاد: Tiara، نسخهٔ `1.6.9`.** این خارج از
  ترتیب رسمی این نقشه بود — پایین را ببینید.

### ناهم‌خوانی مهم: استقرار واقعی جلوتر از گیت‌های P12–P14 رخ داد

طبق این نقشه، `P15` (استقرار هدف) صراحتاً نیازمند `P12` (بسته‌بندی امضاشده،
SBOM)، `P13` (proof کامل رانتایم شبه‌تولید) و `P14` (اجرای واقعی شبکه/TLS بر
پایهٔ survey `P0R.1`) — **هر سه سبز** — است. هیچ‌کدام از این سه به‌صورت رسمی
تکمیل نشده بود، و در عمل هیچ‌کدام از این نقشه هم پیروی نکرد: مالک محصول
مستقیماً یک استقرار Docker Compose واقعی روی سرور Tiara راه انداخت (بدون
survey مکتوب `P0R.1`، بدون SBOM/scan مستند `P12`، بدون proof جداگانهٔ staging
طبق `P13`). این یک شکست فرایند نیست که باید مخفی شود — یک واقعیت است که باید
ثبت شود تا برنامه‌ریزی بعدی روی آن حساب کند، نه روی فرضِ نشده.

**اثر عملی روی هرکدام:**

- `P12` («فقط پس از تثبیت دامنه عملکردی») حالا **جزئاً واقعی** است: امضای
  Ed25519 manifest همین الان در تولید کار می‌کند (`scripts/sign_deployment_manifest.py`،
  فاز `1.6.10`) — ولی SBOM/vulnerability scan رسمی هنوز روی ایمیج واقعاً
  ارسال‌شده به Tiara اجرا نشده. **این باید عقب‌ماندگی حساب شود، نه بلوکه‌کنندهٔ
  استقرار بعدی** — استقرار بعدی نباید منتظر آن بماند، ولی باید هرچه زودتر روی
  همان ایمیج انجام شود.
- `P13` («proof کامل Compose/Nginx شبه‌تولید») عملاً **به‌صورت زنده روی خودِ
  Tiara اتفاق افتاد**، نه در یک محیط staging جدا. مشکلات واقعی که پیدا شد
  (مسیر دوگانهٔ `.env`، فراموشیِ `db-finalize`، مجوز اشتباه manifest) دقیقاً
  همان نوع مشکلی است که `P13` قرار بود **قبل از مشتری واقعی** پیدا کند. همهٔ
  آن‌ها در `1.6.10` رفع کد شدند؛ کاری که باقی می‌ماند این است که یک اجرای
  proof مستقل (طبق تعریف اصلی `P13`) واقعاً یک‌بار کامل انجام شود تا استقرار
  بعدی همین کلاس مشکل را دوباره روی مشتری زنده کشف نکند.
- `P14` («شبکه/TLS/VPN واقعی بر پایهٔ `P0R.1`») هم به‌صورت موردی برای Tiara
  انجام شد: نام‌میزبان داخلی + CA اختصاصی (نه دامنهٔ عمومی، نه IP ثابت) —
  حالتی که این نقشه اصلاً پیش‌بینی نکرده بود. `docs/ops/DOLPHIN_DEPLOYMENT_RUNBOOK.md`
  حالا بخش «۶الف» را برای دقیقاً همین حالت دارد (اضافه‌شده در `1.6.10`)، ولی
  خودِ `P0R.1` (survey مکتوب) برای Tiara هنوز اجرا نشده — تصمیم‌های زیرساختی
  آن پروژه مستند نشدند، فقط اجرا شدند.

### تکالیف باز فوری روی خودِ Tiara (نه کد — عملیات واقعی روی آن سرور)

از گزارش کامل نشست دیپلوی (`tiara_dolphin_deployment_handoff.json`)، هنوز
روی سرور واقعی انجام نشده:

1. چرخاندن پنج رمز/کلیدی که حین گفت‌وگوی پشتیبانی افشا شدند (`DJANGO_SECRET_KEY`
   و هر چهار `POSTGRES_*_PASSWORD`).
2. جابه‌جایی کلید خصوصی امضای manifest و کلید خصوصی CA داخلی از روی هاست
   اپلیکیشن به نگهداری امن/آفلاین.
3. آماده‌سازی حجم بکاپ و گرفتن اولین بکاپ واقعی تاییدشده.
4. باریک‌کردن `AUDIT_TRUSTED_PROXY_CIDRS` به subnet واقعی.
5. DNS مرکزی و توزیع CA از طریق GPO/MDM به‌جای hosts-file دستی.

جزئیات کامل و دستورهای دقیق هرکدام در `DOLPHIN_PROJECT_HANDOFF.md`.

### گام بعدی واقعی — نه فهرست آرزو

با یک مشتری واقعی که الان پول/کسب‌وکار واقعی رویش جاری است، ترتیب اولویت
معقول (نه تصویب‌شده — پیشنهاد برای تصمیم مالک محصول) این است:

1. **بستن تکالیف باز Tiara بالا** — این‌ها امنیتی‌اند، نه ویژگی؛ اولویت اول.
2. **`P1` را واقعاً ببندید.** بخش ۱۴ `DOLPHIN_PROJECT_HANDOFF.md` هنوز
   ده‌ها آیتم باز دارد (`BIZ-001` تا `BIZ-013`؛ دامنه‌های بزرگ ۱۳ تا ۲۰؛
   زیرساخت ۲۱ تا ۲۹؛ تصمیم‌های تازهٔ `P3` یعنی ۳۲ تا ۳۵). هرکدام که برای
   Client-1 (یا هر مشتری بعدی مشابه Tiara) واقعاً لازم است، همان‌جا با یک
   تصمیم مکتوب مالک محصول بسته شود — نه با حدس.
3. **`P12`/`P13` را به‌صورت رسمی، حداقل یک‌بار، تکمیل کنید** — نه چون نقشه
   می‌گوید، بلکه چون استقرار مشتری بعدی نباید دوباره همان کلاس مشکلی را که
   Tiara پیدا کرد از نو کشف کند. این حالا مستندسازیِ گذشته‌نگر نیست؛ سرمایه‌گذاری
   روی استقرار بعدی است.
4. **`P10`/`P11`** («فایل امن» و «یکپارچگی‌های بیرونی») همچنان منتظر تصمیم
   مصوب مالک محصول‌اند طبق شرایط توقف خودشان — این نقشه برایشان چیزی فرض
   نمی‌کند.

---

### P0 — ممیزی کامل مخزن و تطبیق حقیقت مستندات

- **هدف:** آشتی دادن `DOLPHIN_PROJECT_HANDOFF.md`، `DOLPHIN_CLIENT1_CODEX_ROADMAP.md`، `BACKEND_SPEC.md` با کد واقعی؛ استخراج سوالات مسدودکننده.
- **وضعیت:** انجام‌شده و commit شده (`fde384a`).
- **تحویل:** آن commit + گزارش نهایی فارسی آن فاز.

### P0R — اصلاح مستندات پیش از پیاده‌سازی (فاز فعلی)

- **هدف:** رفع تناقض‌های مادی، Git state کهنه، گسترش بی‌پشتوانه دامنه Client 1، و فرض‌های معماری زودهنگام که در commit `fde384a` باقی مانده بود.
- **پیش‌نیاز:** P0 (commit `fde384a`).
- **تغییرات مجاز:** فقط مستندات (`BACKEND_SPEC.md`، `DOLPHIN_PROJECT_HANDOFF.md`، همین فایل) + افزودن `CLAUDE.md` جدید در ریشه. هیچ مدل/migration/service/selector/permission/serializer/view/URL/template/JS/CSS/Dockerfile/`.dockerignore`/Compose/Nginx/تست/وابستگی/تنظیمات رانتایم.
- **فرض ممنوع:** رفتار فعلی کد را به‌جای سیاست تاییدشده بازنویسی نکن؛ هر جا کد و هدف Client 1 فرق دارد، هر سه برچسب `CURRENT CODE BEHAVIOR`/`CLIENT-1 TARGET BEHAVIOR`/`IMPLEMENTATION GAP` را جدا بنویس.
- **تست لازم:** `git status --short`، `git diff --stat`، `git diff --check`، `check`، `makemigrations --check --dry-run`. اجرای کامل test suite لازم نیست چون کدی تغییر نکرد.
- **اثر migration/داده:** هیچ.
- **gate امنیت/مجوز:** بدون تغییر رفتار authorization.
- **اثر backup/restore:** هیچ.
- **completion gate:** سه سند اصلاح‌شده + `CLAUDE.md` جدید در working tree؛ بدون `git add`/`commit`/`push`.
- **stop condition:** بازبینی انسانی این diff، سپس تصمیم مالک محصول درباره سوالات بخش ۱۴ Handoff.
- **تحویل دقیق:** diff کامل، بدون commit؛ گزارش نهایی فارسی این فاز.

### P0R.1 — survey زودهنگام زیرساخت/سرور/شبکه هدف

- **هدف:** پاسخ به سوالات ۲۱ تا ۲۹ بخش ۱۴ Handoff را زودتر جمع‌آوری کن، نه در انتهای نقشه. **اصلاح:** نسخه قبلی این کار را فاز P14 (تقریبا آخرین فاز) گذاشته بود؛ این به‌شدت ریسک استقرار را دیر آشکار می‌کرد.
- **پیش‌نیاز:** هیچ؛ **موازی‌پذیر با P1**.
- **تغییرات مجاز:** فقط مستندسازی survey (پاسخ‌ها را در Handoff بخش ۱۰/۱۴ یا `docs/ops/DEPLOYMENT.md` ثبت کن)؛ بدون تغییر کد.
- **فرض ممنوع:** طراحی مستقیم روی «Windows Server 2008» بدون شواهد دقیق `winver`/`systeminfo`؛ فرض هر توپولوژی شبکه بدون تایید مالک زیرساخت.
- **موارد لازم:** خروجی دقیق `winver` و `systeminfo`؛ آیا «2008» اشاره به Windows Server، SQL Server، یا نرم‌افزار حسابداری است؛ ادیشن/Service Pack/معماری Windows؛ ظرفیت مجازی‌سازی؛ محدودیت نرم‌افزار حسابداری موجود روی همان هاست؛ برند/مدل/firmware روتر تهران و پرند؛ واقعیت‌های IP ثابت/شبکه؛ دامنه Active Directory در برابر دامنه عمومی؛ مالکیت DNS؛ UPS؛ آنتی‌ویروس/EDR؛ تعداد کل و peak کاربران؛ مقصد backup؛ مالکان پشتیبانی/بازیابی/حادثه؛ تصمیم سرور/appliance اختصاصی در برابر ارتقای OS.
- **تست لازم:** ندارد (survey است).
- **اثر migration/داده:** هیچ.
- **gate امنیت/مجوز:** تایید عدم انتشار عمومی PostgreSQL/SSH/RDP/مدیریت کانتینر در هر طرح پیشنهادی.
- **اثر backup/restore:** تعیین مقصد/مالک نهایی backup به‌عنوان ورودی برای P13/P15.
- **completion gate:** پاسخ مکتوب به هر ۹ آیتم بالا.
- **stop condition:** این فاز می‌تواند `BLOCKED_EXTERNAL` بماند بدون مسدودکردن P1/P2/P4 (که به آن وابسته نیستند)؛ اما P14/P15 بدون آن شروع نمی‌شوند.
- **تحویل دقیق:** سند survey تکمیل‌شده.

### P0R.2 — راه‌اندازی PostgreSQL محلی/staging زودهنگام — ✅ انجام‌شده (۲۰۲۶/۰۸/۱۵)

**وضعیت: کامل.** کل زنجیره روی PostgreSQL 17.11 واقعی اجرا شد: مجموعه کامل تست، هر ۷ تست PostgreSQL-only (proof همزمانی/قفل واقعی)، migration تازه از صفر بدون drift، bootstrap نقش‌ها، قرارداد امتیاز چهار-نقشی، `pg_dump` با نقش backup، و `pg_restore` native داخل یک دیتابیس دوم گارد-شده به‌همراه اثبات یکپارچگی schema/migration/داده و قابل‌استفاده بودن نقش اپلیکیشن. شواهد دقیق در Handoff بخش ۰.۰۰.

**blocker قبلی برطرف شد بدون تضعیف تولید.** `scripts/bootstrap-postgres.sh` همچنان در تولید از `psql \password` استفاده می‌کند؛ یک شاخه opt-in و fail-closed اضافه شد که فقط با پرچم صریح و فقط برای شناسه‌های یک‌بارمصرف harness روی loopback کار می‌کند و همان verifier SCRAM-SHA-256 را سمت کلاینت می‌سازد، پس plaintext همچنان به سرور نمی‌رسد. جزئیات: `docs/backend/POSTGRES_TESTING.md`.

**دو نقص واقعی که این فاز کشف کرد** (هر دو رفع شدند): یک رقابت واقعی و بازتولیدپذیر در فرانت — handler دیالوگ محصول و handler ارسال فرم گزارش فقط **پس از** بارگذاری‌های شبکه‌ای متصل می‌شدند، پس در ابتدای بارگذاری صفحه کلیک بی‌اثر بود (اصلاح در `dolphin-app.js`)؛ و دو باگ در خود harness در مرحله sentinel.


- **هدف:** proof واقعی PostgreSQL (نه فقط SQLite) را زود در چرخه قرار بده، نه فقط نزدیک انتهای نقشه. **اصلاح:** نسخه قبلی proof رانتایم PostgreSQL کامل را در فاز P13 (تقریبا آخر) نگه می‌داشت؛ SQLite-only نباید ماژول‌های Inventory/Invoice/Payment/Ledger/گزارش مالی را «کامل» اعلام کند.
- **پیش‌نیاز:** ابزار PostgreSQL native یا Docker روی حداقل یک هاست dev/staging (این هاست فعلی فاقد `docker`/`psql`/`initdb` است — تایید اجرا شد در P0).
- **موازی‌پذیر با P1.**
- **تغییرات مجاز:** فقط اجرای harness موجود (`scripts/test-postgres.ps1`)؛ بدون تغییر کد مگر harness خطای واقعی نشان دهد.
- **فرض ممنوع:** موفقیت SQLite محلی را معادل proof PostgreSQL ندان.
- **تست لازم:** کل مجموعه تست روی PostgreSQL واقعی، migration drift، database role/grant، حداقل یک race/concurrency probe.
- **اثر migration/داده:** اعمال واقعی migration روی cluster موقت/staging.
- **gate امنیت/مجوز:** نقش‌های دیتابیس (init/migration/app/backup) طبق `docs/ops/DATABASE_ROLES.md`.
- **اثر backup/restore:** یک dump + یک restore ایزوله روی این cluster staging.
- **completion gate:** harness کامل + migration drift + حداقل یک concurrency probe روی PostgreSQL واقعی pass شود. **این gate باید سبز باشد پیش از آنکه P4 (Inventory) کامل اعلام شود** — SQLite-only کافی نیست چون Inventory concurrency-sensitive است.
- **stop condition:** بدون هیچ هاست dev/staging دارای ابزار PostgreSQL، این فاز `RUNTIME_UNPROVED` می‌ماند؛ P0/P1/P2 را مسدود نمی‌کند اما P4 را از حالت «کامل» به «Local-only، PostgreSQL proof باقی‌مانده» تنزل می‌دهد.
- **تحویل دقیق:** گزارش proof با شواهد دقیق دستور/خروجی.

### P0R.3 — مقایسه و انتخاب طراحی deployment-profile

- **هدف:** پیش از هرگونه مدل/migration، سه گزینه طراحی را مقایسه و یکی را با تصمیم مستقیم انتخاب کن. **اصلاح:** نسخه قبلی این نقشه مستقیم می‌گفت «مدل `DeploymentProfile` بساز» بدون مقایسه گزینه — این یک فرض معماری زودهنگام بود.
- **پیش‌نیاز:** هیچ؛ **موازی‌پذیر با P1**؛ فقط طراحی روی کاغذ است، نه کد.
- **تغییرات مجاز:** فقط سند مقایسه (در Handoff یا این فایل)؛ هیچ کد.
- **گزینه‌ها:**

```text
Option A — signed external deployment manifest
یک فایل/متغیر امضاشده بیرون از دیتابیس تعیین می‌کند کدام قابلیت‌ها
برای این استقرار فعال‌اند.

Option B — database-backed DeploymentProfile
یک مدل Django/جدول دیتابیس که در سطح استقرار مقداردهی می‌شود.

Option C — signed manifest plus runtime database cache
manifest امضاشده منبع حقیقت است؛ یک کش دیتابیسی برای کوئری سریع در
runtime نگه‌داری می‌شود ولی هرگز منبع حقیقت نیست.
```

- **معیارهای مقایسه که باید برای هر سه گزینه پاسخ داده شود:** مرجع تغییر (کی می‌تواند profile را عوض کند)؛ تایید امضا؛ رفتار fail-closed روی profile نامعتبر/ناشناخته؛ رفتار هنگام شکست راه‌اندازی (startup failure)؛ کارکرد offline؛ وابستگی بین feature‌ها؛ جداسازی feature/role/object-scope؛ audit؛ اثر روی backup/restore؛ rollback؛ تهدید Administrator روی هاست مشتری (بخش ۸ Handoff)؛ قابلیت portability بین چند مشتری؛ جداسازی برندینگ؛ جداسازی secret.
- **تست لازم:** ندارد (فاز طراحی است).
- **اثر migration/داده:** هیچ.
- **gate امنیت/مجوز:** هر گزینه باید صریحا نشان دهد چطور تهدید «مشتری با دسترسی Administrator روی هاست خودش» را می‌پوشاند.
- **اثر backup/restore:** هر گزینه باید اثر خودش روی backup/restore را مشخص کند.
- **completion gate:** جدول مقایسه کامل + یک گزینه با تصمیم مستقیم مالک محصول انتخاب‌شده.
- **stop condition:** **هیچ مدل `DeploymentProfile`، migration، یا کد feature-gate نباید پیش از این انتخاب شروع شود** (`PROFILE-001`، بخش ۷ Handoff).
- **تحویل دقیق:** سند مقایسه + تصمیم مکتوب.

### P0R.4 — سخت‌سازی زودهنگام build-context ایمیج — ✅ انجام‌شده (commit `5efe1c8`)

**وضعیت:** تعریف build اصلاح شد و گیت رگرسیون دارد. اندازه‌گیری واقعی: ۴۰۸ → ۱۴۷ فایل، ۱۴۲ → ۰ مورد ممنوعه. ابزار: `scripts/validate_image_content.py` (دو حالت `--context` و `--listing`)، تست: `common/tests/test_image_content.py`. باقی‌مانده تنها یک کار است: اجرای `--listing` روی یک ایمیج **واقعی** روی هاستی که Docker دارد. متن زیر به‌عنوان قرارداد فاز حفظ شده است.


- **هدف:** پیش از هر build یا توزیع ایمیج Docker، `.dockerignore`/`Dockerfile` را طوری اصلاح کن که `docs/`, فایل‌های `*.md` ریشه (`BACKEND_SPEC.md`, `DOLPHIN_PROJECT_HANDOFF.md`, `DOLPHIN_CLIENT1_CODEX_ROADMAP.md`), `*/tests/**`, `scripts/`, `nginx/`, `compose*.yml`, `requirements-direct.txt`, فایل‌های IDE محلی، source map، و سورس vendor/demo غیرضروری در ایمیج نهایی نباشند؛ یک validator allowlist/denylist محتوای ایمیج اضافه کن.
- **اصلاح:** این باید **زودتر** از بسته‌بندی نهایی release (P12) اتفاق بیفتد، نه هم‌زمان با آن — چون هر تلاش build قبل از این فاز، محتوای ممنوعه را می‌شیپد، حتی یک build آزمایشی.
- **دقت لازم:** این سخت‌سازی به‌تنهایی منطق کسب‌وکار پایتون خوانا را «مخفی» نمی‌کند؛ فقط فایل‌های غیرضروری را از ایمیج بیرون می‌کشد. کامپایل/بسته‌بندی واقعی backend فاز جدای P12 است.
- **پیش‌نیاز:** هیچ؛ **موازی‌پذیر با P1**؛ می‌تواند مستقل از تصمیم‌های کسب‌وکار انجام شود چون فقط build definition است.
- **تغییرات مجاز:** ویرایش `Dockerfile`/`.dockerignore`؛ افزودن اسکریپت validator محتوای ایمیج. **این فاز اجازه تغییر کد اپلیکیشن ندارد** و در P0R اجرا نشد چون P0R فقط-مستندات است — این توضیح فاز است برای اجرای بعدی، نه اجازه اجرا همین حالا.
- **تست لازم:** اجرای validator روی build واقعی (وقتی Docker در دسترس باشد) که فهرست فایل داخل ایمیج را با allowlist مقایسه کند.
- **اثر migration/داده:** هیچ.
- **gate امنیت/مجوز:** هیچ کلید/secret نباید در build context باشد (از قبل با `.env*` exclude پوشش دارد).
- **اثر backup/restore:** هیچ.
- **completion gate:** validator تایید کند هیچ‌کدام از مسیرهای ممنوعه بالا در فهرست فایل ایمیج نیستند.
- **stop condition:** بدون Docker روی این هاست (تایید شد در P0)، این فاز فقط می‌تواند ویرایش `Dockerfile`/`.dockerignore` را انجام دهد؛ اجرای واقعی build/validator منتظر هاستی با Docker می‌ماند.
- **تحویل دقیق:** `Dockerfile`/`.dockerignore` اصلاح‌شده + اسکریپت validator + گزارش (وقتی قابل اجرا شد).

### P1 — بستن تصمیم‌های کسب‌وکار/دامنه باز

- **هدف:** برای هر شناسه باز در `DOLPHIN_PROJECT_HANDOFF.md` بخش ۱۴، یک تصمیم مکتوب مصوب (نه حدس) ثبت شود.
- **پیش‌نیاز:** P0R سبز (بازبینی‌شده و در صورت تایید commit شده توسط انسان).
- **تغییرات مجاز:** فقط به‌روزرسانی بخش تصمیم‌های Handoff/BACKEND_SPEC با تصمیم مصوب؛ هیچ مدل/migration/endpoint.
- **فرض ممنوع:** استنباط قانون کسب‌وکار از صفحه نمایشی Metronic؛ تخمین فرمول مالی بدون منبع؛ فرض وابستگی اجباری بین ماژول‌های مالی (بخش «اصلاح ترتیب وابستگی مالی» بالا).
- **تست لازم:** بدون تغییر کد، فقط اعتبارسنجی مستندات (diff/whitespace).
- **اثر migration/داده:** هیچ.
- **gate امنیت/مجوز:** هیچ.
- **اثر backup/restore:** هیچ.
- **completion gate:** هر ۳۰ آیتم بخش ۱۴ Handoff یا مصوب یا صریحا به فاز دیرتر موکول شده‌اند.
- **stop condition:** هر آیتمی که مصوب نشود مسدود باقی می‌ماند و ماژول وابسته آن (P4-P8) شروع نمی‌شود.
- **تحویل دقیق:** جدول تصمیم به‌روزشده در Handoff، بدون کد جدید.

### P1.7 — هم‌راستاسازی Authorization مدیریت کاربر — ✅ انجام‌شده (۲۰۲۶/۰۸/۱۵)

**وضعیت:** پیاده‌سازی شد. `users.manage_agents` و `users.manage_non_platform` حذف شدند؛ `USER_ADMINS` در لایه سرویس به `{PLATFORM_ADMIN}` محدود شد؛ `UserViewSet._require_admin` هم‌راستا شد. علاوه بر آن Django Admin پشت تنظیم `ENABLE_DJANGO_ADMIN` (پیش‌فرض `false`، مستقل از `DEBUG`) رفت و در nginx با `location ^~ /admin/ { return 404; }` مسدود شد. تست: `accounts/tests/test_user_administration_policy.py` (۱۵ تست) و `common/tests/test_admin_exposure.py` (۱۳ تست). allowlist شبکه مدیریت به `P14` موکول شد. متن زیر قرارداد اصلی فاز است و برای سابقه نگه داشته شده.


- **هدف:** رفتار فعلی کد (`accounts/access.py` capability `users.manage_agents` برای `sales_manager`؛ `accounts/views.py`/`accounts/services.py` که ساخت/ویرایش/reset‌رمز/toggle‌workstream/غیرفعال‌سازی حساب‌های `sales_agent` را به `sales_manager` اجازه می‌دهد) را با سیاست بسته Client 1 (بخش ۲/۶ Handoff: `sales_manager` هیچ قابلیت مدیریت کاربر ندارد) هم‌راستا کن.
- **پیش‌نیاز:** هیچ تصمیم کسب‌وکاری اضافه لازم نیست — سیاست از قبل بسته است (`BIZ-005` resolved). فقط نیاز به تصمیم فنی: آیا این حذف به‌صورت مستقیم در کد عمومی انجام شود یا پشت مکانیزم deployment-profile (P0R.3/P3) gate شود تا مشتری‌های دیگر بتوانند رفتار متفاوت داشته باشند. **این فاز در P0R اجرا نشد** (فقط-مستندات).
- **تغییرات مجاز (در اجرای آینده):** حذف/محدودسازی `users.manage_agents` از `ROLE_CAPABILITIES[SALES_MANAGER]` در `accounts/access.py`، و/یا اضافه‌کردن گیت deployment-profile اگر گزینه profile انتخاب شده باشد؛ بدون تغییر مدل‌های دیگر.
- **فرض ممنوع:** حذف کامل توانایی deployment دیگر (غیر Client-1) برای فعال‌کردن این قابلیت اگر آن مشتری دیگر آن را بخواهد — اگر مکانیزم profile انتخاب شود، این باید per-deployment قابل تنظیم بماند، نه حذف سراسری بدون قابلیت بازگشت.
- **تست‌های لازم (باید قبل از merge این فاز pass شوند):**
  - `sales_manager` نمی‌تواند کاربر بسازد/ویرایش کند/غیرفعال کند/رمز reset کند/role یا workstream تغییر دهد — حتی برای حساب‌های `sales_agent`.
  - `company_it` تحت پروفایل Client 1 یا غیرفعال است یا به‌شدت محدود (طبق تصمیم نهایی).
  - فقط `platform_admin` می‌تواند ساخت/ویرایش/غیرفعال‌سازی/فعال‌سازی مجدد/reset رمز/تغییر role/تغییر workstream را برای Client 1 انجام دهد.
  - تلاش‌های direct-ID و privilege-escalation (مثلا `sales_manager` که سعی می‌کند با ID مستقیم به حساب `company_it` دست بزند) fail-closed می‌مانند.
- **اثر migration/داده:** معمولا هیچ (فقط تغییر capability mapping)؛ اگر profile-gated باشد، additive.
- **gate امنیت/مجوز:** این خودِ فاز یک gate امنیتی است.
- **اثر backup/restore:** هیچ.
- **completion gate:** چهار تست بالا pass؛ رگرسیون کامل accounts app pass.
- **stop condition:** تصمیم فنی «مستقیم در برابر profile-gated» مشخص شود.
- **تحویل دقیق:** commit مجزا و کوچک، مستقل از سایر فازها.

### P2 — اتصال UI فعال و سخت‌سازی کنترل مرده — ✅ انجام‌شده (۲۰۲۶/۰۸/۱۵)

**وضعیت:** نقص `sales_documents/detail.html:16` با افزودن یک کوتیشن بسته اصلاح شد. پوشش رگرسیون در دو سطح اضافه شد (تجزیه DOM صفحه render شده + Chrome واقعی)، هر دو با برگرداندن موقت template به حالت معیوب تایید شدند که واقعا fail می‌شوند. بازبینی هر ۲۸ template served نشان داد هیچ نمونه دیگری از این کلاس نقص وجود ندارد. ادعای قبلی درباره از کار افتادن `maxlength` با اندازه‌گیری در Chrome رد شد (جزئیات در Handoff بخش ۰). مجموعه کامل: ۳۸۹ تست، ماتریس مرورگر ۱۸/۱۸.


- **هدف:** رفع نقص شناخته‌شده `common/templates/common/sales_documents/detail.html:16` (attribute بدون quote بسته) و یک پیمایش تایید مجدد UI فعال.
- **پیش‌نیاز:** P0R سبز؛ مستقل از P1 قابل اجراست چون نقص شناخته‌شده کسب‌وکاری نیست.
- **تغییرات مجاز:** ویرایش template غیر-رفتاری (فقط بستن quote، بدون تغییر فیلد/endpoint)؛ افزودن regression test برای همان template.
- **فرض ممنوع:** بازطراحی فرم یا افزودن فیلد جدید.
- **تست لازم:** browser/UI test مربوط به `sales-documents` detail، `check_html_branding.py`، `git diff --check`.
- **اثر migration/داده:** هیچ.
- **gate امنیت/مجوز:** بدون تغییر.
- **اثر backup/restore:** هیچ.
- **completion gate:** پاراگراف خطای فیلد «reason» واقعا در DOM ساخته می‌شود؛ تست جدید pass.
- **stop condition:** هیچ.
- **تحویل دقیق:** یک commit کوچک، تک‌منظوره.

### P3 — پیاده‌سازی deployment profile — ✅ انجام‌شده (۲۰۲۶/۰۸/۱۵)

**وضعیت: Option C پیاده شد.** manifest امضاشده بیرونی (Ed25519) تنها مرجع feature availability است؛ جدول `common.DeploymentProfileCache` فقط کش مشتق است و هرگز مرجع تصمیم نیست. هر حالت نامعتبر/ناشناخته/دستکاری‌شده fail-closed است و فرآیند بالا نمی‌آید. کلید خصوصی امضا هرگز شیپ نمی‌شود (ابزار امضا در `scripts/` است که از ایمیج exclude شده و فقط کلید عمومی پیکربندی می‌شود). restore یک dump قدیمی نمی‌تواند feature پس‌گرفته‌شده را برگرداند. feature/role/object-scope سه کنترل جدا ماندند و غیرفعال‌کردن feature هیچ داده‌ای حذف نمی‌کند. هیچ expiry/kill-switch اضافه نشد. سند: `docs/backend/DEPLOYMENT_PROFILE.md`؛ تست: `common/tests/test_deployment_profile.py`.

- **هدف:** پیاده‌سازی گزینه انتخاب‌شده در P0R.3 (fail-closed روی profile ناشناخته؛ غیرفعال‌سازی feature داده را حذف نمی‌کند؛ feature/role/object scope جدا).
- **پیش‌نیاز:** **`P0R.3` باید یک گزینه را رسما انتخاب کرده باشد** — این فاز نباید Option A/B/C را خودش انتخاب کند. همچنین P1 (تصمیم حاکمیت/مالکیت پروفایل مصوب باشد).
- **تغییرات مجاز:** طبق گزینه انتخاب‌شده — مدل/migration additive (Option B/C) یا سرویس خواندن/تایید manifest امضاشده (Option A/C)؛ بدون حذف قابلیت موجود.
- **فرض ممنوع:** پیاده‌سازی UI پویای نقش/مجوز (صریحا خارج از scope تا تصمیم جدا، رده C).
- **تست لازم:** fail-closed روی profile ناشناخته/نامعتبر؛ عدم‌حذف داده هنگام غیرفعال‌سازی feature؛ تایید امضا (اگر Option A/C).
- **اثر migration/داده:** additive فقط (اگر مدل دیتابیسی بخشی از گزینه باشد).
- **gate امنیت/مجوز:** تایید عدم افزایش دسترسی پیش‌فرض؛ تایید تهدید Administrator هاست مشتری طبق مقایسه P0R.3.
- **اثر backup/restore:** فیلد/manifest پروفایل باید در backup/restore حفظ شود؛ تست اضافه شود.
- **completion gate:** حداقل دو پروفایل فرضی (مثلا Client-1، demo) قابل تعریف و جدا از role/scope باشند.
- **stop condition:** بدون انتخاب گزینه در P0R.3، این فاز شروع نمی‌شود.
- **تحویل دقیق:** پیاده‌سازی گزینه انتخاب‌شده + تست؛ بدون UI مدیریتی کامل (در فازهای بعد).

### BIZ-007 — تقویم جلالی — ✅ حل‌شده و پیاده‌شده (۲۰۲۶/۰۸/۱۶)

**تصمیم مستقیم مالک محصول، آخرین تغییر محصولی پیش از استقرار لینوکس.** ذخیره‌سازی و API میلادی/ISO می‌مانند؛ هر سطحی که کاربر Client-1 می‌بیند یا پر می‌کند جلالی است؛ منطقه زمانی عملیاتی `Asia/Tehran`. بدون migration و بدون تغییر schema.

یک لایه مشترک کوچک، نه تبدیل پراکنده: `common/jalali.py` + یک templatetag، و بخش تاریخ در `dolphin-app.js`. هیچ وابستگی تازه‌ای اضافه نشد و قفل hash-pinned دست‌نخورده ماند. صحت با ۱۶۸۰۱ بردار مرجع ICU در هر دو جهت و هر دو پیاده‌سازی اثبات شد.

قرارداد: `docs/backend/DATE_AND_CALENDAR.md`. هیچ ادعای انطباق حقوقی/مالیاتی/حسابداری نمی‌شود.

### P4 — موجودی و حرکت انبار — ✅ انجام‌شده (۲۰۲۶/۰۸/۱۶)

**وضعیت: کامل.** اپ `inventory` با `Warehouse`، `StockItem` (وضعیت مشتق) و دفتر append-only `StockMovement`. بهای تمام‌شده میانگین موزون متحرک است و روی هر ردیف حرکت snapshot می‌شود. هر تغییر سطح، پیش از خواندن، قفل ردیف `StockItem` را می‌گیرد. موجودی منفی به‌صورت پیش‌فرض رد می‌شود (`INVENTORY_ALLOW_NEGATIVE_STOCK`). قرارداد معنایی: `docs/backend/INVENTORY_SEMANTICS.md`. تست: `inventory/tests/test_stock.py` (۱۶ تست) به‌علاوه probeهای همزمانی واقعی PostgreSQL در `common/tests/test_postgres_concurrency.py`.

**تصمیم مستقیم مالک محصول (goal ۲۰۲۶/۰۸/۱۶) جایگزین gate «قرارداد مصوب» شد:** برای جزئیات کسب‌وکاری حل‌نشده، به‌جای توقف، پیش‌فرض محافظه‌کارانه و پیکربندی‌پذیر پیاده و مستند شود. هر پیش‌فرض در `config/settings.py` و سند معنایی ثبت شده است.


### P4 — موجودی و حرکت انبار

- **هدف:** پیاده‌سازی Inventory/Stock مطابق تصمیم مصوب P1 (واحد، انبار/مکان، موجودی اول دوره، نوع حرکت، رزرو، منفی‌شدن، اصلاح/برگشت).
- **پیش‌نیاز:** P1 آیتم Inventory مصوب.
- **تغییرات مجاز:** مدل/migration additive، سرویس concurrency-safe (transactional، قفل ردیف یا مشابه)، selector/API/UI جدید.
- **فرض ممنوع:** فرمول بهای تمام‌شده یا سود بدون تصمیم مصوب؛ فرض اینکه Order/Quotation حتما به این ماژول وابسته‌اند (بخش «اصلاح ترتیب وابستگی مالی»).
- **تست لازم:** concurrency (رقابت هم‌زمان روی موجودی)، role/object scope، migration preflight.
- **اثر migration/داده:** additive؛ بدون rewrite داده موجود Product.
- **gate امنیت/مجوز:** نقش مجاز به حرکت انبار طبق ماتریس نقش.
- **اثر backup/restore:** جدول جدید باید در backup schema پوشش داده شود؛ تست restore به‌روزرسانی شود.
- **completion gate:** تست‌های concurrency و role pass روی SQLite محلی **و** روی PostgreSQL واقعی (`P0R.2` سبز)؛ Sale/Product موجود بدون تغییر رفتار. **بدون proof سبز P0R.2، این ماژول فقط «local-only، PostgreSQL proof باقی‌مانده» است، نه کامل.**
- **stop condition:** بدون قرارداد دقیق «موجودی منفی مجاز یا نه» ادامه نده.
- **تحویل دقیق:** ماژول Inventory کامل local + proof PostgreSQL staging.

### P5 — Order و Quotation — ✅ انجام‌شده (۲۰۲۶/۰۸/۱۶)

**وضعیت: کامل.** `Quotation`/`QuotationItem` و `Order`/`OrderItem` با گراف وضعیت اعلام‌شده (گذار خارج از جدول رد می‌شود)، شماره‌گذاری بدون شکاف با قید یکتایی مستقل در دیتابیس، و تبدیل به‌صورت **کپی** (سند مبدأ دست‌نخورده می‌ماند). ویرایش فقط در وضعیت `draft`. سند: `docs/backend/BILLING_SEMANTICS.md`.


### P5 — Order و Quotation

- **هدف:** پیاده‌سازی مطابق تصمیم مصوب P1؛ چرخه (یکی از نمونه‌های بخش «اصلاح ترتیب وابستگی مالی» یا چرخه دیگر تاییدشده)، منبع، شماره‌گذاری، تایید/رد/انقضا.
- **پیش‌نیاز:** P1 مصوب برای هر دو ماژول.
- **تغییرات مجاز:** مدل/migration additive، سرویس تراکنشی، audit.
- **فرض ممنوع:** فرض ضمنی که Order اثر مالی/حسابداری دارد یا حتما به Inventory وابسته است؛ Sale موجود همچنان جدا و بدون‌تغییر می‌ماند.
- **تست لازم:** چرخه وضعیت، role scope، شماره‌گذاری یکتا.
- **اثر migration/داده:** additive.
- **gate امنیت/مجوز:** طبق ماتریس نقش مصوب برای این ماژول.
- **اثر backup/restore:** پوشش backup/restore جدول جدید.
- **completion gate:** تست‌های چرخه/role pass.
- **stop condition:** بدون قرارداد numbering/idempotency تبدیل Quotation→Order (اگر آن چرخه تاییدشده باشد) ادامه نده.
- **تحویل دقیق:** دو ماژول local کامل.

### P6 — Invoice، مالیات، تخفیف، شماره‌گذاری — ✅ انجام‌شده (۲۰۲۶/۰۸/۱۶)

**وضعیت: کامل، با مرز صداقت صریح.** `Invoice`/`InvoiceItem` با snapshot تغییرناپذیر پس از صدور. صدور در **یک تراکنش** بهای تمام‌شده را snapshot می‌کند، موجودی را کسر می‌کند و بدهکاری را در دفتر حساب مشتری ثبت می‌کند؛ کمبود موجودی کل صدور را abort می‌کند.

**این کد هیچ انطباق مالیاتی/حسابداری/حقوقی برای هیچ حوزه قضایی ادعا نمی‌کند.** مالیات به‌صورت پیش‌فرض **خاموش** است و فقط یک درصد پیکربندی‌شده روی یک پایه مشمول واحد اعمال می‌شود. مالیات چندنرخی، معافیت، تهاتر، و هر قالب اظهارنامه عمدا پیاده نشده‌اند. «نمونه فاکتور redacted تاییدشده» که gate قبلی می‌خواست هنوز نرسیده؛ طبق تصمیم مستقیم مالک محصول، به‌جای توقف، معنای محافظه‌کارانه و پیکربندی‌پذیر پیاده و مستند شد.


### P6 — Invoice حسابداری، مالیات، تخفیف، شماره‌گذاری، اصلاح

- **هدف:** فاکتور حسابداری/حقوقی واقعی، مجزا از `Sale`/`SalesDocument` عملیاتی موجود.
- **پیش‌نیاز:** P1 قرارداد کامل منبع/مالیات/تخفیف/رُند/شماره‌گذاری/اصلاح مصوب.
- **تغییرات مجاز:** مدل Invoice/InvoiceItem additive، snapshot غیرقابل‌تغییر، سرویس اصلاح/ابطال audited.
- **فرض ممنوع:** هیچ فرمول مالیات/تخفیف بدون منبع رسمی مصوب؛ فرض منبع اجباری Invoice از Order/Quotation یا فقط از Sale؛ هیچ حذف فیزیکی فاکتور صادرشده.
- **تست لازم:** صحت محاسبه Decimal، rounding، اصلاح/ابطال audited، role scope.
- **اثر migration/داده:** additive؛ بدون rewrite Sale/SalesDocument موجود.
- **gate امنیت/مجوز:** فقط نقش مجاز صدور/اصلاح/ابطال.
- **اثر backup/restore:** الزامی؛ فاکتور داده مالی حیاتی است.
- **completion gate:** نمونه محاسبه redacted تاییدشده توسط مالک محصول با خروجی کد یکسان باشد.
- **stop condition:** بدون نمونه فاکتور redacted واقعی شروع نشود (بخش ۱۴ Handoff، آیتم بالاتر از سایر موارد رده B).
- **تحویل دقیق:** ماژول Invoice local کامل، بدون PDF (در P9).

### P7 — Payment، چک، قسط، دفتر حساب مشتری — ✅ انجام‌شده (۲۰۲۶/۰۸/۱۶)

**وضعیت: کامل.** `Payment` با idempotency، `PaymentAllocation` (نه از دریافت بیشتر، نه از فاکتور بیشتر؛ مازاد به‌صورت بستانکاری روی حساب می‌ماند)، چرخه چک (چک تا وصول پول دریافت‌شده نیست — `BILLING_CHEQUE_CREDITS_ON`)، قسط‌بندی مساوی با باقیمانده گِردکردن روی قسط **اول**، و `CustomerLedgerEntry` فقط-افزودنی با `balance_after` روی هر ردیف. هیچ ردیفی حذف یا ویرایش نمی‌شود؛ برگشت یک ردیف تازه است. append-only در سطح نقش دیتابیس هم اجرا می‌شود.

**پیاده نشده (تصمیم حقوقی/تجاری لازم دارد):** سود تاخیر، جریمه، درگاه پرداخت، مغایرت‌گیری بانکی.


### P7 — Payment، چک، قسط، حساب مشتری

- **هدف:** پرداخت، چک، قسط، دفتر/حساب مشتری مطابق تصمیم مصوب.
- **پیش‌نیاز:** P1 مصوب برای این خانواده؛ اگر تصمیم مصوب تخصیص به Invoice را اجباری کند، P6 هم پیش‌نیاز است — این وابستگی از تصمیم می‌آید، نه از فرض پیش‌فرض.
- **تغییرات مجاز:** مدل additive، سرویس idempotent، ledger append-only.
- **فرض ممنوع:** بدون قرارداد دقیق بدهکار/بستانکار فرمول ننویس؛ فرض اجباری‌بودن Payment→Invoice.
- **تست لازم:** idempotency، reversal/refund، تطبیق مانده.
- **اثر migration/داده:** additive.
- **gate امنیت/مجوز:** نقش مجاز ثبت/تایید پرداخت.
- **اثر backup/restore:** الزامی؛ داده مالی.
- **completion gate:** موازنه دفتر مشتری با مجموع رویدادها تطبیق داشته باشد در تست.
- **stop condition:** بدون تصمیم مصوب تخصیص/idempotency.
- **تحویل دقیق:** ماژول local کامل.

### P8 — مطالبات، سود ناخالص، ارزش موجودی — ✅ انجام‌شده (۲۰۲۶/۰۸/۱۶)

**وضعیت: کامل.** سه گزارش با parity کامل JSON/UI/XLSX طبق الگوی گزارش عملکرد موجود. سود **ناخالص** است و فقط از bahای تمام‌شده snapshot‌شده هنگام صدور محاسبه می‌شود؛ صورت سود و زیان نیست، مبنای حسابداری (نقدی/تعهدی) اعمال نمی‌کند و سربار تخصیص نمی‌دهد.

**نکته صداقت:** فاکتوری که بدون انبار صادر شده bahای تمام‌شده ندارد. چنین فاکتوری به‌عنوان «اندازه‌گیری‌نشده» جداگانه شمارش و از جمع‌ها خارج می‌شود — bahای نامعلوم صفر در نظر گرفته نمی‌شود، چون آن سود را بیش‌ازواقع نشان می‌داد.


### P8 — مطالبات و گزارش سود/زیان مبتنی‌بر منبع

- **هدف:** گزارش مطالبات و سود/زیان فقط از داده واقعی موجود (Invoice/Payment و در صورت وجود Inventory)، نه فرمول حدسی.
- **پیش‌نیاز:** P6، P7؛ P4 فقط اگر تصمیم مصوب سود/زیان به بهای تمام‌شده وابسته باشد.
- **تغییرات مجاز:** selector/serializer/service گزارش فقط-خواندنی؛ بدون مدل جدید در حالت معمول.
- **فرض ممنوع:** انتشار متریک مبهم بدون تعریف دقیق؛ فرض مبنای حسابداری (نقدی/تعهدی) بدون تصمیم مصوب.
- **تست لازم:** صحت فرمول با نمونه عددی تاییدشده، role scope، مخرج صفر.
- **اثر migration/داده:** معمولا هیچ.
- **gate امنیت/مجوز:** فقط نقش مدیریتی.
- **اثر backup/restore:** هیچ (گزارش مشتق).
- **completion gate:** خروجی گزارش با نمونه دستی مالک محصول یکسان باشد.
- **stop condition:** بدون نمونه عددی تاییدشده.
- **تحویل دقیق:** گزارش JSON/UI/XLSX با parity کامل مثل الگوی گزارش عملکرد فعلی.

### P9 — چاپ و PDF — ✅ انجام‌شده کامل (۲۰۲۶/۰۸/۱۶، تکمیل‌شده در فاز FREEZE)

**به‌روزرسانی:** PDF سمت سرور **پیاده شد** و blocker قبلی برطرف شد — بدون افزودن هیچ وابستگی پایتون و بدون بازتولید قفل hash-pinned. همان HTML صفحه چاپ تاییدشده به یک مرورگر headless داده می‌شود و نتیجه چاپ‌شده برگردانده می‌شود (`common/pdf.py`). feature پیش‌فرض خاموش است چون به باینری مرورگر داخل ایمیج نیاز دارد؛ دکمه دانلود فقط با renderer سالم ظاهر می‌شود و چاپ/ذخیره مرورگر همچنان مسیر پذیرفته‌شده روز اول است. شاهد و جزئیات در Handoff بخش ۰.۰۰۰۰۰. متن زیر وضعیت پیش از این تکمیل را برای سابقه نگه می‌دارد.

### P9 (سابقه) — چاپ و PDF تا سقف وابستگی‌های وقت

**وضعیت: عملیاتی.** صفحه چاپ سمت سرور برای فاکتور و پیش‌فاکتور از روی snapshot ذخیره‌شده render می‌شود (`common/templates/common/print_base.html` + stylesheet چاپ در `dolphin.css`): بدون ناوبری، بدون کنترل، فارسی RTL. کاربر با همان دکمه، چاپ یا «ذخیره به‌صورت PDF» مرورگر را می‌گیرد. صفحه‌ای که داده‌اش لود نشود چیزی چاپ نمی‌کند، به‌جای چاپ یک فرم خالی رسمی‌نما.

**آنچه انجام نشد و دلیل دقیق آن:** endpoint تولید PDF سمت سرور (بایت‌استریم) پیاده **نشد**. هیچ کتابخانه PDF یا shaping فارسی در محیط نصب نیست (`reportlab`، `weasyprint`، `fpdf`، `arabic_reshaper`، `bidi` — همه غایب، اجرا و تایید شد) و طبق `docs/ops/DEPENDENCIES.md` افزودن وابستگی باید در یک ایمیج تمیز لینوکس CPython 3.13 با hash-pin حل شود که از این هاست ممکن نیست. نوشتن یک تولیدکننده PDF از صفر نیازمند embedding فونت TrueType، شکل‌دهی متنی عربی (اشکال آغازی/میانی/پایانی/منفرد) و مرتب‌سازی bidi است؛ پیاده‌سازی ناقص آن خروجی فارسی شکسته تولید می‌کند. این کار عمدا انجام نشد به‌جای اینکه نیمه‌کاره شیپ شود. **blocker دقیق: بازتولید قفل وابستگی روی یک هاست لینوکس.**

> **این blocker دیگر برقرار نیست (۲۰۲۶/۰۸/۱۶).** راه‌حل، افزودن کتابخانه نبود بلکه استفاده از موتور رندری بود که از قبل همان صفحه را درست چاپ می‌کند. بخش بالا وضعیت فعلی است؛ این پاراگراف فقط سابقه است.


### P9 — PDF و چاپ

- **هدف:** خروجی PDF عملیاتی برای Invoice/سند نهایی.
- **پیش‌نیاز:** P6.
- **تغییرات مجاز:** سرویس تولید PDF از snapshot موجود؛ بدون تغییر مدل مالی.
- **فرض ممنوع:** طراحی قالب بدون تایید مالک محصول (لوگو/برند/متن حقوقی).
- **تست لازم:** تولید PDF قطعی از داده ثابت (golden test)، عدم نشت داده خارج از scope.
- **اثر migration/داده:** هیچ.
- **gate امنیت/مجوز:** همان scope Invoice.
- **اثر backup/restore:** فایل تولیدی cache/موقت است؛ نیازی به backup اختصاصی ندارد مگر تصمیم دیگری داده شود.
- **completion gate:** نمونه PDF تاییدشده مالک محصول.
- **stop condition:** بدون قالب تاییدشده.
- **تحویل دقیق:** endpoint دانلود PDF.

### P10 — فایل/سند عملیاتی امن

- **هدف:** ذخیره فایل/پیوست عملیاتی طبق کف امنیتی مصوب (دسترسی خصوصی، کلید مبهم سمت سرور، اسکن قرنطینه، پیش‌فرض denied، audit، backup/restore).
- **پیش‌نیاز:** P1 مصوب برای backend/محدودیت نوع/حجم/نگهداری.
- **تغییرات مجاز:** مدل/سرویس/API/UI جدید additive.
- **فرض ممنوع:** ذخیره‌سازی عمومی یا لینک مستقیم بدون احراز هویت.
- **تست لازم:** دسترسی خصوصی، رد نوع/حجم نامعتبر، backup/restore فایل.
- **اثر migration/داده:** additive.
- **gate امنیت/مجوز:** پیش‌فرض denied، فقط scope مصوب.
- **اثر backup/restore:** الزامی و باید جدا از پشتیبان دیتابیس تست شود.
- **completion gate:** بازیابی فایل از backup در محیط ایزوله pass شود.
- **stop condition:** بدون تصمیم مصوب مقصد/نگهداری/RPO فایل.
- **تحویل دقیق:** ماژول فایل local کامل با proof بازیابی.

### P11 — یکپارچگی‌های بیرونی تاییدشده

- **هدف:** آداپتور واقعی برای هر ارائه‌دهنده بیرونی که مستندات رسمی، credential، owner و قرارداد retry/reconciliation آن رسیده باشد (وب‌سایت/فروشگاه، درگاه پرداخت، حسابداری، پیامک زنده، ایمیل، تلفنی).
- **پیش‌نیاز:** مدرک رسمی هر provider (جدا برای هرکدام)؛ `docs/backend/SMS_PROVIDER_ADAPTER_REQUIREMENTS.md` همین الگو را برای پیامک از قبل مشخص کرده.
- **تغییرات مجاز:** پیاده‌سازی adapter پشت interface موجود (مثل `InboundSMSProviderAdapter`)؛ بدون route ingest عمومی بدون امضا/احراز هویت.
- **فرض ممنوع:** اعتماد به IP مبدا به‌تنهایی به‌عنوان اثبات پیام؛ ذخیره متن پیام برخلاف سیاست `not_retained` بدون تصمیم حقوقی جدید.
- **تست لازم:** نمونه sandbox واقعی provider، رد replay/امضای نامعتبر، idempotency.
- **اثر migration/داده:** additive برای فیلدهای adapter-specific.
- **gate امنیت/مجوز:** کلید/credential فقط از کانال secret تاییدشده، هرگز در مستندات/سورس.
- **اثر backup/restore:** بدون تغییر در سیاست فعلی «بدون متن پیام».
- **completion gate:** یک رویداد sandbox واقعی و یک رد replay واقعی pass شود.
- **stop condition:** بدون مدرک رسمی provider مربوطه، آن یکپارچگی مسدود می‌ماند؛ provider دیگر می‌تواند مستقل جلو برود.
- **تحویل دقیق:** هر یکپارچگی جدا commit و جدا evidence.

### P12 — بسته‌بندی نهایی release: امضا، SBOM، signing، reproducible build

- **هدف:** پس از تثبیت دامنه عملکردی و اجرای `P0R.4` (سخت‌سازی build-context)، ارزیابی/پیاده‌سازی بسته‌بندی backend، حذف source map فرانت، manifest امضاشده، checksum، SBOM/vulnerability scan، مالکیت کلید امضا، profile مجوز/feature امضاشده، reproducible build، artifact قابل rollback.
- **اصلاح:** این فاز دیگر شامل سخت‌سازی پایه `.dockerignore` نیست — آن به `P0R.4` منتقل شد چون باید خیلی زودتر اتفاق بیفتد.
- **پیش‌نیاز:** `P0R.4` سبز؛ تصمیم مصوب سیاست تجاری مجوز/پشتیبانی.
- **تغییرات مجاز:** build pipeline، اسکریپت بسته‌بندی/امضا؛ بدون تغییر رفتار تجاری اپلیکیشن.
- **فرض ممنوع:** فرض که compiled/packaged بودن backend به‌تنهایی رازداری مطلق تضمین می‌کند (طبق مدل تهدید بخش ۸ Handoff، تضمین ندارد).
- **تست لازم:** Gitleaks، pip-audit، Syft/Grype، تایید امضا/checksum.
- **اثر migration/داده:** هیچ.
- **gate امنیت/مجوز:** هیچ کلید امضا روی هاست مشتری.
- **اثر backup/restore:** artifact قبلی برای rollback نگه‌داری شود.
- **completion gate:** SBOM/scan تمیز، امضا/checksum معتبر، build reproducible.
- **stop condition:** بدون `P0R.4` سبز، شروع نشود.
- **تحویل دقیق:** artifact امضاشده + گزارش SBOM/scan.

### P13 — proof رانتایم PostgreSQL/Compose/Nginx شبه‌تولید کامل

- **هدف:** proof کامل production-like (نه فقط migration/constraint مثل P0R.2، بلکه کل پشته: Compose boot/health/static/API/write-stop، Nginx/TLS).
- **پیش‌نیاز:** `P0R.2` سبز (پایه)؛ ابزار Docker/Compose/Nginx روی یک هاست staging مصوب.
- **تغییرات مجاز:** فقط اجرای harness/Compose موجود؛ بدون تغییر کد مگر harness خطا نشان دهد.
- **فرض ممنوع:** موفقیت `P0R.2` را معادل proof کامل Compose/Nginx ندان — این فاز پوشش وسیع‌تری دارد.
- **تست لازم:** کل مجموعه تست روی PostgreSQL واقعی، Compose boot/health/static/API/write-stop، Nginx/TLS.
- **اثر migration/داده:** اعمال واقعی migration روی cluster staging.
- **gate امنیت/مجوز:** نقش‌های دیتابیس طبق `docs/ops/DATABASE_ROLES.md`.
- **اثر backup/restore:** dump + restore ایزوله واقعی.
- **completion gate:** harness کامل + Compose boot/health/static/API/write-stop pass.
- **stop condition:** بدون هاست staging دارای Docker/Compose/Nginx.
- **تحویل دقیق:** گزارش proof رانتایم کامل.

### P14 — اجرای واقعی شبکه/TLS/VPN سایت

- **هدف:** بر پایه واقعیت‌های جمع‌آوری‌شده در `P0R.1`، شبکه/TLS/VPN واقعی سایت را برپا و تایید کن. **اصلاح:** این دیگر فاز کشف اطلاعات نیست (آن در `P0R.1` انجام شد) — این فاز اجراست.
- **پیش‌نیاز:** `P0R.1` تکمیل و تصمیم appliance/OS مصوب.
- **تغییرات مجاز:** پیکربندی شبکه/VPN/TLS واقعی؛ بدون تغییر کد اپلیکیشن.
- **فرض ممنوع:** فرض هر توپولوژی که با واقعیت‌های `P0R.1` تطبیق نداشته باشد.
- **تست لازم:** اتصال VPN سایت‌به‌سایت واقعی، گواهی TLS واقعی، تایید عدم انتشار عمومی پورت‌های حساس.
- **اثر migration/داده:** هیچ.
- **gate امنیت/مجوز:** تایید نهایی عدم انتشار عمومی PostgreSQL/SSH/RDP/مدیریت کانتینر.
- **اثر backup/restore:** تعیین نهایی مسیر شبکه به مقصد backup.
- **completion gate:** اتصال واقعی VPN/TLS تایید و مستند شود.
- **stop condition:** بدون `P0R.1` تکمیل‌شده، شروع نشود.
- **تحویل دقیق:** سند پیکربندی شبکه نهایی + شواهد اتصال.

### P15 — استقرار هدف، UAT، آموزش، rollback، sign-off

- **هدف:** استقرار واقعی، UAT چهار پرسونا با داده synthetic، آموزش، تمرین rollback، تایید نهایی مشتری.
- **اصلاح پرسونا:** این چهار **پرسونای UAT** هستند، نه چهار نقش فعال همیشگی Client 1: Platform Admin؛ Sales Manager؛ Sales Agent در workstream `sales`؛ Sales Agent در workstream `after_sales`. **Company IT پرسونای اجباری UAT نیست تا زمانی‌که طبق تصمیم بخش ۲/۶ Handoff غیرفعال است** — اگر یک حساب فنی محدود بعدا با قرارداد جدا تصویب شود، آن‌وقت پرسونای پنجم اضافه می‌شود.
- **پیش‌نیاز:** P12، P13، P14 هرسه سبز.
- **تغییرات مجاز:** فقط استقرار طبق artifact بازبینی‌شده؛ بدون تغییر کد در این فاز.
- **فرض ممنوع:** پرش از هر گیت بالا فقط برای تسریع.
- **تست لازم:** UAT چهار پرسونای بالا، تمرین rollback واقعی، اسکن امنیتی نهایی.
- **اثر migration/داده:** migration واقعی روی دیتابیس تولید (با backup قبل از اجرا).
- **gate امنیت/مجوز:** تایید نهایی دسترسی VPN/شبکه خصوصی طبق `P14`.
- **اثر backup/restore:** اولین backup واقعی تولید و restore drill مستند شود.
- **completion gate:** امضای پذیرش مشتری (sign-off) با شواهد کامل.
- **stop condition:** هر شکست در UAT یا rollback drill، استقرار را به `NO-GO` برمی‌گرداند.
- **تحویل دقیق:** گزارش cutover + sign-off امضاشده + وضعیت انتشار به‌روزشده در Handoff.
