# Worklog

Append checkpoints. A checkpoint is not a stop signal.

## 2026-08-09 - Goal checkpoint 001

- Roadmap phase/task: Phase 0 source and safety reconciliation; Phase 1 durable map start.
- Files inspected: `AGENTS.md`, authoritative specification, durable goal, continuous patch, root manifest, `.gitignore`, `.dockerignore`, requirements, Git state, backend docs, current deployment/top-level backend manifest.
- Current-state corrections: Specification and Git now exist, unlike the old discovery snapshot. Git has initial commit `ef1c7f4`; three source/goal files were untracked at goal start.
- Files changed: `AGENTS.md`, `.gitignore`, `ASSUMPTIONS.md`, `docs/backend/DISCOVERY.md`.
- Files added: `PROJECT_ROADMAP.md`, `PRODUCTION_READINESS_CHECKLIST.md`, `CODEBASE_MAP.md`, `FILE_REVIEW_LEDGER.md`, `WORKLOG.md`, `BLOCKERS.md`.
- Entities/relations/endpoints/migrations: No runtime change in this batch.
- Checks: root and source presence; Git status/log/identity; nested instruction manifest; tracked forbidden-path scan; high-confidence tracked secret-pattern scan; existing diff check.
- Results: Git identity exists; initial commit exists; only goal/spec/patch files were untracked before edits; no tracked private-key or known live-token pattern found; only `.env.example` matched environment path scan.
- Assumptions/blockers: Updated from authoritative specification. External PostgreSQL/Docker/Nginx/TLS proof and listed business decisions remain bounded blockers.
- Regression found/fixed: Old discovery and assumptions falsely said spec and Git were absent; replaced with current facts.
- Exact next task: Review all changed durable files, scan staged candidate paths/content, update Phase 0 evidence, create safe local baseline commit, then verify backend baseline commands and reconcile current implementation against the specification.
- Exact next files: root durable docs, `config/`, `common/`, `auditlog/`, `accounts/`, `sales/`, their migrations/tests.
- Exact next commands: `git status --short`; `git diff --check`; targeted path/secret scans; `python manage.py check --settings=config.test_settings`; migration drift; full tests; schema validation.

## 2026-08-09 - Goal checkpoint 002

- Roadmap phase/task: Phase 0 verified; Phase 2 global identity/Product guards; Phase 3 Company IT and audit scope; Phase 4 stable errors/conflicts.
- Files inspected: all root durable work files; current account auth/role files and tests; shared error/request settings; audit model/service/API/tests; sales model/service/selector/view diffs, migrations `0006`/`0007`, and workflow tests.
- Runtime changes verified: global active normalized-phone identity with fail-closed preflight; strictly positive Product price with fail-closed preflight; Company IT elevated operational rights; Manager user-directory denial while Team scope is unresolved; read-only scoped activity-log API; strict login/`id` input rejection; stable error code/request-ID envelope; confirmed conflicts use HTTP 409.
- Durable files changed: `PROJECT_ROADMAP.md`, `PRODUCTION_READINESS_CHECKLIST.md`, `WORKLOG.md`, `FILE_REVIEW_LEDGER.md`, `BLOCKERS.md`, `CODEBASE_MAP.md`.
- Entities/relations: CustomerPhone active identity is global. Product price must be greater than zero. No relation added.
- Endpoints: Added read-only `/api/v1/activity-logs/` list/retrieve. No audit write route.
- Authorization: Company IT has confirmed operational rights and cannot address Platform Admin user/audit rows. Platform Admin has full CRM/audit scope. Sales Manager user administration and broad directory/audit access stay denied pending exact bounded rules. Sales Agent stays own/created/assigned only.
- Migrations: `sales.0006_global_active_phone_identity`; `sales.0007_product_price_positive`. Both preflight and abort on conflicting existing data; no automatic deletion or rewrite.
- Checks run: `python manage.py test --settings=config.test_settings -v 1` found 69 and all passed; test-settings system check passed; migration dry-run found no changes; schema generation/validation passed; `python -m pip check` found no broken requirements; scoped durable-document `git diff --check` passed.
- Assumptions/blockers: Team schema remains BIZ-005. Limited Manager audit scope is BIZ-011. Undefined report metrics/presentation remain bounded, while unambiguous Phase 5 work may proceed. Native PostgreSQL, Docker, Nginx, TLS, and exact active UI path remain unproved.
- Regression found/fixed: Login and read-only `id` fields had accepted ignored input; Manager could read Platform Admin user details; Company IT operational rights lagged the matrix; errors lacked stable wire codes; audit rows had no approved read API; active phone identity and Product price guards were weaker than the specification.
- Exact next task: Continue active Phase 5 unambiguous report JSON/XLSX work, then run report/full/schema gates. In parallel, finish the remaining Phase 3 route/filter attack matrix and bounded first-party file ledger.
- Exact next files: `reports/`, report tests, `config/urls.py`, requirements, remaining sales tests, then deployment/operations files.
- Exact next commands: targeted report tests; full test suite; schema validation; migration drift; `git diff --check`; reviewed diff/stat.

## 2026-08-09 - Goal checkpoint 003

- Roadmap phase/task: Phase 4 safe fault contract; Phase 5 report/XLSX completion; Phases 6-7 active Persian/Kariz shell; Phase 8 strict production configuration; Phase 9 bounded logs and guarded backup/restore foundation.
- Files inspected: report selectors/serializers/services/views/URLs/workbook/tests; common UI/admin/template/static/tests; common exception, middleware, request-log, system, and backup guard tests; production settings/environment; Compose/Nginx; PostgreSQL backup/restore scripts and operations guide.
- Runtime changes verified: exact role-scoped user-performance JSON/XLSX; non-enumerating Sale Product filter; exact two-decimal money text and half-up average; safe unhandled API 500 envelope; Persian/RTL root/admin shell; strict fail-closed production environment; safe application/edge logs, independent liveness, container log caps; guarded dump and disposable-restore tools.
- Durable files changed: `ASSUMPTIONS.md`, `docs/backend/API_CONTRACT.md`, `docs/backend/ENTITY_CATALOG.md`, `docs/codebase/LANGUAGE_CLEANUP.md`, `docs/codebase/BRANDING_CLEANUP.md`, `PROJECT_ROADMAP.md`, `PRODUCTION_READINESS_CHECKLIST.md`, `WORKLOG.md`, `FILE_REVIEW_LEDGER.md`, `BLOCKERS.md`, `CODEBASE_MAP.md`.
- Entities/relations/migrations: Reports are read-only projections over User, Customer, Product, and Sale. No report, UI, logging, configuration, or backup entity/relation/migration was added. Current migration head remains `sales.0007`.
- Endpoints: Added `GET /api/v1/reports/user-performance/`, `GET /api/v1/exports/user-performance.xlsx`, and first-party `/`; audit/health/schema routes remain as recorded. Both report formats use private no-store responses and one shared scoped result.
- Authorization: Sales Agent report scope is self only. Sales Manager, Company IT, and Platform Admin may report all fixed-role current/inactive users. User IDs fail without existence leaks. Positive Product IDs filter only scoped confirmed Sales, so unknown or hidden/no-match IDs return zero Sale metrics without changing Customer count.
- Checks run: `python manage.py test --settings=config.test_settings -v 1` found 104 and all passed; test-settings system check passed; migration dry-run found no changes; OpenAPI generation/validation passed; collectstatic dry run passed; `python -m pip check` found no broken requirements; production `check --deploy` passed with non-secret safe test values; scoped durable-document diff and new-document whitespace checks passed.
- Assumptions/blockers: Team and Manager audit rules remain unresolved and denied. Final human XLSX labels/cell/style/Jalali rules remain BIZ-007. Live backup destination/schedule/retention/owners/recovery targets remain decisions. Native PostgreSQL, Docker, Nginx, TLS, real backup/restore, and browser proof remain external.
- Regression found/fixed: Reports were absent; Product filtering could have exposed global existence; XLSX numeric cells could lose cents at the maximum money range; unhandled API faults lacked the stable safe envelope; active UI/brand evidence was absent; production inputs/log bounds/request logging/backup guards were incomplete. Isolated implementations and tests now cover the repository-controlled parts.
- Exact next task: Finish Phase 3/10 role-route-filter attack matrix and remaining first-party ledger; then complete deployment/rollback/incident docs and approved UAT fixtures. Run PostgreSQL/Docker/Nginx/browser/backup gates only when the required external tools and decisions exist.
- Exact next files: remaining `sales` API/tests, remaining `common`/`config` ledger paths, Dockerfile/PostgreSQL harness, deployment and incident runbooks, UAT docs/fixtures.
- Exact next commands: targeted access-matrix tests; full test/schema/drift gates; reviewed diff/stat; later `scripts/test-postgres.ps1`, guarded backup/restore commands, Compose/Nginx smoke, and browser smoke on an approved runtime.

## 2026-08-09 - Goal checkpoint 004

- Roadmap phase/task: Phase 1 active-source map closure; Phases 3-4 trust-boundary closure; Phases 8-10 secure deployment source closure; Phase 11 candidate package verification.
- Files inspected: account identity/access/transition code and tests; audit snapshots/API; shared phone, error, CSRF, request-context/logging, and permission code; sales scopes/services/constraints/tests; reports; production environment, image, Compose, Nginx, database scripts, dependency lock, active UI, backend docs, operations runbooks, and root durable truth files.
- Runtime changes verified: server-managed Django identities cannot enter CRM authentication, query, mutation, assignment, or report scope; final active Platform Admin cannot be demoted/deactivated; audit visibility uses stored role snapshots; phone identity accepts only the canonical ASCII `+98` shape; API CSRF and unknown faults return safe request-bound JSON; schema/docs routes are absent in production.
- Deployment changes verified in source: exact dependency versions; root-owned image source and read-only web runtime; split database init/migration/application roles; role/password preparation followed by one locked owner/ACL transaction in both pre-migration bootstrap and post-migration exact-grant finalizer; explicit external database volume name; backend/frontend network split; direct Nginx TLS on 443, fixed-host HTTP redirect, checked HSTS, request IDs, rate limits, and finite timeouts; bounded logs; guarded bootstrap/backup/restore scripts.
- Durable and operator files: backend entity/relation/API/discovery docs; deployment, bootstrap, database-role, TLS, dependency, UAT, backup/restore, rollback, incident, and release guides; roadmap, checklist, map, ledger, blockers, assumptions, and this worklog.
- Entities/relations/migrations: `ActivityLog` now stores nullable actor/object role snapshots. `CustomerPhone.normalized_phone` has the exact canonical ASCII database shape. New migrations are `auditlog.0002_activitylog_role_snapshots` and `sales.0008_customer_phone_normalized_shape`; legacy audit roles are not guessed and invalid stored phones are not rewritten.
- Endpoints: Approved audit, report, export, auth, sales, health, and root UI routes remain. Production removes schema and interactive docs routes. No Sale-correction, Team-admin, optional after-sales, or undefined metric route was added.
- Authorization: CRM identity excludes every staff, superuser, group-linked, or direct-permission account. Company IT audit scope fails closed for unknown legacy or Platform Admin snapshots. Sales Manager user-admin and audit routes remain denied. One active Platform Admin must remain.
- Checks run: full test suite found 158 and passed; test-settings system check passed; migration dry-run found no changes; non-production OpenAPI validation passed with warnings fatal under UTF-8 output; collectstatic dry run passed; `pip check` passed; strict safe-value production deploy check passed with warnings fatal; shell bootstrap syntax passed; 101-path changed/untracked manifest had no forbidden path; high-confidence source secret-pattern scan found no match; fresh final source audit scored 9/10 with no firm repository P0/P1.
- Assumptions/blockers: Direct TLS and database-role topology are fixed in source. Native PostgreSQL, Docker/Nginx runtime, real certificate/hostname/renewal, browser, backup/restore, supply-chain artifact, and approved load evidence remain external. Business decisions in `BLOCKERS.md` remain excluded and fail closed.
- Candidate state: Work in progress. Source/runbook gates are near closed, but one exact user-approved commit/release artifact and its reviewed manifest are not recorded. Production-candidate and production-ready claims remain forbidden.
- Exact next task: After an exact release reference is authorized and reviewed, run Phase 12 only on an approved disposable or target runtime. Preserve the named database volume and collect proof without secrets.
- Exact next files: external evidence records and, only if a real failure appears, the smallest affected source/config/test/runbook slice.
- Exact next commands: `scripts/test-postgres.ps1` with approved native tools; Docker Compose config/build/bootstrap/migrate/up/health checks; Nginx config and HTTP/TLS smoke; guarded backup plus disposable restore; browser language/brand smoke; approved load and current dependency/container scan.

## 2026-08-09 - Goal checkpoint 005

- Roadmap phase/task: Phase 1 active-source inventory verified; Phase 2 bounded-text migration; Phases 3-5 contract proof refresh; Phases 8-10 deployment/input/query hardening; Phase 11 exact current-worktree evidence.
- Files inspected: active first-party backend/config/migration/test paths; production Dockerfile/Compose/Nginx/database tools; backend and operations docs; root durable truth files; exact changed/untracked manifest.
- Runtime changes verified in source: 64 KiB request-body and 32-container JSON-depth limits; stable `payload_too_large` HTTP 413 and safe parse errors; 2,000-character Customer address and 4,000-character limits on five notes/description fields; typed action schemas; flat query growth for eight list routes.
- Deployment changes verified in source: four database logins for init, migration, application, and read-only backup; six normal service definitions plus one profile-only backup service; exact external data and backup volumes; digest-only production image references with no Compose build path; reversible edge write-stop; guarded backup and exact restore boolean for nine core tables, three migration heads, and twelve key constraints.
- Durable files refreshed: `PROJECT_ROADMAP.md`, `PRODUCTION_READINESS_CHECKLIST.md`, `CODEBASE_MAP.md`, `ASSUMPTIONS.md`, `BLOCKERS.md`, `docs/backend/DISCOVERY.md`, `docs/ops/RELEASE_NOTES.md`, and this append-only worklog. `FILE_REVIEW_LEDGER.md` and `docs/ops/SOURCE_MANIFEST.md` remain separate evidence files.
- Entities/relations/migrations: No entity or relation was added. `sales.0009_bounded_free_text` preflights bounded offending IDs, then changes six former unbounded text columns without copying or rewriting stored values. Current heads are `accounts.0002_user_role_constraint`, `auditlog.0002_activitylog_role_snapshots`, and `sales.0009_bounded_free_text`.
- Endpoints: Approved route set is unchanged. Oversized API input now fails with stable HTTP 413; malformed/deep JSON fails safely before view mutation. Production still omits schema/docs routes.
- Authorization: Existing CRM identity, role/object/filter, Company IT audit hiding, Manager fail-closed, and last-active-Platform-Admin rules are unchanged. The backup service has a separate read-only database trust boundary and receives no application or migration credential.
- Checks run: full fast suite found 175 and passed; test-settings system check passed; migration dry-run found no changes; OpenAPI validation with warnings fatal passed; collectstatic dry run passed; `pip check` passed; strict safe-value production deploy check passed; bootstrap/backup shell syntax passed; digest-input validator passed with test-only references; exact 114-path current-worktree manifest had 45 modified and 69 added paths, no delete/rename, no blocked path, and no high-confidence secret-pattern match.
- Assumptions/blockers: Current source rejects mutable/missing production image references but real reviewed digest values, package hashes, SBOM, and scans remain external. Native PostgreSQL, Docker/Nginx, certificate/TLS, browser, real backup/restore, volume, capacity, and load proof remain external. Backup policy and the listed business rules remain decisions. No commit was created.
- Candidate state: Work in progress. The active-file ledger is complete at 152/152 and the current worktree has exact path evidence, but the active operations follow-up and fresh final source audit remain due. One user-approved immutable release reference and proof regenerated against it are still required before a production-candidate claim.
- Exact next task: Finish the active Phase 8-9 operations follow-up, refresh changed-path/test evidence, rerun the final repository audit, then request or use an authorized immutable release reference. Phase 12 begins only on an approved disposable or target runtime.
- Exact next files: smallest affected operations/config/test slice, then `docs/ops/SOURCE_MANIFEST.md`, `FILE_REVIEW_LEDGER.md`, release/checklist/worklog evidence, and external proof records.
- Exact next commands: focused operations tests and shell syntax; full test/check/drift/schema/static/package/deploy/diff gates; exact manifest/policy/secret scans; then approved PostgreSQL, Compose digest pull/boot/write-stop, Nginx/TLS, guarded backup/disposable restore, browser, load, and scanner proof.

## 2026-08-09 - Goal checkpoint 006

- Roadmap phase/task: Phases 8-10 final recovery, rollback, and source-audit closure; Phase 11 exact current-worktree package refresh.
- Files inspected: current/candidate deployment order; base, write-stop, and standalone restore Compose definitions; backup/restore/bootstrap scripts; shared restore SQL; rollback/release/TLS/backup runbooks; focused operations tests; all durable truth and release-evidence files.
- Operations changes verified in source: current-compatible write-stop, backup, and disposable restore now precede candidate input, pull, database start, role/ACL work, and migration; the default restore has no network or database secret, mounts backup data read-only, and builds only a bounded tmpfs cluster; both restore tools share exact checks for nine tables, three heads, ten constraints, and two partial unique phone indexes.
- Recovery changes verified in source: host retention recalculates the archive hash before exact-pair deletion; application rollback persists the prior image in protected input, stops Nginx, regenerates shared static with the prior image, recreates web, then restarts Nginx with write-stop still on and proves the on marker before any reopen.
- Durable evidence refreshed: `FILE_REVIEW_LEDGER.md` has 164 unique existing paths; `docs/ops/SOURCE_MANIFEST.md` exactly matches all 117 changed/untracked paths, comprising 45 modified and 72 added paths; current roadmap, checklist, map, assumptions, blockers, discovery, cleanup proof, release notes, and this worklog use the same facts.
- Entities/relations/migrations: No entity, relation, or migration was added in this follow-up. Heads remain `accounts.0002_user_role_constraint`, `auditlog.0002_activitylog_role_snapshots`, and `sales.0009_bounded_free_text`.
- Endpoints and authorization: No route, business transition, CRM role, object scope, or credential scope changed. The restore job has no business database target or credential. The backup job remains read only.
- Checks run: fresh full fast suite found 177 and passed; focused recovery/runbook tests passed; test-settings check, migration drift, OpenAPI warnings-fatal validation, static dry run, `pip check`, strict safe-value production warnings-fatal check, four-image reference validation, three-shell syntax, exact manifest comparison, forbidden-path scan, high-confidence file-name-only secret scan, and diff check all passed.
- Audit: Fresh final source review scored 9/10 with no firm repository P0/P1 after the rollback write-stop repair. Docker/PostgreSQL/Nginx/TLS/browser/live-restore proof remains external and is not claimed.
- Assumptions/blockers: Existing-data deployment must keep the recovery gate before candidate mutation. Exact backup policy, runtime owners/targets, real reviewed digests/hashes/scans, business decisions, and one user-approved immutable release reference remain open. No commit, stage, push, volume change, database write, or external action occurred.
- Candidate state: Work in progress. Repository-controlled source gates are at 9/10, but Phase 11 cannot close until an exact authorized release reference and regenerated reference-bound proof exist. Production readiness is not claimed.
- Exact next task: With user authorization, bind the 117-path source package to one exact release reference and rerun the reference-bound repository checklist. Then run Phase 12 only on an approved disposable or target runtime; otherwise implement only newly approved business-decision slices.
- Exact next files: authorized release/reference evidence and Phase 12 proof records; source/config/test files only if a real runtime failure appears.
- Exact next commands: exact reference status/diff/staged/path/secret gates; then approved `scripts/test-postgres.ps1`, digest pull/Compose boot, write-stop smoke, Nginx/TLS checks, profile backup plus no-network disposable restore, browser smoke, load proof, SBOM, and current scanners.

## 2026-08-10 - Goal checkpoint 007

- Roadmap phase/task: Phase 1 full-field ledger repair; Phase 11/State B blocker and evidence-truth repair while current backend and operations batches settled.
- Files inspected: durable goal field requirements; the five durable files in this batch; current Git/source-manifest sets; `common/throttles.py`; `common/tests/test_postgres_concurrency.py`; `common/tests/test_sensitive_throttles.py`; `sales/migrations/0010_interaction_contract.py`; `sales/tests/test_scope_attacks.py`; focused backend/operations results.
- Files changed in this batch: `FILE_REVIEW_LEDGER.md`, `BLOCKERS.md`, `PRODUCTION_READINESS_CHECKLIST.md`, `PROJECT_ROADMAP.md`, and this append-only `WORKLOG.md`. No code, backend contract doc, operations runbook, migration, or source-manifest file was edited by this batch.
- Durable proof changes: the ledger has explicit columns for every required goal field, a defined `N/A` rule, one separate exact Phase 1 link per row, and 169 unique rows matching the current scoped first-party set. The dual State B register mirrors 28 IDs: BIZ-001 through BIZ-013, SRC-001 through SRC-002, EXT-001 through EXT-007, and OPS-001 through OPS-006. Literal repository, UI, PostgreSQL, stack, restore, edge, reference, and bootstrap command sets plus exact manual proof contracts are recorded.
- Business decision added: BIZ-013 blocks any guessed user-deactivation/assigned-Lead transition. Current behavior preserves the exact assignee and assignment history, performs no implicit reassignment, and requires an explicit audited elevated reassignment until a block/reassign/temporary-owner rule is approved.
- Entities/relations/migrations: This documentation batch changes no data. It records the new `sales.0010_interaction_contract` head and twelve-constraint restore contract; target-engine zero/upgrade/constraint proof remains external.
- Endpoints and authorization: This documentation batch changes no route or permission. It records sensitive throttling for user writes, Lead reassignment, Sale create/cancel, audit reads, report JSON, and XLSX; it also records inactive-target reactivation and the fail-closed BIZ-013 boundary.
- Checks run: ledger parser found 169 rows, 169 unique paths, zero malformed column/link rows, and exact 169/169 scoped path coverage. Both blocker registers contain the same 28 IDs with zero missing/extra rows. Fresh full fast suite found and ran 199 tests with all passing and six PostgreSQL-only tests skipped on SQLite. Focused operations suite reported 33 pass. Assigned-file `git diff --check` passed with line-ending conversion warnings only.
- Evidence held back: the current Git set has 122 real candidate paths, 45 modified and 77 added, with no temporary wheel path. The prior 117-path source manifest is therefore stale by five exact first-party paths. Phase 1, current schema/final audit, known-limit closure, and production-candidate gates remain `IN_PROGRESS` or `PENDING` until the main final refresh regenerates that manifest and completes all later tests/gates.
- Assumptions/blockers: No immutable release ref is authorized; no commit/stage/push, external action, database write, or destructive cleanup occurred in this batch. All business/human/external inputs are explicit in the mirrored register. Work in progress remains the only allowed claim.
- Exact next task: After all remaining test/scan additions settle, refresh the ledger only for any real new path, regenerate the exact source manifest and release notes, rerun schema/full/diff/path/secret/final-audit gates, then update held statuses. Use `C-REF` only after explicit user authorization.

## 2026-08-10 - Goal checkpoint 008

- Roadmap phase/task: Phase 1 live-path correction after the PostgreSQL-contract, read-only load, and external-scan source/test batches landed; Phase 10/11 proof wording and exact State B close commands refreshed.
- Files inspected: the nine new PostgreSQL-contract/load/scan/migration-test paths plus the updated native PostgreSQL harness; the five durable files assigned to this batch; current Git and source-manifest sets.
- Files changed in this documentation batch: `FILE_REVIEW_LEDGER.md`, `BLOCKERS.md`, `PRODUCTION_READINESS_CHECKLIST.md`, `PROJECT_ROADMAP.md`, and this append-only `WORKLOG.md`. No application code, backend contract doc, operations runbook, migration, source manifest, or release note was edited by this batch.
- Durable proof correction: checkpoint 007 was an interim 169-row/122-path snapshot. The bounded active-file parser now has 179 unique existing goal-field rows, zero malformed rows, and one exact Phase 1 link for every row. Phase 1 remains `IN_PROGRESS`; the old manifest and final settled audit still block verification.
- Exact close contracts: the mirrored register still has 28 IDs with no invented closure. BIZ-010 now names exact target/path/workload/threshold/window/owner/abort/tool inputs and `C-LOAD`. EXT-001 now requires a compatible PostgreSQL bin containing `initdb`, `pg_ctl`, `psql`, `createdb`, and `pg_dump` plus compatible Bash; `C-PG` passes both approved paths. EXT-007 now names the exact digest/host/evidence/reviewer inputs and `C-SCANS`. BIZ-013 still forbids guessed assigned-Lead movement on user deactivation.
- Source behavior recorded, not changed here: the guarded PostgreSQL harness source-covers the full target-engine suite, 0004-to-0010 upgrade, bootstrap/finalizer, ACL/denials, dump, and injected-failure rollback. The bounded harness sends GET only to three health paths and has no credential/body/proxy/redirect support. The scan runbook binds a clean commit and digest-pinned images to restricted, sealed, reviewed evidence.
- Checks run: 26 focused load/PostgreSQL-harness/guard/migration-preflight tests passed; five focused security-scan runbook tests passed. Ledger path/schema/link proof is exact at 179/179. The final blocker mirror and assigned-file whitespace proof are run after this append.
- Evidence held back: the full 199-test run is interim because later tests enlarged the suite. Current Git has 134 candidate paths, 47 modified and 87 added. The prior 117-path manifest is stale by 17 exact paths with zero stale rows; the temporary wheel path is absent. OpenAPI, combined full suite, final source scan/audit, manifest, and immutable-ref gates remain pending.
- Entities, routes, authorization, and migrations: this documentation batch changes none. It records the existing `sales.0010` head, PostgreSQL proof harness, sensitive throttling, inactive-target reactivation, and fail-closed BIZ-013 boundary only.
- Candidate state: Work in progress. No commit, stage, push, external run, database write, or destructive action occurred in this batch.
- Exact next task: after the remaining live batch settles, reconcile any last real path, regenerate the exact source manifest and release notes, rerun combined full/schema/diff/path/secret/final-audit gates, then update held statuses. Run `C-REF` only after explicit authorization and Phase 12 commands only on approved external targets.
- Post-append proof: both canonical blocker tables have the same 28 unique expected IDs with zero missing, extra, or mirror difference; assigned-file `git diff --check` passes with line-ending conversion warnings only. The settled operations batch separately reports 47 linked tests passing plus check/no-drift and new-file whitespace proof.

## 2026-08-10 - Goal checkpoint 009

- Roadmap phase/task: Final backend and operations audit fixes, reference-bound manifest refresh, complete post-audit local release gate, and State B handoff. Phase 11 repository work is `DONE`; Phase 12 remains `BLOCKED` on exact external/human inputs.
- Reference/path truth: user-created pushed checkpoint `8f3c540efe7c1e1f80f31e0a1d991d0328dfe62e` equals `origin/main`. Its exact delta from durable base `50a978abc206e43032ce96b36dc0433366198e60` is 134 paths: 47 modified, 87 added, zero deleted, zero renamed. Final audit/evidence work is an uncommitted 25-modified-path overlay wholly inside that same 134-path set, so combined path cardinality stays 134 and SRC-002 remains open for one user-created final reference.
- Final backend fixes: fail-closed restored-schema SQL, exact reverse managed-role membership denial/proof/injection, JSON-only versioned API negotiation with typed `406`/`415`, and JSON error/throttle contracts for XLSX including `429`. The final real-CSRF login/logout test uses the exact JSON content type. No route or migration was added.
- Final operations fixes: the scan/release contract binds application, PostgreSQL, and Nginx runtime digests, the Python build-base digest, five scanner digests, a source-to-image build record, per-runtime SBOM/vulnerability results, public TLS proof, restricted sealed metadata/reports, reviewer disposition, and an out-of-band integrity anchor.
- Portable cache evidence: production uses the platform temporary root for one bounded 10,000-entry file throttle cache shared by workers in the approved single web container. Horizontal scale remains blocked on an approved shared store and new runtime proof.
- Durable evidence: `FILE_REVIEW_LEDGER.md` remains exact at 179 unique live rows with one Phase 1 link each. The source manifest records the 134-path pushed checkpoint plus exact final overlay. `BLOCKERS.md` and the readiness checklist mirror 28 BIZ/SRC/EXT/OPS IDs, including fixed inbound/outbound direction truth for BIZ-003 and no guessed Lead move for BIZ-013.
- Focused proof: backend audit ran 60 tests; operations audit ran 31; root overlap ran 47; the real-CSRF correction ran three focused tests. Each passed. Backend and operations audits both scored 9/10 and found no firm repository P0/P1.
- Final local release proof: the post-audit full fast suite found 232 and completed successfully with six PostgreSQL-only skips. Test-settings check, migration no-drift, UTF-8 warnings-fatal OpenAPI validation, collectstatic dry run, `pip check`, four-image validator, three Bash syntax checks, three PowerShell parses, safe-value production deploy check, and final diff/path/manifest parsers pass. Deploy check emitted only the deliberate HSTS subdomain/preload warnings; Bash under WSL emitted only its proxy warning.
- External proof boundary: Docker, Nginx, `initdb`, `pg_ctl`, `psql`, `pg_dump`, `pg_restore`, `createdb`, and `dropdb` are absent. No live PostgreSQL, container/edge/TLS/browser/load/backup/restore/scanner proof ran. No secret, volume, production data, or external system changed.
- Entities/relations/migrations: no final-audit entity, relation, or migration was added. Heads remain `accounts.0002`, `auditlog.0002`, and `sales.0010`; native PostgreSQL zero/upgrade/ACL/constraint execution remains EXT-001.
- Endpoint/OpenAPI and authorization: route set is unchanged. API behavior now returns typed safe JSON for unsupported media/accept negotiation and XLSX failures. Existing CRM actor/object boundaries stay fail closed; managed database roles cannot inherit another managed role; BIZ-013 remains a decision block.
- Candidate state: `production candidate; external verification pending`. This is State B only, not production ready and not one immutable deploy/rollback artifact. SRC-002 plus all explicit BIZ/EXT/OPS rows remain open. No automatic commit, stage, push, database write, or external action occurred for the final overlay.
- Exact next task: user creates and names one final immutable reference containing the 25-path overlay, then `C-REF` and `C-REPO` regenerate proof against it. Independent approved owners may then run only the exact Phase 12 close commands for the remaining BIZ/EXT/OPS rows.
