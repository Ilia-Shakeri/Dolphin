# تغییرنامه — ForooshBin / فروش‌بین

این فایل می‌گوید هر نسخه دقیقاً چه چیزی است: چه چیزی **اضافه** شد، چه چیزی
**تغییر** کرد، چه چیزی **حذف** شد و چه چیزی **رفع** شد.

شمارهٔ نسخهٔ در حال اجرا در فایل [`VERSION`](VERSION) در ریشهٔ مخزن است. همان فایل
را `config/settings.py` می‌خواند (`FOROOSHBIN_VERSION`)، در پاورقی پنل نمایش داده
می‌شود و در طرح‌وارهٔ OpenAPI می‌نشیند. برای بالا بردن نسخه فقط همان یک فایل تغییر
می‌کند.

## قاعدهٔ شماره‌گذاری

`MAJOR.MINOR.PATCH` — نسخه‌بندی معنایی، با این معنا برای این محصول:

| بخش | چه وقت بالا می‌رود | نمونه |
|---|---|---|
| **MAJOR** | قراردادی که مشتری روی آن حساب کرده می‌شکند: اختیارِ یک نقش عوض می‌شود، یک قاعدهٔ حسابداری تغییر معنا می‌دهد، یا مهاجرتی لازم است که برگشت‌پذیر نیست | ۰.x → ۱.۰.۰ |
| **MINOR** | قابلیت تازه‌ای اضافه می‌شود و کار قبلی سر جایش می‌ماند؛ مهاجرت‌ها فقط افزایشی‌اند | ۱.۰.۰ → ۱.۱.۰ |
| **PATCH** | فقط رفع اشکال یا اصلاح مستندات؛ نه فیلدی اضافه می‌شود نه رفتاری عوض | ۱.۱.۰ → ۱.۱.۱ |

## کدام نسخه، کجا

| محیط | نسخه | تاریخ |
|---|---|---|
| صحنه‌آزمایی (Nerkhbaan) | `1.2.1` | ۲۰۲۶-۰۸-۲۶ |
| TIARA (تولید) | `1.0.0` | ۲۰۲۶-۰۸-۲۰ |

نسخه‌های پس از `1.0.0` هنوز به TIARA نرفته‌اند. `1.3.1` هنوز روی صحنه‌آزمایی هم نرفته است.

> **دربارهٔ شماره‌های پیش از ۱.۰.۰:** این پروژه تا زمان تحویل تگ نسخه نداشت.
> شماره‌های دوره‌های پیش از تحویل **بعداً و از روی تاریخچهٔ واقعی Git** گذاشته
> شده‌اند تا بشود مسیر کار را خواند. آن‌ها هرگز به‌عنوان نسخهٔ منتشرشده تحویل کسی
> نشده‌اند؛ تنها نسخه‌ای که واقعاً تحویل شده `1.0.0` است.

---

## [1.4.0] — ۲۰۲۶-۰۸-۳۱

میزهای مالی، آن‌طور که واقعاً خوانده می‌شوند. **بدون مهاجرت.**

MINOR است نه PATCH: فیلتر تازه روی API، گذارهای تازه در چرخهٔ چک، و رفتار تازه
در «پرداخت شده» فاکتور — همه افزایشی، هیچ قراردادی نشکسته.

### فهرست‌ها — دریافت‌ها، پرداخت‌ها، چک‌ها، فاکتورها

- مقادیر هر ستون **وسط‌چین** شد. یک‌بار روی خودِ جدول، نه روی تک‌تک سلول‌ها، تا
  ستونی که بعداً اضافه شود هم بدون یادآوری وسط‌چین باشد.
- ستون «زمان» شد **«تاریخ ثبت»** و ساعت از مقدارش برداشته شد.
- **وضعیت چک، وضعیت خودِ چک است.** خرج کردن یک چک، سند دریافتی را که با آن آمده
  می‌بندد، پس چکِ زندهٔ خرج‌شده «ابطال‌شده» خوانده می‌شد. حالا روی روش چک همان
  مقداری دیده می‌شود که صفحهٔ چک‌ها نشان می‌دهد: «خرج شده».
- ستون «سررسید» در فاکتورها ستونی از خط تیره بود؛ جایش را **«نوع فاکتور»** گرفت
  و کنارش فیلتر رسمی/غیررسمی نشست.

### جزئیات دریافت و پرداخت

- **پدینگ**: کارت‌ها `card-body` نداشتند، برای همین input‌ها به لبهٔ کارت چسبیده
  بودند.
- **راست‌چین**: فیلدهای پولی `dir="ltr"` دارند تا ارقامشان جابه‌جا نشود، و همین
  آن‌ها را تنها فیلدهای چپ‌چینِ یک ستون راست‌چین کرده بود. حالا `text-align`
  جداگانه ست می‌شود.
- **یک فرم تخصیص به‌جای دو تا.** «تخصیص به فاکتور» و «تقسیم بین چند فاکتور» یک
  عملیات با تعداد سطر متفاوت بودند زیر دو عنوان. حالا یک فرم است، انتخاب فاکتور
  **قابل جستجو با شمارهٔ فاکتور** است، و با یک سطر باز می‌شود.
- «ابطال دریافت» حذف شد. در میز پرداخت‌ها کل بخش تخصیص حذف شد، چون فاکتوری برای
  تسویه وجود ندارد.
- روی روش چک، کنترل «وضعیت» سند پنهان است؛ وضعیت چک تنها وضعیت روی صفحه است.
- **پرداخت دیگر خودش را دریافت صدا نمی‌زند.** هر دو میز یک مسیر جزئیات دارند و
  سایدبار با پیشوند URL تطبیق می‌داد. حالا view جهت سند را می‌خواند و صفحه
  عنوان، مسیر بازگشت و `data-nav-match` را از آن می‌گیرد.

### فاکتور

- **«پرداخت شده» دیگر تایپ نمی‌شود.** عددی که اپراتور وارد می‌کرد فاکتور را
  تسویه‌شده نشان می‌داد بدون آنکه سندی، تخصیصی یا ردی در دفتر ساخته شود. حالا
  جمع تخصیص‌های میز دریافت‌هاست و «مانده» از آن پیروی می‌کند.
- **نرخ مالیات هرگز به فاکتور نمی‌رسید.** فرم آن را می‌گرفت، سرویس قبولش داشت، و
  درخواستِ میان این دو اصلاً حملش نمی‌کرد.
- چند کالا در یک فاکتور، مشتریِ قابل جستجو، و **تخفیف درصدی** که روی
  `discount_percent` خودِ سطرها می‌نشیند — بدون ساختن مفهوم تازه در سرور.
- «سفارش» و «سررسید» از کارت جزئیات حذف شدند.

### چک — عملیات قابل برگشت شد

«وصول» و «خرج کردن» پایانی بودند، پس یک دکمهٔ اشتباه برای همیشه می‌ماند. حالا هر
وضعیت می‌تواند به **«در انتظار»** برگردد، و فقط به آنجا.

**پول هم برمی‌گردد**، که مهم‌ترین بخش است: خروج از «وصول» اعتبارِ ثبت‌شده را پس
می‌گیرد و پرداخت به PENDING برمی‌گردد تا چک بتواند دوباره وصول شود — بدون این،
اصلاح اعمال می‌شد و هیچ پولی جابه‌جا نمی‌شد. دفتر append-only است، پس هر سه حرکت
خوانا می‌مانند.

### ثبت پرداخت — ترتیب سؤال‌ها

روی روش چک، **نوع چک** اول می‌آید چون شکل بقیه را تعیین می‌کند، و **شماره چک**
کنارش. زیر آن عنوان **«اطلاعات گیرنده»**:

- **چک مشتری**: گیرنده + تاریخ پرداخت. «مبلغ» حذف شد — مبلغ همان چک است و پرسیدن
  دوبارهٔ آن دعوت به عددی است که با سند خرج‌شده نمی‌خواند.
- **چک تازه**: گیرنده + مبلغ. «تاریخ پرداخت» حذف شد — هنوز چیزی پرداخت نشده؛ چک
  سررسید خودش را دارد.

میز دریافت‌ها دست‌نخورده ماند: چکِ دریافتی همچنان طرف حساب، مبلغ و تاریخ ورود
خودش را دارد.

**خرج کردن چک آن را ثبت‌شده هم می‌کند.** چکی که از دست ما خارج شده، به‌تعریف ثبت
شده است؛ پیش از این «ثبت نشده» می‌ماند و صفحهٔ چک‌ها سندی را که رفته بود، نافایل
نشان می‌داد.

### انجام نشد — «تاریخ صدور» به‌عنوان input

`Invoice` قید `invoice_draft_has_no_issue_time` را دارد: `issued_at` یک پیش‌نویس
باید NULL باشد، چون صدور همان چیزی است که آن را می‌گذارد. پذیرفتن این فیلد باعث
می‌شد هر ذخیره قید را بشکند و به شکل «شمارهٔ سند تکراری» گزارش شود.

نگه‌داشتن تاریخِ اعلامیِ اپراتور یا آن قید را می‌خواهد یا فیلدی جدا که تا لحظهٔ
صدور نگهش دارد. هر دو تصمیم کسب‌وکار است نه سیم‌کشی، پس ساخته نشد و یک تست دلیلش
را ثبت می‌کند.

---

## [1.3.13] — ۲۰۲۶-۰۸-۲۹

سایدبار جمع‌شده، همان‌طور که خودِ قالب دارد. **بدون مهاجرت.**

### اضافه شد — باز شدن موقت با هاور

با `data-kt-app-sidebar-hoverable="true"` روی `<body>`. این قابلیتِ خودِ قالب
است و تماماً در CSS آن پیاده شده؛ هیچ جاوااسکریپتی پشتش نیست. نشانگر که روی
نوار برود باز می‌شود و که برود بسته می‌شود، هر دو با انیمیشن.

### حذف شد — لوگویی که دکمه می‌شد

۱.۳.۱۱ نشان را دکمه کرده بود که با هاور به چِوران تبدیل می‌شد و سایدبار را باز
می‌کرد. پس گرفته شد: لوگویی که زیر نشانگر به کنترلِ دیگری تبدیل می‌شود به کاربر
نمی‌گوید کدام‌یک را قرار است بگیرد. حالا دوباره یک لینک ساده به خانه است، و باز
کردن بر عهدهٔ همان دکمه‌ای است که بسته بود — که در حالت جمع‌شده دیده می‌شود.

### رفع شد — علتِ واقعی، بعد از دو نسخه

سایدبار باریک نمی‌شد و ۱.۳.۱۱ و ۱.۳.۱۳ هر دو قاعده نوشتند تا کاری را به زور
انجام دهند که قالب از قبل بلد بود: عرض ثابت، `min-width: 0`، `overflow-x`،
وسط‌چین کردن آیکون‌ها، برچسب شناور برای هر عنوان، و رنگ و مکانِ دستیِ دکمه.

علت یک خط مارک‌آپ بود: لینکِ برند کلاس `d-flex` بوت‌استرپ را داشت که
`!important` است و بر `display: none` قالب غلبه می‌کرد؛ بلوکِ عریض در جریان
می‌ماند و — چون آیتم فلکس `min-width: auto` دارد — سایدبار را به اندازهٔ محتوای
خودش باز نگه می‌داشت. `d-flex` به یک `span` داخلی منتقل شد و همهٔ آن قاعده‌ها
غیرلازم شدند.

### باقی ماند — یک ایراد در خودِ قالب

قالب عرض را از یک اعلان می‌گیرد و با تغییر متغیر جمع می‌کند. **جمع شدن کار
می‌کند، باز شدن نه**: متغیر به ۲۶۵ برمی‌گردد و عرضِ استفاده‌شده روی ۷۵ می‌ماند،
یعنی سایدبارِ جمع‌شده هرگز باز نمی‌شود.

در دموی خودِ فروشنده — که به‌صورت محلی سرو شد و هیچ کدی از این پروژه در آن نبود —
همین اندازه‌گیری شد: بعد از toggle خودش، متغیر ۲۶۵px و عرض محاسبه‌شده ۷۵px.
پس برای هر حالت یک اعلان `width` جدا نوشته می‌شود تا چیزی که عوض می‌شود *قاعدهٔ
منطبق* باشد نه مقدار داخل یکی. در هر دو جهت انیمیشن دارد.

> **تصحیح.** در جریان همین کار یک بار نتیجه گرفتم این ادعا غلط است و رفعش را
> برداشتم؛ اشتباه بود و فقط جهتِ جمع شدن آزموده شده بود.

### رفع شد — جمع کردن که خودش را باطل می‌کرد

دکمهٔ toggle روی لبهٔ سایدبار است ولی فرزندِ آن است، پس نشانگری که «جمع کن» را
زد هنوز داخل سایدبار است و هاور بلافاصله بازش می‌کند. سایدبار هم چون باز می‌ماند،
دکمه هرگز از زیر نشانگر کنار نمی‌رود. در مرورگر واقعی دیده شد: کلیک ویژگی را
عوض کرد و عرض تا وقتی نشانگر تکان نخورد روی ۲۶۵ ماند. قالب برای همین ۳۰۰
میلی‌ثانیه `.animating` می‌گذارد، که برای انیمیشن کافی است و برای نشانگرِ ساکن نه.

حالا تا وقتی نشانگر واقعاً بیرون برود، `data-kt-app-sidebar-hoverable` از `body`
برداشته می‌شود. با ویژگی، نه با یک کلاسِ خودمان: همهٔ قاعده‌های peek در قالب به
همین ویژگی گره خورده‌اند، پس برداشتنش هم عرض و هم محتوای بازشده را با هم خاموش
می‌کند — کلاسی که با `:hover` بجنگد فقط عرض را می‌گرفت و برندِ عریض داخل جعبهٔ
۷۵ پیکسلی رندر می‌شد.

### تغییر کرد — دکمه، با کلاس‌های خودِ قالب

`position-absolute top-50 start-100 translate-middle rotate`. این یک
`inset-inline-start` دستی را بازنشسته کرد که دو بار باگ RTL ساخته بود، و چرخش
چِوران را از `[dir=rtl] .app-sidebar-toggle.active i` می‌گیرد نه از قاعدهٔ ما.

### شواهد

کنار هم با دموی فروشنده اندازه‌گیری شد: نوار ۷۵، wrapper ۲۶۵ که نوار برشش
می‌زند، آیکون‌ها داخل نوار، عنوان‌ها `opacity: 0`، برند عریض پنهان. peek تست
مرورگر واقعی دارد، چون رویداد ساختگی `:hover` تولید نمی‌کند و فقط `ActionChains`
نشانگر واقعی را حرکت می‌دهد.

---

## [1.3.12] — ۲۰۲۶-۰۸-۲۹

نمودارها با کتابخانهٔ خودِ قالب خریداری‌شده کشیده می‌شوند. **بدون مهاجرت.**

### تغییر کرد — ApexCharts جای SVG دست‌نویس را گرفت

تا ۱.۳.۱۱ نمودارها با SVG دست‌نویس کشیده می‌شدند تا باندل ۳٫۶ مگابایتی قالب وارد
نشود. مالک محصول پس از دیدن همین هزینه، صریح گفت نمودارهای خودِ قالب را می‌خواهد
و اندازهٔ پنل مهم نیست. این نسخه همان است.

- **ApexCharts فقط داخل `plugins/global/plugins.bundle.js` هست** — قالب نسخهٔ
  مستقلی از آن ندارد. پس کل باندل بار می‌شود: وزن دارایی‌های پنل از حدود ۵۷۶
  کیلوبایت به حدود ۴٫۲ مگابایت می‌رود و jQuery و select2 و dropzone هم می‌آیند
  که هیچ‌کدام در این پنل استفاده نمی‌شوند.
- هر سه شکل (`renderDonutChart`, `renderAreaChart`, `renderBarChart`) امضای
  قبلی‌شان را نگه داشتند، پس هیچ فراخوانی‌ای عوض نشد.
- نوار افقی شد، چون برچسب‌ها نام‌های فارسی با طول دلخواه‌اند.

### رفع شد — باندل از ایمیج بیرون می‌ماند

`plugins.bundle.js` در **چهار جا** به‌عنوان «بارگذاری‌نشده» علامت خورده بود:
`STATICFILES_COLLECT_IGNORE`، `.dockerignore`، `scripts/validate_image_content.py`
و `common/tests/test_static_assets.py`. اگر فقط تگ اسکریپت اضافه می‌شد، در
توسعه کار می‌کرد و **در ایمیج تولید، هر نمودار ۴۰۴ می‌گرفت** — همان شکستی که
`EXPECT_PRESENT` برای آن نوشته شده بود. هر چهار جا اصلاح شد و
`validate_image_content --context` سبز است.

### رفع شد — نمودار با تغییر پوستهٔ پنل عوض نمی‌شد

Apex رنگ‌ها را هنگام کشیدن داخل SVG می‌نشاند، از جمله رنگ متن. نموداری که در
حالت روشن کشیده شده بود، بعد از رفتن به تاریک همان جوهر روشن را نگه می‌داشت و
عدد وسط دونات با `--bs-gray-800` روی کارت تیره **تقریباً نامرئی** می‌شد.

حالا یک `MutationObserver` روی `data-bs-theme` هر نمودار زنده را دوباره می‌کشد.
اندازه‌گیری شد: روشن `#1B84FF/#17C653/#7239EA/#F6C000` ↔ تاریک
`#006AE6/#00A261/#883FFF/#C59A00`، و جوهر مرکز `#252f4a` ↔ `#b5b7c8`.

### رفع شد — قلم فارسی در نمودارها

`plugins.bundle.rtl.css` خودِ قالب روی `.apexcharts-text` قلم `Inter` را با
`!important` می‌گذارد و همان بر استایل inline که Apex از `chart.fontFamily`
می‌سازد غلبه می‌کند. **Inter هیچ گلیف فارسی ندارد**، پس هر برچسب محور به قلم
جایگزینِ سیستم می‌افتاد و با بقیهٔ کارت فرق می‌کرد. یک قاعده این را برمی‌گرداند.

ممنوعیت `font-family:` در شیوه‌نامهٔ پوششی **حذف نشد، باریک شد**: هر
`font-family` باید روی گره‌های `.apexcharts-*` باشد. شیوه‌نامه هنوز حق ندارد
قلم پنل را انتخاب کند؛ فقط حق دارد یک انتخابِ غلطِ فروشنده را پس بزند.

### تغییر کرد — انیمیشن ورود خاموش شد

Apex نمودار را با `requestAnimationFrame` از حالت خالی به هندسهٔ واقعی می‌برد.
نموداری که جایی سوار شود که فریم تولید نمی‌شود — تب پس‌زمینه، کارتِ هنوز پنهان،
مرورگر headless — **برای همیشه روی فریم خالی می‌ماند**. دقیقاً همین اندازه‌گیری
شد: مسیر هر میله `M0.101 … L0.101`، عرض صفر.

با خاموش شدن آن، همان میله ۷۱۶ پیکسل شد و چهار سطل صفر واقعاً صفر ماندند. ضمناً
چون تغییر پوسته حالا هر نمودار را از نو می‌کشد، نگه‌داشتنش یعنی تکرار نیم‌ثانیه
انیمیشن روی همهٔ نمودارها در هر بار جابه‌جایی روشن/تاریک.

### تغییر کرد — رنگ‌ها معنا دارند

- **سنی مطالبات**: پنج رنگ که بالا می‌روند — سبز، آبی، زرد، نارنجی، قرمز. پیش‌تر
  دو سطل وسط هر دو زرد بودند، یعنی «۳۱ تا ۶۰» و «۶۱ تا ۹۰» به یک اندازه بد
  به نظر می‌رسیدند. نارنجی از `--bs-orange` می‌آید چون قالب رنگی در آن فاصله
  ندارد.
- **ترکیب سود**: درآمد آبی، بهای تمام‌شده زرد، سود ناخالص سبز.
- Apex میله‌ها را با شفافیت ۰٫۸۵ پر می‌کند؛ روی کارت روشن هر رنگ رنگ‌پریده
  می‌شد. حالا کدر است، وگرنه رمپ معنایی خوانده نمی‌شود.

### جهت محورها — و اینکه چرا برنگشت

نمودار دست‌نویسِ قبلی محور زمان را برعکس می‌کرد تا قدیمی‌ترین نقطه سمت راست
بنشیند. معادل Apex یعنی `xaxis.reversed: true` آزموده شد:

- روی نمودار ناحیه‌ای **بی‌اثر** است؛ اندازه‌گیری شد که قدیمی‌ترین دسته با وجود
  آن همچنان در `x=66` سمت چپ ماند.
- روی میلهٔ افقی **بدتر از بی‌اثر** است و هر میله را به عرض صفر جمع می‌کند.

خودِ قالب هم نه `reversed` می‌گذارد نه `opposite`، و نمودارهایش در بیلد RTL
چپ‌به‌راست خوانده می‌شوند. پس همهٔ اینها هم همان‌طورند. **این یک تغییر رفتار
واقعی نسبت به نسخهٔ دست‌نویس است، نه فراموشی.**

### تست‌هایی که عمداً به‌روز شدند

- `test_billing_shell_browser` سنجه‌هایش را از `div`های قدیمی به SVG خودِ Apex
  برد. برچسب‌ها از `tspan` خوانده می‌شوند نه از گروه، چون Apex کنار هر برچسب یک
  `<title>` می‌گذارد و متنِ گروه هر نام را **دوبار** دارد.
- `test_auth_shell_browser` کمکی `chart_names()` گرفت که منتظر کشیده شدن Apex
  می‌ماند؛ خواندن مستقیم `.text` با رسم ناهمگام مسابقه می‌داد.
- استثنای «تنها URL مجاز در اسکریپت» **بازنشسته شد**: آن استثنا برای فضای‌نام
  XML بود که `createElementNS` لازم داشت، و با رفتن رسمِ دست‌نویس دیگر چیزی برای
  پوشاندن ندارد. حالا اسکریپت **هیچ** رشتهٔ URL‌شکل ندارد.
- چهار تست تازه: باندل بار می‌شود، **پیش از** `scripts.bundle.js` بار می‌شود،
  رسم با `new ApexCharts(` است نه `createElementNS`، و هر نمودار پیش از
  استفادهٔ دوبارهٔ ظرفش `destroy()` می‌شود.

### رفع شد — نشتی نمودار روی هر فیلتر

Apex بیرون از فرزندانِ ظرف هم DOM و شنونده نگه می‌دارد، پس `replaceChildren()`
تنها پاکش نمی‌کند. حالا هر رسمِ دوباره نمونهٔ قبلی را `destroy()` می‌کند —
وگرنه هر بار اعمالِ فیلتر یک نمودار زندهٔ رهاشده جا می‌گذاشت.

---

## [1.3.11] — ۲۰۲۶-۰۸-۲۹

سایدبار جمع‌شده که واقعاً خوانا باشد، و پروفایل که از داشبورد به منوی کاربر
منتقل شد. **بدون مهاجرت.**

### رفع شد — سایدبار جمع‌شده خالی به نظر می‌رسید

آیکون‌ها **گم نشده بودند؛ بیرون از نوار کشیده می‌شدند.**

قالب عمداً `wrapper` را در حالت جمع‌شده روی عرض کامل نگه می‌دارد و می‌گذارد
سایدبارِ ۷۵ پیکسلی برشش بزند — این‌طوری حالت hover-to-peek خودش کار می‌کند، که
این پنل از آن استفاده نمی‌کند. نتیجه: لینک‌ها ۲۳۴ پیکسل می‌ماندند و
`justify-content: center` هر آیکون را وسط همان ۲۳۴ پیکسل می‌گذاشت.

اندازه‌گیری شد: آیکون روی **x=1447** بود در حالی که نوار قابل‌دیدن **۱۵۱۰ تا
۱۵۸۵** بود. با باریک کردن منو به عرض نوار، هر ۱۱ آیکون داخل آمدند.

### تغییر کرد — نشان، خودش دکمهٔ باز کردن است

در حالت جمع‌شده تنها چیزِ بالای نوار همان نشان است، پس کلیکِ بدیهی باید کار
مفید را بکند. حالا:

- **`<button>` است، نه لینک.** «رفتن به داشبورد» از قبل اولین گزینهٔ خودِ منوی
  زیرش است.
- **با هاور، یک چِوران رویش می‌آید** تا پیش از فشرده شدن بگوید چه می‌کند.
- **دکمهٔ شناور جمع/باز قبلی در حالت جمع‌شده پنهان می‌شود** — دو راه برای یک کار،
  روی هم، گیج‌کننده است.
- باز کردن از طریق همان toggle واقعی انجام می‌شود، پس `KTToggle` همچنان تنها
  صاحب وضعیت است و همان کوکی را می‌نویسد. **دو کنترل، یک منبع حقیقت.**

### اضافه شد — برچسب کنار آیکون‌ها

یک نوار ۷۵ پیکسلی با ۱۱ آیکون بی‌برچسب، از خواننده می‌خواهد ۱۱ شکل را حفظ کند.

حالا با هاور، **نام همان بخش کنار نشانگر شناور می‌شود** و آیکون هم روشن می‌شود.
نام از خودِ `.menu-title` می‌آید که از قبل در DOM است — نه یک `title` تازه — پس
یک منبع برای کلمه وجود دارد و تأخیر tooltip بومی هم در کار نیست.

> **یک اشتباه که دوباره تکرار شد.** برچسب اول با `inset-inline-end` نوشته شد و
> روی **x=1595** افتاد، بیرون از پنجرهٔ ۱۶۰۰ پیکسلی. زیر RTL، `inline-start`
> لبهٔ **راست** است؛ برای فرستادن برچسب به چپ باید `inset-inline-start` داد.
> همین اشتباه در ۱.۳.۸ روی پاپ‌آپ پوسته هم رخ داده بود.

### تغییر کرد — آدمک روی دکمهٔ کاربر

۱.۳.۸ خورشید/ماه پوسته را آنجا گذاشته بود، که کنترلِ حسابِ کاربر را شبیه یک کلید
پوسته می‌کرد. پوسته از قبل خودش را روی ردیف «حالت» داخل همان منو گزارش می‌کند،
که همان‌جایی است که خواننده دنبالش می‌گردد.

### تغییر کرد — «پروفایل من» یک مودال شد

از کارت داخل داشبورد به یک dialog با تم پنل منتقل شد که از منوی کاربر باز
می‌شود:

- **از هر صفحه‌ای در دسترس است.** قبلاً ویرایش پروفایل یعنی رفتن به داشبورد و از
  دست دادن کاری که در جریان بود.
- **در داشبورد جای درستی نداشت**: کنار اعداد شرکت می‌نشست، در حالی که فرمِ
  خودِ صاحب حساب است.
- **منتقل شد، نه کپی.** دو نسخه از یک فرم از هم جدا می‌افتند، و `setupProfile()`
  با id پیدایشان می‌کند — پس نسخهٔ دوم یعنی «کدام‌یک پر می‌شود» به ترتیب سند
  بستگی پیدا می‌کرد. یک تست همین را می‌بندد.
- **در اولین باز شدن بارگذاری می‌شود، نه در بارگذاری صفحه.** حالا در پوستهٔ همهٔ
  صفحه‌هاست؛ گرفتن زودهنگامش یعنی یک درخواست اضافه روی هر صفحه، برای فرمی که
  بیشتر بازدیدها اصلاً بازش نمی‌کنند.

### تست‌هایی که عمداً به‌روز شدند

پنج فایل تست مرورگر، «دیده شدن `#profile-form` روی داشبورد» را نشانهٔ پایان
ورود گرفته بودند. این همیشه یک نماینده بود — اتفاقاً آخرین چیزی که داشبورد رسم
می‌کرد — و با رفتن پروفایل به dialog دیگر درست نبود.

حالا روی **دکمهٔ حساب کاربری** صبر می‌کنند: در هر صفحه‌ای هست، و به جای هیچ فرمی
وابسته نیست. نامِ کاربر هم با `textContent` **خوانده** می‌شود نه دیده — چون آن
عنصر `d-none d-md-flex` دارد و انتظارِ «دیده شدن» روی موبایل هرگز برآورده
نمی‌شد.

`test_desktop_login_profile_and_logout_flow` حالا خودِ مودال را باز و بسته
می‌کند، چون این تست دربارهٔ پروفایل است.

### حذف شد — سقف تعداد اعلان‌های شیوه‌نامه

این عدد در چهار نسخه ۱۲۰ → ۱۴۰ → ۱۶۰ → ۱۸۰ رفته بود و هر بار هم با دلیل موجه.
در ۱.۳.۸ داخل خودِ تست نوشته شد: «پیش از بالا بردن دوباره، جایش را عوض کنید.»
این همان لحظه است.

سقفی که فقط بالا می‌رود، **رشد پنل را اندازه می‌گیرد نه انضباطش را** — و هر بار
یک بحث بازبینی می‌خواست که هیچ یافته‌ای تولید نمی‌کرد. قاعدهٔ واقعی
(«کامپوننت‌های قالب را از نو نساز») تست مستقل خودش را دارد و به عدد نیاز ندارد.
پیش از حذف، سه اعلان زائد هم برداشته شد — از جمله یک قاعدهٔ عرض که اندازه‌گیری
نشان داد **هیچ کاری نمی‌کرد**.

---

## [1.3.10] — ۲۰۲۶-۰۸-۲۹

رفع اشکالات ممیزی ۲۰۲۶/۰۸/۲۹. **بدون مهاجرت، بدون فیلد تازه، بدون تغییر رفتار
درست.** طبق جدول شماره‌گذاری همین فایل این یک **PATCH** است.

### رفع شد — صدور فاکتور روی PostgreSQL کار نمی‌کرد

`issue_invoice()` دکوراتور `@transaction.atomic` نداشت
(`billing/services.py`). روی PostgreSQL خطای
`TransactionManagementError: select_for_update cannot be used outside of a
transaction` می‌داد؛ یعنی **صدور هر فاکتوری خطای ۵۰۰ بود.**

یک **رگرسیون** بود: کامیت اولیهٔ `d391b9e` دکوراتور را داشت و `41411e0`
(نسخهٔ `1.2.0`) حذفش کرده بود. docstring خودِ تابع در تمام این مدت می‌گفت
«All three effects share this transaction with the status change» — یعنی مستند،
تضمینی را توصیف می‌کرد که کد نداشت.

### رفع شد — تغییر وضعیت چک روی PostgreSQL کار نمی‌کرد

`transition_cheque()` (`billing/payments.py`) همان مشکل را داشت. یعنی **هر چهار
دکمهٔ وضعیت چک** که در `1.3.7` اضافه شدند روی تولید خطا می‌دادند.

جدا از خطا، این تابع چهار چیز می‌نویسد — وضعیت چک، سطر تاریخچه، وضعیت پرداخت، و
سطر دفتر. بدون تراکنش، شکست وسط کار حالت ناسازگار به جا می‌گذاشت.

### رفع شد — ثبت پرداخت بدون `customer` خطای ۵۰۰ می‌داد

`register_payment()` آرگومان `customer` را **اجباری** می‌خواست، ولی از `1.2.1`
که این فیلد برای پرداختی nullable شد، DRF دیگر اجباری‌اش نمی‌دانست و وقتی کلید در
درخواست نبود اصلاً پاسش نمی‌داد → `TypeError` → ۵۰۰.

حالا پیش‌فرضش `None` است و قاعدهٔ «دریافتی مشتری می‌خواهد» — که چند خط پایین‌تر
بود و هرگز اجرا نمی‌شد — مثل هر قاعدهٔ دیگری ۴۰۰ برمی‌گرداند.

نکتهٔ ظریف: ارسال صریح `{"customer": null}` همیشه درست کار می‌کرد. فقط **نبودِ
کلید** می‌شکست، که دقیقاً همان شکلی است که یک فراخوان معمولی می‌فرستد.

### اضافه شد — گیتی که این دسته را می‌بیند

**مهم‌تر از هر سه رفع بالا.**

هر سه اشکال با ۱۰۸۱ تست سبز زنده بودند، چون تست‌ها روی SQLite‌اند و تولید روی
PostgreSQL. جنگو `select_for_update()` بیرون از تراکنش را فقط روی بک‌اندی رد
می‌کند که قفل ردیف دارد؛ SQLite عبارت را **بی‌صدا حذف می‌کند**.

`common/tests/test_row_locks_are_transactional.py` دو بررسی دارد:

- **ایستا:** کل درخت را با AST می‌خواند و هر تابع عمومی‌ای را که
  `select_for_update()` صدا می‌زند و تراکنش خودش را ندارد، با نام و شماره خط رد
  می‌کند. چون خواندنِ منبع است، توابعی را هم می‌گیرد که **هیچ تستی صدایشان
  نمی‌زند** — و همین بود که رگرسیون را زنده نگه داشت.
- **زمان اجرا:** مقدار `has_select_for_update` را روی اتصال `True` می‌کند تا
  جنگو دقیقاً همان بررسی PostgreSQL را اجرا کند، بدون نیاز به سرور PostgreSQL.

اثبات شد که کار می‌کند: با برگرداندن موقتِ رگرسیون ۱.۲.۰، تست دقیقاً
`billing/services.py:1068 issue_invoice()` را نام برد.

### رفع شد — نشت سطل throttle بین تست‌ها (باز هم)

سه کلاس پرترافیک `cache.clear()` در `setUp` نداشتند و بسته به ترتیب اجرا ۴۲۹
می‌گرفتند. همان الگوی ده کلاس دیگر این مجموعه.

### آنچه اشتباه گزارش شده بود و اصلاح می‌شود

ممیزی «N+1 روی فهرست کاربران» را گزارش کرده بود. **اشتباه بود.** با شمارش واقعی
کوئری سنجیده شد: **۶ کوئری برای ۲۵ ردیف** — ثابت، نه N+1. `crm_identities`
از `Exists()` استفاده می‌کند و serializer هیچ ForeignKey‌ای را دنبال نمی‌کند، پس
`select_related` چیزی برای انجام دادن ندارد. تغییری داده نشد.

مورد «۴۰۹ به‌جای ۴۰۰ روی تلفن تکراری» هم دست‌نخورده ماند: ۴۰۹ برای تعارض از نظر
معنایی درست است و عوض کردنش یک تصمیم محصولی است، نه رفع اشکال.

---

## [1.3.9] — ۲۰۲۶-۰۸-۲۸

سه پاسخ مالک محصول، پیاده‌شده. **بدون مهاجرت.**

### اضافه شد — یک سند، دیده‌شده از هر دو میز

چکی که از مشتری گرفته شده و بعد به دیگری داده شده، **یک سند است** و باید در هر دو
فهرست دیده شود: هم همان دریافتی است که ثبت شد، هم پولی که از مجموعه خارج شده.

پس میز پرداخت‌ها حالا `desk=disbursement` می‌پرسد، نه `direction=disbursement`، و
سرور آن را این‌طور می‌فهمد: پرداختی‌ها **به‌علاوهٔ** دریافتی‌هایی که چکشان خرج
شده.

> **این یک نمای دوم از همان ردیف است، نه ردیف دوم.** ثبت یک سند پرداخت برای خرج
> کردن چک، همان پول را دو بار می‌شمرد و حساب مشتری را بابت چکی بدهکار می‌کرد که
> اصلاً پول ما نبوده. برای همین `spend_received_cheque` هیچ‌چیز نمی‌سازد و این
> کار یک **کوئری** است، نه یک سند.

`direction` سرجایش ماند و کار خودش را می‌کند؛ آن دو، دو سؤال متفاوت‌اند. تغییر
وضعیت چک همچنان فقط از تب چک‌ها انجام می‌شود.

چکی که هنوز در انتظار است یا وصول شده، در میز پرداخت‌ها **دیده نمی‌شود** — اولی
هنوز دست ماست و دومی پولی است که آمده، نه رفته.

### تغییر کرد — ابطال یک‌طرفه شد

سند ابطال‌شده دیگر دوباره تأیید نمی‌شود؛ باید **از نو ثبت** شود.

۱.۳.۷ عکسش را اجازه می‌داد و یک بستانکاری تازه ثبت می‌کرد. از نظر حساب درست بود و
باز هم چیز درستی برای پیشنهاد دادن نبود: اپراتوری که ردیف اشتباه را ابطال کرده،
می‌توانست برش گرداند و جز دو سطر دفتر ردی نماند — در حالی که ثبت دوباره، سندی
می‌سازد که شماره‌اش می‌گوید واقعاً کِی وارد شده. گزینهٔ «تأییدشده» روی سند
ابطال‌شده حالا غیرفعال است، نه اینکه پیشنهاد شود و بعد رد.

### تغییر کرد — کاهش مبلغ دیگر رد نمی‌شود

تخصیص اجباری نیست: پول می‌تواند روی حساب مشتری بنشیند بی‌آنکه به فاکتوری بخورد.

پس مبلغ کمتر دیگر رد نمی‌شود. تخصیص‌هایی که جا نمی‌شوند **آزاد می‌شوند** تا آنچه
می‌ماند جا شود، و بقیه سرجایشان می‌مانند — آزادسازی از همان مسیر `release_allocation`
می‌گذرد، پس مبلغ پرداختی و وضعیت تسویهٔ هر فاکتور مثل آزادسازی دستی برمی‌گردد و
خودِ آزادسازی هم ثبت می‌شود.

**از جدیدترین شروع می‌کند**، چون تخصیص‌های قدیمی‌تر آن‌هایی هستند که اپراتور
به‌احتمال بیشتر هنوز منظورش است.

---

## [1.3.8] — ۲۰۲۶-۰۸-۲۸

سایدبار جمع‌شونده که واقعاً جمع می‌شد اما تغییری نشان نمی‌داد، و انتخاب پوسته که
حالا یک ردیف با پاپ‌آپ کوچک است. **بدون مهاجرت.**

### رفع شد — سایدبار با کلیک جمع نمی‌شد

کلیک روی دکمه، فقط برچسب‌های منو را پنهان می‌کرد؛ **عرض سایدبار تا بارگذاری بعدی
صفحه تکان نمی‌خورد.**

علتش دقیق است: عرض سایدبار `var(--bs-app-sidebar-width)` است و دکمه همان **متغیر
سفارشی** را عوض می‌کند، نه خودِ اعلان `width` را. کرومیوم عرضی را که از `var()`
حل می‌شود دوباره اعمال نمی‌کند وقتی یک `transition` روی `width` مسلح باشد. در
صفحهٔ زنده اندازه گرفته شد: **با هر کلیک متغیر از ۲۶۵ به ۷۵ می‌رفت و جعبه روی ۲۶۵
می‌ماند**؛ با خاموش کردن transition، همان کلیک‌ها بلافاصله کار کردند.

پس transition عرض حذف شد. جمع شدن حالا فوری است — که معاملهٔ درستی است: سایدباری
که فوری حرکت کند بهتر از انیمیشنی است که هرگز اجرا نمی‌شود. قالب همین transition
را داخل قاعدهٔ جمع‌شدهٔ خودش تعریف می‌کند، پس همان انتخابگر اینجا تکرار شده تا
وزنش بیشتر باشد. زیر نقطهٔ شکست دست‌نخورده است؛ آنجا سایدبار کشویی است و با
`transform` حرکت می‌کند.

### رفع شد — لوگوی ریز، «نمایش داده نمی‌شد»

نمایش داده **می‌شد**: به عرض یک ستون پیکسل و بلندتر از صفحه.

`<img>` صفت‌های `width="1278" height="1230"` را دارد تا مرورگر پیش از رسیدن فایل،
جای درست را رزرو کند. CSS فقط عرض را می‌داد، پس **صفت ارتفاع سرجایش می‌ماند** و
نشان ۲۹×۱۲۳۰ رندر می‌شد. یک `height: auto` روی `.brand-mark` کافی بود؛ حالا
۲۹×۲۸ است.

### تغییر کرد — انتخاب پوسته، یک ردیف با پاپ‌آپ

سه گزینه از حالت تخت داخل منوی کاربر درآمدند. حالا **یک ردیف با عنوان «حالت»**
هست که آیکون پوستهٔ فعلی را در لبه‌اش نشان می‌دهد، و با **هاور یا کلیک** یک
پاپ‌آپ کوچک کنارش باز می‌شود: روشن، تاریک، سیستم.

- **هاور و کلیک، هر دو.** فقط هاور، صفحهٔ لمسی و کاربر کیبورد را جا می‌گذاشت.
- **سمت باز شدن اندازه‌گیری می‌شود، نه فرض.** منوی کاربر به یک لبهٔ پنجره چسبیده،
  پس سمتِ ترجیحی همیشه سمتی نیست که جا دارد. اگر پاپ‌آپ از صفحه بزند بیرون، به
  سمت دیگر می‌چرخد.
- **آیکون ردیف به هیچ اسکریپتی نیاز ندارد**: هر دو آیکون در DOM هستند و قاعدهٔ
  `theme-light-show`/`theme-dark-show` خود قالب از روی `data-bs-theme` تصمیم
  می‌گیرد کدام دیده شود — پس از همان اولین فریم درست است.

> **یک نکته که وقت گرفت.** پاپ‌آپ کلاس `.menu-sub` قالب را دارد که تا وقتی KTMenu
> کلاس `.show` را اضافه نکند `display: none` است. اینجا هیچ‌کس `.show` اضافه
> نمی‌کند — اسکریپت خود پروژه بازش می‌کند — پس پاپ‌آپ **۰×۰** می‌ماند هر کاری که
> اسکریپت می‌کرد. حالا `display` صریحاً گفته شده و `[hidden]` هم کنارش.

### اضافه شد — نسخه روی آدرس CSS و JS

`forooshbin.css` و `forooshbin-app.js` حالا با `?v={{ forooshbin_version }}`
لینک می‌شوند.

بدون این، مرورگری که نسخهٔ دیروز را نگه داشته بعد از هر دیپلوی همان را استفاده
می‌کند و پنل، **مارک‌آپ تازه را روی CSS و اسکریپت کهنه** اجرا می‌کند. همین موضوع
در جریان همین کار سه بار وقت گرفت — تغییرها روی سرور بودند و در مرورگر نبودند.
باندل‌های فروشنده دست‌نخورده‌اند؛ آن‌ها یکجا و به‌ندرت عوض می‌شوند.

### سقف شیوه‌نامه از ۱۶۰ به ۱۸۰ رفت — و این بار یک تست دیگر هم اضافه شد

اول ۶ اعلان حذف شد: پاپ‌آپ دیگر فاصله‌ای از منو ندارد، پس آن `::before` که فقط
برای پل زدن روی همان فاصله وجود داشت، چیزی نمی‌خرید.

ولی این **سومین بار پیاپی** است که این سقف بالا می‌رود (۱۲۰ → ۱۴۰ → ۱۶۰ → ۱۸۰) و
هر بار هم با دلیل موجه. این یعنی **عدد، ابزار درستی نیست** — نه اینکه قاعده غلط
باشد. پس قاعدهٔ واقعی حالا **تست مستقل خودش** را دارد: «کامپوننت‌های قالب را
بازنساز». پیش از بالا بردن دوبارهٔ عدد، باید جایش را با همان تست عوض کرد؛ عددی که
فقط بالا می‌رود، رشد پروژه را اندازه می‌گیرد نه انضباطش را.

---

## [1.3.7] — ۲۰۲۶-۰۸-۲۸

صفحهٔ جزئیات سند مالی قابل اصلاح شد — برای مدیر پلتفرم و فقط او. **بدون مهاجرت.**

### تغییر کرد — کارت جزئیات

- **«تخصیص‌یافته» و «تخصیص‌نیافته» رفتند.** تخصیص بخش خودش را پایین همین صفحه
  دارد، با نام فاکتورها. دو عدد در بالای کارت همان را تکرار می‌کردند بی‌آنکه
  بگویند کدام فاکتور، و خواننده باید پایین می‌رفت تا بفهمد.
- **«شماره پیگیری» فقط روی حواله بانکی است.** روی نقدی و چک ردیفش خالی نیست،
  اصلاً نیست.
- **«وضعیت» دو مقدار دارد**: تأییدشده و ابطال‌شده. «در انتظار وصول» جزوشان نیست —
  آن حالتی است که پرداختِ چکی خودش از آن عبور می‌کند، نه پاسخی که اپراتور بدهد.
- **روی چک، «حالت چک» اضافه شد** کنار «وضعیت چک». **هر دو خواندنی‌اند**، حتی وقتی
  بقیهٔ کارت قابل ویرایش است: این دو فیلد توصیفی نیستند و جابه‌جا شدنشان برای پول
  معنا دارد، پس از صفحهٔ چک‌ها و از مسیر سرویس‌هایی می‌گذرند که می‌دانند هر حرکت
  چه هزینه‌ای دارد.

### اضافه شد — اصلاح سند

هر فیلد **جز «شماره» و «ثبت‌کننده»** برای مدیر پلتفرم قابل ویرایش است، با دکمهٔ
«ذخیره تغییرات». آن دو هرگز اضافه نمی‌شوند: اولی هویت سند است و دومی کسی که ثبتش
کرده، و رکوردی که بشود نویسنده‌اش را بازنویسی کرد، رکورد نیست.

**`POST /api/v1/payments/<id>/correct/`** تازه است. نقش **هم در endpoint و هم در
سرویس** بررسی می‌شود: قابل‌ویرایش بودن یک input روی صفحه یک راحتی است، هیچ‌وقت
مجوز نیست. تستی از روی HTTP این را می‌بندد — مدیر فروش ۴۰۳ می‌گیرد.

### دفتر بازگو می‌شود، نه بازنویسی

بخش سختِ این کار فیلدها نبود. پرداخت تأییدشده از قبل یک بستانکاری در دفتر ثبت
کرده؛ عوض کردن مبلغ یا مشتریِ آن، همان سطر را غلط می‌کند.

دفتر append-only است، پس **هیچ سطری بازنویسی نمی‌شود**: سطر قدیمی برگردانده
می‌شود و سطر تازه ثبت می‌شود — که در حسابداری دوطرفه، تعریفِ «اصلاح» است. و
معناى تازه‌ای اختراع نشد: `cancel_payment` و `_post_payment_credit` از قبل همین
دو حرکت را بلد بودند و اینجا فقط به ترتیب صدا زده می‌شوند.

    مبلغ ۱٬۰۰۰٬۰۰۰ → ۲٬۵۰۰٬۰۰۰ روی یک دریافت تأییدشده:
      payment_received   بستانکار ۱٬۰۰۰٬۰۰۰
      payment_cancelled  بدهکار   ۱٬۰۰۰٬۰۰۰
      payment_received   بستانکار ۲٬۵۰۰٬۰۰۰

اثر خالص دقیقاً ۲٬۵۰۰٬۰۰۰ است و **هر سه حرکت دیده می‌شوند**. جابه‌جا کردن سند به
مشتری دیگر هم همین‌طور: پول از حساب اولی برمی‌گردد و روی دومی می‌نشیند.

ابطال از این مسیر، از `cancel_payment` می‌گذرد — پس تخصیص‌ها آزاد می‌شوند.
برگرداندن یک سند ابطال‌شده، قرینهٔ همان است: یک بستانکاری تازه، **افزوده** می‌شود
نه اینکه برگشتِ قبلی پاک شود.

### رفع شد — دو اشکال که حین همین کار پیدا شد

- **مبلغ کمتر از آنچه تخصیص یافته، حالا با پیام رد می‌شود.** پایگاه داده هم
  جلویش را می‌گرفت، ولی به شکل `IntegrityError` — یعنی ۵۰۰ که به اپراتور هیچ
  نمی‌گفت. همان قاعده، این بار با کلمه، پیش از آنکه چیزی نوشته شود.
- **`update_payment` در `views.py` وارد نشده بود** و endpoint با `NameError`
  می‌شکست. با اولین فراخوانی واقعی در مرورگر پیدا شد.

### تست‌هایی که عمداً به‌روز شدند

`test_manager_receipts_stock_then_orders_invoices_and_takes_payment` مقدار
«تأییدشده» را از یک input می‌خواند؛ حالا وضعیت یک select است، پس **هر دو** سنجیده
می‌شود: مقدارِ ذخیره‌شده (`confirmed`) و برچسبی که خوانده می‌شود (تأییدشده). و
چون «تخصیص‌یافته» از کارت رفته، شاهدِ تخصیص حالا خود ردیف تخصیص است.

---

## [1.3.6] — ۲۰۲۶-۰۸-۲۸

دو میز جدا برای دریافت و پرداخت، و چکی که هر دو محورش دستی جابه‌جا می‌شود.
**بدون مهاجرت.**

### تغییر نام

- «اسناد بازرگانی» → **«اسناد فروش»**
- «صندوق و دریافت» → **«اسناد مالی»**

### اضافه شد — میز پرداخت‌ها

`/disbursements/` کنار `/payments/`. **یک قالب و یک هندلر، دو میز**: یک دریافت و
یک پرداخت همان سندند که از دو جهت خوانده می‌شوند، پس واژه‌ها از
`payment_direction` می‌آید و بقیه مشترک است. کپی دوم دویست خط مارک‌آپ، فردا از
اصلش جدا می‌افتاد.

- ستون طرف حساب روی میز پرداخت **«گیرنده»** است و روی میز دریافت «مشتری».
- هر میز فقط جهت خودش را فهرست می‌کند (`?direction=`)، وگرنه هر دو صفحه یک
  ردیف‌ها را زیر دو عنوان نشان می‌دادند.

### تغییر کرد — فهرست دریافت‌ها و پرداخت‌ها

- ستون‌های **«جهت»** و **«تخصیص‌یافته»** حذف شدند. هر میز یک جهت دارد، پس ستونی
  که آن را در هر ردیف تکرار می‌کرد چیزی نمی‌گفت.
- **فیلتر وضعیت حذف شد.**
- دکمه‌های «اقساط» و «چک» از این صفحه رفتند؛ هرکدام ورودی خودشان را در سایدبار
  دارند.

### تغییر کرد — فرم ثبت

- **دکمه‌های «دریافتی/پرداختی» حذف شدند.** جهت را صفحه تعیین می‌کند، نه فرم.
- **«کارت» از روش‌ها حذف شد.** مدل هنوز می‌پذیردش و ردیف‌های قدیمی دست‌نخورده‌اند —
  چیزی بازتفسیر یا پاک نشد — فقط دیگر قابل انتخاب نیست.
- **نقدی:** «شماره رسید» رفت. **حواله بانکی:** «شماره حساب/شبا» رفت و نام بانک
  شد «بانک مقصد» (روی پرداخت: «بانک مبدأ»). **چک:** «شماره مرجع» و «صاحب حساب»
  رفتند و بانک شد «بانک مبدأ» (روی پرداخت: «بانک مقصد»).
- **طرف حساب قابل جستجو شد**، چون دفتر مشتریان قرار است از حد یک dropdown ساده
  بگذرد. پس از انتخاب، فقط نام نشان داده می‌شود.

> **چرا `<select>` بومی زیرش ماند.** جعبهٔ جستجو روی select واقعی سوار است؛ همان
> select مقدار را نگه می‌دارد و همان چیزی است که ثبت می‌شود. ویجتی که select را
> **جایگزین** کند باید یک **کپی** از مقدار نگه دارد، و کپی‌ای که جدا بیفتد نامی
> نشان می‌دهد که ثبت نخواهد شد.
>
> و تعویض در **جاوااسکریپت** انجام می‌شود، نه در مارک‌آپ: تا وقتی اسکریپت اجرا
> نشده، صفحه یک `<select>` سالم دارد. اول با `hidden` در قالب نوشته شده بود و
> **تست مرورگر گرفتش** — سلنیوم حاضر نشد عنصر نامرئی را بچرخاند، و درست هم بود:
> ادعای «اگر اسکریپت نیامد باز هم کار می‌کند» با `hidden` در قالب، حرف نادرستی
> بود.

### اضافه شد — پرداخت با چکِ مشتری

روی میز پرداخت، «نوع چک» دو مقدار دارد: **چک مشتری** یا **چک تازه**. با «چک
مشتری» فقط «شماره چک» پرسیده می‌شود و میان چک‌های دریافتیِ در انتظار جستجو
می‌شود.

این کار **هیچ سند و هیچ چک تازه‌ای نمی‌سازد** — همان ابزار است که دست عوض می‌کند،
و به `spend/` می‌رود. ساختن ردیف دوم یعنی همان مبلغ دو بار شمرده شود.

### اضافه شد — هر دو محور چک از خود ردیف

- ستون **عملیات**: چهار دکمه — برگشت، خرج کردن، وصول، در انتظار. مقصدی که گراف
  وضعیت اجازه نمی‌دهد **غیرفعال** نشان داده می‌شود، نه پنهان: دکمه‌ای که با عوض
  شدن وضعیتِ ردیف‌ها می‌آید و می‌رود، شبیه خرابی رندر است.
- ستون **عملیات دوم**: «ثبت شده» / «ثبت نشده». هر دو همیشه هستند و حالت فعلی
  به‌صورت فشرده نشان داده می‌شود، پس عرض ستون با تغییر حالت تکان نمی‌خورد.
- **`POST /api/v1/cheques/<id>/registration/`** تازه است. عمداً روی پرداختِ زیرش
  **هیچ اثری ندارد**: ثبت‌شدن می‌گوید کاغذ کجاست، نه اینکه پول رسیده — و اگر
  اثر داشت، «ثبت‌شده» کردن یک دسته چک، یک دسته بستانکاری ثبت می‌کرد که کسی
  دریافت نکرده.
- دکمهٔ «دریافت‌ها» از این صفحه رفت.

### تغییر کرد — چک همیشه خام ثبت می‌شود

هر چکی که از میز دریافت یا پرداخت ثبت شود، **همیشه** «در انتظار» و «ثبت نشده»
شروع می‌کند. **در سرویس** اعمال می‌شود نه در فرم، تا درخواستی که دستکاری شده
نتواند ابزاری را در حالتی بگذارد که کسی انتخابش نکرده. `is_registered` هنوز
پذیرفته و بعد نادیده گرفته می‌شود، تا فراخوان قدیمی خطا ندهد.

### تغییر کرد — فیلترها کشیده شدند

فیلتر هر فهرست حالا یک grid است که کل عرض کارت را می‌گیرد: **۲۲ قالب**.
`flex-wrap` با `w-auto` باعث می‌شد هر کنترل فقط اندازهٔ محتوایش باشد و سه select
کوتاه وسط یک کارت پهن جمع شوند. دکمهٔ «اعمال» به اندازهٔ متن خودش می‌ماند —
کشیدنش روی یک‌سوم ردیف، شبیه فیلتر چهارم می‌شد.

### سقف شیوه‌نامه از ۱۴۰ به ۱۶۰ رفت

باز هم تصمیم، نه دور زدن: اول ۴ اعلان که قالب از قبل داشت حذف شد. آنچه ماند یک
grid فیلتر و یک combobox جستجوشونده است — و combobox یک **کامپوننت** است، که این
تست معمولاً درست می‌کند جلویش را بگیرد. اینجا هست چون combobox خود قالب،
select2 است که در همان bundle ۳.۵ مگابایتی بارگذاری‌نشده زندگی می‌کند.

---

## [1.3.5] — ۲۰۲۶-۰۸-۲۷

حالت روشن، تاریک و سیستم برای کل پنل. **بدون مهاجرت.**

### اضافه شد

- **سه حالت** در منوی کاربر (بالا، سمت چپ): **روشن**، **تاریک**، و **سیستم** که
  از تنظیم خود سیستم‌عامل پیروی می‌کند.
- **آیکون‌های خود قالب**: `ki-night-day`، `ki-moon` و `ki-screen`.
- **دکمهٔ کاربر خودش می‌گوید الان کدام حالت روشن است** — دو آیکون در DOM هستند و
  قاعدهٔ `theme-light-show`/`theme-dark-show` خود قالب تصمیم می‌گیرد کدام دیده
  شود. یعنی از همان **اولین فریم** درست است، بدون هیچ اسکریپتی از ما.

خودِ سازوکار مال قالب خریداری‌شده است: `KTThemeMode` منو را می‌بندد، «سیستم» را
با `prefers-color-scheme` حل می‌کند، `data-bs-theme` را روی `<html>` می‌نویسد و
انتخاب را نگه می‌دارد. رنگ‌ها هم از قبل آنجا بودند — بوت‌استرپ ۵.۳ و متغیرهای
قالب، **۱۵۷ متغیر** برای خاکستری‌ها، پس‌زمینهٔ صفحه و سایدبار. **هیچ پالتی در این
پروژه تعریف نشد.**

### آنچه این پروژه اضافه کرد — اسکریپتی که از اولین نقاشی جلو می‌زند

`KTThemeMode` روی `DOMContentLoaded` اجرا می‌شود، و تا آن لحظه کسی که حالت تاریک
دارد **یک صفحهٔ سفید دیده است**. یک اسکریپت درون‌خطی در `<head>`، پیش از همهٔ
شیوه‌نامه‌ها، این را حل می‌کند.

دو کلید و دو معنی: `data-bs-theme-mode` همان چیزی است که کاربر **انتخاب** کرده
(روشن/تاریک/سیستم) و `data-bs-theme` چیزی است که همان الان **به آن حل می‌شود**.
فقط انتخاب ذخیره می‌شود؛ «سیستم» باید در هر بار بارگذاری دوباره حل شود، وگرنه
کسی که «سیستم» را انتخاب کرده، بعد از عوض کردن تنظیم سیستم‌عاملش جواب دیروز را
می‌گیرد.

اگر `localStorage` در حالت ناشناس دسترسی ندهد، پنل در حالت پیش‌فرض بالا می‌آید:
پنلی با پوستهٔ اشتباه یک ایراد است، پنلی که بالا نیاید یک قطعی است.

> **از فایل قالب کپی نشد، و دلیلش مهم است.** قالب همین اسکریپت را دارد، ولی
> `layouts/dark-sidebar.html` در این مخزن از یک **ترجمهٔ ماشینی آسیب دیده**:
> داخل شناسه‌ها `mode` با «حالت» جایگزین شده (`defaultThemeحالت`)، `getItem`
> کوچک‌نویس شده (`getitem`)، و یک `?` از ternary افتاده. کپی‌کردنش یک اسکریپت
> خراب به پنل می‌آورد. همان ایده اینجا **درست** نوشته شده.

### چرا منو داخل منوی کاربر است، نه پشت دکمهٔ جدا

قالب این سه گزینه را در یک dropdown تودرتو می‌گذارد که `KTMenu` با Popper جای‌گذاری
می‌کند، و Popper در همان bundle ۳.۵ مگابایتی است که این دیپلوی بارگذاری نمی‌کند.
`KTThemeMode` به هیچ‌کدام نیاز ندارد: `[data-kt-element="theme-mode-menu"]` را
هرجا باشد پیدا می‌کند و کلیک‌ها را خودش می‌بندد. پس گزینه‌ها ردیف‌های ساده‌ای در
منویی هستند که از قبل باز است — همان‌جایی که خواسته شده بود.

### سند چاپی تاریک نمی‌شود

`print_base.html` قالب پنل را extend نمی‌کند، پس هیچ‌چیز آن را پوسته‌دار نمی‌کند و
فاکتور در هر حالتی سیاه روی سفید می‌ماند. این ساختاری است، نه شانسی — و یک تست
نگهش می‌دارد، چون extend کردنش شبیه یک مرتب‌سازی بی‌ضرر به نظر می‌رسد و بی‌صدا
شروع می‌کند فاکتور تاریک چاپ کردن.

### آزموده شد

هر سه حالت در مرورگر واقعی: «سیستم» با سیستم‌عامل تاریک → تاریک، همان «سیستم» با
سیستم‌عامل روشن → روشن، و «تاریک» صریح روی سیستم‌عامل روشن → تاریک و ماندگار بعد
از جابه‌جایی صفحه. روی صفحهٔ جزئیات دریافت در حالت تاریک، **هیچ عنصری نسبت کنتراست
زیر ۳:۱ نداشت**.

---

## [1.3.4] — ۲۰۲۶-۰۸-۲۷

سایدبار جمع‌شونده، و ظاهر مدرن برای فهرست‌های انتخاب. **بدون مهاجرت.**

### اضافه شد — سایدبار جمع می‌شود و یادش می‌ماند

- **دکمهٔ جمع/باز کردن** روی لبهٔ داخلی سایدبار، وسط نوار لوگو: همان‌جا در هر
  صفحه، با منو اسکرول نمی‌شود، و برای پیدا کردنش لازم نیست کسی بگردد.
- **جمع‌شده یعنی ۷۵ پیکسل**: فقط نشان کوچک و آیکون‌ها می‌مانند. عنوان‌ها، فلش‌ها
  و زیرمنوهای آکاردئونی می‌روند — در ۷۵ پیکسل جایی برای باز شدنشان نیست.
- **حالت روی سرور خوانده می‌شود، نه با اسکریپت بعد از بارگذاری.** این پنل بین
  صفحه‌ها بارگذاری کامل دارد؛ اگر حالت را جاوااسکریپت برمی‌گرداند، سایدبارِ
  جمع‌شده در **هر جابه‌جایی** اول باز نقاشی می‌شد و بعد بسته می‌شد.

خودِ جمع شدن کار قالب خریداری‌شده است: CSS آن به `data-kt-app-sidebar-minimize`
روی `<body>` واکنش نشان می‌دهد و `KTToggle` همان را جابه‌جا می‌کند و کوکی‌اش را
می‌نویسد. آنچه این پروژه اضافه کرد، سه چیزی است که قالب نمی‌تواند بدهد: خواندن
کوکی سمت سرور، یک لوگوی دوم برای ریل باریک، و جای درست دکمه.

> **سه قاعده‌ای که باید وزن بیشتری از ابزارهای خود قالب داشته باشند.** این‌ها
> `!important` یا انتخابگر سنگین‌تر دارند و هیچ‌کدام سلیقه‌ای نیست:
>
> - `min-width: 0` روی سایدبارِ جمع‌شده. یک flex item به‌طور پیش‌فرض
>   `min-width: auto` دارد یعنی «هرگز کوچک‌تر از محتوایم» — بدون این، متغیر عرض
>   ۷۵ پیکسل می‌گفت، قاعده هم می‌خواندش، و جعبه همچنان ۲۶۵ پیکسل اندازه می‌گرفت
>   چون متن برند تو‌یش جا نمی‌شد. **همین یک اعلان بود که کل قابلیت را از کار
>   انداخته بود.**
> - `display: none !important` روی برند بازشده، چون `d-flex` بوت‌استرپ خودش
>   `!important` است.
> - انتخابگر `.app-sidebar .app-sidebar-toggle` برای پنهان‌کردن دکمه در موبایل،
>   چون `.btn.btn-icon` قالب دو کلاس است و یک کلاس به آن می‌بازد.

زیر نقطهٔ شکست، سایدبار کشویی است و جمع کردنش معنایی ندارد، پس دکمه پنهان می‌شود.

### تغییر کرد — ظاهر فهرست‌های انتخاب

هر انتخاب در این پنل یک `<select>` واقعی است و **همان می‌ماند**. فهرستش از API
با `fillSelect` پر می‌شود، مقدارش در ده‌ها جا با `.value` خوانده می‌شود و تست‌ها
رویش ادعا دارند؛ جایگزین کردنش با یک listbox اسکریپتی یعنی نگه‌داشتن یک **کپی**
از مقدار، و کپی‌ای که از اصل جدا بیفتد به اپراتور برچسبی نشان می‌دهد که همان چیزی
نیست که ثبت خواهد شد.

پس کنترل بومی استایل شد، نه اینکه پشت یکی دیگر پنهان شود:

- جعبهٔ بسته کاملاً از آنِ ماست: حالت hover، حلقهٔ focus، و فلشی که هنگام focus
  رنگ می‌گیرد.
- **`color-scheme: light dark`** روی هر `select` — تنها دستگیره‌ای که یک صفحه
  روی فهرستی دارد که خودِ مرورگر می‌کشد. بدون آن، فهرست باز در حالت تاریک سفید
  باز می‌شود. (این همان چیزی است که ۱.۳.۵ رویش سوار می‌شود.)
- `option` و `optgroup` رنگ و فاصلهٔ خودِ پنل را می‌گیرند، و ردیف راهنما (همانی
  که `fillSelect` با مقدار خالی می‌نویسد) کم‌رنگ‌تر است چون یک انتخاب نیست.

جایی که مرورگری این‌ها را نپذیرد، `color-scheme` همچنان فهرست را خوانا نگه
می‌دارد؛ یعنی افت به فهرست پلتفرم است، نه به فهرست ناخوانا.

---

## [1.3.3] — ۲۰۲۶-۰۸-۲۷

یک دور دیباگ روی کل پنل: هر صفحه با داده واقعی باز شد، فرم‌ها پر و ثبت شدند، و
آنچه شکست اینجا آمده. **بدون مهاجرت.**

### رفع شد — تقسیم دریافت از طریق API اصلاً کار نمی‌کرد

`POST /api/v1/payments/<id>/allocate-across/` که در ۱.۳.۱ اضافه شد، **هر شناسهٔ
فاکتوری را «pk نامعتبر» رد می‌کرد**. یعنی قابلیت بند ۳.۲ از روز اول در عمل
غیرقابل‌استفاده بود.

علتش دقیقاً همان چیزی است که تست‌های آن نسخه نمی‌توانستند بگیرند:
`AllocatePaymentSerializer` دامنهٔ فاکتورها را در `__init__` تنظیم می‌کرد، و یک
serializer که به‌عنوان فیلد تودرتو استفاده می‌شود (`many=True`) **یک‌بار و در
زمان تعریف کلاس** ساخته می‌شود — جایی که هیچ request و هیچ context‌ای وجود ندارد.
پس نسخهٔ تودرتو برای همیشه `Invoice.objects.none()` می‌ماند.

حالا دامنه در `ScopedIssuedInvoiceField.get_queryset()` حل می‌شود که DRF آن را
**در زمان اعتبارسنجی هر درخواست** صدا می‌زند، و `self.context` روی یک فیلد
bind‌شده از والدینش بالا می‌رود؛ پس همان فیلد هم مستقل و هم تودرتو درست کار
می‌کند.

> **چرا تست‌های ۱.۳.۱ این را نگرفتند.** آن‌ها `allocate_payment_across()` را
> مستقیم صدا می‌زدند و یک فراخوانی مستقیم سرویس هرگز به serializer نمی‌رسد.
> `billing/tests/test_payment_split_api.py` تازه اضافه شد و از روی HTTP می‌رود.
> تست سرویس و تست API یک تست نیستند.

### رفع شد — مبلغ خام در گزارش عملکرد

جدول و کارت‌های «عملکرد کاربران» تنها جایی در پنل بودند که مبلغ را از مسیر
`money` رد نمی‌کردند: `sales_amount` و `average_sale_amount` عیناً همان چیزی چاپ
می‌شدند که API می‌فرستد (`12500000.00`) — بدون گروه‌بندی و با اعشاری که هیچ‌جای
دیگر پنل نشان داده نمی‌شود. مقدار پیش‌فرض روی خود قالب هم `۰.۰۰` بود.

### رفع شد — ستون‌های فاکتور چاپی با پاورقی خودش نمی‌خواند

روی فاکتور واقعی، ستون تخفیف `۵٬۰۰۰٬۰۰۱` جمع می‌زد زیر پاورقی‌ای که
`۵٬۰۰۰٬۰۰۰` می‌گفت. همین برای مالیات و جمع کل.

`printed_line_breakdown` مقادیر **اعشاری ذخیره‌شده** را پخش می‌کرد و جمعشان دقیق
بود — بعد صفحه هر سطر را برای نمایش رو به بالا رند می‌کرد. **رند رو به بالا جمع‌پذیر
نیست**: دو سطر که هرکدام کسر داشتند، هرکدام یک ریال گرفتند و ستون یک ریال از
جمع کل جلو زد. حالا کل محاسبه در **ریال صحیح** انجام می‌شود — همان واحدی که خواننده
می‌بیند — با تقسیم به روش «جمع دونده» که تضمین می‌کند اجزا دقیقاً به کل برسند.
پاورقی هم از همان محاسبه برمی‌گردد، پس ستون و جمع دیگر نمی‌توانند از هم جدا شوند.

### رفع شد — `payment_made` هیچ نام فارسی‌ای نداشت

این مقدار در ۱.۲.۱ به `CustomerLedgerEntry.EntryType` اضافه شد و نه نقشهٔ
برچسب‌ها یادش گرفت و نه فیلتر صفحهٔ دفتر. نتیجه: یک ردیف واقعی دفتر رشتهٔ انگلیسی
`payment_made` را در جدولی فارسی چاپ می‌کرد و اصلاً قابل فیلتر کردن نبود.
`adjustment_debit` و `adjustment_credit` هم در فیلتر نبودند.

`common/tests/test_label_coverage.py` تازه اضافه شد و **هر هشت نقشهٔ برچسب و دو
فیلتر را برابر enum خودشان می‌سنجد**. همین نوع خطا دو بار در سطح پایگاه داده به
این پروژه ضربه زده (فهرست دستی داخل `CheckConstraint`)؛ این بار در سطح نمایش.

### رفع شد — `[object Object]` به‌جای پیام خطا

خطای یک serializer تودرتو به شکل `[{invoice: [...]}]` برمی‌گردد و
`showError` آن را مستقیم `join` می‌کرد. حالا ساختار پیمایش می‌شود، پس هر شکلی که
DRF بدهد متن خوانا می‌شود.

### اضافه شد

- **فیلتر `direction` روی `GET /api/v1/payments/`.** جهت از ۱.۲.۱ یک فیلد
  درجه‌یک است و همان فهرست نمودار جهت دارد، ولی endpoint حاضر نبود روی آن فیلتر
  کند — «فقط پولی که خارج شده را نشان بده» سؤالی بود که API دربارهٔ ستونی که خودش
  ذخیره می‌کند نمی‌توانست جواب دهد.
- **ستون «جهت» روی فهرست دریافت‌ها**، و ستون «مشتری / گیرنده» که برای پرداختی به
  `payee` برمی‌گردد. پیش از این یک پرداختی بدون مشتری فقط «—» نشان می‌داد، در حالی
  که نام گیرنده روی همان ردیف ذخیره بود؛ و هیچ چیز در ردیف نمی‌گفت پول به کدام
  سمت رفته.
- **`mark_sale` حالا `sold_at` را پیش‌فرض می‌گذارد.** ستون NOT NULL بود و بدون
  مقدار `IntegrityError` می‌داد (یعنی ۵۰۰) به‌جای خطای اعتبارسنجی که هر فیلد
  دیگری در همان تابع می‌دهد. serializer از قبل همین پیش‌فرض را داشت، پس رفتار
  پنل عوض نمی‌شود.

### رفع شد — نشت سطل throttle بین تست‌ها، دوباره

دو تست در اجرای کامل ۴۲۹ می‌گرفتند (یکی از آن‌ها به شکل `KeyError: 'id'` ظاهر
می‌شد، چون بدنهٔ پاسخِ throttle‌شده اصلاً `id` ندارد). سطل با شناسهٔ کاربر کلید
می‌خورد و تست‌های rollback‌شده همان شناسه‌ها را دوباره می‌سازند، پس یک کلاس خرج
کلاس قبلی را به ارث می‌برد. تست‌های تازهٔ همین نسخه ترتیب اجرا را عوض کردند و این
دو را از آستانه رد کردند.

`cache.clear()` به `setUp` هر دو اضافه شد — همان کاری که ده کلاس پرترافیک دیگر در
این مجموعه می‌کنند.

> **این الگو تکرارشونده است.** هر کلاس تستی که درخواست زیاد می‌فرستد باید خودش
> سطل را پاک کند، و یادش می‌رود. یک راه‌حل سامانه‌ای (پاک کردن خودکار پیش از هر
> تست) ارزش بررسی دارد؛ اینجا الگوی موجود دنبال شد تا تغییر کم‌خطر بماند.

### یک نکته که ثبت می‌شود — همین اشکال قبلاً یک بار رفع شده بود

docstring کلاس `DocumentLineScopeTests` دقیقاً همین خطا را توصیف می‌کند:
«serializer تودرتو پیش از bind شدن ساخته می‌شود، پس `__init__` خودش context خالی
می‌بیند». برای فیلد `product` با `ScopedLineItemsMixin` حل شده بود — و در ۱.۳.۱
همان اشتباه روی `invoice` تکرار شد.

دلیلش خودِ راه‌حل قبلی است: آن mixin از **والد** می‌خواهد که `scope_line_products()`
را صدا بزند، و صدا زدنش یادِ کسی می‌رود. `ScopedIssuedInvoiceField` چیزی از والد
نمی‌خواهد؛ دامنه را خودش در `get_queryset()` حل می‌کند، پس فراموش‌شدنی نیست.

`DocumentLineInputSerializer` عمداً دست‌نخورده ماند — کار می‌کند و تست رگرسیون
دارد — ولی همان شکنندگی را دارد و اگر روزی serializer تودرتوی تازه‌ای اضافه شود،
جای اول برای نگاه کردن همین است.

### تست‌هایی که عمداً به‌روز شدند

- `test_manager_to_agent_daily_workflow_and_company_report` رشتهٔ خام `20.00` را
  انتظار داشت — همان اشکالی که بالا رفع شد. حالا `۲۰ ریال` را می‌خواهد.
- تست‌های `printed_line_breakdown` برای قرارداد تازه (ریال صحیح + جمع دقیق)
  بازنویسی شدند.

### آنچه بررسی شد و سالم بود

هر ۴۹ صفحهٔ پنل و ۳۴ endpoint با داده واقعی باز شدند: بدون ۵۰۰، بدون خطای
کنسول، بدون asset‌ـی که ۴۰۴ بدهد. RTL، `lang="fa"`، فونت IRANSansWeb و هر سه
شیوه‌نامه بار می‌شوند و صفحه در عرض ۳۴۶ پیکسل هم سرریز افقی ندارد. نمودارهای
هر فهرست، نمودار عملکرد، و نمودار شهر مشتریان همه رسم می‌شوند.

---

## [1.3.2] — ۲۰۲۶-۰۸-۲۷

بقیهٔ پاسخ‌های مالک محصول: بندهای ۵، ۶، ۷، ۸ و ۹، و فارسی‌شدن رویدادهای سامانه.
با این نسخه، **همهٔ پرسش‌های `OPEN_FINANCIAL_QUESTIONS.md` بسته شده‌اند** جز
بند ۶.۲ که خودِ مالک محصول پرسش برگشتی داده است.

### بند ۹ — چاپ، شبیه نمونهٔ `kala-1.pdf`

- **دو بلوک هویت** کنار هم: «مشخصات فروشنده» و «مشخصات خریدار»، هرکدام با نام،
  شماره ثبت/شماره ملی، شماره اقتصادی، شهر، نشانی کامل، کدپستی و تلفن — همان
  ترتیب نمونه. مقادیر از snapshot بند ۲ می‌آیند؛ اگر فاکتور پیش از وجود snapshot
  صادر شده باشد از پروندهٔ جاری خوانده می‌شود و **پانویس می‌گوید که چنین شده**.
- **جدول یازده‌ستونی** با **مالیات روی هر سطر**، دقیقاً مثل نمونه.
- **مبلغ به حروف، به ریال** (بند ۹.۲) — `billing/words.py`.
- **جای مهر و امضا** برای فروشنده و خریدار (بند ۹.۴). **بارکد و QR ندارد**
  (بند ۹.۳).

> **چرا ستون مالیات محاسبه می‌شود و ذخیره نمی‌شود.** سند ذخیره‌شده یک عدد مالیات
> در سرصفحه دارد که روی جمعِ پس از تخفیف حساب شده — تنها شکلی که می‌شود آن را با
> `total_amount` سنجید. نمونهٔ چاپی مالیات را سطر‌به‌سطر می‌خواهد، پس ستون‌ها
> ساخته می‌شوند و **یک قاعده مهم است: جمعشان باید دقیقاً همان چیزی باشد که ذخیره
> شده.** تخفیف سرصفحه و مالیات به نسبت سهم هر سطر پخش می‌شوند و **باقی‌ماندهٔ
> گِرد کردن به سطر آخر** می‌رود. سندی که جمع ستون‌هایش با پاورقی خودش نخواند،
> از سندی بدون آن ستون‌ها بدتر است. هشت تست همین را می‌بندند.

### بند ۸ — ابطال و صدور مجدد

- **`reissue_invoice()`** و `POST /api/v1/invoices/<id>/reissue/`: فاکتور صادرشده
  باطل می‌شود و **یک پیش‌نویس تازه** با همان اقلام و سرصفحه ساخته می‌شود، در یک
  تراکنش. پیش‌نویس و نه فاکتور صادرشده — چون معمولاً همان چیزی که باعث صدور مجدد
  شده باید اصلاح شود، و شمارهٔ رسمی هم باید در لحظهٔ صدور گرفته شود.
- **دلیل ابطال در توضیحات خود فاکتور نوشته می‌شود** (بند ۸.۲)، با پیشوند
  `[ابطال]` و **افزوده به** یادداشت قبلی، نه جایگزین آن. رویدادنگار همین را ثبت
  می‌کند، ولی رویدادنگار چاپ و تحویل نمی‌شود؛ توضیحات می‌شود.
- صدور مجدد **هیچ قاعده‌ای را سست نمی‌کند**: فاکتوری که پول رویش تخصیص خورده
  همچنان تا آزادسازی تخصیص‌ها رد می‌شود.
- ۸.۱ (بدون ریست سالانه) و ۸.۳ (بدون محدودیت زمانی) از قبل درست بودند.

### بند ۷ — گزارش‌ها روی **مبنای نقدی**

پاسخ «نقدی» یک پاسخ است ولی سه چیز را هم‌زمان عوض می‌کند، پس هر سه تثبیت شده‌اند:

- **دوره روی تاریخ دریافت پول فیلتر می‌شود، نه تاریخ صدور فاکتور.** فاکتوری که
  فروردین صادر و اردیبهشت پرداخت شده، مال اردیبهشت است.
- **فاکتور صادرشدهٔ پرداخت‌نشده هیچ سهمی ندارد.** آن یک مطالبه است و جایش گزارش
  مطالبات است.
- **فاکتور نیمه‌پرداخت به اندازهٔ پرداخت‌شده‌اش سهم دارد، و بهای تمام‌شده به همان
  نسبت شناسایی می‌شود** — وگرنه فروشی که نصفش وصول شده، تمام هزینه‌اش را مقابل
  نصف درآمدش نشان می‌داد و زیانی گزارش می‌شد که رخ نداده.

`basis="accrual"` رفتار قبلی را نگه می‌دارد. انتخاب مالک محصول نیست و هیچ‌جای پنل
آن را صدا نمی‌زند؛ هست تا **تفاوتی که این پاسخ ایجاد کرده دیدنی و آزمودنی باشد**،
نه فقط ادعاشده.

- **۷.۶** بدون قاعدهٔ دوم حل می‌شود: فاکتور ابطالی تخصیص‌هایش آزاد شده، پس چیزی
  از آن وصول نشده؛ و تخفیف هم از قبل بیرون است چون وصولی مقابل مبلغ پس از تخفیف
  سنجیده می‌شود.
- **۷.۴** با بند ۴.۲ حل شد: چک وصول‌نشده حالا دریافت‌شده حساب می‌شود و وضعیتش
  «در انتظار» است.
- **۷.۲، ۷.۳، ۷.۷** از قبل درست بودند — گزارش سود همین حالا **بازهٔ دلخواه با
  تاریخ شمسی** می‌گیرد. ۷.۵ (نبود ماژول هزینه) کاری لازم نداشت.

### بند ۶ — دفتر برای بازاریاب

- **قابلیت تازهٔ `ledger.own`** به نقش بازاریاب داده شد و `ledger_entries_for`
  او را به **مشتریان خودش** محدود می‌کند، با استفادهٔ دوباره از `customers_for`
  نه با نوشتن قاعدهٔ دوم.
- **عمداً `ledger.company` گشاد نشد.** اجازه و دامنهٔ دسترسی دو کنترل جدا هستند:
  این می‌گوید بازاریاب اصلاً می‌تواند دفتری ببیند، و selector می‌گوید دفترِ چه
  کسی. گشاد کردن `ledger.company` یعنی دادن دید سراسری و بعد پس گرفتنش با یک
  queryset — همان شکلی که یک فیلتر جاافتاده را به مانده‌ی همهٔ مشتریان تبدیل
  می‌کند.
- ۶.۱ (مانده مثبت = بدهکاری مشتری) از قبل در خود مدل نوشته شده بود.

### بند ۵ — کاری لازم نداشت

- **۵.۱** «برگشت نداریم؛ بازپرداخت را پرداختی ثبت کن» — از ۱.۲.۱ همین است:
  پرداختی وجود دارد و `allocate_payment` هر چیزی جز دریافتی را رد می‌کند.
- **۵.۲** «مدیر اصلی ثبت و تایید می‌کند، فعلا» — از قبل همین است: ثبت سند مالی
  فقط برای نقش‌های ارشد است و مرحلهٔ تایید جدا وجود ندارد.

### رویدادهای سامانه فارسی شدند

«رویداد های سامانه باید فارسی باشند چون سایت و پنل فارسی است.»

- `auditlog/labels.py` — نام فارسی برای **هر ۶۶ عملیاتی که کد واقعاً ثبت می‌کند**.
- **مقدار ذخیره‌شده عوض نشده.** `ActivityLog.operation` همان شکل ASCII‌ـش را نگه
  داشته، چون قرارداد ماشین است: رویدادنگار با آن فیلتر می‌شود، API آن را می‌دهد،
  و ردیف‌های نوشته‌شده آن را دارند. ترجمه هنگام نوشتن یعنی هر ردیف تاریخی با نامی
  که ردیف‌های تازه دارند پیدا نشود، و یعنی متن نمایشی داخل رکورد حسابرسی — همان
  جدولی که باید از همه سخت‌تر بازنویسی شود.
- ترجمه در `operation_display` سریالایزر می‌نشیند، **کنار** مقدار ذخیره‌شده نه
  به‌جایش.
- عملیات بدون ترجمه به شکل ذخیره‌شده‌اش نمایش داده می‌شود — زشت، ولی هرگز خالی.
- **یک تست کد را می‌خواند** و هر `operation=` را با این جدول می‌سنجد، پس افزودن
  رویداد بدون ترجمه همین‌جا گرفته می‌شود نه در پنل. تستی هم هست که مطمئن شود
  برچسب‌ها واقعاً فارسی‌اند و دو رویداد یک نام ندارند.

### آنچه هنوز باز است

**فقط بند ۶.۲.** مالک محصول در پاسخ به «کدام رویدادها به دفتر پست شوند؟» پرسیده
بود «دفتر چیست؟». توضیح داده شد؛ تا پاسخ ندهد این یک بند باز می‌ماند. بقیهٔ
`OPEN_FINANCIAL_QUESTIONS.md` بسته است.

---

## [1.3.1] — ۲۰۲۶-۰۸-۲۷

پاسخ‌های مالک محصول به پرسش‌های باز مالی: بندهای ۱، ۲، ۳ و ۴ پیاده شدند.

### یادداشت شماره‌گذاری — این نسخه از جدول بالا پیروی نمی‌کند

**این نسخه به تصمیم صریح مالک محصول `1.3.1` شماره خورده است** («این تغییرات ورژن
۱.۳.۰ میشن و فعلا سراغ ۲.۰.۰ نرو»، و سپس «put the version 1.3.1»). تحلیل زیر
نگه داشته شده تا بعداً کسی این را نقض ناخواستهٔ جدول نخواند — این دومین باری است
که شمارهٔ نسخه به تصمیم مالک محصول از تحلیل جدا می‌شود؛ بار قبل `1.2.1` بود.

طبق جدول همین فایل، MAJOR یعنی «یک قاعدهٔ حسابداری تغییر معنا می‌دهد» یا «مهاجرتی
لازم است که برگشت‌پذیر نیست». با آن تعریف، این تغییر **MAJOR است، نه PATCH**، و
دو دلیل دارد:

- **چک حالا پیش از وصول، حساب مشتری را بستانکار می‌کند.** معنای «مانده مشتری» عوض
  شده است. همان مشتری با همان چک، امروز عدد دیگری نشان می‌دهد. این تنها یک قابلیت
  تازه نیست؛ تفسیر هر مانده‌ای در محصول را جابه‌جا می‌کند.
- **مقادیر وضعیت چک در API عوض شده‌اند.** `registered`/`deposited`/`returned`/
  `cancelled` دیگر وجود ندارند. هر مصرف‌کننده‌ای که این رشته‌ها را می‌شناخت
  می‌شکند.

آنچه این خطر را کوچک می‌کند و احتمالاً پشتوانهٔ تصمیم مالک محصول است: تنها
مصرف‌کنندهٔ API همین پنل است و همراه با آن به‌روز شده، و مهاجرت — برخلاف تعریف
MAJOR — **برگشت‌پذیر است و هیچ داده‌ای را پاک نمی‌کند** (پایین‌تر).

### تغییر کرد

- **رند کردن مبلغ حالا رو به بالا است، نه نیم‌به‌بالا.** (بند ۱.۴)
  `۹۹۹٫۴۹` که پیش از این `۹۹۹` چاپ می‌شد، حالا `۱٬۰۰۰` است. قاعده این است که عددی
  که به مشتری نشان داده می‌شود نباید از آنچه بدهکار است کمتر باشد؛ بهایش حداکثر
  یک ریال بیش‌نمایی است. هر دو مسیر نمایش — `money()` در جاوااسکریپت و فیلتر
  `money` در جنگو — یک قاعده دارند و تست، رقم‌به‌رقم یکی بودنشان را نگه می‌دارد.
  **مبلغ ذخیره‌شده دست‌نخورده است**؛ این فقط نمایش است. هیچ اعشاری در پنل نمایش
  داده نمی‌شود.
- **چک دو محور شد: حالت و وضعیت.** (بند ۴.۱)
  کارکنان هیچ‌وقت چک را «یکی از هفت حالت» نمی‌دیدند؛ دو پرسش جدا داشتند. حالا:
  - **حالت** (`is_registered`): ثبت شده / ثبت نشده
  - **وضعیت** (`status`): در انتظار / وصول شده / برگشت / خرج شده
  دو محور مستقل‌اند — چکی می‌تواند ثبت‌شده و هنوز در انتظار باشد.
- **چک هنگام دریافت، حساب مشتری را بستانکار می‌کند.** (بند ۴.۲)
  پیش‌فرض `BILLING_CHEQUE_CREDITS_ON` از `cleared` به `registration` رفت. دیپلویی
  که طور دیگری حساب می‌کند، همچنان می‌تواند با تنظیمات بگوید.
- **چک برگشتی، مبلغ را به مانده فاکتور برمی‌گرداند.** (بند ۴.۳)
  چون اعتبار هنگام دریافت داده شده، برگشت باید آن را پس بگیرد. از مسیر
  `cancel_payment` انجام می‌شود، پس تخصیص‌ها آزاد می‌شوند و **هر دو حرکت در دفتر
  می‌مانند** — برگشت قابل حسابرسی است، نه چکی که انگار هرگز نبوده.

### اضافه شد

- **`Cheque.registered_on`** — تاریخ ثبت چک، که اپراتور می‌گوید و معمولاً پیش از
  روزی است که به دست ما رسیده. (بند ۴.۴) تاریخ روز همان `created_at` است که سامانه
  مهر می‌زند و کسی ویرایشش نمی‌کند؛ تاریخ سررسید همان `due_date` است.
- **`Cheque.is_registered`** و فیلتر `is_registered=true|false` روی
  `GET /api/v1/cheques/`.
- **`Cheque.legacy_status`** — وضعیت هفت‌مقداری پیش از ۱.۳.۱، عیناً نگه‌داشته.

### مهاجرت

`billing/migrations/0011` **برگشت‌پذیر است و هیچ چیز را پاک نمی‌کند.** مقدار
اصلی هر ردیف پیش از بازنویسی در `legacy_status` کپی می‌شود و مهاجرت معکوس دقیقاً
همان را برمی‌گرداند.

> **یک فرض که باید تأیید شود.** `returned` (عودت به مشتری) و `cancelled`
> (لغوشده) در میان چهار وضعیت تازه جایی ندارند. هر دو به `pending` نگاشت شده‌اند،
> نه به `bounced` — چون «برگشت» یعنی بانک پرداخت نکرده، و دربارهٔ این ردیف‌ها
> چنین چیزی رخ نداده. این نگاشت **پولی را زنده نمی‌کند**: پرداخت زیر هر کدام
> پیش‌تر لغو شده بود و همهٔ اعداد محصول از وضعیت پرداخت می‌آیند، نه از وضعیت چک.
> اثرش فقط برچسبی است که در فهرست چک‌ها دیده می‌شود. اگر برچسب جدا لازم است،
> `legacy_status` حقیقت را دارد و یک مهاجرت بعدی بدون هیچ از دست رفتنی می‌تواند
> اصلاحش کند.

### رفع شد

- فهرست مقادیر مجاز در قید `cheque_status_valid` دیگر دستی نگه‌داشته نمی‌شود؛ از
  `CHEQUE_STATUS_VALUES` می‌آید که کنار خود enum است. این فهرست دو بار از enum عقب
  افتاده بود و هر دو بار در تولید شکست. یک تست حالا یکی بودنشان را می‌بندد.

### بند ۲ — هویت و نشانی، هنگام صدور روی سند قفل می‌شود

فاکتور صادرشده سند چیزی است که **قبلاً اتفاق افتاده**، پس نباید بعداً عوض شود.
تا پیش از این، هویت خریدار زنده خوانده می‌شد؛ یعنی اصلاح یک غلط تایپی در شمارهٔ
اقتصادی یک مشتری، **بی‌صدا هر سند مالیاتی صادرشده به او را بازنویسی می‌کرد** —
دقیقاً آن یک کاری که یک سند مالیاتی هرگز نباید بکند.

- **۱۵ ستون تازه روی `Invoice`**: نام، شماره ملی/شناسه ملی، شماره اقتصادی، نشانی،
  کدپستی، شهر و تلفنِ **خریدار**؛ و همان‌ها به‌علاوهٔ شمارهٔ ثبت برای **فروشنده**.
- **فقط یک بار نوشته می‌شوند، توسط `issue_invoice`**، در همان تراکنش تغییر وضعیت.
  پس فاکتور صادرشده هرگز بدون snapshot وجود ندارد.
- **نشانی از پروندهٔ مشتری می‌آید و به هیچ عنوان دستی وارد نمی‌شود** (خواستهٔ صریح
  مالک محصول). این با «نبودِ راه نوشتن» تضمین شده، نه با یک اعتبارسنجی: هیچ فیلد
  serializer، هیچ ورودی فرم و هیچ مسیر API‌ای این ستون‌ها را نمی‌نویسد. **یک تست
  همین را می‌بندد** و اگر روزی یکی از این ستون‌ها نوشتنی شود، شکست می‌خورد.
- **فاکتور غیررسمی هم snapshot می‌گیرد.** آن هم چاپ و تحویل می‌شود؛ دلیلی ندارد
  نسخهٔ آن جابه‌جا شود در حالی که نسخهٔ رسمی ثابت می‌ماند.
- هویت فروشندهٔ دیپلوی از محیط می‌آید: `KARIZ_SELLER_REGISTRATION_NUMBER`،
  `KARIZ_SELLER_ADDRESS`، `KARIZ_SELLER_POSTAL_CODE`، `KARIZ_SELLER_CITY`،
  `KARIZ_SELLER_PHONE`. نبودشان مانع صدور نمی‌شود؛ فقط نام و شناسه و شماره
  اقتصادی لازم‌اند.

ستون‌ها روی ردیف‌های قدیمی خالی‌اند، و خالی یعنی «snapshot گرفته نشده».

### بند ۳ — یک دریافت بین چند فاکتور، و حذف اضافه‌پرداخت

- **`allocate_payment_across()`** و `POST /api/v1/payments/<id>/allocate-across/`:
  یک دریافت، چند فاکتور، **یک ثبت**. (بند ۳.۱ و ۳.۲) روی صفحهٔ دریافت، بخش تازهٔ
  «تقسیم بین چند فاکتور» جایی است که اپراتور می‌گوید سهم هر فاکتور چقدر است؛ خالی
  گذاشتن یعنی تا سقف مانده همان فاکتور.
- **همه‌اش یا هیچ‌کدام.** تقسیم یک تصمیم است که در یک صفحه گرفته می‌شود؛ اجرایی که
  دو فاکتور را تسویه کند و روی سومی بشکند، اپراتور را با وضعیتی رها می‌کند که
  هرگز انتخابش نکرده. تراکنش مشترک یعنی تنها چیزی که اتفاق افتاده، همان خطایی است
  که گزارش شده.
- **هیچ قاعده‌ای برای دسته‌ای بودن سست نمی‌شود**: همان مشتری، فقط فاکتور صادرشده،
  هرگز بیش از مانده فاکتور، هرگز بیش از باقی‌ماندهٔ دریافت. هر سطر از خود
  `allocate_payment` می‌گذرد.
- **پس از هر تخصیص، از مانده هر فاکتور کم و به `paid_amount` آن اضافه می‌شود** —
  همان چیزی که خواسته شد.
- **پرداخت جزئی همچنان مجاز است** (بند ۳.۳)، داخل تقسیم هم.
- **اضافه‌پرداخت حذف شد** (بند ۳.۴). دریافتی که از بدهی مشتری بیشتر باشد **رد
  می‌شود**، پیش از آنکه چیزی نوشته شود.

> **یک قید عمدی روی بند ۳.۴.** این قاعده فقط وقتی اعمال می‌شود که مشتری دست‌کم یک
> فاکتور صادرشده داشته باشد. دریافت از مشتری‌ای که هیچ فاکتوری ندارد، «پول روی
> حساب» است نه اضافه‌پرداخت — و بند ۳.۱ صریحاً می‌گوید دریافت می‌تواند
> تخصیص‌نیافته بماند و بعداً تخصیص یابد، که دقیقاً همین است. سنجیده شد: خواندن
> سخت‌گیرانه‌تر **۵۰ تست** را می‌شکست، یعنی گرفتن بیعانه پیش از فاکتور در این
> محصول جریان عادی است. با این قید، ۱ تست تغییر کرد — همانی که عمداً
> اضافه‌پرداخت می‌ساخت تا رد شدنش را بسنجد.

بازپرداخت از این مسیر نمی‌گذرد: طبق پاسخ بند ۵.۱، بازپرداخت به‌صورت **پرداختی**
ثبت می‌شود.

### آنچه هنوز انجام نشده

بندهای ۵، ۶ (از جمله «دفتر چیست؟» که باید توضیح داده و دوباره پرسیده شود)، ۷،
۸ (ابطال و صدور مجدد) و ۹ (چاپ شبیه نمونهٔ `kala-1.pdf`، مبلغ به حروف، مالیات
ستونی روی هر سطر). و فارسی‌کردن رویدادهای سامانه، که آخرین کار است.

---

## [1.2.1] — 2026-08-26

اسناد مالی دوجهته: دریافتی و پرداختی، برای هر سه نوع سند.

### یادداشت شماره‌گذاری — این نسخه از جدول بالا پیروی نمی‌کند

**این نسخه به تصمیم صریح مالک محصول `1.2.1` شماره خورده است.** طبق جدول ابتدای
همین فایل، PATCH یعنی «نه فیلدی اضافه می‌شود نه رفتاری عوض»، و این تغییر پنج
فیلد و دو مقدار enum تازه دارد و رفتار تازه می‌آورد — یعنی با آن تعریف **MINOR
است، نه PATCH**. تحلیل کامل پایین آمده تا بعدا کسی این را نقض ناخواستهٔ جدول
نخواند.

چرا MINOR بود و نه MAJOR: **هیچ `RunPython`‌ای در مهاجرت‌ها نیست.** هیچ ردیف
موجودی بازنویسی یا بازتفسیر نمی‌شود؛ سه `AlterField` فقط فهرست مقادیر مجاز را
گشاد می‌کنند یا یک NOT NULL را شل می‌کنند. اگر enum چک واقعا به چهار حالت جمع
می‌شد — یعنی REGISTERED و DEPOSITED در یک مقدار ادغام می‌شدند — آن مهاجرت
برگشت‌ناپذیر بود و طبق همان جدول MAJOR می‌شد. مالک محصول تصمیم گرفت فقط `spent`
اضافه شود، پس آن اتفاق نیفتاد.

**پیامد عملی:** کسی که فقط شماره‌ها را می‌بیند، از `1.2.0` به `1.2.1` انتظار یک
رفع اشکال دارد، نه چهار مهاجرت و پنج ستون تازه. هنگام استقرار باید همان دقت یک
نسخهٔ MINOR به کار برود.

### اضافه شد

- **`Payment.direction`** با دو مقدار «دریافتی» و «پرداختی»، پیش‌فرض دریافتی.
  هر ردیف پیش از این فیلد دریافتی بوده، پس پیش‌فرض تاریخ را بازتفسیر نمی‌کند.
- **`Payment.payee`** — گیرندهٔ پول در پرداختی. متن آزاد، چون گیرنده اغلب مشتری
  نیست و ساختن مدل تأمین‌کننده برای نگه داشتن یک نام، تصمیم بزرگ‌تری از این کار
  است.
- **`Payment.customer` حالا می‌تواند خالی باشد** — ولی فقط در پرداختی. دو قید
  پایگاه داده این را نگه می‌دارند: دریافتی همچنان مشتری می‌خواهد، و پرداختی
  گیرنده می‌خواهد.
- **`Cheque.bank_account`** (شماره جاری)، **`Cheque.source`** (چک خودمان / چک
  مشتری)، و **`Cheque.paid_to`** (به چه کسی خرج شد).
- **`Cheque.Status.SPENT`** — حالت هفتم، نه جایگزین شش تای قبلی.
- **`CustomerLedgerEntry.EntryType.PAYMENT_MADE`**.
- **`spend_received_cheque()`** و `POST /api/v1/cheques/<id>/spend/`.
- **نمودار «مبلغ به تفکیک جهت»** کنار نمودار روش.
- ۲۱ تست تازه در `billing/tests/test_bidirectional_payments.py`.

### قاعده‌های رفتاری

- **فقط دریافتی به فاکتور تخصیص می‌خورد.** پرداختی هرگز مانده فاکتور را کم
  نمی‌کند؛ اگر می‌کرد، پولی که از شرکت خارج شده بدهی مشتری را کم می‌کرد. این در
  سرویس اعمال می‌شود نه با قید پایگاه داده، چون یک CheckConstraint روی
  PaymentAllocation نمی‌تواند ستون direction را که روی جدول دیگری است بخواند —
  و همین در تست تثبیت شده.
- **دریافتی حساب مشتری را بستانکار می‌کند، پرداختی بدهکار.** جهت از قاعده‌ای
  می‌آید که خود `CustomerLedgerEntry` دربارهٔ خودش نوشته است («بدهکار یعنی
  بدهی مشتری بیشتر می‌شود»)، نه از قاعده‌ای که اینجا اختراع شده باشد.
- **پرداختی بدون مشتری هیچ سطری در دفتر نمی‌سازد** و ساختارا نمی‌تواند: مشتری
  روی `CustomerLedgerEntry` فیلد اجباری است.
- **خرج کردن چک هیچ چک یا سند تازه‌ای نمی‌سازد.** همان چک ثبت‌شده است که به
  دیگری داده می‌شود؛ ساختن ردیف دوم یعنی همان مبلغ دوبار شمرده شود. سند دریافتِ
  زیر آن بسته می‌شود، چون پول دیگر از آن مسیر نمی‌آید.
- **چک سپرده‌شده به بانک قابل خرج کردن نیست** — دست ما نیست.

### رفع شد

- **نمودار «دریافتی به تفکیک روش» حالا `direction=receipt` را فیلتر می‌کند.**
  بدون آن، از این پس مبالغ پرداختی هم رویش جمع می‌شد و عددی گزارش می‌شد که
  کاملا موجه به نظر می‌رسید و غلط بود. هیچ عدد موجودی عوض نمی‌شود، چون هر ردیف
  امروز دریافتی است.
- دو قید `CHECK` موجود فهرست مقادیر مجازشان را دستی نگه می‌داشتند و مقادیر تازه
  را نمی‌شناختند (`cheque_status_valid`، `ledger_entry_type_valid`). تست‌ها
  گرفتندشان؛ بدون آن، اولین چک خرج‌شده و اولین پرداختی به مشتری در تولید شکست
  می‌خورد.
- **نشت سطل throttle بین تست‌ها.** سطل با شناسهٔ کاربر کلید می‌خورد و تست‌های
  rollback‌شده همان شناسه‌ها را دوباره می‌سازند، پس یک تست خرجِ تست قبلی را به
  ارث می‌برد و به‌جای چیزی که بررسی می‌کرد ۴۲۹ می‌گرفت. `cache.clear()` به
  `setUp` اضافه شد.

### آنچه عمداً حدس زده نشد

- **بخش E همچنان باز است** جز آنچه صریحا خواسته شد: تخصیص اجباری یا اختیاری،
  اضافه‌پرداخت، برگشت پرداخت، و اینکه چه کسی ثبت و چه کسی تایید می‌کند.
- **`source=customer_endorsed` امروز روی هیچ ردیفی نوشته نمی‌شود.** طبق طرح،
  خرج کردن چک مشتری ردیف تازه‌ای نمی‌سازد، پس این مقدار جایی برای نشستن ندارد.
  در enum ماند چون خواسته شده بود، ولی نوشته نمی‌شود.
- **بند D.10 (snapshot هنگام صدور) همچنان باز است** و به این کار ربطی پیدا
  نکرد.
- مسیر `manual_paid_entry` دست‌نخورده ماند؛ یک تست تثبیت می‌کند که هیچ‌کدام از
  این تغییرها آن را تکان نداده است.

---

## [1.2.0] — 2026-08-26

فاز مالی و نمودار روی صفحات لیست، در چهار مرحله که هرکدام جدا کامل و تست شد.

**MINOR است نه PATCH:** سه فیلد تازه روی مدل، چهار مهاجرت افزایشی، و سه نقطهٔ
انتهایی تازه. هیچ قاعدهٔ کسب‌وکاری موجود عوض نشد و اختیار هیچ نقشی تغییر نکرد؛
ولی قراردادی که یک استقرار روی آن حساب می‌کند بزرگ‌تر شده است.

**آنچه عمداً حدس زده نشد:** `OPEN_BUSINESS_DECISIONS.md` بخش D بندهای ۳ تا ۶
(نرخ مالیات، شمول قیمت، ترتیب تخفیف/مالیات، رند کردن)، بند ۱۰ (snapshot هنگام
صدور)، و کل بخش E جز آنچه صریحا خواسته شد. بند ۷ (شماره‌گذاری) با تصمیم مالک
محصول بسته و پیاده شد.

---

### فاز ۱: فاکتور رسمی / غیررسمی

#### اضافه شد

- **`Invoice.invoice_type`** با دو مقدار «رسمی» و «غیررسمی»، پیش‌فرض غیررسمی.
  این پاسخ به پرسش D.2 در `OPEN_BUSINESS_DECISIONS.md` است: این استقرار هر دو
  را صادر می‌کند و تمایز در سطح سند ثبت می‌شود، نه در سطح استقرار.
- **`Customer.economic_code`** — ایران دو شناسهٔ متفاوت می‌دهد: شخص حقیقی کد
  ملی دارد (همان `national_id` موجود) و شخص حقوقی شناسهٔ ملی **به‌علاوهٔ** شمارهٔ
  اقتصادی. این دو عدد از هم مشتق نمی‌شوند، پس دو ستون جدا دارند.
- **هویت فروشنده در سطح استقرار** — `KARIZ_SELLER_LEGAL_NAME`،
  `KARIZ_SELLER_NATIONAL_ID`، `KARIZ_SELLER_ECONOMIC_CODE`. خالی بودنشان معنادار
  است: استقراری که پرشان نکرده نمی‌تواند فاکتور رسمی صادر کند و در لحظهٔ صدور رد
  می‌شود، به‌جای چاپ سند مالیاتی با فروشندهٔ خالی.
- **دو مهاجرت افزایشی**: `billing/0006_invoice_invoice_type.py` و
  `sales/0019_customer_economic_code.py`.
- ۱۳ تست تازه در `billing/tests/test_official_invoice.py`.

#### تغییر کرد

- صدور فاکتور رسمی وقتی هویتی کم باشد **رد می‌شود** و همهٔ موارد کم را یک‌جا
  گزارش می‌کند. بررسی در لحظهٔ صدور است نه پیش‌نویس، تا اپراتور بتواند سند را
  بسازد و بعد هویت‌ها را کامل کند.
- فرم فاکتور انتخابگر نوع دارد و زیر آن فهرست چیزهایی که هنوز کم است دیده
  می‌شود. آن فهرست فقط آینهٔ سرور است؛ تصمیم با سرویس است.

- **سری شمارهٔ رسمی، جدا و بدون‌شکاف** (`Invoice.official_number`) — تصمیم مالک
  محصول در پاسخ به D.7. سری `OINV-` مستقل از `INV-` است.
  - شماره **در لحظهٔ صدور** گرفته می‌شود، نه هنگام ساخت. اگر هنگام ساخت گرفته
    می‌شد، پیش‌نویسی که رها شود یا رسمی ساخته و بعد غیررسمی شود، شماره‌ای از این
    سری را مصرف کرده بود که روی هیچ سند مالیاتی نمی‌نشیند — یعنی دقیقاً همان
    شکافی که این سری نباید داشته باشد.
  - **لغو، شماره را آزاد نمی‌کند.** معنای بدون‌شکاف همین است.
  - دو قید پایگاه داده: یکتایی مشروط (خالی‌ها مستثنا) و اینکه فقط فاکتور رسمی
    می‌تواند شمارهٔ رسمی داشته باشد.

#### آنچه عمداً پیاده **نشد**

`OPEN_BUSINESS_DECISIONS.md` بخش D بندهای ۳ تا ۶ هنوز باز است: نرخ مالیات،
شمول یا عدم شمول قیمت، ترتیب تخفیف نسبت به مالیات، و قاعدهٔ رند کردن. هیچ‌کدام
حدس زده نشد. (بند ۷ — شماره‌گذاری — با تصمیم مالک محصول بسته شد و بالا پیاده
شده است.) یک تست صریح تثبیت می‌کند که فاکتور رسمی و
غیررسمی با همان سطرها **همان ارقام** را می‌دهند؛ اگر روزی فرق کنند آن تست
می‌شکند، و باید بشکند.

---

---

### فاز ۲: اسناد مالی / دریافت‌ها

#### تغییر کرد

- **فرم ثبت دریافت به حالت (mode) تبدیل شد**، به‌جای یک فرم با فهرست کشویی. هر
  روش فقط فیلدهای خودش را نشان می‌دهد:
  - **نقدی** — شماره رسید، تاریخ دریافت، مبلغ.
  - **حواله بانکی** — نام بانک، شماره حساب/شبا، شماره پیگیری، تاریخ، مبلغ.
  - **چک** — از همان مدل `Cheque` موجود؛ **در این دور دست نخورد** و اصلاحش به
    فاز بعد موکول شد.
  - **کارت** — حذف **نشد**؛ داده‌های قبلی روی آن هست. آخرین حالت است و کم‌رنگ‌تر،
    ولی کاملا کار می‌کند.
- برچسب فیلد مرجع با روش عوض می‌شود («شماره رسید» در برابر «شماره پیگیری»). روی
  رکورد یک ستون است، چون یک مفهوم است؛ ولی دو کلمه برای کسی که تایپش می‌کند
  یکی نیستند.
- مبلغ حالا از همان اینپوت ریالی گروه‌بندی‌شوندهٔ بقیهٔ پنل استفاده می‌کند، و
  تاریخ دریافت اینپوت جلالی دارد (خالی یعنی «حالا»).

#### اضافه شد

- **`Payment.bank_name` و `Payment.bank_account`** — حواله بانکی تا امروز جایی
  برای بانک مبدأ و شماره حساب نداشت. هر دو اختیاری‌اند: اپراتوری که حواله را از
  روی صورتحساب ثبت می‌کند ممکن است شمارهٔ حساب را نداشته باشد، و رد کردن رسید به
  آن دلیل یعنی پول اصلا ثبت نشود.
- **یک قید پایگاه داده**: این دو ستون فقط روی `bank_transfer` می‌نشینند. رسید
  نقدی‌ای که شماره حساب داشته باشد یعنی فرم در رکورد اشتباه نوشته است.
- مهاجرت افزایشی `billing/0008_payment_bank_account_payment_bank_name_and_more.py`.
- ۹ تست تازه در `billing/tests/test_payment_methods.py`، به‌علاوهٔ تست مرورگر که
  خود حالت‌ها را کلیک می‌کند.

#### آنچه عمداً دست نخورد

`Cheque` و چرخهٔ وضعیتش، تخصیص پرداخت به فاکتور، اضافه‌پرداخت، و برگشت پرداخت —
همگی در `OPEN_BUSINESS_DECISIONS.md` بخش E باز هستند و هیچ‌کدام حدس زده نشد. چک
همچنان تا وصول، حساب مشتری را بستانکار نمی‌کند.

---

---

### فاز ۳: زیرساخت نمودار + صفحهٔ مشتریان

#### تصمیم

گزینهٔ **الف** انتخاب شد: `renderLineChart` دست‌نویس با SVG، کنار
`renderBarChart`. دلیلش در `docs/frontend/CHARTS_GROUNDWORK.md` ثبت شد — سری
زمانی اینجا ده‌ها نقطه دارد نه هزاران (اندپوینت بیش از ۱۲۰ سطل را رد می‌کند)،
ApexCharts همچنان فقط داخل باندل ۳٫۵ مگابایتی در دسترس است، و RTL و تاریخ جلالی
و رقم فارسی باید برای هر نمودار جداگانه پیکربندی می‌شد به‌جای اینکه یک‌بار نوشته
شود. گزینهٔ ب برای روزی باز ماند که واقعا نقطهٔ زیاد یا zoom لازم شود.

#### اضافه شد

- **`renderLineChart`** — سری زمانی با SVG دست‌نویس. مقیاس با `viewBox`، پس روی
  موبایل بدون کد اندازه‌گیری خوانا می‌ماند. محور راست‌به‌چپ است. هر نقطه یک
  `<title>` دارد، یعنی تولتیپ بومی و قابل خواندن برای صفحه‌خوان، بدون یک خط کد
  موقعیت‌دهی.
- **دو اندپوینت تازه** در `reports/customer_views.py`:
  - `GET /api/v1/reports/customer-cities/` — پراکندگی شهری با درصد.
  - `GET /api/v1/reports/customer-growth/` — تعداد ثبت‌شده در هر هفته/ماه به
    همراه جمع تجمعی.
- **دو نمودار زیر جدول مشتریان**: پراکندگی شهری (میله‌ای) و روند رشد (خطی، با
  انتخاب هفتگی/ماهانه/بازهٔ دلخواه).
- ۱۸ تست در `reports/tests/test_customer_insights.py` و ۲۰ بررسی رفتاری روی خود
  رندرکننده.

#### جزئیاتی که عمدی‌اند

- **سطل خالی صفر گزارش می‌شود، نه حذف.** خطی که میان دو نقطه با فاصلهٔ سه‌ماهه
  کشیده شود در حالی که بقیهٔ فاصله‌ها یک‌ماهه است، دربارهٔ شیب دروغ می‌گوید.
- **خط تجمعی از جایی شروع می‌شود که دفتر واقعا ایستاده بود** — مشتریان قدیمی‌تر
  از بازه، ارتفاع شروع خط‌اند، نه صفر.
- **مشتری بدون شهر با استانش شمرده می‌شود؛ بدون هیچ‌کدام در ردیف «ثبت‌نشده».**
  حذفشان یعنی همهٔ درصدها سهمشان را بیش از واقع نشان دهند.
- **هر دو نمودار روی `customers_for` ساخته شده‌اند**، نه روی `Customer.objects`.
  بازاریاب فقط دفتر خودش را می‌شمارد — و فقط دفتر حقیقی را، چون همان selector
  این را هم اعمال می‌کند. تست‌ها هر دو را تثبیت می‌کنند.

#### رفع شد

- تست `test_the_script_carries_no_placeholder_or_third_party_reference` هر
  `http://` را رد می‌کرد و فضای‌نام XML لازم برای `createElementNS` را هم گرفت.
  آن رشته آدرس نیست، شناسه است و هیچ مرورگری واکشی‌اش نمی‌کند. به‌جای ضعیف کردن
  قاعده، دقیقا همان یک رشته استثنا شد و **یک تست دوم اضافه شد** که تضمین می‌کند
  استثنا همان یکی بماند و به در باز تبدیل نشود.

---

---

### فاز ۴: نمودار برای بقیهٔ صفحات لیست

#### اضافه شد

- **یازده نمودار، یک جدول اعلانی** — `reports/list_charts.py`. هر ورودی سه چیز را
  صریح نام می‌برد: قابلیت استقرار، اختیارهای نقش، و سازنده‌ای که از selector همان
  ماژول شروع می‌کند. یک view به‌جای یازده تا، ولی هیچ‌کدام از آن سه کنترل مشترک
  نیست — چیزی که مشترک است فقط لوله‌کشی است.
- `GET /api/v1/reports/list-chart/<key>/` — قابلیت لازم برای هر کلید در لحظهٔ
  درخواست حل می‌شود، پس استقراری که ماژولی ندارد برای نمودارش هم ۴۰۴ می‌دهد،
  دقیقا مثل صفحه‌هایش.
- کارت نمودار روی: فاکتورها، سفارش‌ها، دریافت‌ها، محصولات، دسته‌بندی‌ها، سرنخ‌ها،
  خدمات پس از فروش، اسناد پستی، موجودی انبار، فروش‌ها، تماس‌ها.
- بارگذاری با `data-list-chart` در قالب انجام می‌شود، نه با تابعی برای هر صفحه.
  صفحهٔ دوازدهم فقط یک کارت در قالب و یک ورودی در جدول لازم دارد، بدون هیچ
  جاوااسکریپت تازه‌ای.

#### دو انحراف از فهرست درخواستی، هر دو به‌خاطر خود مدل

- **فاکتورها بر اساس وضعیت تسویه.** `settlement_status` ستون نیست؛ یک property
  پایتونی روی `is_manually_settled`، `status`، `paid_amount` و `total_amount`
  است. با **خواندن همان property** شمرده می‌شود، نه با بازنویسی قاعده در SQL —
  چون آن روز که دو نسخه از هم فاصله بگیرند، نمودار بی‌صدا با فاکتوری که از آن
  آمده تناقض پیدا می‌کند.
- **ارزش موجودی** هم ستون نیست؛ `quantity * average_cost` است و در همان کوئری
  جمع زده می‌شود.

#### جزئیاتی که عمدی‌اند

- **دریافت لغوشده شمرده نمی‌شود.** نمودار پول دریافتی نباید پولی را بشمارد که
  پس گرفته شده است.
- **فروش لغوشده در نمودار بازاریاب نیست.**
- **دسته‌بندی بدون محصول فعال، با صفر دیده می‌شود** نه اینکه از صفحهٔ خودش غیب
  شود (`filter=` روی aggregate، نه روی queryset).
- **دنبالهٔ بلند در ردیف «سایر» جمع می‌شود، نه حذف** — تا جمعی که خواننده
  می‌بندد با فهرست بالای همان صفحه بخواند.
- **خطای نمودار فقط همان کارت را می‌گیرد.** نقشی که اختیارش را ندارد یا استقراری
  که ماژول را ندارد، فهرست بالای صفحه‌اش دست‌نخورده کار می‌کند.

#### تست

۱۴ تست تازه که **هر یازده سازنده را اجرا می‌کنند** — روی پایگاه دادهٔ خالی، با
دامنهٔ مدیر، و با دامنهٔ بازاریاب. هیچ‌کدام نمودارها را تک‌تک نام نمی‌برند، پس
نمودار دوازدهم از همان لحظهٔ اعلان پوشش داده می‌شود. خطای املایی در نام یک فیلد
یا اختیاری که هیچ نقشی ندارد، اینجا می‌شکند نه روزی که کسی آن یک صفحه را باز کند.

---

---

## [1.1.3] — 2026-08-26

نسخهٔ نمودارها و پاک‌سازی. هیچ مهاجرتی ندارد، هیچ قاعدهٔ کسب‌وکاری عوض نشده و
اختیار هیچ نقشی تغییر نکرده است.

### اضافه شد

- **یک رندرکنندهٔ واحد نمودار میله‌ای** — `renderBarChart` در
  `forooshbin-app.js`. ورودی‌اش `{label, value, display}` است: `value` طول میله
  را تعیین می‌کند و `display` همان متنی است که خواننده می‌بیند و از قبل قالب‌بندی
  شده. جدا نگه داشتن این دو کل طراحی است — با این کار یک نمودار نمی‌تواند
  به‌اشتباه همان عددی را چاپ کند که با آن اندازه گرفته.
  - گزینه‌ها: `ariaLabel`، `limit` (چند تای برتر)، `sort` (برای دنبالهٔ ثابت
    خاموش)، `keepZero` (دستهٔ خالی را حذف نکن، خالی بکش).
  - **هیچ کتابخانه‌ای اضافه نشد.** ApexCharts داخل باندل ۳٫۵ مگابایتی پوستهٔ
    خریداری‌شده است که `collectstatic` آن را کنار می‌گذارد، و هر نموداری که این
    گزارش‌ها لازم دارند مقایسه‌ای میان چند سطر است. مبنای تصمیم در
    `docs/frontend/CHARTS_GROUNDWORK.md` نوشته شده است.
- **سه نمودار تازه:**
  - **سنی مطالبات** — پنج بازهٔ سررسید به ترتیب ثابت. بازهٔ خالی حذف نمی‌شود؛
    «هیچ مطالبهٔ بیش از ۹۰ روز نداریم» خبر خوبِ خواننده است، نه دادهٔ غایب.
  - **ترکیب درآمد** — درآمد، بهای تمام‌شده و سود ناخالص کنار هم. سود منفی میله
    ندارد ولی رقمش چاپ می‌شود، پس زیان با «چیزی برای کشیدن نبود» پنهان نمی‌ماند.
  - **ارزش موجودی** — ده کالای با بیشترین ارزش؛ بقیه در جدول پایین می‌مانند.
- **`scripts/build-image.sh`** — تگ را از `VERSION` می‌خواند، پس نمی‌تواند نسخه‌ای
  را نام ببرد که داخل ایمیج نیست، و هر دو build-arg لازم Dockerfile را می‌دهد.
- **`docs/frontend/CHARTS_GROUNDWORK.md`**.

### تغییر کرد

- **دو رندرکنندهٔ نمودار به یکی تبدیل شدند.** پیش از این دو کپیِ تقریبا یکسان
  وجود داشت که از هم فاصله گرفته بودند — و همین دو نقص زیر را ساخته بود.
- **مستندات: پنج سند حذف شد** — `RELEASE_NOTES.md` و `SOURCE_MANIFEST.md` (هر دو
  در سطر اول خودشان را «historical, not live status» اعلام کرده بودند)،
  `DEPLOYMENT_BOOTSTRAP.md` (داخل بخش ۱.۱۵ رانبوک ادغام شد)، و دو گزارش
  پاک‌سازیِ تمام‌شده. تاریخچهٔ Git همه را نگه داشته است.

### رفع شد

- **هر استقرار تازه در همان بوت اول شکست می‌خورد.** `new_deployment.py` رازها را
  با `token_urlsafe(32)` می‌ساخت که ۴۳ کاراکتر است، در حالی که
  `config/production_env.py` هر `DJANGO_SECRET_KEY` زیر ۵۰ کاراکتر را رد می‌کند.
  هنوز هیچ استقراری با آن ابزار ساخته نشده بود، پس کسی متوجه نشده بود. تست
  کنارش حداقل ۱۶ را بررسی می‌کرد — قاعدهٔ اسکریپت bootstrap برای رمز پایگاه
  داده — و راضی بود. حالا فایل تولیدشده به خودِ `validate_production_environment`
  داده می‌شود؛ رازها ۶۴ کاراکترند.
- **`displayDay` دو بار تعریف شده بود** و نسخهٔ دوم برنده می‌شد، پس نگهبانِ
  `YYYY-MM-DD` خام — که روز تقویمی است و نباید از منطقهٔ زمانی رد شود — هرگز اجرا
  نمی‌شد.
- **نمودار عملکرد مبلغ خام چاپ می‌کرد** (`12500000.00`) کنار جدول‌هایی که
  `۱۲،۵۰۰،۰۰۰ ریال` می‌خواندند.
- **نمودار پیامک ورودی تاریخ میلادی و رقم لاتین نشان می‌داد** در پنلی که همه‌جایش
  جلالی است. `local_date` یک `DateField` است و خام می‌آید؛ همان چیزی که نگهبان
  بالا برایش نوشته شده بود.
- **رانبوک در حالت قفل‌شدگی گمراه می‌کرد.** نوشته بود
  `bootstrap_platform_admin` وقتی یک مدیر پلتفرم **فعال** وجود دارد اجرا نمی‌شود.
  کد `filter(role=PLATFORM_ADMIN).exists()` است، بدون هیچ شرط `is_active` — پس
  غیرفعال کردن تنها مدیر، این مسیر را باز نمی‌کند. دقیقا همان وضعیتی که این بخش
  در آن خوانده می‌شود.
- **دستور build رانبوک هرگز کار نمی‌کرد**: `docker build .` خالی با
  `base name (${PYTHON_BASE_IMAGE}) should not be blank` متوقف می‌شود.

### پس گرفته شد

- **توضیح TTY در نسخهٔ قبل اشتباه بود.** ادعا شده بود psql هنگام وجود TTY رمزها
  را از ترمینال می‌خواند و مقادیر `.env` را دور می‌ریزد. `-T` دو انتشار است که
  اعمال شده و پرسش‌ها همچنان چاپ می‌شوند، در حالی که پشتیبان‌گیریِ بلافاصله بعدش
  با همان نقش موفق است — یعنی مقادیر لوله‌شده خوانده می‌شوند. پرسش‌ها انعکاس psql
  هنگام خواندن stdin است و مسیر غیرتعاملی کنارش عمدا فقط برای پایگاه دادهٔ
  یک‌بارمصرف باز است.

---

## [1.1.2] — 2026-08-25

نسخهٔ ابزار استقرار و پرداخت‌های ظاهری. هیچ منطق کسب‌وکاری، هیچ مهاجرتی و هیچ
تغییری در اختیارات نقش‌ها ندارد؛ به همین دلیل PATCH است و نه MINOR — با اینکه
یک ابزار تازه به `scripts/` اضافه شده، آن ابزار بیرون از برنامهٔ سرویس‌شده است.

### اضافه شد

- **`scripts/new_deployment.py`** — یک استقرار تازه را از یک مجموعه پاسخ
  می‌سازد: نام پروژه، نام پایگاه داده، هر چهار نقش، هر دو والیوم و پنج راز
  تولیدشده، همه از یک `--slug` مشتق می‌شوند تا نتوانند با هم اختلاف پیدا کنند.
  فهرست قابلیت‌ها و قاعده‌های وابستگی از `common/deployment/registry.py`
  **ایمپورت** می‌شود، نه رونویسی، پس ابزار و برنامهٔ در حال اجرا هرگز از هم دور
  نمی‌افتند. چیزی نمی‌سازد و چیزی را بالا نمی‌آورد؛ یک فایل با دسترسی `0600`
  می‌نویسد و قدم‌های بعدی را چاپ می‌کند. `.env` موجود را هرگز بازنویسی نمی‌کند.
- **`scripts/deploy.sh`** — دستور یک‌مرحله‌ای انتشار، به‌همراه `--status`،
  `--rollback` و `--no-backup`. پیش از شروع سه چیز را بررسی می‌کند که سکوتشان
  گران تمام می‌شود: کهنه بودن `nginx/default.conf` در checkout، اشغال بودن پورت
  ۸۰ به‌دست پروژهٔ دیگر، و اشتراک والیوم پایگاه داده با پروژهٔ دیگر.
- **بررسی تازه:** اگر تگ ایمیج شکل `vX.Y.Z` داشته باشد و با فایل `VERSION`
  داخل همان ایمیج یکی نباشد، انتشار **انجام نمی‌شود**. دقیقاً همان اشتباهی که
  این نسخه را لازم کرد: ایمیجی با تگ `v1.1.2` ساخته شد در حالی که `VERSION`
  داخلش هنوز `1.1.1` بود، و پاورقی پنل — که راست می‌گفت — `۱.۱.۱` نشان داد.

### تغییر کرد

- **جزئیات ظاهری جدول‌ها و دیالوگ‌ها**، همه داخل پالت و قالب خریداری‌شده و بدون
  دست زدن به رنگ، فونت یا ساختار صفحه:
  - **فاصلهٔ دکمه‌های عملیات در سطرهای جدول.** این دکمه‌ها در جاوااسکریپت پشت هم
    به سلول اضافه می‌شوند، پس هیچ فاصله‌ای بینشان نبود و روی موبایل دو هدف لمسی
    به هم چسبیده بودند. روی خودِ دکمه‌ها اعمال شد نه روی کلاس سلول، چون فقط سه
    سلول از ده سلول عملیاتِ برنامه آن کلاس را دارند.
  - **ارقام هم‌عرض (tabular) برای ستون‌های عددی.** ستون‌های `dir="ltr"` — قیمت،
    شمارهٔ فاکتور، شمارهٔ تلفن — عمودی خوانده می‌شوند و رقم‌های با عرض متفاوت
    مقایسهٔ نگاهی را سخت می‌کنند.
  - **برجسته شدن سطر زیر نشانگر.** جدول مشتریان نه ستون دارد و سخت‌ترین کارِ
    خواندنش، نگه داشتن چشم روی یک سطر است. روی لمس غیرفعال است، چون مرورگر
    موبایل حالت hover را شبیه‌سازی می‌کند و روی آخرین سطر لمس‌شده گیر می‌کند.
  - **ارتفاع ورودی‌ها روی نشانگر لمسی** به ۴۴ پیکسل رسید، هم‌اندازهٔ دکمه‌های
    کنارشان؛ تراکم دسکتاپ دست‌نخورده ماند.
  - **`dvh` به‌جای `vh` برای ارتفاع دیالوگ.** `100vh` روی موبایل یعنی صفحه با
    نوارهای جمع‌شده، پس پاورقی دیالوگ — همان‌جا که دکمه‌ها هستند — زیر لبهٔ صفحه
    می‌افتاد. روی گوشی، فاصلهٔ امن (`env(safe-area-inset-bottom)`) هم رعایت شد.
  - ورود کوتاه دیالوگ (۱۶۰ms، فقط مقیاس و شفافیت تا چیزی جابه‌جا نشود)، پشت
    `prefers-reduced-motion`.
  - `.sr-only` بازنشانی `padding` و `border` را کم داشت.

## [1.1.1] — 2026-08-25 — روی صحنه‌آزمایی، آزموده و در حال اجرا

دو دفتر مشتری، و فاکتورهای مشتری در صفحهٔ جزئیات او.

> **یادداشت شماره‌گذاری.** این نسخه به درخواست مالک محصول `1.1.1` شماره خورده
> است. طبق جدول بالای همین فایل، افزوده شدن فیلد `Customer.kind`، یک مهاجرت
> افزایشی و دو نقطهٔ انتهایی تازه با تعریف **MINOR** می‌خواند، نه PATCH. شماره
> عمداً و با تصمیم مالک محصول این‌طور گذاشته شد؛ اینجا ثبت می‌شود تا بعداً کسی
> تصور نکند جدول نقض شده است.

### اضافه شد

- **دو دفتر مشتری — حقیقی و حقوقی (`Customer.kind`).** یک جدول، دو فهرست: همان
  ستون‌ها و همان فیلترها یک دفتر دیگر را می‌خوانند، به‌جای صفحهٔ دومی که همه‌چیز
  را تکرار کند. پیش‌فرض **حقیقی** است و عمداً خالی نیست — برخلاف `Product.unit`
  که خالی‌اش یعنی «هنوز ثبت نشده». اگر خالی می‌ماند، هر مشتریِ پیش از این فیلد در
  **هیچ‌کدام** از دو فهرست دیده نمی‌شد و از دفتری که بازاریاب با آن کار می‌کند
  ناپدید می‌شد.
  - مهاجرت افزایشی `sales/migrations/0018_customer_kind_customer_customer_kind_valid.py`
    (یک فیلد + یک قید بررسی).
  - دو دکمهٔ **حقیقی** و **حقوقی** زیر دکمهٔ «مشتری جدید».
  - ورودی **نوع مشتری** در فرم ساخت مشتری.
  - پارامتر `kind` روی `GET /api/v1/customers/` و روی خروجی اکسل.
- **ورودی اکسل مشتریان** — `POST /api/v1/customers/import-xlsx/`.
  - همان گردش رفت‌وبرگشت محصولات: سرستون‌های خروجی و ورودی یکی‌اند و ستون‌ها
    **بر اساس نام** تطبیق داده می‌شوند.
  - **تشخیص تکراری با شمارهٔ تلفن** انجام می‌شود — همان چیزی که در بقیهٔ این کد
    هویت مشتری است و در پایگاه داده هم قید یکتایی دارد. کد ملی هم وقتی نوشته
    شده باشد بررسی می‌شود. شماره پیش از مقایسه نرمال می‌شود، پس `09121110005` و
    `+989121110005` یک نفرند.
  - **محدودیت آگاهانه:** ردیفی که نه تلفن دارد و نه کد ملی، با هیچ‌چیز قابل
    مقایسه نیست و ساخته می‌شود.
  - لیستی که در پنجره انتخاب شده بر ستون `kind` داخل فایل **مقدم است**؛ کاربر یک
    لیست را برای ورود انتخاب کرده و پخش‌کردن ردیف‌هایش بین دو دفتر آن چیزی نیست
    که خواسته.

### تغییر کرد

- **دامنهٔ دید بازاریاب به دفتر حقیقی محدود شد** (`customers_for`). این یک قاعدهٔ
  دامنهٔ داده است، نه پنهان‌سازی در قالب: بازاریاب نه دکمه‌ها را می‌بیند، نه با
  `?kind=legal` چیزی می‌گیرد، نه می‌تواند مشتری حقوقی را با شناسه باز کند، نه
  می‌تواند یکی بسازد یا مشتری خودش را به آن دفتر منتقل کند. چون پیش‌فرض فیلد
  «حقیقی» است، هیچ مشتریِ موجودی از دید هیچ بازاریابی خارج نمی‌شود.
- **دکمهٔ «دریافت XLSX» به «خروجی لیست» تغییر نام داد** و کنارش **«ورودی لیست»**
  اضافه شد. هر دو یک پنجره باز می‌کنند که اول می‌پرسد کدام لیست؛ خروجی بعد از آن
  دکمهٔ **دانلود** می‌دهد و ورودی، **بارگذاری فایل** و دکمهٔ **بارگذاری**.
- **چیدمان فیلترهای صفحهٔ مشتریان:** جست‌وجو و مرتب‌سازی سمت راست، دکمهٔ «اعمال»
  سمت چپ، هر دو با فاصله از لبه‌های کارت.
- **ستون‌های خروجی مشتریان** حالا `kind` و `address` هم دارند، و ستون تلفن شمارهٔ
  خوانا (`raw_phone`) را می‌دهد نه شکل نرمال‌شده را — چون همان است که کاربر
  می‌خواند و دوباره می‌نویسد.
- سرویس `create_customer_with_phone` و `update_customer` حالا `kind` را
  اعتبارسنجی می‌کنند و نام فیلد درست را در خطا می‌گویند؛ قید پایگاه داده به‌تنهایی
  خطایی می‌داد که هیچ فیلدی را نام نمی‌برد.
- `nginx/default.conf` و `FILE_UPLOAD_PATHS` حالا هر دو مسیر بارگذاری را پوشش
  می‌دهند.

### حذف شد

- **باکس «سفارشات مرتبط» از صفحهٔ جزئیات مشتری**، و جایش **«فاکتورهای مرتبط»**
  نشست. هر سطر یک سند است: شماره، وضعیت، تسویه، مبلغ کل، مانده، تاریخ صدور. هر
  دو ستون تسویه و مانده نمایش داده می‌شوند چون می‌توانند با هم نخوانند — فاکتوری
  که دستی تسویه شده «پرداخت‌شده» خوانده می‌شود در حالی که ماندهٔ متعارفش
  دست‌نخورده است، و نشان‌ندادن یکی از این دو یعنی صفحه دروغ بگوید.

---

## [1.1.0] — 2026-08-24 — روی صحنه‌آزمایی؛ جداگانه تحویل نشد

نسخهٔ کاتالوگ محصولات و واحد پول. همهٔ تغییرها افزایشی‌اند: هیچ دادهٔ موجودی
دست‌کاری نمی‌شود و هیچ اختیاری از هیچ نقشی گرفته نمی‌شود.

### اضافه شد

- **واحد محصول (`Product.unit`).** فهرست ثابتِ «جعبه، عدد، کارتن، کیلوگرم، گرم»؛
  متن آزاد نیست، چون دو املای مختلف از یک واحد روی دو سند مختلف می‌نشیند و آن دو
  سند با هم اختلاف پیدا می‌کنند. مقدار خالی مجاز است و معنایش «هنوز ثبت نشده»
  است، نه یک پیش‌فرض خاموش — پیش‌فرض گذاشتن یعنی نوشتن واحد اشتباه روی فاکتور
  مشتری.
  - مهاجرت افزایشی `sales/migrations/0017_product_unit_product_product_unit_valid.py`
    (یک فیلد + یک قید بررسی). هیچ مهاجرت قدیمی بازنویسی نشده است.
  - ستون **واحد** در فهرست محصولات.
  - `unit` و `unit_display` در `ProductSerializer`.
- **خروجی اکسل کاتالوگ محصولات** — `GET /api/v1/exports/products.xlsx`.
  دامنهٔ دید دقیقاً همان دامنهٔ فهرست محصولات است.
- **ورودی اکسل محصولات** — `POST /api/v1/products/import-xlsx/`.
  - ستون‌ها **بر اساس نام سرستون** تطبیق داده می‌شوند، نه جای ستون؛ پس فایلی که
    ستون‌هایش جابه‌جا شده باشد هم درست خوانده می‌شود، و فایلی که از این سامانه
    نیامده باشد به‌کل رد می‌شود به‌جای آنکه ستون‌به‌ستون در فیلد اشتباه بنشیند.
  - سرستون‌های خروجی و ورودی **یکی‌اند**: کاربر اول خروجی می‌گیرد، رویش
    می‌نویسد و همان فایل را برمی‌گرداند.
  - **محصول تکراری اضافه نمی‌شود.** تکراری بودن از روی کد محصول (SKU) و بدون
    حساسیت به بزرگی و کوچکی حروف تشخیص داده می‌شود. ورود اطلاعات هرگز محصول
    موجود را بازنویسی نمی‌کند.
  - پاسخ سه عدد جدا می‌دهد — ثبت‌شده، تکراری، نامعتبر — و پنل هر سه را به کاربر
    می‌گوید: تعداد ثبت موفق، تعداد تکراریِ اضافه‌نشده، و تعداد ردیف نامعتبر.
  - هر ردیف از همان سرویس `create_product` می‌گذرد که فرم دستی از آن می‌گذرد، پس
    اعتبارسنجی، بررسی دسترسی و ردّ ممیزی یکسان است.
  - سقف‌ها: ۵۰۰۰ ردیف در هر بار، و ۵ مگابایت حجم فایل.
- **دو دکمهٔ «خروجی اکسل» و «ورودی اکسل»** در صفحهٔ محصولات.
- **فایل `VERSION`**، نمایش نسخه در پاورقی پنل، و همین فایل `CHANGELOG.md`.

### تغییر کرد

- **همهٔ قیمت‌های پنل به ریال و با جداکنندهٔ سه‌رقمی.** اعشار (`٫۰۰`) از نمایش
  برداشته شد — ریال در عمل واحد خردتر ندارد و `٫۰۰` دائمی فقط عدد را طولانی‌تر
  می‌کند. گرد کردن روی رشتهٔ رقم‌ها انجام می‌شود نه از راه `float`، چون مبلغ
  همان‌طور که ذخیره شده معتبر است و رفت‌وبرگشت اعشاری می‌تواند رقم آخرش را جابه‌جا
  کند. **مقدار ذخیره‌شده در پایگاه داده دست‌نخورده و با همان دو رقم اعشار باقی
  می‌ماند؛ این تغییر فقط نمایشی است.**
- **قیمت‌ها همان‌طور که تایپ می‌شوند گروه‌بندی می‌شوند**، پس لازم نیست کسی «،» را
  دستی بزند. این روی همهٔ فیلدهای مبلغ پنل اعمال شده است، نه فقط محصولات: قیمت
  محصول، قیمت واحد سطر سند، بهای واحد حرکت انبار، مبلغ دریافت، مبلغ تخصیص، مانده
  اول دوره، و کادر «پرداخت‌شده»ی فاکتور.
- **صفحهٔ محصول تازه:** ورودی «بارکد داخلی» جای خود را به **واحد** داد.
- **صفحهٔ جزئیات محصول:** همان جایگزینی، به‌علاوهٔ نمایش واحد.
- **فیلترهای صفحهٔ محصولات** تمام عرض کارت را می‌گیرند و جست‌وجو بزرگ‌ترین سهم را
  دارد؛ پیش از این وسط‌چین به نظر می‌رسیدند.
- **حد حجم بدنهٔ درخواست** حالا دو مقدار دارد: مقدار عمومی (۲۵۶ کیلوبایت،
  اندازه‌گیری‌شده از بزرگ‌ترین سندی که API می‌پذیرد) و یک مقدار جدا و همچنان
  کراندار برای مسیر بارگذاری فایل (۵ مگابایت). بدون این تفکیک، برای اینکه یک
  مسیر فایل بگیرد باید حد همهٔ مسیرها بالا می‌رفت. `nginx/default.conf` هم همین
  تفکیک را دارد و باید با تنظیمات هم‌گام بماند.
- سرویس `create_product` حالا خودش `unit` را اعتبارسنجی می‌کند. پیش از این قید
  پایگاه داده آن را می‌گرفت، اما به شکل `IntegrityError` که این ماژول با پیام
  «کد محصول تکراری است» گزارشش می‌کرد — یعنی نام فیلد اشتباه به کاربر می‌گفت.

### حذف شد

- **فیلتر مرتب‌سازی صفحهٔ محصولات، به‌طور کامل** — همان کنترلی که پنج گزینهٔ
  «نام، کد، کمترین قیمت، بیشترین قیمت، تازه‌ترین» را داشت. فهرست حالا به ترتیب
  نام مدل می‌ماند و پارامتر `ordering` دیگر از این صفحه فرستاده نمی‌شود.
  (نقطهٔ انتهایی API همچنان مرتب‌سازی را می‌پذیرد؛ فقط این کنترل از رابط برداشته
  شده است.)

### رفع شد

- **پدینگ فرم جزئیات محصول.** محتوای فرم داخل `card-body` قرار گرفت؛ پیش از این
  ورودی‌ها به لبهٔ چپ و راست کارت می‌چسبیدند و با بقیهٔ فرم‌های پنل هم‌شکل نبودند.
- قیمت در فهرست محصولات خام چاپ می‌شد (`12500000.00`) در حالی که هر جدول دیگری
  از قالب‌بند مشترک استفاده می‌کرد.
- دو کادر خواندنیِ «قیمت واحد» و «مبلغ کل» در صفحهٔ جزئیات فروش هم خام بودند.

---

## [1.0.0] — 2026-08-20 — **نسخهٔ تحویل‌شده**

نسخه‌ای که به شرکت **TIARA** تحویل داده شد.

### اضافه شد

- دارایی‌های هویتی برند: favicon، مانیفست سایت، لوگو.
- [`docs/ops/FOROOSHBIN_DEPLOYMENT_RUNBOOK.md`](docs/ops/FOROOSHBIN_DEPLOYMENT_RUNBOOK.md)
  — تنها سند مرجع نصب، راه‌اندازی، به‌روزرسانی، پشتیبان‌گیری، بازیابی و
  عیب‌یابی. هر دستور آن در برابر خود مخزن بررسی شده است.

### رفع شد

- **استقرار به‌خاطر نام پایگاه داده رد می‌شد.** یک بررسی هویتی که از پاک‌سازی نام
  برند به‌جا مانده بود، استقرار سالمِ صحنه‌آزمایی را با پیام
  `POSTGRES_INIT_USER must use the frooshbin role identity` متوقف می‌کرد. آن
  بررسی از هیچ چیزی محافظت نمی‌کرد و از هر سه لایه برداشته شد.
- ارتفاع لوگوی صفحهٔ ورود که باعث اسکرول شدن صفحه می‌شد.
- یک مسیر دارایی ایستای منسوخ در مستندات (`/static/common/favicon.ico` که به
  `common/brand/` منتقل شده بود).

---

## [0.7.0] — 2026-08-18 — تصمیم‌های محصول و ممیزی

### اضافه شد

- **جامعهٔ هدف کمپین** (`TargetAudienceMember`) با وضعیت مشتق‌شده؛ شمارهٔ تلفن
  هویت عضو است.
- **سرنخ‌ها** واژگان وضعیت گرفتند و مشتری‌شان اختیاری شد.
- **تسویهٔ دستی فاکتور**: یک بازنویسیِ نمایشیِ سطح‌فاکتور، **یک‌طرفه و
  برگشت‌ناپذیر**، بدون هیچ اثر حسابداری. هیچ رکورد پرداخت، تخصیص یا دفتر معینی
  ساخته نمی‌شود و ماندهٔ متعارف مشتری تکان نمی‌خورد.
- **فعال/غیرفعال کردن برگشت‌پذیر** مشتری و محصول به‌دست مدیر پلتفرم؛ هیچ دادهٔ
  تاریخی حذف نمی‌شود.
- همهٔ صفحه‌های سفارش، فاکتور، کمپین و کال‌سنتر روی اثاثیهٔ خود قالب Metronic
  بازنویسی شدند.

### تغییر کرد

- **سفارش مالک چرخهٔ موجودی است**؛ فاکتور هرگز موجودی را جابه‌جا نمی‌کند
  (`BILLING_INVOICE_AFFECTS_STOCK` به‌طور پیش‌فرض خاموش).
- خنثی بودن نسبت به تکرار: پرچم `stock_applied` به‌همراه شمارندهٔ `stock_revision`.

### حذف شد

- **پیش‌فاکتور (Quotation) از پنل برداشته شد** — از سایدبار هم دیده نمی‌شود.
  قابلیت حذف نشد، خاموش شد (`DEFAULT_OFF_FEATURES`)؛ پس داده‌های تاریخی سر جایشان
  می‌مانند و هر استقرار دیگری می‌تواند روشنش کند.

### رفع شد

پنج نقص با شدت بالا از ممیزی کد:

- **نشت کلید خنثی‌سازی تکرار در پرداخت** — کلید به‌تنهایی تطبیق داده می‌شد و
  پرداخت مشتری دیگری را برمی‌گرداند، و در ضمن یک پرداخت واقعی دوم را بی‌صدا
  می‌بلعید.
- **رقابت روی موجودی در PostgreSQL** — `IntegrityError` بدون savepoint گرفته
  می‌شد.
- **ماندهٔ دفتر معین** از `balance_after` جدیدترین سطر خوانده می‌شد، پس سطرهای با
  تاریخ عقب‌افتاده ناپدید می‌شدند. حالا از جمع بدهکار منهای بستانکار حساب می‌شود.
- **کلید نشست از API بیرون می‌رفت** — با ارجاع HMAC جایگزین شد.
- صفحهٔ «دسترسی مجاز نیست» ماژول خودش را اجرا می‌کرد و خطای اجرانشده می‌داد.

---

## [0.6.0] — 2026-08-17 — رابط کاربری روی قالب خریداری‌شده

### تغییر کرد

- کل رابط سرویس‌شده روی قالب **Metronic RTL** (نسخهٔ سایدبار تیره) بازسازی شد.
- فهرست نادیده‌گیریِ `collectstatic` واقعی شد: دارایی‌های جمع‌آوری‌شده از ۱۰۵
  مگابایت به ۱۱ مگابایت رسید.

### رفع شد

- اجرای قالب در ایمیج Docker جا افتاد.
- استقرار بدون دامنه (فقط IP) پشتیبانی شد.
- همهٔ اختیارات (capabilities) از کانتینر برنامه گرفته شد.
- فاکتور چاپی روی کاغذ و روی گوشی خوانا شد.

---

## [0.5.0] — 2026-08-16 — انبار، مالی و هویت ForooshBin

### اضافه شد

- ماژول‌های **انبار** و **مالی** با معنای کراندار؛ **دفتر معین فقط-افزودنی**
  (`CustomerLedgerEntry`).
- گزارش‌های مالی در رابط فارسی.
- چاپ فاکتور و پیش‌فاکتور به PDF روی سرور.
- **تاریخ جلالی** در سراسر رابط؛ ذخیره‌سازی متعارف همچنان میلادی ISO است.

### تغییر کرد

- نام محصولِ رو به مشتری به **ForooshBin / فروش‌بین** تغییر کرد.
- نقش `company_it` مهار شد؛ شکاف‌های نشست و خروجی بسته شد.

---

## [0.4.0] — 2026-08-15 — امنیت و اثبات اجرا

### اضافه شد

- **مانیفست استقرار امضاشده (Ed25519)** به‌عنوان تنها منبع درستیِ فهرست
  قابلیت‌ها؛ در صورت نبود یا نامعتبر بودن، **بسته** می‌شود نه باز.
- اثبات بازیابی نسخهٔ پشتیبان روی PostgreSQL واقعی.

### تغییر کرد

- مدیریت کاربران به مدیر پلتفرم محدود شد.
- در معرض بودن Django Admin مهار شد.
- گیت‌های امنیتی پایگاه داده واقعاً بسته می‌شوند.

---

## [0.3.0] — 2026-08-13 … 2026-08-14

### اضافه شد

- **اسناد فروش** و تاریخچهٔ وضعیت پستی.
- **خدمات پس از فروش** با CRUD، تاریخچه و کنترل دسترسی نقش‌محور.
- **پیامک ورودی** با گزارش و ریزنمایی.
- **دسته‌بندی محصولات**.
- گزارش جزئیات عملکرد کاربر.

---

## [0.2.0] — 2026-08-11 … 2026-08-12

### اضافه شد

- سلسله‌مراتب سایدبار و جای‌گیرِ ماژول‌های بعدی.
- کد پستی و دسته‌بندی در پروندهٔ مشتری.
- فیلتر وضعیت فعال محصول.

### تغییر کرد

- واژگان «مشتری» ↔ «بازاریاب (کال سنتر)» دو بار در کل قالب‌ها و آزمون‌ها اصلاح
  شد تا با معنای مورد نظر مدیر محصول یکی شود.

---

## [0.1.0] — 2026-08-09 … 2026-08-10 — پایه

### اضافه شد

- ساختار پروژهٔ Django/DRF، احراز هویت و مدیریت کاربران.
- مدیریت محصول و فروش (نما، قالب، سریالایزر).
- اسکریپت‌های آمادگی بار و اعتبارسنجی ایمیج انتشار.
