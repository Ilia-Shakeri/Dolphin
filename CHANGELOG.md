# تغییرنامه — Dolphin / دلفین

این فایل می‌گوید هر نسخه دقیقاً چه چیزی است: چه چیزی **اضافه** شد، چه چیزی
**تغییر** کرد، چه چیزی **حذف** شد و چه چیزی **رفع** شد.

> نام محصول در نسخه‌های قدیمی‌تر این فایل ForooshBin/فروش‌بین بود (پیش از آن
> با نام داخلی Kariz)؛ ورودی‌های تاریخی زیر عمداً به همان اسمی که آن زمان
> واقعاً منتشر شده بود اشاره می‌کنند، نه با نام فعلی بازنویسی شده‌اند — شرح
> کامل تصمیم در `[1.6.9]`.

شمارهٔ نسخهٔ در حال اجرا در فایل [`VERSION`](VERSION) در ریشهٔ مخزن است. همان فایل
را `config/settings.py` می‌خواند (`DOLPHIN_VERSION`)، در پاورقی پنل نمایش داده
می‌شود و در طرح‌وارهٔ OpenAPI می‌نشیند. برای بالا بردن نسخه فقط همان یک فایل تغییر
می‌کند.

## قاعدهٔ شماره‌گذاری

`MAJOR.MINOR.PATCH` — نسخه‌بندی معنایی، با این معنا برای این محصول:

| بخش | چه وقت بالا می‌رود | نمونه |
|---|---|---|
| **MAJOR** | قراردادی که مشتری روی آن حساب کرده می‌شکند: اختیارِ یک نقش عوض می‌شود، یک قاعدهٔ حسابداری تغییر معنا می‌دهد، یا مهاجرتی لازم است که برگشت‌پذیر نیست | ۰.x → ۱.۰.۰ |
| **MINOR** | قابلیت تازه‌ای اضافه می‌شود و کار قبلی سر جایش می‌ماند؛ مهاجرت‌ها فقط افزایشی‌اند | ۱.۰.۰ → ۱.۱.۰ |
| **PATCH** | فقط رفع اشکال یا اصلاح مستندات؛ نه فیلدی اضافه می‌شود نه رفتاری عوض | ۱.۱.۰ → ۱.۱.۱ |

## کدام نسخه، کجا

| محیط | نسخه | تاریخ |
|---|---|---|
| صحنه‌آزمایی (Nerkhbaan) | `1.2.1` | ۲۰۲۶-۰۸-۲۶ |
| TIARA (تولید) | `1.0.0` | ۲۰۲۶-۰۸-۲۰ |

نسخه‌های پس از `1.0.0` هنوز به TIARA نرفته‌اند. `1.3.1` هنوز روی صحنه‌آزمایی هم نرفته است.

> **دربارهٔ شماره‌های پیش از ۱.۰.۰:** این پروژه تا زمان تحویل تگ نسخه نداشت.
> شماره‌های دوره‌های پیش از تحویل **بعداً و از روی تاریخچهٔ واقعی Git** گذاشته
> شده‌اند تا بشود مسیر کار را خواند. آن‌ها هرگز به‌عنوان نسخهٔ منتشرشده تحویل کسی
> نشده‌اند؛ تنها نسخه‌ای که واقعاً تحویل شده `1.0.0` است.

---

## [1.11.1] — ۲۰۲۶-۰۹-۰۵

### ممیزی کل مخزن، بدون تغییر منطق یا ظاهر

طبق دستور مستقیم مالک محصول: کل پروژه دیباگ شود و هر باگی پیدا شد رفع
شود، بدون تغییر تم یا منطق اصلی.

**مانع همین نشست:** سرویس PostgreSQL محلی از `۲۰۲۶-۰۸-۲۷` عملاً از کار
افتاده بود — ویندوز سرویس را «در حال اجرا» نشان می‌دهد، اما
`pg_ctl status` می‌گوید «سروری در حال اجرا نیست»، هیچ‌چیز روی پورت ۵۴۳۲
گوش نمی‌دهد، و فایل‌های لاگ از همان تاریخ خالی‌اند. اجرای مجدد سرویس نیاز
به دسترسی مدیر دارد که این نشست ندارد؛ تلاش مستقیم `pg_ctl start` هم
ساکت شکست خورد. یعنی **مجموعهٔ تست (۱۶۸۲+ مورد) در این نشست قابل اجرا
نبود** — مطمئن‌ترین راه پیدا کردن باگ واقعی همین‌جا در دسترس نبود.

**آنچه بدون نیاز به پایگاه‌داده واقعاً بررسی شد:**

- `python -m pyflakes` روی کل کد اولیه (`accounts`, `billing`, `chat`,
  `common`, `config`, `inventory`, `sales`) — فقط واردات بلااستفادهٔ
  بی‌ضرر (بیشتر در فایل‌های تست، یا الگوی شناخته‌شدهٔ `from
  config.settings import *` در تنظیمات لایه‌ای) و سه متغیر محلیِ
  بلااستفاده در تست‌های مرورگری پیدا شد — رفع شدند (پایین).
- اسکن AST سفارشی روی هر فایل پایتون برای **متد تکراری داخل یک کلاس**
  (خطای خاموشی که پایتون هیچ هشداری برایش نمی‌دهد و می‌تواند یک تست را
  بی‌صدا نصفه کند) → صفر مورد؛ همین اسکن هر فایل را هم پارس کرد → صفر خطای
  نحوی در کل مخزن.
- `python manage.py check` → «System check identified no issues».
- `python manage.py makemigrations --check --dry-run` → «No changes
  detected» (مدل‌ها و مهاجرت‌ها هم‌خط‌اند).
- `python manage.py spectacular --validate --fail-on-warn` → خروج ۰.
- `python scripts/check_html_branding.py` → `HTML_BRANDING_PASS
  files=248`.
- جست‌وجوی الگویی برای `except:` خالی، `except ...: pass`، `print(` باقی‌مانده
  در کد محصول، `assertTrue(True)`/`assertFalse(False)` بی‌معنا، و
  `pdb`/`breakpoint()` فراموش‌شده → صفر مورد در هرکدام.

**رفع‌شده:** سه متغیر محلیِ کاملاً بلااستفاده در تست‌های مرورگری —
`common/tests/test_auth_shell.py` (خواندن `dolphin.css` که هرگز خوانده
نمی‌شد؛ ادعای همان تست از قبل توسط تست دیگری در همان فایل پوشش داده
می‌شود)، `common/tests/test_auth_shell_browser.py` (اندازه‌ای که هرگز
سنجیده نمی‌شد؛ بررسی واقعی چند خط پایین‌تر با خواندن تازه انجام می‌شود)،
`common/tests/test_sales_shell_browser.py` (نشانی صفحه‌ای که هرگز استفاده
نمی‌شد). هیچ‌کدام رفتار تست را عوض نکرد — فقط کد مرده حذف شد.

**چیزی که رفع نشد چون باگ نبود:** واردات بلااستفاده در نزدیک ۳۰ فایل
(بیشتر تست) دست‌نخورده ماند — نظافت لینتی است، نه اشکال رفتاری، و طبق
قاعدهٔ مخزن ارزشش کمتر از ریسک دست‌زدن به این‌همه فایل برای هیچ تغییر
رفتاری است.

### شواهد اجراشده

بالا، کامل. **مجموعهٔ تست پایتون در این نشست اجرا نشد** (مانع
زیرساختی PostgreSQL، شرح بالا) — باید پیش از استقرار، روی محیطی با
پایگاه‌دادهٔ در دسترس، کامل اجرا شود.

---

## [1.11.0] — ۲۰۲۶-۰۹-۰۵

### ویزارد چندمرحله‌ای صدور فاکتور و سفارش، مثل قالب مترونیک

طبق دستور مستقیم مالک محصول: دیالوگ صدور فاکتور/سفارش باید یک «ویزارد
چندمرحله‌ای» شود — دقیقاً الگوی قالب خریداری‌شده
(`utilities/modals/wizards/create-account.html`، مؤلفهٔ واقعی `KTStepper`
که خودِ تم می‌آورد)، نه یک فرم تک‌صفحه‌ای با همهٔ فیلدها روی هم.

**هر دو دیالوگ** (`create-invoice-dialog`، `create-order-dialog`) اکنون
سه مرحله دارند: مشخصات سند → اقلام → بازبینی و ثبت. ناوبری «بعدی»/«قبلی»
همان کلاس‌های `current`/`first`/`between`/`last` را که `KTStepper` واقعی
خودش مدیریت می‌کند به کار می‌گیرد — نمایش/پنهانی دکمه‌ها هم از همان
استایل‌شیت تم است، نه CSS تازه. «بعدی» فقط با معتبر بودن فیلدهای الزامیِ
همان مرحله (بررسی بومی مرورگر، `:invalid`) جلو می‌رود؛ دکمهٔ ثبتِ واقعی
همچنان `type="submit"` داخل همان فرم قبلی است، پس منطق ارسال/ریدایرکت
موجود در `setupDocumentList` **هیچ تغییری نکرد**. هیچ کتابخانهٔ
اعتبارسنجی (FormValidation.js)، SweetAlert2، jQuery یا مودال بوت‌استرپ —
که دموی خودِ قالب برای این ویزارد به کار می‌برد — اضافه نشد، چون هیچ‌کدام
جای دیگری در این کدبیس استفاده نمی‌شوند؛ خطای سرور همچنان با همان
`showError`/`data-error-for` نمایش داده می‌شود.

**سفارش، قابلیت واقعی هم گرفت، نه فقط ظاهر تازه:** پیش از این دیالوگ
سفارش فقط یک کالا می‌پذیرفت («سفارش به‌صورت پیش‌نویس با یک سطر ساخته
می‌شود؛ سطرهای بعدی در صفحهٔ جزئیات ثبت می‌شوند» — متن حذف‌شدهٔ همین
دیالوگ). API سفارش از قبل آرایهٔ `items` با هر تعداد سطر را می‌پذیرفت؛
محدودیت فقط در همین فرم بود. افزودن‌ردیفِ فاکتور به یک تابع مشترک
(`createLineItemRows`) تبدیل و توسط هر دو ویزارد استفاده شد، پس سفارش هم
اکنون می‌تواند چند کالا را در همان مرحلهٔ ثبت بیاورد — و همچنان می‌شود
سطر افزود یا ویرایش کرد، دقیقاً مثل قبل. مشتریِ سفارش هم اکنون قابل
جست‌وجوست (همان الگوی مشتریِ فاکتور)، برای یکدستی روی همان فهرست مشتریان.

**بازبینی پیش از ثبت:** آخرین مرحله مقادیر واقعاً واردشده (مشتری، نوع/
تاریخ یا انبار/ارسال، تعداد اقلام، نرخ مالیات/تخفیف برای فاکتور) را فقط
بازتاب می‌دهد — هیچ جمع یا مالیاتی این‌جا از نو محاسبه نمی‌شود؛ آن حساب
همچنان کاملاً سمت سرور است.

**رفتار دیالوگ:** باز کردن دوبارهٔ هرکدام از این دو دیالوگ (مثلاً بعد از
انصراف) اکنون فرم را ریست می‌کند، سطرها را به یک ردیف خالی برمی‌گرداند،
و ویزارد را به مرحلهٔ اول برمی‌گرداند (`setupDocumentList` یک `onOpen`
تازه پذیرفت؛ برای بقیهٔ دیالوگ‌های ساخت که ویزارد نشدند تغییری نکرد).

یک کامنت نادرست هم در همین بخش پاک شد: دربارهٔ `tax_rate` نوشته بود «جمع
شد و بعد این‌جا دور ریخته شد» — درحالی‌که کد واقعی از قبل `tax_rate` را
در بدنهٔ درخواست می‌فرستاد؛ کامنت با کد در تناقض بود.

### شواهد اجراشده

**اجرای `python manage.py test` در این نشست ممکن نشد**: سرویس ویندوزیِ
PostgreSQL روی میزبان «در حال اجراست» (`Get-Service` این را تأیید می‌کند)
اما پورت آن از محیط اجرای این نشست پاسخ نمی‌دهد — همان‌timeout، هم با
`psycopg.connect` مستقیم و هم با یک تلاش خام TCP، و با یا بدون غیرفعال
کردن sandbox این ابزار. این یک مانع زیرساختی همین اجراست، نه چیزی که با
این تغییرات مرتبط باشد؛ پیش از استقرار باید دوباره اجرا شود. آنچه واقعاً
در همین نشست بررسی و تأیید شد:

- `node -c common/static/common/dolphin-app.js` → بدون خطای نحوی.
- `python -m py_compile` روی هر دو فایل تست تغییریافته/تازه → بدون خطا.
- `python manage.py check` (بدون نیاز به پایگاه‌داده) → «System check
  identified no issues».
- `django.template.loader.get_template` روی هر دو قالب ویرایش‌شده
  (`invoices/list.html`، `orders/list.html`) → بدون خطای نحو قالب.
- شمارش برچسب‌های `<div>`/`<form>`/`<dialog>` باز و بسته در هر دو فایل →
  متوازن.
- ۱۱ تست تازه در `common/tests/test_invoice_order_wizard.py` نوشته شد
  (رندر مرحله‌ها، عدم استفاده از کتابخانهٔ اعتبارسنجی/مودال اضافه، گیت
  شدن «بعدی» روی فیلدهای الزامی، اشتراکی‌بودن افزودن‌ردیف، ارسال همهٔ
  سطرهای سفارش نه فقط اولی، ریست‌شدن روی بازکردن دوباره) و تست‌های
  مرورگر واقعیِ موجود در `test_billing_shell_browser.py` برای عبور از
  مراحل تازه اصلاح شدند — اما هیچ‌کدام در این نشست واقعاً اجرا نشدند؛
  نتیجه‌شان تا رفع مانع پایگاه‌داده نامعلوم است.

---

## [1.10.0] — ۲۰۲۶-۰۹-۰۵

### فیلترهای فهرست‌ها به‌شکل پاپ‌آپ، مثل قالب مترونیک

طبق دستور مستقیم مالک محصول: فیلترها باید مثل قالب آماده باشند — کلیک روی
یک دکمه، یک پاپ‌آپ باز شود، فیلترها داخلش تنظیم شوند — نه یک ردیف فیلتر
همیشه‌نمایان بالای هر جدول.

**۲۵ قالب** فرم `class="list-filters"` دارند (مشتریان، سرنخ‌ها، فاکتورها،
سفارش‌ها، محصولات، انبارها، گزارش‌ها، …)، هرکدام با فیلدهای خودش و
اتصال ثبت خودش (`setupPagedList({form, ...})` یا مشابه، مستقیم روی همان
عنصر فرم). به‌جای بازسازی هرکدام جداگانه، یک تابع عمومی تازه
(`setupListFilterPopovers`) هر فرم `.list-filters` را پیدا می‌کند و
**همان عنصر موجود را** (نه کپی، نه بازسازی) داخل یک پنل کشویی جابه‌جا
می‌کند که با یک دکمهٔ تازهٔ «فیلتر» باز/بسته می‌شود — دقیقاً الگوی خودِ
قالب خریداری‌شده (دکمهٔ «فیلتر» + پنل `.menu-sub-dropdown` در صفحهٔ
نمونهٔ مشتریان همان قالب).

**چون هیچ فرمی کپی یا بازسازی نمی‌شود**، هر شناسه، هر نام فیلد، و هر
شنوندهٔ رویداد که کد هر صفحه از قبل به آن متکی بود، بی‌تغییر باقی می‌ماند
— `getElementById` عنصر را هرجای سند باشد پیدا می‌کند، پس جابه‌جایی در
درخت DOM هیچ کدی را در ۲۵ صفحه نمی‌شکند.

**باز/بسته‌شدن**، همان الگوی دست‌ساختهٔ زنگولهٔ یادآور/جست‌وجو/منوی کاربر
(کلاس `.show`)، نه `data-kt-menu-trigger` — چون `KTMenu` به Popper نیاز
دارد که در این استقرار لود نمی‌شود. یک دکمهٔ «بازنشانی» (`type="reset"`)
هم به‌صورت عمومی اضافه شد، بدون نیاز به دانستن فیلدهای هر صفحه — ریست
بومی مرورگر خودش می‌داند فرم را به مقادیر پیش‌فرض برگرداند؛ بعد از آن
دوباره ثبت می‌شود تا فهرست هم واقعاً به حالت بدون فیلتر برگردد، نه فقط
فیلدها.

### شواهد اجراشده

`python manage.py test --settings=config.test_settings` → **۱۶۸۲ تست، صفر
شکست** (`OK (skipped=16)`) — ۱۱ تست تازه + ۴ تست مرورگر واقعیِ موجود که به
باز‌کردن صریح پاپ‌آپ پیش از تعامل با فیلدها اصلاح شدند (فیلدها دیگر تا باز
نشدن پاپ‌آپ در دسترس Selenium نیستند، دقیقاً همان رفتاری که کاربر واقعی هم
می‌بیند). `spectacular --validate --fail-on-warn` → خروج ۰.
`check_html_branding.py` → `HTML_BRANDING_PASS files=248`. تست زندهٔ
مرورگر: صفحهٔ مشتریان (جست‌وجو، اعمال، بستهٔ خودکار پس از اعمال، بازنشانی
با بارگذاری دوبارهٔ فهرست کامل)، فاکتورها (باز/بستن با Escape)، و گزارش
مطالبات (شامل دکمهٔ «خروجی اکسل» که داخل همان فرم بود) — همه بدون سرریز
افقی در عرض ۳۷۵ پیکسل.

---

## [1.9.0] — ۲۰۲۶-۰۹-۰۵

### متن نمودارها بیرون از خودِ نمودار + گفت‌وگوی داخلی به‌شکل درویر کنار صفحه

طبق دستور مستقیم مالک محصول: «نوشته‌های توی نمودارها نباید وارد نمودارها
بشه»، و چت باید آیکون سربرگ + پنل کناری شبیه قالب مترونیک داشته باشد و
زنده باشد.

### نمودارها — متن همیشه بیرون از شکل رسم‌شده

هر دو تابع مشترک رسم نمودار (`renderDonutChart`، `renderBarChart` در
`dolphin-app.js`) که کل پنل — گزارش‌ها، داشبورد، نمودار هر صفحهٔ فهرستی —
از همان‌ها استفاده می‌کنند، اصلاح شدند:

- **نمودار دایره‌ای:** درصد روی برش‌های نازک یا هم‌پوشانی می‌کرد یا از لبهٔ
  حلقه بیرون می‌زد. برچسب‌های روی شکل کاملاً حذف شدند؛ درصد به legend
  (که خودش از قبل بیرون از نمودار است) منتقل شد: «نام برش — ۴۲٪».
- **نمودار میله‌ای:** سه راه‌حل امتحان و **هرکدام با اندازه‌گیری واقعی روی
  نمودار رندرشده رد شدند**، نه فقط حدس زده:
  1. `plotOptions.bar.dataLabels.position:"top"` — روی میلهٔ افقی
     غیرپشته‌ای هیچ اثری نداشت؛ لنگرگاه برچسب همچنان ~۲۹ پیکسل داخل نوک
     میله می‌ماند.
  2. `offsetX` بزرگ + درصد ثابت روی محور — برای برچسب فقط-مقدار جواب داد
     ولی برای رشتهٔ «نام — مقدار» (تقریباً دو برابر پهن‌تر) کافی نبود.
  3. ستون جدا برای نام دسته روی محور y — محاسبهٔ عرض گاتر خودِ Apex برای
     نام‌های فارسی اشتباه بود (گاتر ۴۵ پیکسل برای برچسبی که ۱۵۰-۱۶۰ پیکسل
     رندر می‌شود)؛ حتی با `grid.padding.left` درست هم یک پیکسل جابه‌جا
     نشد.

  راه‌حل نهایی: **نام دسته و مقدار در یک برچسب ترکیبی** («نام — مقدار»)
  رسم می‌شوند، از همان مکانیزم offset+anchor که واقعاً کار می‌کند (`offsetX:
  40`، `textAnchor:"end"` — مقدار درست برای RTL، اندازه‌گیری‌شده نه حدسی)،
  و محور مقدار به‌جای درصد ثابت با **اندازه‌گیری واقعی پهنای پیکسلیِ پهن‌ترین
  برچسب** (`canvas.measureText`) در برابر عرض واقعیِ رندرشدهٔ خودِ نمودار
  محاسبه می‌شود — نه حدس زده. نتیجه: کلید (نام) و مقدار هر دو همیشه، روی هر
  عرضی، کاملاً بیرون از میلهٔ رنگی می‌نشینند.

### چت داخلی — آیکون سربرگ + درویر کناری، زنده

صفحهٔ مستقل `/chat/` برداشته شد؛ به‌جایش یک آیکون در سربرگ (دقیقاً همان
الگوی `kt_drawer_chat` خودِ قالب مترونیک — `data-kt-drawer` واقعی، همان
مؤلفه‌ای که سایدبار از قبل استفاده می‌کند، نه بازسازی‌شده) یک پنل از کنار
باز می‌کند: فهرست گفت‌وگوها یا یک گفت‌وگوی باز، با دکمهٔ بازگشت بین این دو
حالت — چون دموی خودِ قالب فقط یک گفت‌وگوی ثابت دارد و این پنل چندین
گفت‌وگو دارد.

**زنده بودن**، بدون زیرساخت وب‌سوکت تازه (که در کدبیس اصلاً وجود ندارد):
هر دو فاصلهٔ نظرسنجی فقط وقتی درویر واقعاً بازست اجرا می‌شوند — بررسی‌شده
با همان کلاس `drawer-on` خودِ قالب، هر جور که باز شده باشد (آیکون، یا
مکانیزم overlay/Escape خودِ قالب) — و باز کردن درویر بلافاصله یک‌بار
نظرسنجی می‌کند، بدون صبر برای تیک بعدی. یک پیام از طرف دیگر، بدون هیچ
رفرشی، در تست زندهٔ مرورگر ظرف ۴ ثانیه روی صفحه ظاهر شد.

### شواهد اجراشده

`python manage.py test --settings=config.test_settings` → **۱۶۷۱ تست، صفر
شکست** (`OK (skipped=16)`) — ۳۴ تست تازه (رفتار نمودار، درویر چت، و دو
تست مرورگر واقعیِ موجود که به‌جای ستون محور قدیمی به برچسب ترکیبی تازه
اصلاح شدند). `spectacular --validate --fail-on-warn` → خروج ۰.
`check_html_branding.py` → `HTML_BRANDING_PASS files=248`. تست زندهٔ
مرورگر: نمودار دایره‌ای و میله‌ای در چند صفحهٔ گزارش با فاصلهٔ مثبت ۱۰-۳۶
پیکسل بین میله و برچسب روی هر سه؛ بدون سرریز افقی در عرض ۳۷۵ پیکسل؛ درویر
چت باز/بسته، ارسال و دریافت زندهٔ پیام، دکمهٔ بازگشت، دیالوگ گفت‌وگوی تازه.

---

## [1.8.6] — ۲۰۲۶-۰۹-۰۴

### داشبورد نقش‌محور

طبق دستور مستقیم مالک محصول و اولویت ۱ تحلیل قبلی. داشبورد ضعیف‌ترین سطح
پنل نسبت به بقیه بود: چهار کاشی با یک عدد، یک پنل عملکرد، و (برای بازاریاب)
یک صف کار. هر صفحهٔ دیگری نمودار و فیلتر و رقم واقعی داشت؛ اولین صفحه‌ای که
هر کسی می‌بیند کمترین را داشت.

**سه بخش تازه، و محتوایشان به نقش بستگی دارد چون خودِ پرسش‌ها فرق می‌کنند:**

1. **نوار KPI** — رقم به‌همراه **مقایسه**، نه عدد خالی. «۶۰٬۵۰۰٬۰۰۰ ریال این
   ماه» چیز زیادی نمی‌گوید؛ «…، ۱۲٪ بیشتر از ماه گذشته» چیزی می‌گوید که
   خواننده واقعاً می‌خواست. فروش این ماه، تعداد فروش، مطالبات باز، تماس‌های
   هفت روز اخیر، پرونده‌های باز، بسته‌شده در این ماه — هرکدام فقط برای نقشی
   که منبعش را می‌بیند.
2. **روند فروش دوازده هفتهٔ اخیر** (نمودار ناحیه‌ای) — جهت کسب‌وکار بدون
   بازکردن گزارش.
3. **کار کجا نشسته است** (نمودار دایره‌ای) — سرنخ‌ها به تفکیک وضعیت برای سمت
   فروش، پرونده‌ها به تفکیک وضعیت برای سمت پس‌ازفروش.

**پنل از روی چیزی که خواننده می‌بیند ساخته می‌شود، نه اینکه بعداً فیلتر شود.**
بازاریاب اصلاً KPI مطالبات ندارد (نه اینکه صفر ببیند)؛ کارشناس پس‌ازفروش
روند فروش ندارد؛ استقراری که هیچ‌کدام از منابع را ندارد صفحه‌ای می‌بیند
دقیقاً مثل قبل از وجود این بخش.

**دو نکتهٔ محاسباتی که ارزش نگه‌داشتن دارند:**
- مقایسهٔ «این ماه» با **همان بازهٔ سپری‌شده** از ماه قبل انجام می‌شود، نه با
  کل ماه قبل؛ وگرنه روز سوم هر ماه، هر داشبوردی یک سقوط نشان می‌داد.
- مطالبات در پایتون جمع می‌شود نه با `SUM()` روی ستون‌ها، چون `balance_due`
  یک property است که «تسویهٔ دستی» را رعایت می‌کند — جمع ستونی بی‌صدا با بقیهٔ
  صفحات اختلاف پیدا می‌کرد.

**فیچر تازهٔ `dashboard_insights`** — بدون وابستگی، چون هر منبعش جدا گیت
می‌شود؛ مثل `reminders`/`global_search`/`customer_timeline` پیش‌فرض روشن.

### یک بازآرایی کوچک

قالب‌بندی «ریال گروه‌بندی‌شده با ارقام فارسی» که `reports/list_charts.py`
داشت به `common/formatting.py` منتقل شد و `list_charts` از همان‌جا می‌خواند —
به‌جای نسخهٔ سوم. فیلتر قالب (`money_tags`) عمداً سر جایش ماند: پرسش دیگری
جواب می‌دهد (مبلغ داخل صفحهٔ رندرشده، با «—» خودش برای مقدار خالی).

### سه ایراد واقعی که همین فاز پیدا و رفع کرد

1. **نمودار با عرض صفر.** نسخهٔ اول کارت‌ها را پر می‌کرد، هر دو نمودار را
   mount می‌کرد و **بعد** بخش را از حالت hidden درمی‌آورد — ApexCharts ظرفش
   را هنگام رندر اندازه می‌گیرد و ظرفِ داخل یک والدِ `display:none` صفر
   اندازه می‌گیرد. نتیجه: دو کارت با عنوان و فضای خالی. **این همان کلاس
   باگی است که `[1.7.19]` یک‌بار بهایش را داده بود.** ترتیب اصلاح شد و تست
   ترتیب را نگه می‌دارد.
2. **تداخل نام `data-kpi`.** پنل عملکرد در همین صفحه از قبل صاحب این
   attribute بود؛ کارت‌های تازه هم از آن استفاده می‌کردند و
   `[data-kpi]` ده عنصر با دو معنی برمی‌گرداند. روی صفحه بی‌ضرر، ولی تله‌ای
   برای هر کوئری بعدی. به `data-dashboard-kpi` تغییر کرد.
3. **`href="#"` در HTML سروشده.** لینک «همه» بسته به نقش به `/leads/` یا
   `/after-sales/` می‌رود، پس در قالب placeholder بود — و گیت خودِ این پنل
   (`test_role_aware_landing_...`) درست آن را رد کرد. حالا لینک را اسکریپت
   می‌سازد؛ هیچ لینکِ به‌هیچ‌جا به مرورگر نمی‌رسد.

### شواهد اجراشده

`python manage.py test --settings=config.test_settings` → **۱۶۴۷ تست، صفر
شکست** (`OK (skipped=16)`) — ۲۸ تست تازه (شکل پنل هر نقش، ریاضی مقایسهٔ
دوره، تسویهٔ دستی، پنجرهٔ روند، گیت فیچر، تطابق تعداد `path` هر آیکون با CSS
قالب، ترتیب mount نمودار، و نبود تداخل attribute).
`spectacular --validate --fail-on-warn` → خروج ۰. `check_html_branding.py` →
`HTML_BRANDING_PASS files=249`. تست زندهٔ مرورگر با دادهٔ واقعی و سه نقش:
مدیر پلتفرم شش KPI + هر دو نمودار، بازاریاب فقط فروش/تماس خودش (۳۷٬۰۰۰٬۰۰۰
در برابر ۶۰٬۵۰۰٬۰۰۰ شرکت) **بدون** KPI مطالبات، کارشناس پس‌ازفروش فقط
پرونده‌ها بدون روند فروش؛ تم روشن و تاریک هر دو.

---

## [1.8.5] — ۲۰۲۶-۰۹-۰۴

### دیباگ کامل پنل با دیتای خام

طبق دستور مستقیم مالک محصول، یک پیمایش کامل روی کل پنل با دادهٔ واقعی — همان
کاری که `[1.7.13]` کرد، این‌بار روی کدبیس ۱.۸.x با فیچرهای تازه.

**دیتا (همه از راه لایهٔ سرویس، نه نوشتن مستقیم مدل):** ۶ کاربر با هر پنج
نقش، ۳ محصول + دسته‌بندی، ۲ انبار با گردش واقعی، ۶ مشتری (حقیقی و حقوقی،
یکی ثبت‌شده توسط خودِ بازاریاب)، ۵ سرنخ با تخصیص، ۲ عضو جامعهٔ هدف، ۵ تماس،
۲ فروش، ۲ سند پستی، ۱ سفارش تأییدشده، ۴ فاکتور (۳ صادرشده + ۱ پیش‌نویس)،
مانده اول دوره، ۳ دریافت + ۱ پرداخت + ۳ چک با سررسیدهای مختلف، طرح اقساط
۴قسطی، ۲ پروندهٔ پس‌ازفروش (یکی با قرار، یکی بسته‌شده)، ۳ پیوست، ۳ پیامک
ورودی، و یک گفت‌وگوی داخلی.

**پیمایش سمت سرور:** ۲۱۹ مسیر × ۵ نقش = **۱۰۹۵ درخواست**. صفر خطای ۵۰۰،
صفر استثنای مهارنشده. ماتریس دسترسی کامل استخراج و بررسی شد.

**پیمایش مرورگر واقعی:** ۴۶ صفحهٔ پنل با نقش مدیر پلتفرم، و صفحات مربوطه با
نقش بازاریاب و کارشناس پس‌ازفروش. تقویم سرنخ (۵ رویداد، «شهریور ۱۴۰۵»)،
نمودارهای ApexCharts، تایم‌لاین مشتری (۱۲ رویداد از ۹ نوع)، زنگولهٔ یادآور،
جست‌وجوی سراسری، پنل پیوست‌ها و صفحهٔ چاپ فاکتور همه با دادهٔ واقعی تأیید شدند.

**صحت ریاضی گزارش‌ها — همه دقیق:** مطالبات ۱۰۲٬۵۰۰٬۰۰۰ = مجموع مانده سه
فاکتور صادرشده (پیش‌نویس درست کنار گذاشته شد)؛ ارزش موجودی ۱٬۳۴۵٬۸۰۰٬۰۰۰ =
دقیقاً `Σ(تعداد × میانگین بها)`؛ میانگین متحرک بهای پمپ
`(۴۰×۱۲٬۰۰۰٬۰۰۰ + ۱۰×۱۲٬۵۰۰٬۰۰۰)/۵۰ = ۱۲٬۱۰۰٬۰۰۰` درست؛ عملکرد کاربران
منطبق بر فروش‌های ثبت‌شده؛ دفتر مشتری برابر مانده اول دوره؛ یادآورها ۷ مورد
با پنجره‌های درست (چک سررسید ۲۰ روز آینده درست بیرون ماند).

### یک ناسازگاری واقعی، پیدا و رفع شد

سه نقطهٔ خواندنی به نقشی که اختیارش را نداشت **۲۰۰ با پاسخ خالی** می‌دادند،
درحالی‌که نقطهٔ فهرست متناظرشان به همان نقش **۴۰۳** می‌داد:

- `/api/v1/outbound-sms/` (صفحهٔ `/sms/` برای همان کاربر ۴۰۳ بود)
- `/api/v1/exports/customers.xlsx`
- `/api/v1/exports/products.xlsx`

**هیچ نشتی دیتایی نبود** — سلکتورها سطرها را از قبل خالی کرده بودند و فایل
خروجی فقط سرستون داشت. ولی «یک فایل خالی» یعنی «مشتری‌ای وجود ندارد»، نه
«این مال شما نیست». این دقیقاً همان قاعده‌ای است که `[1.7.13]` با
`HasSalesCapability` برقرار کرد و `after_sales` و `billing` هم از آن پیروی
کردند؛ این سه نقطه جا مانده بودند. حالا هر سه با همان الگو گیت شدند.

**هیچ نقشی محدودتر نشد:** بازاریاب همچنان دفترچهٔ خودش را خروجی می‌گیرد
(۲۰۰، با دامنهٔ خودش). فقط نقشی که از اول هیچ سطری نمی‌دید، حالا به‌جای فایل
خالی، ۴۰۳ می‌گیرد. یک تست موجود که رفتار قدیمی را تثبیت کرده بود به‌روزرسانی
شد — نامش (`test_an_agent_is_refused_send_and_the_log`) از اول همین را
می‌گفت، ولی assertion‌اش پیاده‌سازی را ثبت کرده بود.

### دو چیزی که شبیه باگ بودند و نبودند

- **۵۰۳ روی `/invoices/<id>/print.pdf`.** این هاست مرورگری برای تولید PDF
  ندارد، پس `PdfRendererUnavailable` می‌دهد و یک صفحهٔ فارسی با راهنمای
  «از دکمهٔ چاپ مرورگر استفاده کنید» برمی‌گرداند. بررسی شد که **لینک دانلود
  PDF در صفحهٔ چاپ اصلاً رندر نمی‌شود** وقتی موتور در دسترس نیست؛ فقط URL
  مستقیم به آن می‌رسد. رفتار طراحی‌شده، نه نقص.
- **ارقام لاتین در ستون‌های تعداد.** بررسی شد و **قرارداد مستند خودِ پنل
  است** (`[1.7.14]`): مبلغ و تاریخ فارسی، تعداد/مقدار در سلول جدول لاتین،
  و شمارش داخل جملهٔ فارسی (صفحه‌بندی، پیام‌ها، برچسب نمودار، بج‌ها) فارسی.
  کاشی‌های داشبورد و ستون‌های تعداد گزارش عملکرد با همین قاعده سازگارند.
  عمداً تغییر داده نشد.

### شواهد اجراشده

`python manage.py test --settings=config.test_settings` → **۱۶۱۹ تست، صفر
شکست** (`OK (skipped=16)`) — ۵ تست تازه در
`common/tests/test_capability_consistency.py` که خودِ جفت‌بودن را نگه
می‌دارند (هر خروجی باید همان چیزی را جواب دهد که فهرست متناظرش می‌دهد)، نه
یک فهرست کد وضعیت. `spectacular --validate --fail-on-warn` → خروج ۰.
`check_html_branding.py` → `HTML_BRANDING_PASS files=249`. پیمایش دوبارهٔ
۱۰۹۵ درخواستی پس از رفع: سه ردیف تغییرکرده حالا دقیقاً با فهرست متناظرشان
یکی‌اند.

---

## [1.8.4] — ۲۰۲۶-۰۹-۰۴

### رفع دو خطای تولید طرح‌وارهٔ OpenAPI + تبدیل همان گیت مستند به گیت اجراشونده

در `[1.8.3]` هنگام کار روی چیز دیگری، اجرای دستیِ
`manage.py spectacular --validate --fail-on-warn` دو خطا نشان داد که هیچ‌کدام
مال آن نسخه نبودند و مدت‌ها بی‌صدا مانده بودند:

- `AttachmentDeleteView` (از `1.7.11`)
- `ChatThreadReadView` (از `1.8.0`)

**علت.** هر دو `POST` روی یک `APIView` سادهٔ بدون بدنه‌اند. تولیدکنندهٔ
طرح‌واره برای یک متد نویسنده تلاش می‌کند سریالایزر درخواست را حدس بزند، چیزی
پیدا نمی‌کند و برای آن ویو خطا ثبت می‌کند. **تولید متوقف نمی‌شود** — مسیر در
خروجی می‌ماند با بدنهٔ درخواستِ حل‌نشده — و دقیقاً به همین دلیل چیزی سر و صدا
نکرد: فقط `--fail-on-warn` این را به خروج غیرصفر تبدیل می‌کند، و فقط کسی که
آن دستور را دستی بزند می‌بیندش.

**رفع.** هر دو `request=None` گرفتند — همان قراردادی که از قبل در ۸ جای دیگر
این مخزن برای `POST` بدون بدنه استفاده می‌شود (`accounts/views.py`،
`billing/views.py`، `inventory/views.py`، `sales/views.py`).

**رفع ریشه‌ای‌تر.** `docs/ops/RELEASE_CHECKLIST.md` و راهنمای استقرار هر دو
مدت‌هاست همین دستور را به‌عنوان گیت انتشار نام می‌برند، و
`common/tests/test_backup_scripts.py` هم چک می‌کند که هنوز نامش را ببرند —
ولی **هیچ‌چیز اجرایش نمی‌کرد**. حالا `common/tests/test_openapi_schema.py`
همان گیت را در فرایند خودِ تست اجرا می‌کند: صفر خطا، صفر هشدار، و بررسی این‌که
مسیرهای حساس واقعاً منتشر شده‌اند. **کارکردنش اثبات شد**: با برگرداندن موقتِ
یکی از دو رفع، تست شکست خورد؛ بعد دوباره اعمال شد.

### شواهد اجراشده

`manage.py spectacular --validate --fail-on-warn` → **خروج ۰، صفر خطا، صفر
هشدار** (پیش از این: ۲ خطا). `python manage.py test --settings=config.test_settings`
→ **۱۶۱۴ تست، صفر شکست** (`OK (skipped=16)`) — ۵ تست تازه.

---

## [1.8.3] — ۲۰۲۶-۰۹-۰۴

### تایم‌لاین ۳۶۰ درجهٔ مشتری

اولویت ۳ از همان تحلیل، و آخرین قلم آن. صفحهٔ مشتری از قبل چهار جعبه داشت —
تلفن‌ها، سرنخ‌های مرتبط، تماس‌های مرتبط، فاکتورهای مرتبط — و هرکدام پرسش خودش
را خوب پاسخ می‌داد. چیزی که دیده نمی‌شد **داستان** بود: یکشنبه زنگ زدیم،
دوشنبه قیمت دادیم، سه‌شنبه فاکتور شد، پنجشنبه پول آمد، هفتهٔ بعد شکایت کرد.
خواندن این یعنی چهار جدول با چهار ترتیب مختلف و کلی حدس.

حالا یک ستون زمانی، از ۹ منبع: تماس، سرنخ، سفارش، فاکتور، دریافت/پرداخت،
سند فروش، پروندهٔ پس‌ازفروش، پیوست، و پیامک ورودی/خروجی.

**دو تصمیم مخصوص همین فیچر:**

1. **ادغام در پایتون، نه در SQL.** ۹ مدل با ۹ ستون تاریخ مختلف بدون یک ویوی
   union یا جدول رویداد denormalize‌شده یک کوئری ایندکس‌دار نمی‌شوند، و هر دو
   یعنی مهاجرت برای یک صفحه. هر منبع حداکثر ۱۰ ردیف می‌دهد (جست‌وجوی ایندکس‌دار
   روی کلید خارجی خودش) و ادغام چند ده تاپل را در حافظه مرتب می‌کند — پس یک
   مشتری با ۳۰۰ تماس، فاکتورش را هم روی صفحه می‌بیند، نه دیواری از تماس.
2. **زمان خودِ رویداد، نه `created_at`.** تماس `occurred_at` دارد، فاکتور
   `issued_at`، پرداخت `received_at`. مرتب‌کردن بر اساس زمان ساخته‌شدن سطر،
   یک فاکتور با تاریخ گذشته را جای اشتباه می‌گذاشت — دقیقاً همان چیزی که این
   صفحه برای درست‌کردنش وجود دارد.

### یک بازآرایی: واژه‌های فارسی، یک منبع در پایتون

چند enum مدل برچسب انگلیسی دارند (`Invoice.Status` = `Draft/Issued/…`،
`Payment.Method` = `Cash/Card/…`)، چون مقدار ذخیره‌شده قرارداد است و برچسبش
هیچ‌وقت خوانده نمی‌شد: هر صفحه خودش ترجمه می‌کرد. این تایم‌لاین اولین خوانندهٔ
پایتونی است که جمله را سمت سرور می‌سازد، پس اولین جایی بود که می‌توانست
«Draft» را به کاربر نشان دهد. به‌جای یک کپی دوم، نگاشت‌های فارسیِ سمت پایتون
به `common/labels.py` منتقل شدند و `ui_views` از همان‌جا می‌خواند.

### یک باگ واقعی که با خواندن CSS خودِ قالب پیدا شد

آیکون‌های duotone قالب از چند `span.path*` تودرتو کشیده می‌شوند و **تعدادشان
برای هر آیکون فرق دارد**: `ki-call` هشت‌تا، `ki-calendar-tick` شش‌تا،
`ki-delivery` پنج‌تا. هر سهٔ فیچر تازهٔ این سه نسخه (زنگوله، جست‌وجو،
تایم‌لاین) دوتا هاردکد کرده بودند، یعنی آیکون تلفن یک‌چهارم کشیده می‌شد.
حالا تعداد از سرور با هر گروه/رویداد می‌آید — همان کاری که `WIDGET_STYLE` در
`ui_views` برای کاشی‌های داشبورد از قبل می‌کرد. مورد تیزتر: `ki-paper-clip`
اصلاً آیکون duotone نیست و هیچ `path` ندارد، پس با کلاس `ki-duotone` **خالی**
رندر می‌شد؛ برای پیوست‌ها `ki-file` جایگزین شد.

### شواهد اجراشده

`python manage.py test --settings=config.test_settings` → **۱۶۰۹ تست، صفر
شکست** (`OK (skipped=16)`) — ۳۳ تست تازه در
`common/tests/test_customer_timeline.py` (منابع، ترتیب زمانی، برچسب فارسی و
نشت‌نکردن برچسب انگلیسی، دامنهٔ شیء، ۴۰۴ به‌جای ۴۰۳، سقف هر منبع، و مطابقت
تعداد `path` هر آیکون با CSS خودِ قالب). `check_html_branding.py` →
`HTML_BRANDING_PASS files=249`. تست زندهٔ مرورگر با یک داستان واقعی (سه تماس
در سه تاریخ، سرنخ، چک، پروندهٔ پس‌ازفروش): ترتیب درست، تاریخ جلالی، برچسب‌های
فارسی، ریل عمودی با نشانگر رنگی هر نوع، تم روشن و تاریک، و عرض باریک بدون
سرریز.

---

## [1.8.2] — ۲۰۲۶-۰۹-۰۴

### جست‌وجوی سراسری در سربرگ

اولویت ۲ از همان تحلیل. تا حالا پیداکردن یک مشتری با شمارهٔ تلفن یعنی: یادت
بیاید کدام صفحه مشتری‌ها را دارد، بازش کنی، و فیلتر همان صفحه را پر کنی — با
۵۱ صفحه، این بزرگ‌ترین هزینهٔ ناوبری پنل بود. حالا یک ذره‌بین در سربرگ (یا
`Ctrl`/`⌘`+`K`) هشت ماژول را با یک پرسش می‌گردد: مشتریان، سرنخ‌ها، محصولات،
فاکتورها، سفارش‌ها، دریافت/پرداخت، اسناد فروش، و خدمات پس از فروش.

**همان سه شکل `common/reminders.py`، به همان دلایل:** بدون جدول و بدون
ایندکس تازه (هر منبع یک `icontains` روی همان ستون‌هایی است که صفحهٔ فهرست
خودش هم با آن‌ها جست‌وجو می‌کند)؛ دامنهٔ شیء دوباره نوشته نشد (هر کوئری از
سلکتور خودِ ماژول شروع می‌شود)؛ و فیچر تازهٔ `global_search` مثل `reminders`
عمداً پیش‌فرض روشن است.

**ارقام هر دو طرف نرمال می‌شوند.** پنل از `[1.7.14]` ارقام را فارسی چاپ
می‌کند، پس کسی که شمارهٔ فاکتور را از روی صفحه کپی می‌کند «۱۴۰۵-۰۰۱۲» تایپ
می‌کند و باید همان را پیدا کند؛ کسی هم که با صفحه‌کلید لاتین شماره تلفن
می‌زند باید همان مشتری را پیدا کند. شمارهٔ تلفن به‌صورت `+98…` ذخیره می‌شود،
پس `0912…`، `98912…` و `0098912…` همه پیش از تطبیق پیشوندشان را از دست
می‌دهند؛ و زیر سه رقم اصلاً روی تلفن جست‌وجو نمی‌شود، چون «۰۹» با همهٔ
شماره‌های دفترچه می‌خوانَد.

**چیزهای ریز که مهم‌اند:** درخواست فقط بعد از آرام‌گرفتن تایپ می‌رود (نه
به‌ازای هر کلید)، و پاسخ کهنه هرگز روی پاسخ تازه‌تر نمی‌نشیند (شمارندهٔ
ترتیب) — همان باگ کلاسیک جعبهٔ جست‌وجو که با تایپ سریع خودش را نشان می‌دهد.
هر گروه حداکثر ۵ ردیف می‌دهد و اگر بیشتر بود لینک «همه» به صفحهٔ همان ماژول
می‌برد. `Enter` اولین نتیجه را باز می‌کند.

### یک باگ واقعی که فقط مرورگر زنده نشانش داد

پنل بازشونده به دکمهٔ خودش لنگر داشت، پس از هرجای سربرگ که آیکونش بود ۳۰۰
پیکسل جلو می‌رفت — روی عرض ۳۷۵ پیکسل، لبهٔ دور پنل جست‌وجو ۳۸ پیکسل بیرون از
صفحه می‌افتاد و بریده می‌شد؛ زنگولهٔ `[1.8.1]` هم فقط به‌زور جا می‌شد، یعنی
روی گوشی باریک‌تر آن هم می‌برید. زیر شکست `sm`، هر دو پنل حالا به خودِ
نمایشگر سنجاق می‌شوند، با `top` از متغیر ارتفاع سربرگ خودِ قالب.

### یک بازآرایی کوچک

ردیف‌های هر دو پنل سربرگ حالا یک کلاس مشترک دارند (`topbar-list-*` به‌جای
`reminder-*` نسخهٔ قبل): یک لیست در یک پنل، دو محتوا.

### شواهد اجراشده

`python manage.py test --settings=config.test_settings` → **۱۵۷۶ تست، صفر
شکست** (`OK (skipped=16)`) — ۳۹ تست تازه در `common/tests/test_global_search.py`.
`check_html_branding.py` → `HTML_BRANDING_PASS files=249`.
`node --check dolphin-app.js` → پاس. تست زندهٔ مرورگر: «رضا» چهار ماژول را
هم‌زمان آورد؛ شماره تلفن کامل و ناقص و با ارقام فارسی هر سه پیدا شدند؛
`Enter` اولین نتیجه را باز کرد؛ `Ctrl+K` باز کرد و فوکوس داد و `Escape`
بست؛ لینک «همه» وقتی نتایج از سقف گروه بیشتر شد ظاهر شد؛ و عرض موبایل
۳۷۵ پیکسل بعد از رفع باگ کامل داخل صفحه جا شد.

---

## [1.8.1] — ۲۰۲۶-۰۹-۰۴

### زنگولهٔ یادآور در سربرگ

اولویت ۱ از تحلیل «چه چیزی پنل را عالی می‌کند» (۲۰۲۶/۰۹/۰۴). پنل همهٔ
تاریخ‌های مهم را از قبل ذخیره می‌کرد — پیگیری بعدی سرنخ، قرار پس‌ازفروش،
سررسید چک، سررسید قسط — ولی هیچ‌کدام خودشان را نشان نمی‌دادند؛ کاربر باید
یادش می‌ماند تقویم را باز کند، بعد صفحهٔ چک‌ها را، بعد اقساط را. حالا یک
زنگوله در سربرگ هر چهار سؤال را یک‌جا می‌پرسد.

**سه تصمیم که هرکدام جایگزین داشتند:**

1. **بدون مدل تازه، بدون مهاجرت.** یادآور از روی همان تاریخ سررسیدی ساخته
   می‌شود که ماژول صاحبش از قبل دارد (هر چهار ستون `db_index` دارند). ذخیرهٔ
   نسخهٔ دوم فقط دریفت می‌ساخت: سطر جابه‌جا می‌شد و یادآور نه.
2. **بدون حالت «خوانده‌شد»/«رد کردن».** شمارنده کارِ *واقعاً سررسیدشده* را
   می‌شمارد و وقتی کار انجام شد خالی می‌شود — پیگیری جابه‌جا شد، چک وصول شد —
   نه وقتی کسی نگاهی به پنل انداخت. یک استریم قابل‌بستن، یک چک سررسیدگذشتهٔ
   واقعی را با یک کلیک اشتباه ساکت می‌کرد؛ صف کار نمی‌کند.
3. **هر منبع سه کنترل خودش را نگه می‌دارد.** فیچر این‌جا چک می‌شود (استقرار
   بدون `payments` یادآور چک نمی‌گیرد)، ولی مجوز نقش و دامنهٔ شیء **دوباره
   نوشته نشدند**: هر کوئری از سلکتور خودِ همان ماژول شروع می‌شود
   (`leads_for`، `after_sales_requests_for`، `cheques_for`،
   `installments_for`). بازاریاب پیگیری خودش را می‌بیند و از پول شرکت هیچ —
   بدون این‌که یک قاعده دوبار نوشته شود.

**فیچر تازه `reminders`** در `registry.py` — برخلاف `custom_branding` و
`internal_chat` **عمداً پیش‌فرض روشن** است: ماژول جداگانه‌ای نیست که مشتری
بخرد و دادهٔ خودش را ندارد؛ فقط سطرهایی را نشان می‌دهد که همان کاربر از قبل
می‌تواند دستی باز کند. هر استقرار همچنان می‌تواند از راه manifest خاموشش کند.
برای TIARA (فریز روی `1.6.9`) خاموش می‌ماند.

### سه باگ واقعی که همین فاز پیدا و رفع کرد

1. **مصرف سهمیهٔ مشترک throttle.** نسخهٔ اول این دو مسیر
   `SensitiveRateThrottle` داشت — و `sensitive` یک سطل مشترک ۳۰/دقیقه به‌ازای
   هر کاربر است که همهٔ ویوهای حساس از آن می‌خورند. زنگوله در هر بار
   بارگذاری صفحه + هر دقیقه یک‌بار poll می‌کند، پس کاربری که سریع در پنل کار
   می‌کرد سطل را خالی می‌کرد و **عمل حساس بعدی روی مسیری کاملاً دیگر** ۴۲۹
   می‌گرفت، بدون هیچ توضیحی روی صفحه. تست‌های مرورگر واقعی همان بار اول این
   را گرفتند (دو تست شکست خورد). throttle برداشته شد — این یک خواندن معمولی
   است، مثل بقیهٔ مسیرهای فهرست پنل که هیچ‌کدام throttle ندارند.
2. **باگ اولویت CSS.** قالب خریداری‌شده
   `.btn .badge { position: relative }` دارد که یک کلاس تنها را می‌بَرد؛
   نشان شمارنده به‌جای گوشهٔ زنگوله، **کنارش** و داخل دکمهٔ ۴۰ پیکسلی
   می‌نشست و آیکون را کج می‌کرد. فقط با مرورگر زنده دیده شد. با انتخابگر
   مبتنی بر id رفع شد، بدون `!important`.
3. **ارقام لاتین.** زیرنویس قسط «قسط 1» چاپ می‌شد نه «قسط ۱» — خلاف قاعدهٔ
   `[1.7.14]`. با همان `to_persian_digits` موجود رفع شد.

### شواهد اجراشده

`python manage.py test --settings=config.test_settings` → **۱۵۳۷ تست، صفر
شکست** (`OK (skipped=16)`) — ۳۶ تست تازه در `common/tests/test_reminders.py`
(پنجرهٔ سررسید، دامنهٔ شیء هر سه نقش، گیت فیچر، سقف هر گروه، نبودِ حالت
خوانده‌شد، سهمیهٔ throttle، و اولویت CSS). `check_html_branding.py` →
`HTML_BRANDING_PASS files=249`. `node --check dolphin-app.js` → پاس. تست
زندهٔ مرورگر با دادهٔ واقعی (۵ مشتری، ۴ سرنخ با سررسیدهای مختلف، ۱ قرار
پس‌ازفروش، ۳ چک، ۴ قسط): بازشدن پنل با هر چهار گروه و تاریخ جلالی و ارقام
فارسی، سرنخ «هفتهٔ بعد» درست بیرون ماند، کلیک روی یک ردیف به صفحهٔ همان سرنخ
رفت، حالت خالی («کار سررسیدشده‌ای ندارید») با جابه‌جاکردن همهٔ تاریخ‌ها به
آینده، تم روشن و تاریک، عرض موبایل ۳۷۵ پیکسل بدون سرریز، و Escape/کلیک بیرون
برای بستن با بازگشت فوکوس.

---

## [1.8.0] — ۲۰۲۶-۰۹-۰۴

### چت داخلی پنل

طبق دستور مستقیم مالک محصول، مرحلهٔ دوم پس از دیباگ بصری `[1.7.19]`: یک
گفت‌وگوی داخلی واقعی داخل خودِ پنل، با پالت و اجزای بصری همان قالب خریداری‌شدهٔ
Metronic (`apps/chat/` — فقط مرجع بصری، هیچ کد دموی آن استفاده نشد).

**دامنه، آگاهانه محدود:** گفت‌وگوی مستقیم (دو نفره) بین هر دو کاربر عادی همین
استقرار، فارغ از نقش یا workstream — دقیقاً همان کاربردی که
`DOLPHIN_FEATURE_MAP_AND_ROADMAP.md` §۳.۳ از قبل نوشته بود («هماهنگی بین
بازاریاب و مدیر»). چت گروهی (نسخهٔ دیگر همین دموی قالب) ساخته نشد — کسی
نخواسته بود؛ دامنه حدس زده نمی‌شود.

**چه چیزی ساخته شد:**
- اپ تازهٔ `chat/` با همان معماری هر اپ دیگر این کدبیس: `models.py`
  (`ChatThread`، `ChatParticipant` با ستون `last_read_at` خودش — «خوانده‌شده»
  مکان‌نمای هر بیننده روی کل رشته است، نه پرچمی روی هر پیام —،
  `ChatMessage` تغییرناپذیر)، `selectors.py` (محدودهٔ دید: فقط دو عضو خودِ
  گفت‌وگو)، `services.py` (`get_or_create_direct_thread` خنثی نسبت به تکرار،
  `send_message`، `mark_thread_read`)، `serializers.py`، `views.py`،
  `urls.py`، و ۲۸ تست.
- فیچر تازهٔ کاملاً مستقل `internal_chat` در `registry.py` — پیش‌فرض خاموش،
  همان الگوی `custom_branding`: تا کسی صریح نخواهد، خاموش می‌ماند؛ روشن‌شدنش
  با یک manifest تازه است، بدون هیچ تغییر کد.
- بدون وب‌ساکت: این کدبیس هیچ‌جا زیرساخت channel-layer ندارد، پس «زنده»
  یعنی نظرسنجی دوره‌ای (polling) — همان الگویی که اگر روزی صف کار بازاریاب
  یا هر گزارش دیگری زنده می‌شد، به آن می‌رسید. گفت‌وگوی باز هر ۴ ثانیه، فهرست
  گفت‌وگوها و نشان تعداد نخوانده در نوار کناری (روی *هر* صفحه، نه فقط
  `/chat/`) هر ۲۰-۱۲ ثانیه.
- صفحهٔ `/chat/`: فهرست گفت‌وگوها (آواتار حرف‌اول رنگی، پیش‌نمایش آخرین پیام،
  نشان تعداد نخوانده) + پنجرهٔ گفت‌وگوی فعال (حباب پیام رفت/آمد به سبک همان
  قالب) + دیالوگ «گفت‌وگوی تازه» با فهرست همکاران. Enter برای ارسال،
  Shift+Enter برای خط تازه.

### شواهد اجراشده

`python manage.py test --settings=config.test_settings` → تست کامل، صفر
شکست (۲۸ تست تازهٔ `chat`، به‌علاوه به‌روزرسانی تست فیچرهای withheld
Client‑1 به شش مورد). `check_html_branding.py` →
`HTML_BRANDING_PASS files=249`. `node --check dolphin-app.js` → بدون خطا.
تست زندهٔ مرورگر با دو کاربر واقعی (یک مدیر پلتفرم و یک بازاریاب، در نشست‌های
جدا): شروع گفت‌وگو از فهرست همکاران، ارسال و دریافت پیام، نشان نخواندهٔ نوار
کناری روی صفحهٔ داشبورد (نه فقط `/chat/`)، خواندن خودکار هنگام باز کردن
گفت‌وگو، و **دریافت زندهٔ یک پیام تزریق‌شده از سمت سرور در همان تب باز، بدون
هیچ تازه‌سازی دستی، ظرف کمتر از ۶ ثانیه** — اثبات مستقیم کارکرد نظرسنجی
دوره‌ای.

---

## [1.7.19] — ۲۰۲۶-۰۹-۰۴

### دیباگ بصری کامل پنل: دو باگ واقعی پیدا و رفع شد

طبق دستور مستقیم مالک محصول («کل صفحات و باکس‌ها را در کد و مرورگر واقعی
بررسی کن و همه جزئیات ریز اشتباه را رفع کن»)، یک بازرسی سیستماتیک روی
تقریباً همهٔ صفحات سرو‌شدهٔ پنل (داشبورد، مشتریان، سرنخ‌ها، فاکتورها، سفارش‌ها،
گزارش‌ها، تقویم‌ها، چک/اقساط/دریافت، انبار، کاربران، رویدادها، برندینگ) انجام
شد — هم با خواندن کد (CSS/قالب‌ها) و هم با یک مرورگر واقعی روی یک نمونهٔ
اجراشدهٔ محلی با دادهٔ واقعی (کاربر با هر پنج نقش، مشتری، سرنخ، فروش، فاکتور).

**نتیجهٔ صادقانه:** پاس‌های فاصله‌گذاری قبلی (`1.7.2` تا `1.7.4`) و بازبینی‌های
بعدی پنل را واقعاً تمیز نگه داشته بودند — بازرسی خودکار (فاصلهٔ عنوان کارت تا
بدنه، padding سرستون جدول، فاصلهٔ بین دکمه‌ها) روی هیچ‌کدام از این صفحات چیزی
پیدا نکرد. آنچه پیدا شد دو باگ واقعی و مشخص بود، هر دو فقط با اجرای زندهٔ
مرورگر کشف شدند (نه با خواندن کد به‌تنهایی):

1. **نمودار عملکرد کاربران گاهی کاملاً نامرئی می‌شد** (داشبورد و
   `/reports/user-performance/`). علت ریشه‌ای: `renderPerformanceChart(...)`
   قبل از برداشتن `hidden` از باکس والدش صدا زده می‌شد؛ ApexCharts عرض
   ظرفش را همان لحظه اندازه می‌گیرد، و ظرفی که هنوز `display:none` است عرض
   صفر می‌دهد — نموداری با `width:0` که بعداً هم هرگز خودش را دوباره
   اندازه‌گیری نمی‌کند (چون `animations:{enabled:false}` عمداً برای مشکل
   مشابه دیگری تنظیم شده بود). با جابه‌جایی یک خط (نمایش باکس، بعد رسم
   نمودار) رفع شد؛ با چند بار رفرش واقعی پیاپی روی حالت خراب و رفع‌شده تأیید
   شد.
2. **واحد کالا روی فاکتور چاپی به انگلیسی خام** (`piece` به‌جای «عدد»).
   `common/templates/common/invoices/print.html` مستقیم `product.unit` را
   می‌خواند — مقدار خامِ ذخیره‌شدهٔ `TextChoices`، نه برچسب فارسی‌اش — درحالی‌که
   همه‌جای دیگر پنل (از جمله همین صفحه برای بقیهٔ فیلدها) از `get_unit_display`/
   `unit_display` استفاده می‌کند. با همان یک تابع رفع شد.

### شواهد اجراشده

`python manage.py test --settings=config.test_settings` → **تست کامل، صفر
شکست** (یک تست تازه، `test_printed_invoice_shows_the_persian_unit_label_not_
the_stored_code`، رگرسیون همان باگ دوم را می‌گیرد). `node --check
common/static/common/dolphin-app.js` → بدون خطا. تست زندهٔ مرورگر: ورود با
هر پنج نقش روی دادهٔ واقعی، بازرسی خودکار فاصله‌گذاری روی بیش از ۲۰ صفحه (صفر
مورد)، بازتولید و رفع باگ نمودار با چند رفرش پیاپی در چند اندازهٔ صفحه (۱۲۸۰،
۱۴۴۰)، تأیید بصری موبایل (۳۷۵px)، و تأیید مستقیم HTML چاپ‌شدهٔ فاکتور بعد از
رفع (`عدد` به‌جای `piece`).

---

## [1.7.18] — ۲۰۲۶-۰۹-۰۴

### نظافت مستندات — بدون تغییر کد

طبق درخواست مستقیم مالک محصول («نظافت مستندات رو انجام بده»)، دو سند
وضعیت/نقشه‌راه به‌روز شدند؛ هیچ فایل کد یا تست تغییر نکرد.

- `DOLPHIN_FEATURE_MAP_AND_ROADMAP.md` بخش ۸ («جمع‌بندی و اولویت
  پیشنهادی»): خط پایانی متناقض («فازهای ۴ و ۵ هنوز پیاده‌سازی نشده‌اند» —
  درحالی‌که همان قلم‌ها چند خط بالاتر ✅ بودند، از نسخهٔ قدیمی‌تر همین بخش
  جا مانده بود) حذف شد. چهار قلم انجام‌شده که در آن فهرست نبودند اضافه
  شدند: ارقام فارسی (`[1.7.14]`)، کنسول مرکزی (`[1.7.15]`)، برند
  اختصاصی+دسکتاپ (`[1.7.16]`)، پیش‌نمایش زنده+بستهٔ استقرار (`[1.7.17]`).
  فاز الف (برندسازی TIARA) برچسب «⏸️ متوقف» گرفت تا با تصمیم فریز
  `[1.7.9]` هم‌خوان باشد. در پایان بخش، فهرست صریح آنچه واقعاً هنوز باز
  است اضافه شد: قرارداد فیچرهای تازهٔ TIARA، سند حسابداری تأییدشده، بررسی
  امنیتی سطح ۳ مینی‌اپ (دیپلوی خودکار SSH)، و دامنه‌های باز کسب‌وکار بزرگ‌تر.
- `DOLPHIN_PROJECT_HANDOFF.md`: یادداشت راهنمای خواندن به ابتدای فایل
  اضافه شد. این سند دو بخش دارد — ژورنال معکوس‌زمانی بالا (رجیستر معتبر
  وضعیت فعلی، جدیدترین بالا) و بخش‌های شماره‌گذاری‌شدهٔ `۰` تا `۱۴` نزدیک
  انتهای فایل که یک عکس فوری ثابت از یک ممیزی مستقل در ۲۰۲۶/۰۸/۱۴ تا ۱۷
  است، پیش از شروع ژورنال. طبق قانون مخزن آن بخش بازنویسی نشد؛ فقط صریح
  علامت‌گذاری شد که هر ادعای آن (مثل «۳۴۲ تست» یا «وضعیت انتشار:
  NO-GO») مال همان تاریخ است، نه وضعیت امروز، و تصمیم‌های واقعاً باز در
  `DOLPHIN_FEATURE_MAP_AND_ROADMAP.md` بخش ۸ فهرست‌بندی شده‌اند.

### شواهد

تغییر فقط در دو فایل مستندات؛ `git diff --stat` هر دو فایل را نشان
می‌دهد، بدون تغییر در `.py`/`.js`/`.html`. اجرای تست تکراری لازم نبود
(بدون تغییر کد) — طبق قاعدهٔ مخزن دربارهٔ گیت‌های سراسری فقط در نقاط
یکپارچگی معنادار.

---

## [1.7.17] — ۲۰۲۶-۰۹-۰۴

### پیش‌نمایش زنده در کنسول + بستهٔ استقرار (zip)

طبق دستور مستقیم مالک محصول، سناریوی واقعی زیر پیاده شد: «مشتری زنگ
می‌زند، اسم و ماژول‌های موردنظرش را می‌گوید؛ من کنسول را روی لپ‌تاپ خودم
باز می‌کنم، اسم را وارد و ماژول‌ها را تیک می‌زنم، پیش‌نمایش می‌گیرم که
کامل چک کنم، و اگر اوکی بود خروجی می‌گیرم و روی سرور مشتری دیپلوی می‌کنم.»

**پیش‌نمایش زنده (`scripts/preview_runner.py`).** یک دکمهٔ تازه کنار
فیچرهای تیک‌خورده — بدون نیاز به کلید خصوصی. یک نمونهٔ *واقعیِ* موقتِ کل
کدبیس بالا می‌آید: دیتابیس SQLite تازه (migrate‌شده)، `manage.py
runserver` روی یک پورت محلی دیگر، با یک manifest امضاشده با یک کلید
Ed25519 یک‌بارمصرفِ درون‌حافظه‌ای — هرگز کلید واقعی اپراتور، هرگز روی
دیسک. اسم تایپ‌شده خودکار فیچر `custom_branding` را روشن و مستقیم در
`BrandSettings` همان پیش‌نمایش می‌نشاند، پس اپراتور دقیقاً چیزی را می‌بیند
که مشتری خواهد دید — نه ماکاپ. یک ورود موقت (کاربری/گذرواژهٔ تصادفی، فقط
همان پنجره) هم ساخته می‌شود. حداکثر یک پیش‌نمایش هم‌زمان؛ توقف دستی یا
خروج از کنسول همیشه پاک‌سازی می‌کند. این پیش‌نمایش هیچ ربطی به چیزی که
واقعاً امضا و صادر می‌شود ندارد.

**بستهٔ استقرار (zip).** وقتی شناسهٔ استقرار و دامنه پر شوند، کنار دو
دانلود قبلی یک zip هم ساخته می‌شود: manifest.json + dolphin.env + یک
`DEPLOY-STEPS.txt` مخصوص همان مشتری با دستور آمادهٔ `quickstart.sh`
(رمزها روی خودِ سرور مشتری ساخته می‌شوند — امن‌ترین راه، طبق راهنمای
استقرار بخش ۱.۰) و راه جایگزینِ استفادهٔ مستقیم از همان .env، برای وقتی که
لازم باشد.

**دو باگ واقعی، هر دو فقط با تأیید زندهٔ مرورگری کشف شدند — نه با تست
خودکار:**
1. `CSRF_COOKIE_SECURE`/`SESSION_COOKIE_SECURE` در `config/settings.py` از
   `DEBUG` در زمان *import خودِ آن فایل* مشتق می‌شوند؛ `DEBUG = True` بعد
   از `from config.settings import *` این دو را دوباره محاسبه نمی‌کند.
   نتیجه: کوکی‌ها `Secure` می‌ماندند، روی `http://127.0.0.1` فرستاده
   نمی‌شدند، و هر ورودی به پیش‌نمایش با خطای CSRF رد می‌شد — چیزی که
   `django.test.Client` (بدون HTTP واقعی) هرگز نمی‌بیند. رفع شد با override
   صریح؛ حالا `common/tests/test_preview_runner.py` این را با یک HTTP واقعی
   (کوکی‌جار، امضای CSRF دوگانه) دوباره چک می‌کند.
2. دکمهٔ پیش‌نمایش داخل همان فرم اصلی بود؛ مرورگر همهٔ فیلدهای `required`
   آن فرم (کلید امضا) را قبل از هر submit — حتی با `formaction` جدا — چک
   می‌کند، پس هیچ درخواستی نمی‌رفت. با `formnovalidate` رفع شد.

### شواهد اجراشده

`python manage.py test --settings=config.test_settings` → **۱۴۷۲ تست، صفر
شکست** (`OK (skipped=16)`) — ۳۴ تست تازه: ۱۱ در `test_preview_runner.py`
(شامل چند بوت کامل و واقعی — ۱۰۰+ ثانیه، عمداً کند، چون فقط یک HTTP واقعی
باگ کوکی‌های امن را نشان می‌داد) + ۲۳ در `test_manifest_builder.py`
(اعتبارسنجی فرم پیش‌نمایش، مسیرهای HTTP با preview_runner موک‌شده، و
محتوای واقعی zip بستهٔ استقرار). `check_html_branding.py` →
`HTML_BRANDING_PASS files=248`. تست زندهٔ مرورگر کامل: تیک‌زدن
customers+leads، تایپ اسم «غزال»، کلیک «پیش‌نمایش زنده»، دیدن بنر با
اطلاعات ورود، باز کردن پیش‌نمایش در تب تازه (صفحهٔ ورود با «غزال» به‌جای
Dolphin)، ورود واقعی با کاربری/گذرواژهٔ ساخته‌شده، دیدن فقط ماژول‌های
تیک‌خورده در نوار کناری، توقف پیش‌نمایش و تأیید غیرقابل‌دسترس‌شدن پورت؛
جداگانه، ساخت manifest+.env با شناسهٔ استقرار، دانلود بستهٔ zip و تأیید
بایت‌های واقعی آن (امضای PK، سه فایل داخلش).

---

## [1.7.16] — ۲۰۲۶-۰۹-۰۴

### برند اختصاصی اختیاری (لوگو و اسم) + مینی‌اپ دسکتاپ برای کنسول

طبق دستور مستقیم مالک محصول — اولویت دوم تحلیل قبلی — دو چیز جداگانه ساخته
شد: (۱) امکان اینکه هر مشتری، اگر بخواهد، اسم و لوگوی خودش را به‌جای
دلفین روی پنلش ببیند، و اگر نخواهد، پیش‌فرض دلفین بماند؛ (۲) نسخهٔ
دسکتاپیِ کنسول محلی (`1.7.15`) برای کنترل بهتر، جدا از پنل اصلی.

**برند اختیاری.** `custom_branding` — یک فیچر تازه در `common/deployment/
registry.py`، دقیقاً مثل `customers` یا `inventory`، **پیش‌فرض خاموش**
(تصمیم مالک محصول: امن‌ترین پیش‌فرض، برند خودِ دلفین است، نه برند مشتری).
وقتی برای یک استقرار روشن شود، همان استقرار یک صفحهٔ تنظیمات تازه پیدا
می‌کند، `/branding/` — فقط مدیر پلتفرم همان استقرار (نه دلفین!) — که در آن
اسم نمایشی و یک فایل لوگو (jpeg/png/webp، حداکثر ۲ مگابایت) تنظیم می‌شود.
بلافاصله اثر می‌گذارد: سربرگ، نوار کناری، صفحهٔ ورود (پیش از احراز هویت)،
عنوان صفحات، پاورقی، و سربرگ/پاورقی سند چاپی فاکتور. خالی‌گذاشتن فیلدها
یعنی همچنان پیش‌فرض دلفین. لوگو در ستون `bytea` دیتابیس ذخیره می‌شود، نه
فایل — همان تصمیمِ از قبل گرفته‌شدهٔ `attachments.Attachment` (فایل‌سیستم
کانتینر فقط‌خواندنی است و هیچ‌وقت `MEDIA_ROOT` نداشته)، دوباره استفاده‌شده.
رنگ برند عمداً پارامتری نشد (فقط لوگو و اسم خواسته شده بود).

معماری این تصمیم را مستقیماً از مرز امنیتی سطح ۱/۲ ابزار ساخت manifest
گرفت (سؤال کاربر: «چطور این تغییرات به سرور واقعی برسد؟» → «فقط تولید
فایل تازه، بدون اتصال زنده»): تغییر فیچر `custom_branding` هم دقیقاً مثل
هر فیچر دیگر، فقط با یک manifest تازه از کنسول ممکن است — بدون هیچ کانال
شبکه‌ای زنده‌ای که این کد نگه دارد.

**مینی‌اپ دسکتاپ.** `scripts/manifest_builder.py --desktop` — همان سرور و
همان صفحات کنسول (`1.7.15`)، حالا در یک پنجرهٔ بومی به‌جای تب مرورگر
(کتابخانهٔ `pywebview`، اختیاری، فقط با `pip install -r scripts/
requirements-console.txt` — چیز دیگری در این مخزن به آن وابسته نیست، پس
هرگز داخل ایمیج نمی‌رود). یک لانچر پاورشل هم اضافه شد،
`scripts/dolphin-console.ps1`. اگر pywebview نصب نباشد، پیام روشنی می‌دهد
و حالت مرورگر معمولی همچنان بدون هیچ وابستگی تازه‌ای کار می‌کند. نسخهٔ
هاست‌شده/وبِ همین کنسول («سایت هم بتونه داشته باشه») عمداً برای این مرحله
ساخته نشد — تصمیم صریح مالک محصول در پاسخ به سؤال روشن‌سازی: فعلاً فقط
محلی/دسکتاپ کافی است.

یک باگ واقعی حین تأیید زندهٔ مرورگری کشف و رفع شد: یک خط جامانده از یک
ویرایش قبلی، `template_name` صفحهٔ `/branding/` را بی‌صدا به قالب گزارش
ارزش موجودی برمی‌گرداند — کد وضعیت همچنان ۲۰۰ بود، پس تست خودکاری که فقط
`status_code` را چک می‌کرد رد نمی‌شد. رفع شد و تستِ همان صفحه حالا محتوا/
قالب واقعی را هم چک می‌کند، نه فقط کد وضعیت.

### شواهد اجراشده

`python manage.py test --settings=config.test_settings` → **۱۴۴۴ تست، صفر
شکست** (`OK (skipped=16)`) — تازه: ۳۵ تست برند (`common/tests/
test_branding.py`: مدل/سرویس، API، رندر قالب‌ها، دسترسی صفحهٔ تنظیمات) +
۴ تست حالت دسکتاپ (`common/tests/test_manifest_builder.py`، بدون نصب واقعی
pywebview — با یک ماژول جعلی تزریق‌شده به `sys.modules`). سه تست موجود که
با فیچر تازه برخورد کردند اصلاح شدند: تست تعداد فیچرهای withheld
مشتری‌۱ (`test_ui_connectivity.py`)، چک «بدون فیلد رنگ برند»ی که حالا
`custom_branding` را هم می‌دید (`test_manifest_builder.py`)، و جدولِ
گرنت‌های دقیقِ دیتابیس (`test_database_privileges.py` — جدول جدید
`common_brandsettings` هم به `scripts/bootstrap-postgres.sh` و هم به
تست اضافه شد). `check_html_branding.py` → `HTML_BRANDING_PASS files=248`.
تست زندهٔ مرورگر: ورود، رفتن به `/branding/` (بعد از رفع باگِ بالا)، تنظیم
اسم «تیارا» و ذخیره، بازگشت به صفحهٔ ورود **قبل از لاگین دوباره** و دیدن
اسم تازه به‌جای Dolphin، بررسی سربرگ داشبورد و پاورقی/سربرگ فاکتور چاپی —
همه با «تیارا» به‌جای «Dolphin | دلفین».

---

## [1.7.15] — ۲۰۲۶-۰۹-۰۳

### کنسول مرکزی چند-مشتریه (سطح ۲.۵ مینی‌اپ ساخت پنل)

طبق دستور مستقیم مالک محصول («کنسول مرکزی چند-مشتریه را شروع و کامل کن»)،
اولین اولویت از تحلیل قبلی «چه چیزی برای عالی و کامل شدن پنل مانده» ساخته
شد: `scripts/manifest_builder.py` حالا یک بخش دوم دارد، `/console/` —
بایگانی محلیِ همهٔ manifestهایی که تا امروز از همین ابزار امضا شده‌اند.

**چه چیزی نیست، عمداً.** این یک اتصال زنده به سرور هیچ مشتری نیست — نه
پینگ می‌کند، نه وضعیت سلامت می‌بیند، نه هیچ پورتی روی سرور مشتری باز
می‌کند. همان مرز امنیتی سطح ۱/۲ («فقط `127.0.0.1`، کلید خصوصی هرگز روی
سرور مشتری») این تصمیم را از قبل تعیین کرده بود: «دیدنِ نسخه/فیچرهای
اعلام‌شده» (کار این کنسول) و «دیدنِ وضعیت زندهٔ سلامت» (که سند نقشه‌راه
خودش هم آن را احتمالاً محصولی جدا می‌داند) دو چیز کاملاً متفاوت‌اند.

**چه چیزی هست.**
- `scripts/deployment_records.py` — یک بایگانی JSON محلی، اتمیک (نوشتن
  روی فایل موقت و جایگزینی، نه ویرایش درجا) با یک رکورد به‌ازای هر
  `slug`ی که تابه‌حال با «شناسهٔ استقرار + دامنه» یک manifest ساخته.
  مسیرش (`scripts/.dolphin-console/`) به `.gitignore` اضافه شد — نامی از
  هیچ مشتری واقعی هرگز وارد سورس مشترک نمی‌شود.
- هر بار که فرم موجود سطح ۲ با slug+host کامل یک manifest می‌سازد، همان
  ثبت خودکار هم در کنسول درج/به‌روزرسانی می‌شود — بدون فیلد اجباری تازه؛
  اگر نوشتن ناموفق باشد (چک‌اوتِ فقط‌خواندنی، مشکل دسترسی) فقط یک اعلان
  می‌دهد، هرگز manifest/.env معتبری که همین لحظه ساخته شد را رد نمی‌کند.
- `/console/` — فهرست همهٔ استقرارهای ثبت‌شده (مشتری، دامنه، نسخه، تعداد
  فیچر، ایمیج، آخرین امضا)، تازه‌ترین اول.
- `/console/<slug>/` — صفحهٔ هر مشتری: فرم امضای manifest تازه (فیچر/نسخه
  از پیش تیک‌خورده از آخرین حالت ثبت‌شده؛ فیلدهای کلید همیشه خالی، چون
  هرگز ذخیره نمی‌شوند)، تولید اختیاری `.env` تازه (چک‌باکس، پیش‌فرض
  خاموش — رمزهای تصادفی جدید با آنچه سرور واقعی همین الان اجرا می‌کند
  فرق دارد، پس خودکار نیست)، ویرایش نام‌نمایشی/یادداشت بدون نیاز به کلید،
  و حذف رکورد (تصریح‌شده در متن و در تأیید مرورگری: فقط بایگانی محلی،
  هیچ اثری روی سرور مشتری ندارد).
- `_page()`ی فرم تکی، بدون تغییر رفتار، به قطعات مشترک (`_STYLE`،
  `_profile_options_html`، `_feature_checkboxes_html`،
  `_FEATURE_DEPENDENCY_SCRIPT`) بازتوزیع شد تا صفحات کنسول و فرم تکی هرگز
  از هم واگرا نشوند — تأیید شد با اجرای ۲۹ تست موجود قبل از هر تغییر دیگر.

Evidence: `python manage.py test --settings=config.test_settings` →
۱۴۰۵ تست، ۰ خطا (`OK`, `skipped=16`) — ۲۵ تست تازه: ۹ تای
`test_deployment_records.py` (بایگانی: رفتار خالی، رفت‌وبرگشت، حفظ
`created_at` روی به‌روزرسانی، رد اسلاگ نامعتبر پیش از نوشتن، حذف اسلاگ
ناشناخته، نوشتن اتمیک) + ۱۶ تای تازه در `test_manifest_builder.py`
(ثبت خودکار رکورد از فرم موجود، صفحات کنسول، و مسیرهای واقعی HTTP روی
`ThreadingHTTPServer` واقعی — فهرست، جزئیات، امضای تازه، بازتولید اختیاری
`.env`، ویرایش، حذف، ۴۰۴ برای اسلاگ ناشناخته). ریسک آلودگی تست
(نوشتن تصادفی در `scripts/.dolphin-console/` واقعی این چک‌اوت هنگام اجرای
تست) شناسایی و رفع شد: کلاس‌های تستی که اکنون رکورد می‌سازند
`deployment_records.DEFAULT_STORE_PATH` را در `setUp` به یک پوشهٔ موقت
منحرف می‌کنند — بعد از اجرای کامل تست‌ها تأیید شد
`scripts/.dolphin-console/` در چک‌اوت واقعی همچنان وجود ندارد.
`check_html_branding.py` → `HTML_BRANDING_PASS files=247` (بدون تغییر —
این ابزار فایل HTML سرویس‌دهی‌شده نمی‌سازد، رشتهٔ پایتونی می‌سازد). تأیید
زنده در مرورگر: یک manifest واقعی با slug+host ساخته شد، در `/console/`
ظاهر شد، صفحهٔ جزئیاتش فیچرهای ذخیره‌شده را از پیش تیک‌خورده نشان داد،
یک امضای تازه (فیچر اضافه) رکورد را به‌روزرسانی کرد بدون تولید `.env`
ناخواسته (چک‌باکس خاموش)، فرم ویرایش نام‌نمایشی/یادداشت را بدون کلید
ذخیره کرد، و حذف رکورد را از فهرست برداشت.

---

## [1.7.14] — ۲۰۲۶-۰۹-۰۳

### ارقام مبالغ فارسی شد

طبق دستور مستقیم مالک محصول، تصمیم بازِ ثبت‌شده در `[1.7.13]` («ارقام
لاتین در نمایش مبالغ، عمداً تغییر نکرد») حالا بسته شد: **همه جا که مبلغ
ریالی نمایش داده می‌شود، ارقام فارسی است** — دقیقاً هم‌ارز با تاریخ جلالی
و شمارش که همیشه فارسی بودند.

بررسی نشان داد این یک تغییر، نه سه تا: سه پیاده‌سازی مستقل و موازیِ همان
قاعدهٔ گروه‌بندی (جداکنندهٔ عربی `،`، بدون اعشار، سقف‌گیری به‌جای رند) در
کدبیس وجود دارد — یکی برای صفحات زنده (`money()` در `dolphin-app.js`)،
یکی برای سند چاپی/PDF فاکتور (`common/templatetags/money_tags.py`)، و
یکی برای نمودارهای میله‌ای فهرست‌ها (`reports/list_charts.py`). هر سه
اصلاح شدند تا نتیجه یکسان بماند، نه فقط یکی. جالب اینکه کامنت خودِ
`list_charts.py` از قبل یک مثال با ارقام فارسی («۵۳۶٬۸۲۵٬۰۰۰ ریال») به‌عنوان
شکل درست نشان می‌داد — پیاده‌سازی واقعی هنوز به آن نرسیده بود.

- **`dolphin-app.js`، `money()`.** ارقام گروه‌بندی‌شده از راه `toPersianDigits`
  می‌گذرند، بعد از تمام محاسبات (رند/سقف‌گیری/جداکننده) که همه روی ارقام
  لاتین ساده انجام می‌شود — فقط نمایش نهایی عوض شد.
- **`common/templatetags/money_tags.py`.** همان تبدیل، با کمکی مشترکِ
  `common.jalali.to_persian_digits` — همان تابعی که تاریخ‌های همین سند چاپی
  را از قبل فارسی می‌کرد.
- **`reports/list_charts.py`.** `_money()` از کمکی محلی همان فایل
  (`_persian_digits`) استفاده کرد — همان تابعی که شمارش نمودارها را از
  قبل فارسی می‌کرد.
- ورودی‌های پولی (`data-money-input`) هم همین را می‌گیرند: هرچه تایپ شود
  با جداکننده و رقم فارسی نمایش داده می‌شود، و `moneyValue()` (که از قبل
  `toLatinDigits` صدا می‌زد) بی‌تغییر همان رقم لاتین تمیز را به API می‌فرستد.
- شناسه‌ها (کد ملی، شمارهٔ اقتصادی، تعداد/مقدار) **تغییر نکردند** — این‌ها
  مبلغ نیستند و از اول از راه `money()` رد نمی‌شدند.

### شواهد اجراشده

`python manage.py test --settings=config.test_settings` → **۱۳۸۰ تست، صفر
شکست** (`OK (skipped=16)`؛ یک تست تازه اضافه شد — تطبیق رقم بین دو
پیاده‌سازی `money()`). هر تست موجودی که رشتهٔ مبلغ لاتین را انتظار داشت
(فرمت‌بندی، نمودار فهرست‌ها، دو تست سناریوی مرورگر واقعی روی فروش و
زنجیرهٔ تجاری) به رقم فارسی به‌روزرسانی شد — نه رفتار تغییر کرد، فقط
انتظار تست با رفتار تازه یکی شد. `check_html_branding.py` →
`HTML_BRANDING_PASS files=247`. تست زندهٔ مرورگر: فیلد پولی واقعی تایپ شد
و `۱۸،۵۰۰،۰۰۰` نمایش داد در حالی که مقدار واقعیِ آمادهٔ ارسال به API دقیقاً
`18500000` ماند؛ سند چاپی فاکتور با مبلغ واحد/جمع/مالیات/مانده همه فارسی،
کنار مبلغ به حروف و تاریخ جلالی که از قبل فارسی بودند.

---

## [1.7.13] — ۲۰۲۶-۰۹-۰۳

### دیباگ سراسری با دیتای خام

پنجمین و آخرین مرحله از ترتیب مصوب مالک محصول (`[1.7.9]`): «همهٔ اپشن‌ها و
فیچرها و صفحه‌ها تست بگیر، مشکلات را لیست کن و برطرف کن — لاجیک و تم اصلی
تغییری نکند».

**روش.** یک دیتاست خام واقعی، کاملاً از راه لایهٔ سرویس (نه نوشتن مستقیم
ORM)، تا وضعیتی ساخته شود که خودِ برنامه هم می‌تواند بسازد: ۶ کاربر با هر
پنج نقش، ۴ مشتری، ۴ سرنخ (یکی دیرکرده، یکی تکمیل‌شده)، تماس، جامعهٔ هدف،
فروش، سند پستی با تغییر وضعیت، پیش‌فاکتور/سفارش/۳ فاکتور (۲ صادرشده، ۱
پیش‌نویس)، ۴ سند مالی (نقدی/حواله/خروجی/چک) + طرح اقساط + مانده اول دوره،
۲ پروندهٔ پس‌ازفروش با قرار (یکی گذشته)، پیامک ورودی و خروجی (موفق و
ناموفق)، و ۵ پیوست روی هر پنج نوع رکورد. سپس سه پیمایش:

1. **سمت سرور** — همهٔ ۴۷ مسیر UI و ۲۶ مسیر API، با هر پنج نقش.
2. **مرورگر** — همان صفحات با مدیر پلتفرم، بازاریاب و کارشناس پس‌ازفروش؛
   خطای کنسول و درخواست ناموفق شبکه در هر صفحه.
3. **صحت داده** — تطبیق تعداد ردیف‌ها با دیتای کاشته‌شده، و بازبینی ریاضی
   گزارش‌ها.

**نتیجهٔ پیمایش.** صفر خطای ۵۰۰، صفر خطای کنسول، صفر درخواست ناموفق. ماتریس
دسترسی دقیقاً همان چیزی بود که مستندات می‌گویند (مدیر فروشگاه بدون کاربران و
لاگ؛ بازاریاب بدون پول/پس‌ازفروش/ادمین و با ۴۰۴ روی رکورد خارج از دامنه‌اش؛
کارشناس پس‌ازفروش فقط پرونده‌های خودش). ریاضی گزارش‌ها دقیق بود (ارزش
موجودی ۱۷۵ عدد / ۸۳۱٬۰۰۰٬۰۰۰ ریال، مطالبات ۲۶٬۹۰۰٬۰۰۰ ریال). هر ۵ خروجی
XLSX درست پاسخ دادند.

**سه ایراد واقعی پیدا و رفع شد:**

1. **برچسب‌های ساعت تقویم انگلیسی بودند.** در همان شبکه‌ای که شمارهٔ روز
   (`۷ ۸ ۹`)، عنوان ماه (`شهریور ۱۴۰۵`) و نام روزهای هفته همه عمداً فارسی/
   جلالی شده بودند، ساعت هر رویداد `8:50p` نوشته می‌شد و محور ساعت نمای
   هفته/روز `12am, 1am, 2am`. علت: باندل FullCalendar هیچ locale فارسی
   ندارد و این دو برچسب، برخلاف بقیه، هیچ‌وقت override نشده بودند. حالا
   `eventTimeFormat`/`slotLabelFormat` شکل را ۲۴ساعته می‌کند (قرارداد
   ایرانی) و دو کمکی کوچک ارقام را فارسی می‌کنند — چیزی که هیچ گزینهٔ
   فرمت پوشش نمی‌دهد. `all-day` و `+N more` هم فارسی شدند. **هر دو تقویم**
   (سرنخ از `1.7.1` و پس‌ازفروش از `1.7.12`) با هم اصلاح شدند.
2. **دکوریتور تکراری.** `spend_received_cheque` در `billing/payments.py`
   دو بار `@transaction.atomic` داشت — بی‌ضرر ولی یک savepoint اضافه و
   آشکارا سهوی.
3. **گزارش سود بدون توضیح مبنای نقدی.** گزارش طبق تصمیم مالک محصول (بند
   ۷.۱) مبنای نقدی دارد، یعنی فاکتور صادرشده‌ای که هنوز دریافتی به آن
   تخصیص نیافته رقمی نشان نمی‌دهد. صفحه این را هیچ‌جا نمی‌گفت، و گزارشِ
   صفر در کنار ۲۶٬۹۰۰٬۰۰۰ ریال فاکتور صادرشده مثل خرابی به‌نظر می‌رسید.
   یک بند توضیحی اضافه شد. **لاجیک تغییر نکرد** — فقط متن.

**یک مورد گزارش شد و عمداً تغییر نکرد:** مبالغ با ارقام لاتین و جداکنندهٔ
فارسی نمایش داده می‌شوند (`26،900،000 ریال`)، در حالی که تاریخ و شمارش با
ارقام فارسی‌اند. تابع `money()` با دقت نوشته شده (BigInt، قاعدهٔ رند
مستندشده)، پس این می‌تواند یک انتخاب عمدی باشد نه یک ایراد؛ عوض‌کردن یک
قرارداد سراسری نمایش پول تصمیم مالک محصول است، نه چیزی که این پاس دیباگ
یک‌طرفه انجام دهد.

### شواهد اجراشده

`python manage.py test --settings=config.test_settings` → **۱۳۷۹ تست، صفر
شکست** (`OK (skipped=16)`). `check_html_branding.py` →
`HTML_BRANDING_PASS files=247`. تست زندهٔ مرورگر پس از رفع: هر دو تقویم در
نمای ماه و هفته، ساعت `۲۰:۵۰` و محور `۰۰:۰۰ ۰۱:۰۰ ۰۲:۰۰ …`، صفر رشتهٔ
انگلیسی باقی‌مانده، صفر خطای کنسول، و تم دست‌نخورده.

---

## [1.7.12] — ۲۰۲۶-۰۹-۰۳

### تقویم خدمات پس از فروش

چهارمین مرحله از ترتیب مصوب مالک محصول (`[1.7.9]`). نیمهٔ باقی‌ماندهٔ فاز ه
(بخش ۳.۲ نقشه‌راه) — سمت سرنخ در `1.7.1` انجام شده بود؛ `AfterSalesRequest`
فیلد «قرار بعدی» نداشت.

- **`AfterSalesRequest.next_appointment_at`** — همان الگوی
  `Lead.next_follow_up_at`؛ `null` یعنی چیزی زمان‌بندی نشده. **مستقیماً
  قابل نوشتن نیست** — فقط از راه عمل تازهٔ زیر، چون زمان‌بندی قرار قاعدهٔ
  مجوز و «پرونده بسته» خودش را دارد که یک نوشتن آزاد روی سریالایزر دور
  می‌زد.
- **`schedule_after_sales_appointment`** (aftersales/services.py) —
  دقیقاً همان قاعدهٔ مجوز `transition_after_sales_status`: نقش ارشد
  (`sales_manager`/`company_it`/`platform_admin`)، یا همان کارشناس
  workstream پس‌ازفروشی که پرونده به او واگذار شده — نه هر کارشناسی.
  `appointment_at=None` قرار را لغو می‌کند، رد نمی‌شود؛ پرونده بسته
  زمان‌بندی نمی‌پذیرد. هر بار یک ردیف `AfterSalesHistory` (رویداد تازهٔ
  `appointment_scheduled`) + یک ردیف audit log.
- **`POST /api/v1/after-sales/<id>/schedule-appointment/`** + فیلتر
  پنجرهٔ تقویم (`appointment_from`/`appointment_to` روی
  `next_appointment_at`) در `AfterSalesRequestViewSet` — دقیقاً کپی
  `LeadViewSet._filter_by_follow_up`.
- **صفحهٔ `/after-sales/calendar/`** — همان تنظیمات FullCalendar تقویم
  سرنخ (شبکهٔ جلالی، پاپ‌آور، دیرکرد). دو تفاوت واقعی، نه تزئینی: (۱) رنگ
  از روی باز/بسته است نه سه‌وضعیتی سرنخ، چون وضعیت پس‌ازفروش متن آزاد
  است، نه enum ثابت؛ (۲) کشیدن رویداد از راه همان عمل
  `schedule-appointment` می‌رود، نه یک PATCH مستقیم، چون یک PATCH مستقیم
  قاعدهٔ مجوز/پروندهٔ‌بسته را دور می‌زد.
- صفحهٔ جزئیات پرونده هم فیلد «قرار بعدی» و هم فرم زمان‌بندی گرفت.

### شواهد اجراشده

`python manage.py test --settings=config.test_settings` → **۱۳۷۹ تست، صفر
شکست** (`OK (skipped=16)`، شامل تست‌های تازهٔ زمان‌بندی/لغو/دسترسی/فیلتر
تقویم). **یک باگ واقعی در دیباگ کامل این مرحله پیدا و رفع شد، پیش از
commit:** افزودن چند فراخوان POST حساس (`schedule-appointment`) به مجموعه
تست‌ها، سقف throttle مشترک را برای تست‌های بعدی (`billing`) که کش را پاک
نمی‌کردند رد کرد — ۸ تست بی‌ربط با ۴۲۹ شکست خوردند وقتی کل مجموعه با هم
اجرا شد (هرچند هرکدام تنها اجرا می‌شدند سبز بودند). اصلاح شد با `cache.
clear()` در `setUp`/`tearDown` کلاس تست تازه — همان الگویی که چند فایل
تست دیگر (`billing/tests/test_payment_correction.py` و بقیه) از قبل
استفاده می‌کنند. `check_html_branding.py` → `HTML_BRANDING_PASS
files=247`. تست زندهٔ مرورگر: تقویم با رویداد واقعی رندر شد (پاپ‌آور کامل
با موضوع/مشتری/کارشناس/زمان/شرح)، کلیک به جزئیات رفت، فرم زمان‌بندی روی
صفحهٔ جزئیات یک درخواست واقعی زد (۲۰۰) و مقدار روی صفحه فوراً به‌روز شد،
بدون خطای کنسول.

---

## [1.7.11] — ۲۰۲۶-۰۹-۰۳

### پیوست فایل

سومین مرحله از ترتیب مصوب مالک محصول (`[1.7.9]`). پیش از کد، سؤالات باز
`BACKEND_SPEC.md` بخش «Files and documents — gates P10» از مالک محصول
پاسخ گرفتند: رکوردها (مشتری، سرنخ، فاکتور، سند فروش، پس‌ازفروش)؛ نوع/حجم
(فقط عکس jpeg/png/webp و PDF، حداکثر ۱۰ مگابایت — پیشنهادی، تایید شد)؛
نگه‌داری (برای همیشه، حذف فقط با مدیر یا مدیر پلتفرم — پیشنهادی، تایید
شد). اسکن بدافزار و سقف حجم کل استقرار **همچنان باز ماندند** — هیچ‌کدام
اختراع نشدند.

- **اپ تازهٔ `attachments/`.** مدل `Attachment` با پنج کلید خارجی nullable
  (مشتری/سرنخ/فاکتور/سند فروش/درخواست پس‌ازفروش) و یک `CheckConstraint`
  دیتابیسی که «دقیقاً یکی» را تضمین می‌کند — نه یک `GenericForeignKey`،
  که هیچ‌جای دیگر این کدبیس استفاده نشده و ارجاع واقعی/`on_delete=PROTECT`
  را با یک زوج `(content_type_id, object_id)` بدون قید عوض می‌کرد.
- **ذخیره‌سازی در خودِ PostgreSQL (`BinaryField`)، نه دیسک.** بررسی کد نشان
  داد فایل‌سیستم کانتینر `web` کاملاً read-only است (`test_web_filesystem_
  is_read_only_and_source_is_root_owned`) و این کدبیس هرگز `MEDIA_ROOT`
  نداشته؛ افزودن یکی یعنی Volume Docker تازه + تغییر ابزار بکاپ + تغییر
  nginx روی استقراری که همین الان اجرا می‌شود — ریسک واقعی زیرساخت برای
  فیچری با سقف ۱۰ مگابایت در هر فایل. ذخیره در ستون `bytea` یعنی صفر تغییر
  زیرساخت؛ داده با همان حجم/بکاپ دیتابیس موجود سوار می‌شود.
- **نوع فایل از روی بایت‌های خودش تشخیص داده می‌شود، نه ادعای کلاینت.**
  چهار بررسی magic-byte دستی (JPEG/PNG/WEBP/PDF) — نه `imghdr`، که در
  پایتون ۳.۱۳ (نسخهٔ اجرای این استقرار) از کتابخانهٔ استاندارد حذف شده.
- **مجوز آپلود = همان ظرفیتی که خودِ آن نوع رکورد برای نوشتن لازم دارد**
  (`customers.manage`، `leads.manage`، `invoices.manage`، `sales_documents.
  manage`، یا `after_sales.manage`/`after_sales.work`) — تکرار نشده، بلکه
  از selectorهای همان اپ‌ها عیناً استفاده شده. **حذف بدون توجه به نوع
  والد فقط برای نقش‌های مدیر/مدیر فنی/مدیر پلتفرم** — تصمیم مالک محصول،
  حتی برای کسی که خودش آن را آپلود کرده.
- **حذف با POST، نه فعل HTTP DELETE.** طبق قراردادی که
  `common/tests/test_commercial_shell.py` از قبل روی کل باندل مشترک
  `dolphin-app.js` چک می‌کند (هیچ‌جای فایل نباید فعل DELETE داشته باشد —
  هر تغییر از راه POST می‌رود، مثل bulk-delete موجود)، نقطهٔ پایانی حذف
  `POST /api/v1/attachments/<id>/delete/` است.
- **یک partial مشترک، نه پنج پیاده‌سازی.**
  `common/includes/attachments_panel.inc` + یک تابع جاوااسکریپت عمومی
  (`setupAttachmentsPanel`) در هر پنج صفحهٔ جزئیات موجود گنجانده شد
  (مشتری، سرنخ، فاکتور، سند فروش، پس‌ازفروش).
- فیچر تازهٔ `attachments` در `registry.py` — برای TIARA (Client-1) فعلاً
  **خاموش** نگه داشته شد، نه به‌خاطر ناتمام بودن، بلکه چون آن استقرار طبق
  تصمیم `[1.7.9]` فریز است؛ یک manifest تازه بدون هیچ تغییر کدی می‌تواند
  روشنش کند.
- تنظیمات تازه: `KARIZ_ATTACHMENT_MAX_BYTES` (پیش‌فرض ۱۰ مگابایت — سقف
  واقعی، هرگز بیش از قید ثابت دیتابیس). `nginx/default.conf` و
  `FILE_UPLOAD_PATHS`/`FILE_UPLOAD_MAX_MEMORY_SIZE` برای مسیر آپلود
  به‌روزرسانی شدند، هماهنگ با الگوی موجود دو مسیر ایمپورت xlsx.

### شواهد اجراشده

`python manage.py test --settings=config.test_settings` → **۱۳۶۰ تست، صفر
شکست** (`OK (skipped=16)`، ۲۵ تست تازهٔ `attachments` — شامل قید «دقیقاً
یک والد» و «نوع مجاز» در سطح دیتابیس، تشخیص واقعی نوع فایل روی محتوای
واقعی نه پسوند/ادعای کلاینت، و یک چرخهٔ کامل HTTP چندبخشی واقعی از آپلود
تا دانلود). دو باگ واقعی در دیباگ کامل این مرحله پیدا و رفع شدند، هر دو
پیش از commit، نه بعد از دیپلوی: (۱) قرارداد دسترسی PostgreSQL جدول تازه
را بدون دسترسی تعریف‌شده رد می‌کرد؛ (۲) اولین پیاده‌سازی حذف از فعل HTTP
DELETE استفاده می‌کرد، که قرارداد آزمون‌شدهٔ همین کدبیس آن را رد می‌کند.
`check_html_branding.py` → `HTML_BRANDING_PASS files=246`. تست زندهٔ
مرورگر (با یک فایل PNG واقعی، تزریق‌شده به input واقعی از راه DataTransfer
تا مسیر آپلود واقعی خودِ صفحه اجرا شود، نه یک فراخوانی API دستی): آپلود
موفق روی صفحهٔ جزئیات مشتری با کاربر بازاریاب، ردیف جدید با حجم/نوع/
بارگذاری‌کننده درست، دکمهٔ حذف برای بازاریاب اصلاً نمایش داده نشد (طبق
انتظار)، دانلود واقعی بایت‌به‌بایت با فایل اصلی یکی بود، بدون خطای کنسول.

---

## [1.7.10] — ۲۰۲۶-۰۹-۰۳

### پیامک — هستهٔ ارسال (Outbound SMS core)

طبق تصمیم مالک محصول ثبت‌شده در `[1.7.9]` (TIARA فریز، تمرکز روی تکمیل پنل
برند خودِ Dolphin به ترتیب: مینی‌اپ سطح ۲ ← **پیامک** ← پیوست فایل ← تقویم
خدمات پس از فروش ← دیباگ سراسری). این نسخه مرحلهٔ دوم است. طبق دستور صریح
این مرحله، **هر فیچر تازه باید کاملاً ماژولار باشد** — روشن/خاموش‌شدن
per-customer از همان سیستم manifest/`registry.py` موجود، نه یک کلید جدا.

- **`communications/sms.py` — هستهٔ provider-agnostic.** بدون ادغام هیچ
  gateway ایرانی خاصی (کاوه‌نگار/ملی‌پیامک/قاصدک/...) — هرکدام قرارداد HTTP
  متفاوتی دارند و هاردکد کردن یکی در کد مشترک یعنی یا بقیهٔ استقرارها کد
  مرده حمل کنند یا بدتر، این کد بی‌اجازه فرض یک قرارداد ادغام‌نشده را بکند.
  یک provider پشتیبانی می‌شود: `"http"` — یک درخواست HTTP عمومی که کاملاً از
  تنظیمات این استقرار (`KARIZ_SMS_API_URL`/`_BODY_TEMPLATE`/`_HEADERS`/
  `_SENDER_ID`/`_TIMEOUT_SECONDS`) ساخته می‌شود. جایگزینی مقادیر (`__SMS_TO__`،
  `__SMS_BODY__`، `__SMS_SENDER__`) روی مقدار JSON از‌قبل parse‌شده انجام
  می‌شود، نه روی متن خام — دقیقاً همان کلاس آسیب‌پذیری‌ای که `template.format()`
  روی متن باز می‌کرد (نقل‌قول یا خط تازه در متن پیامک، JSON را خراب می‌کرد).
  بدون کتابخانهٔ پایتون تازه — `requirements.txt` دست‌نخورده می‌ماند، دقیقاً
  همان استدلال `common/pdf.py` برای عدم افزودن کتابخانهٔ PDF.
- **`communications.OutboundSMS`** — یک ردیف برای هر تلاش ارسال، موفق یا
  ناموفق (هرگز حذف یا ویرایش نمی‌شود؛ برخلاف `InboundSMS`، متن پیامک نگه
  داشته می‌شود چون این کد خودش آن را نوشته، نه یک شخص ثالث).
- **`send_outbound_sms`** (communications/services.py) — قفل actor فعال روی
  یک تراکنش کوتاه (قبل از تماس شبکه، نه در طول آن)، اعتبارسنجی متن/گیرنده
  پیش از هر تلاش، سپس یک تلاش واقعی که همیشه دقیقاً یک ردیف ثبت می‌کند —
  شکست provider خطا نیست، یک نتیجهٔ قابل‌ممیزی است. فقط ظرفیت `sms.company`
  (همان ظرفیت گزارش پیامک ورودی؛ بازاریاب فعلاً نه) مجاز به ارسال است —
  تصمیمی محافظه‌کارانه و مستند، نه فراموشی.
- **صفحهٔ پنل «ارسال پیامک»** (`/sms/`) — فرم ارسال (مشتری/سرنخ/شماره، دقیقاً
  یکی) + لاگ پیامک‌های اخیر با وضعیت و جزئیات هر تلاش.
- فیچر تازهٔ `outbound_sms` در `common/deployment/registry.py` — کاملاً
  مستقل، بدون وابستگی اجباری (مثل `inbound_sms`)، در manifest هر استقرار
  قابل روشن/خاموش‌کردن.
- `scripts/bootstrap-postgres.sh` — دسترسی PostgreSQL برای جدول تازه
  (`SELECT, INSERT` فقط، append-only، دقیقاً مثل `communications_inboundsms`)
  اضافه شد؛ بدون این، deploy واقعی روی جدولی که هیچ نقشی دسترسی به آن ندارد
  گیر می‌کرد.

### شواهد اجراشده

`python manage.py test --settings=config.test_settings` → **۱۳۳۵ تست، صفر
شکست** (`OK (skipped=16)`، شامل ۴۱ تست تازهٔ `communications` — از‌جمله یک
سرور HTTP واقعی (`ThreadingHTTPServer`) که provider را سرتاسری امتحان
می‌کند: درخواست واقعی، جایگزینی امن placeholder با نقل‌قول/خط‌تازه در متن،
پاسخ ناموفق ۴xx به‌عنوان نتیجهٔ ثبت‌شده نه استثنا، قالب/هدر خراب، آدرس
غیرقابل‌دسترس، و عدم درز کلید API به `status_detail` ذخیره‌شده). یک باگ واقعی
پیدا و رفع شد: تست قرارداد دسترسی دیتابیس (`test_database_privileges.py`)
جدول تازه را بدون دسترسی تعریف‌شده رد کرد — قبل از push رفع شد، نه بعد از
deploy. `check_html_branding.py` → `HTML_BRANDING_PASS files=246`. تست زندهٔ
مرورگر: با یک سرور HTTP echo محلی به‌جای gateway واقعی، هم ارسال موفق هم
ارسال ناموفق را در پنل واقعی (ورود واقعی، مشتری واقعی، لاگ زنده) امتحان
کرد — بدنهٔ دقیق دریافتی سرور echo (گیرنده/متن/فرستنده) با آنچه در فرم تایپ
شد یکی بود.

---

## [1.7.9] — ۲۰۲۶-۰۹-۰۳

### مینی‌اپ ساخت پنل — سطح ۲: پیش‌نویس `.env`

طبق تصمیم مالک محصول (۲۰۲۶/۰۹/۰۳): TIARA (نسخهٔ `1.6.9`، مستقر) فعلاً فریز
است تا قرارداد فیچر تازه بسته شود؛ تمرکز فعلی تکمیل پنل برای برند خودِ
Dolphin است، با شروع از سطح ۲ همان مینی‌اپ ساخت پنل که در `1.7.8` سطح ۱
گرفت (بخش ۶ در `DOLPHIN_FEATURE_MAP_AND_ROADMAP.md`).

- `scripts/manifest_builder.py` یک فیلدست اختیاری تازه گرفت: شناسهٔ استقرار
  (slug)، دامنه/آی‌پی، ایمیج اپلیکیشن، مسیر manifest روی سرور مقصد، و روز
  نگه‌داری بکاپ. وقتی slug و دامنه/آی‌پی هر دو پر شوند، کنار manifest
  امضاشده یک `secrets/.env` پیش‌نویس هم ساخته می‌شود.
- پیش‌نویس با فراخوانی مستقیم `new_deployment.env_lines` — همان تابعی که
  خط‌فرمان `scripts/new_deployment.py` استفاده می‌کند — ساخته می‌شود، نه با
  تکرار منطق: همان رمزهای تصادفی (`secrets.token_urlsafe(48)`)، همان
  ترتیب و کامنت‌ها. `KARIZ_DEPLOYMENT_MANIFEST_KEYS` از قبل با کلید عمومیِ
  همان manifestِ همین ارسال پر شده — دو خروجی یک تحویل هرگز با هم قاطی
  نمی‌شوند.
- slug/دامنه با همان الگوهای `new_deployment.py` (`SLUG_PATTERN`،
  `HOST_PATTERN`) اعتبارسنجی می‌شوند — یک کپی دستی نه. اعتبارسنجی
  all-or-nothing است: slug بدون دامنه یا برعکس رد می‌شود، نه نیمه‌ساخته.
- **بدون فیلد «رنگ برند».** طرح اولیهٔ بخش ۶ چنین فیلدی را نام برده بود، اما
  هیچ تنظیمی در کد امروز رنگ برند per-deployment را نمی‌خواند (برندسازی طبق
  `CLAUDE.md` ثابت روی Dolphin/دلفین است) — فیلدی که به جایی وصل نیست
  دکور است، فیچر نیست. این تصمیم در docstring خودِ فایل هم ثبت شده.
- چیزی سمت سرور این ابزار ذخیره نمی‌شود — دقیقاً مثل manifest، `.env`
  فقط یک‌بار در صفحه نمایش داده و به‌صورت دانلود مستقیم (`Blob`) در دسترس
  است.
- `common/tests/test_manifest_builder.py`: ۱۵ تست تازه (فیلدست کامل،
  all-or-nothing، اعتبارسنجی slug/host/روز نگه‌داری، آی‌پیِ بدون دامنه،
  مقادیر پیش‌فرض، رمزهای تازه در هر ارسال، فقدان فیلد رنگ برند) — ۲۹ تست
  کل فایل.
- `DOLPHIN_FEATURE_MAP_AND_ROADMAP.md` §۶ و §۸ به‌روزرسانی شدند: سطح ۲
  علامت انجام‌شده خورد؛ §۸ ترتیب تازهٔ ۵ مرحله‌ای مصوب مالک محصول را ثبت
  کرد (این مرحله، سپس پیامک، پیوست فایل، تقویم خدمات پس از فروش، دیباگ
  سراسری).

### شواهد اجراشده

`python manage.py test --settings=config.test_settings` → **۱۳۰۵ تست،
صفر شکست** (`OK (skipped=16)`). `python scripts/check_html_branding.py` →
`HTML_BRANDING_PASS files=245` (بدون تغییر، چون هیچ HTML این نسخه تغییر
نکرد). تست زندهٔ مرورگر: فرم کامل با یک کلید امضای واقعی پر و ارسال شد —
هم manifest، هم `.env` تولید شدند، خط `KARIZ_DEPLOYMENT_MANIFEST_KEYS` در
هر دو فایل عیناً یکسان بود، هر دو لینک دانلود (`Blob`) واقعی و قابل‌کلیک
بودند، بدون خطای کنسول.

---

## [1.7.8] — ۲۰۲۶-۰۹-۰۳

### ۱) تقویم پیگیری: بهتر و کاربرپسندتر شد

طبق درخواست مستقیم مالک محصول («تقویم رو بهتر و userfriendly تر بکن»، سپس
«اسم روزهای هفته رو کامل بنویسه و تو حالت روز/هفته قاطی نکنه»):

- **رنگ وضعیت دیگر گم نمی‌شود.** `eventDisplay: "block"` اضافه شد — قبلاً
  هر رویدادِ ساعت‌دار (اکثر رویدادها) در نمای ماه فقط یک نقطهٔ کوچک رنگی
  می‌گرفت؛ حالا در هر سه نما، همیشه بلوک رنگی کامل است.
- **دیرکرد دیده می‌شود.** سرنخ «در انتظار تکمیل»ی که تاریخ پیگیری‌اش گذشته،
  حاشیهٔ نقطه‌چین می‌گیرد (بدون رنگ چهارم) + آیتم توضیحی در راهنما.
- **پیش‌نمایش با هاور.** به‌جای tooltip یک‌خطی مرورگر، هاور یک پاپ‌آور
  واقعی نشان می‌دهد: نام مشتری، بج وضعیت (+ «دیرکرد» اگر لازم)، کارشناس،
  منبع، کمپین، زمان دقیق، پیش‌نمایش یادداشت — همه با `textContent` ساخته
  می‌شود، نه HTML دستی. کلیک روی رویداد هنوز مستقیم به جزئیات سرنخ می‌رود.
- **اسم کامل روزهای هفته.** حروف تک‌کاراکتری قبلی (ی/د/س/چ/پ/ج/ش) در
  اندازهٔ فونت سرستون تقویم به‌سختی از هم قابل‌تشخیص بودند (چ/ج/پ همه یک
  شکل پایه دارند). حالا همیشه اسم کامل نوشته می‌شود — در نمای ماه فقط اسم
  روز (چون شمارهٔ روز از قبل توی خانه هست)، در نمای هفته/روز اسم روز روی
  یک خط و شمارهٔ روز زیرش، تا اسم‌های بلند مثل «چهارشنبه» مجبور نشوند کنار
  یک عدد در ستون‌های باریک نمای هفته جا بشوند.
- یک باگ واقعی هم رفع شد: پاس اندازه‌گیری داخلی FullCalendar (برای «+N
  مورد دیگر») گاهی رویداد را قبل از رسیدن اطلاعات کامل mount می‌کرد و
  کرش می‌داد؛ حالا آن پاس نادیده گرفته می‌شود، mount واقعی بعدی طبیعی کار
  می‌کند.

### ۲) مینی‌اپ ساخت پنل — سطح ۱: فرم ساخت Manifest

طبق درخواست مستقیم و ادامهٔ مستقیم بخش ۶ در
`DOLPHIN_FEATURE_MAP_AND_ROADMAP.md` («سطح ۱ — فرم ساخت manifest»):

- `scripts/manifest_builder.py` — یک فرم وب کوچک، کاملاً مستقل از پنل
  مشتری و از هر ایمیجی که ساخته می‌شود (کل `scripts/` طبق `.dockerignore`
  هرگز داخل ایمیج نمی‌رود). اجرا: `python scripts/manifest_builder.py`،
  خودکار در مرورگر باز می‌شود (`http://127.0.0.1:8799/`).
- فیچرها را با تیک می‌گیرد، وابستگی ناقص را خودکار اضافه می‌کند (همان
  `resolve_features` که `quickstart.sh` هم استفاده می‌کند)، و همان تابع
  امضای `sign_deployment_manifest.build_manifest` را مستقیم صدا می‌زند —
  بدون منطق رمزنگاری تازه، بدون تکرار کد امضا.
- خروجی یک صفحه است: لینک دانلود `manifest.json` + خط کلید عمومی آمادهٔ
  چسباندن در `KARIZ_DEPLOYMENT_MANIFEST_KEYS` — کاری که قبلاً دو دستور
  جدا بود.
- مرز امنیتی («کلید خصوصی هرگز نباید وارد این فرم شود مگر روی ماشین
  مالک پلتفرم») با محدودیت شبکه‌ای واقعی تضمین می‌شود، نه فقط تذکر: سرور
  فقط روی `127.0.0.1` گوش می‌دهد و هر درخواست با هدر `Host` غیر از
  `127.0.0.1`/`localhost` را با ۴۰۰ رد می‌کند. کلید فقط از روی مسیر فایل
  خوانده می‌شود — هرگز آپلود، لاگ یا در صفحه نمایش داده نمی‌شود.
- `common/tests/test_manifest_builder.py` (۱۴ تست تازه) — شامل یک تست که
  هر manifest ساخته‌شده را از مسیر تاییدِ واقعیِ برنامه
  (`common/deployment/manifest.verify_manifest_bytes`) عبور می‌دهد، نه فقط
  فرض درست‌بودنش، به‌علاوهٔ یک سرور HTTP واقعی روی پورتی که سیستم‌عامل
  می‌دهد برای تست مرز `Host`.
- `DOLPHIN_FEATURE_MAP_AND_ROADMAP.md` §۶ به‌روزرسانی شد: «سطح ۱» و ردیف
  اول «چیزی که کم داریم» علامت انجام‌شده خوردند؛ سطح ۲/۳ همچنان باز.

### شواهد اجراشده

`python manage.py test --settings=config.test_settings` → **۱۲۹۰ تست،
صفر شکست** (`OK (skipped=16)`، شامل ۱۴ تست تازهٔ manifest_builder).
تست زندهٔ مرورگر روی داده‌ی واقعی: هر سه نمای تقویم با اسم کامل روزها،
پاپ‌آور، دیرکرد، بدون خطای کنسول؛ فرم manifest_builder سرتاسری روی سرور
واقعی امتحان شد (زنجیرهٔ وابستگی چندلایه `payments→invoices→{customers,
products}`، مسیر کلید نامعتبر، فیچر ناموجود، بدون فیچر — همه رد صحیح).

---

## [1.7.7] — ۲۰۲۶-۰۹-۰۳

مستندسازی و پاک‌سازی، بدون تغییر رفتار پنل.

### ۱) راهنمای استقرار: دسترسی به ریپوی خصوصی و رجیستری خصوصی

طبق دو سؤال مستقیم مالک محصول («داستان manifest چیه؟ حذفش کنیم؟» و
«سرتیفیکیت لازمه؟») و راهنمای استقراری که پیش‌تر خواسته شده بود:

- بررسی کد نشان داد **manifest و TLS هر دو hard-requirement هستند، نه
  اصطکاک الکی**: `config/production_settings.py` با
  `DEPLOYMENT_MANIFEST_REQUIRED = True` بدون manifest معتبر اصلاً بالا
  نمی‌آید (و یک تست واقعی همین را قفل کرده)، و nginx بدون فایل گواهی
  پورت ۴۴۳ را باز نمی‌کند — پس حذف هیچ‌کدام پیشنهاد نشد.
- به‌جایش روشن شد که `scripts/quickstart.sh --self-sign --self-signed-tls`
  (از قبل موجود، نسخهٔ `1.7.6`) همین الان هر دو را در همان یک دستور
  خودکار می‌کند — مالک محصول self-signed را برای تست/دمو انتخاب کرد.
- `docs/ops/DOLPHIN_DEPLOYMENT_RUNBOOK.md` بخش «۱.۰ مسیر یک‌دستوری»
  دو پاراگراف گرفت: چون ریپوی گیت‌هاب خصوصی است، کد را با یک SSH deploy
  key فقط-خواندنی و تک‌مخزنی (نه یک personal access token کل حساب)
  بیرون بکشید؛ و اگر ایمیج در یک رجیستری خصوصی (مثل GHCR) باشد، هاست
  مشتری یک `docker login` با PAT محدود به `read:packages` لازم دارد.

### ۲) نقشهٔ راه: نگاشتِ سند زمینه‌ای تیم در برابر کد واقعی

طبق یک سند زمینه‌ای دیگر که مالک محصول به اشتراک گذاشت (خروجی مکالمهٔ
تیم دربارهٔ تبدیل Dolphin به محصولی ماژولار و چندمشتری، با ۱۵ سؤال باز).
بررسی نشان داد اکثر پایهٔ فنی این سؤال‌ها از قبل پاسخ داده شده — نه چیزی
که باید از صفر طراحی شود:

- `DOLPHIN_FEATURE_MAP_AND_ROADMAP.md` بخش ۶ یک زیربخش تازه گرفت که
  نگاشت را ثبت می‌کند: «Signed offline license/entitlement file» ==
  همان manifest امضاشدهٔ Ed25519 موجود؛ «یک/چند Image استاندارد به‌جای
  build جدا per-customer» == معماری فعلی registry+manifest؛ نصب/آپدیت
  Offline و Air-gapped == از قبل در `deploy.sh`/`quickstart.sh`. آنچه
  واقعاً هنوز باز است هم صریح فهرست شد: مفهوم رسمی «Core» در کد، کنسول
  مدیریت مرکزی چندمشتریه، GUI ساخت manifest، و مدل قیمت‌گذاری/SaaS/AI —
  این چهارتای آخر تصمیم کسب‌وکاری‌اند، کد چیزی برایشان حل نمی‌کند.
- تصمیم مالک محصول ثبت شد: اولویت فعلی همین ثبتِ وضعیت است، نه کد جدید؛
  schema فعلی manifest (فقط ست فیچر + یک `profile_id` ثابت) کافی است؛
  تنها فیلد آیندهٔ مدنظر، هویت/نام مشتری داخل manifest است — شروع نشده،
  اولویت پایین.
- در همان مسیر، ۳ ارجاعِ خرابِ `OPEN_BUSINESS_DECISIONS.md` (فایلی که
  ۲۰۲۶/۰۹/۰۱ داخل `BACKEND_SPEC.md` ادغام شده بود) به آدرس درست
  (`DOLPHIN_PROJECT_HANDOFF.md` §۱۴ / بخش «Open business decisions —
  elaboration» در `BACKEND_SPEC.md`) اصلاح شد.

### ۳) تست زندهٔ سیستم manifest، سرتاسری

طبق درخواست مستقیم («یه دونه manifest واقعی بساز و تست کن»). یک کلید
Ed25519 واقعی و یک manifest واقعی (پروفایل `client-1`، ۹ فیچرِ
زنجیره‌وابستگی‌سازگار) ساخته شد و دقیقاً همان توابعی که خودِ اپلیکیشن
موقع بالاآمدن و هر درخواست فرا می‌خواند (`configure_from_settings`،
`feature_enabled`) مستقیماً رویش اجرا شد — نه یک شبیه‌سازی جدا. ۲۰ چک،
همه پاس: بارگذاری صحیح، `feature_enabled` برای هر ۱۶ فیچر دقیقاً مطابق
انتظار، و رد صحیحِ چهار حالت خرابکاری (payload دستکاری‌شده، کلید عمومی
اشتباه، `key_id` نامعتمد، فایل غایب) به‌علاوهٔ دو حالت رد در لحظهٔ امضا
(فیچر ناموجود، وابستگی ناقص). همه‌چیز فقط در پوشهٔ scratchpad ساخته و
در پایان پاک شد — هیچ‌چیز وارد ریپو یا `secrets/` نشد.

### ۴) پاک‌سازی ریپو

طبق درخواست مستقیم. بررسی نشان داد هیچ سند یا تست تکراری/بی‌مصرفی وجود
ندارد (۷ سند ریشه دقیقاً همان‌هایی است که `README.md` نقشه کرده؛ از بین
۱۱۲ فایل تست هیچ‌کدام الگوی نام منسوخ‌شده نداشت) — چیزی برای حذف
ساختگی پیدا نشد. دو مورد واقعی حذف شدند:
- `dolphin-manifest-signing.pem` — کلید امضای باقی‌مانده در ریشهٔ ریپو
  (در `.gitignore`، هرگز commit نشده بود)؛ همان چیزی که
  `test_the_repository_carries_no_private_signing_key` این چند نوبت
  همیشه رویش شکست می‌خورد. حذفش تست را هم سبز کرد.
- `graphify-out/` — کش میانیِ ۱۳ مگابایتیِ یک اجرای قبلیِ `/graphify`
  (بدون فایل خروجی نهایی)، ردگیری‌نشده در گیت.

### شواهد اجراشده

`python manage.py test --settings=config.test_settings` → **۱۲۷۶ تست،
صفر شکست** (`OK (skipped=16)`) — برای اولین‌بار در این چند نسخه بدون
همان یک شکست شناخته‌شدهٔ قبلی، چون فایل کلید مسببش حذف شد. تعداد skip
عوض نشد (همان ۱۶ تای قبلی، مربوط به زیرساخت غیرقابل‌دسترس روی این هاست).

---

## [1.7.6] — ۲۰۲۶-۰۹-۰۲

پنج درخواست جدا از مالک محصول در یک نوبت.

### ۱) متغیرهای محیطی KARIZ_*: بررسی شد، تغییری در کد لازم نبود

طبق شکِ درست‌بنیاد مالک محصول («check the KARIZ_ env var task and fix it»)،
بررسی دقیق نشان داد **کد از اول درست بوده** — چیزی که نیاز به رفع داشت
توضیح نادرست `CLAUDE.md` بود، نه خودِ متغیرها.

- `.env.example` (کامنت خودش)، `README.md`، و ورودی `[1.6.9]` در
  `DOLPHIN_PROJECT_HANDOFF.md` — هر سه، از همان روز تغییر نام — صریحاً
  می‌گویند نگه‌داشتن پیشوند `KARIZ_*` برای این حدود ۳۰ متغیر استقرار
  **عمدی** بوده: `compose.yml`، فیلتر envsubst در nginx، و
  `config/production_env.py` دقیقاً همین اسم‌ها را می‌خوانند، و یک `.env`
  واقعیِ در حال اجرا (روی Nerkhbaan) هم دقیقاً همین اسم‌ها را دارد — تغییرِ
  نام یعنی آن محیط تا وقتی `.env`اش دستی به‌روز نشود بالا نمی‌آید، بدون
  هیچ فایده‌ای برای کاربر نهایی (چون این‌ها هیچ‌جا دیده نمی‌شوند).
- تنها `CLAUDE.md` ادعا می‌کرد این‌ها هم به `DOLPHIN_*` تغییر کرده‌اند —
  که با کد واقعی و سه سند دیگر در تناقض بود. طبق قاعدهٔ خودِ همین فایل
  («کد فعلی بر نوشتهٔ کهنه ارجحیت دارد؛ سند را اصلاح کن») بخش Branding
  اصلاح شد تا این استثنا را با همان دلیلِ مستندشده بیان کند، به‌جای ادعای
  نادرستِ «همه‌جا عوض شده».
- **هیچ کد یا اسکریپتی تغییر نکرد** — فقط `CLAUDE.md`.

### ۲) استقرار روی سرور تازه: یک دستور، امن و حرفه‌ای

طبق درخواست مستقیم («deploying it on a new server is very hard... make it
way easier and simpler... but safe and professional»):

- `scripts/quickstart.sh` اضافه شد — دقیقاً همان توالیِ بخش ۱ (نصب اول)
  راهنمای `DOLPHIN_DEPLOYMENT_RUNBOOK.md` را با فراخوانی همان ابزارهای
  از قبل موجود (`new_deployment.py`، `sign_deployment_manifest.py`،
  `build-image.sh`، `prepare-backup-volume.sh`، `deploy.sh`) به ترتیب
  اجرا می‌کند — نه یک مکانیزم تازه، فقط ارکستراسیون. هیچ گیت امنیتی
  دورزده نشد.
- مسیر مستندشده (ایمیج بازبینی‌شده، مانیفست امضاشده جای دیگر، گواهی
  واقعی) با سه پرچم قابل‌جایگزینی برای یک سرور اختصاصیِ تک‌نفره:
  `--build` (ساخت ایمیج از همین چک‌اوت)، `--self-sign` (امضای مانیفست
  با یک کلید یک‌بارمصرف همین‌جا)، `--self-signed-tls` (گواهی خودامضا) —
  هرکدام صریحاً یک مرز امنیتیِ مستندشده را در ازای سادگی معامله می‌کند و
  همین صریح در راهنما و در `--help` گفته شده.
- `--dry-run` کل نقشه (هر دستوری که اجرا می‌شد، به‌همان ترتیب، با
  مقادیر واقعی) را بدون لمس کردن هیچ چیزی چاپ می‌کند.
- `scripts/new_deployment.py` یک پرچم کمکی گرفت:
  `--print-resolved-features` — فهرست ویژگی‌های نهایی (با وابستگی‌های
  خودکار) را چاپ می‌کند تا `quickstart.sh` دقیقاً همان فهرست را امضا
  کند، بدون بازنویسیِ قانون وابستگی در شل.
- `docs/ops/DOLPHIN_DEPLOYMENT_RUNBOOK.md` یک بخش ۱.۰ تازه گرفت که این
  مسیر را معرفی می‌کند؛ بخش‌های دستیِ قبلی (۱.۱ تا ۱.۱۶) دست‌نخورده
  ماندند — هم برای رفع اشکال، هم چون `quickstart.sh` دقیقاً همان‌ها را
  صدا می‌زند.

### ۳) تقویم: فیلتر روز/هفته/ماه، مثل قالب مترونیک

طبق درخواست مستقیم. باندل FullCalendar از قبل این نماها را داشت
(`dayGridMonth`، `timeGridWeek`، `timeGridDay`) — دموی خودِ قالب مترونیک
(`assets/js/custom/apps/calendar`) هم دقیقاً همین سه دکمه را کنار هم
می‌گذارد.

- سرستون تقویم پیگیری سه دکمه گرفت: «ماه»، «هفته»، «روز».
- سرستون هر روز در نمای هفته/روز، برخلاف نمای ماه، شمارهٔ روز جلالی را
  هم کنار نام روز هفته نشان می‌دهد — چون در این دو نما، برخلاف ماه،
  هیچ شبکهٔ شماره‌دارِ دیگری برای مشخص‌کردن تاریخ وجود ندارد.
- عنوان بالای تقویم در نمای «روز» تاریخ دقیق (روز/ماه/سال) نشان می‌دهد،
  نه فقط «ماه/سال» — تنها نمایی که غیر از این، هیچ نشانهٔ دیگری از
  اینکه دقیقاً کدام روز است روی صفحه نمی‌ماند.

### ۴) لوگو و نام برند، وسط‌چین

طبق درخواست مستقیم. علت پیدا شد: `.app-sidebar-logo` خودِ قالب
`justify-content: space-between` است — برای دو عنصر کنار هم طراحی شده،
اما این هدر همیشه فقط یک عنصر فعال داشته (دکمهٔ باز/بستن با
`position: absolute` از جریان فلکس بیرون است) — و «فاصله‌گذاری بین دو
چیز» با یک چیز، آن یک چیز را به لبه می‌چسباند. یک قاعده اضافه شد:
`.app-sidebar-logo { justify-content: center; }` — امن، چون جایگاه
دکمه از جریان فلکس می‌آید، نه از `justify-content`.

### ۵) هالهٔ لوگو در حالت جمع‌شده: بررسی و رفع شد

طبق درخواست مستقیم. بررسی مستقیمِ computed style روی کل زنجیرهٔ DOM
(نه فقط حدس) نشان داد هیچ `box-shadow`/`filter`/پس‌زمینه‌ای در CSS
نبود — چیزی که «هاله» دیده می‌شد، خودِ سایه‌روشنِ نرم لوگو بود (درست
برای نشان بزرگ صفحهٔ ورود) که در اندازهٔ ~۲۹px حالت جمع‌شده، بدون هیچ
مرزی، محو به نظر می‌رسید. یک نشان کوچکِ تخت و پرکنتراست، رفعِ کاملش
است و اسمی که الان وجود ندارد؛ به‌جایش (طبق انتخاب مالک محصول از میان
گزینه‌های ارائه‌شده) یک مربع گردِ کم‌رنگ به رنگ برند پشت نشان اضافه شد
تا لبهٔ محوش جایی برای تمام‌شدن داشته باشد — همان الگویی که خودِ دموی
این قالب هم برای نشان‌های کوچک به کار می‌برد.

- انتخابگر این قاعده عیناً با دو شرطِ نمایش خودِ قالب برای این عنصر
  یکی است (نه یک انتخابگر ساده‌تر) — چون این پروژه همیشه
  `data-kt-app-sidebar-hoverable="true"` دارد، هاور روی نوارِ جمع‌شده
  آن را موقتاً باز می‌کند و قاعدهٔ خودِ قالب این نشان را دوباره مخفی
  می‌کند؛ یک `display: flex` کم‌دقیق‌تر از قاعدهٔ قالب، رویش برنده
  می‌شد و نشان را همزمان با نام کامل برند روی هم نشان می‌داد.
- تست زنده گرفته شد و همین باگِ دقیقِ specificity یک‌بار واقعاً رخ داد
  (اول با یک انتخابگر کوتاه‌تر) — با اندازه‌گیری مستقیمِ
  `getComputedStyle` بعد از انتشار پیدا شد، نه با فرض درست‌بودنِ چیزی
  که کامپایل شد.

### شواهد اجراشده

`python manage.py test --settings=config.test_settings` → **۱۲۷۶ تست**،
همان یک شکست محیطیِ از قبل شناخته‌شده، بدون شکست تازه.
`scripts/check_html_branding.py` → `HTML_BRANDING_PASS files=245`.
`scripts/validate_image_content.py --context` → `IMAGE_CONTENT_PASS`.
تست زندهٔ مرورگر: هر سه نمای تقویم (ماه/هفته/روز) با دادهٔ واقعی کار
می‌کنند؛ سرستون سایدبار در حالت باز وسط‌چین است؛ در حالت جمع‌شده نشان
داخل مربع کم‌رنگ برند دیده می‌شود؛ هاور روی نوارِ جمع‌شده به‌درستی نام
کامل را بدون همپوشانی با نشان نشان می‌دهد؛ بازگشت به حالت باز/بسته چند
بار بدون رگرسیون تکرار شد.

---

## [1.7.5] — ۲۰۲۶-۰۹-۰۲

سه درخواست جدا از مالک محصول در یک نوبت.

### ۱) ادمین همه نقش را بسازد، جز ادمین تازه

پیش از این، `assignable_roles` نقش «مدیر پلتفرم» را فقط برای همان نقش
(مدیر پلتفرم) در فهرست نقش‌های قابل‌واگذاری می‌گذاشت — یعنی یک مدیر پلتفرم
هم موقع ساخت کاربر تازه و هم موقع تغییر نقش یک کاربر موجود می‌توانست آن را
انتخاب کند، و می‌شد مدیر پلتفرمِ دومی ساخت. طبق تصمیم صریح مالک محصول
(«ادمین باید بتونه همه مدل کاربری ایجاد کنه به جز ادمین جدید. ادمین باید
یدونه بمونه»):

- `accounts/access.py`: `assignable_roles` دیگر هرگز `platform_admin` را در
  فهرست نمی‌گذارد — نه برای ساخت کاربر تازه، نه برای تغییر نقش، حتی برای
  خودِ یک مدیر پلتفرم که دارد عمل را انجام می‌دهد. صفحهٔ ساخت کاربر و صفحهٔ
  تغییر نقش هر دو از همین یک تابع می‌خوانند، پس هرگز با هم اختلاف ندارند.
- `accounts/services.py`: `change_user_role` یک قفل صریح اضافه گرفت —
  حتی اگر کسی مستقیماً به API بزند (نه از صفحه)، نقش `platform_admin` از
  این مسیر اعطا نمی‌شود.
- تنها راه ساخت مدیر پلتفرم همان مسیر قبلی و بیرون از پنل می‌ماند:
  `manage.py bootstrap_platform_admin`، که خودش هم از قبل اگر مدیر پلتفرمی
  از پیش وجود داشته باشد امتناع می‌کند.

### ۲) تیک‌زدن ردیف‌ها + حذف واقعی — فقط برای مدیر پلتفرم

هیچ مدلی در این پنل تا امروز واقعاً «حذف» نمی‌شد — یک کلاس پایه به اسم
`NoDestroyModelViewSet` عمداً DELETE را روی همه چیز می‌بست، و جایگزین همیشه
«غیرفعال‌سازی» بود. مالک محصول، پس از توضیح همین معماری، صریحاً خواست:
حذف واقعی و برگشت‌ناپذیر اضافه شود، اما فقط مدیر پلتفرم به آن دسترسی داشته
باشد، با هشدار روشن دربارهٔ برگشت‌ناپذیر بودن، و طراحی چک‌باکس دقیقاً مثل
بخش «مشتریان» قالب آماده مترونیک — دکمهٔ حذف فقط وقتی حداقل یک ردیف تیک
خورده باشد نمایش داده شود.

- `common/viewsets.py`: `NoDestroyModelViewSet` به `AdminHardDeleteModelViewSet`
  تغییر نام گرفت و یک `HardDeleteMixin` تازه دارد — `DELETE` تکی و یک اکشن
  `POST /api/v1/<resource>/bulk-delete/` (حداکثر ۲۰۰ شناسه در هر بار)، هر
  دو فقط برای نقش `platform_admin` (چک شده در خودِ ویوست، صرف‌نظر از اینکه
  کلاس دسترسیِ بیرونی چه اجازه‌ای می‌داد). این کار امن است چون تقریباً
  همهٔ کلیدهای خارجی این پروژه از قبل `on_delete=PROTECT` هستند — رکوردی که
  رکورد دیگری به آن وابسته است، خودِ دیتابیس اجازهٔ حذفش را نمی‌دهد؛ نتیجه
  به‌جای خطای ۵۰۰، پیام فارسی «سوابق وابسته دارد» با کد ۴۰۹ است. هر حذف،
  پیش از انجام شدن، در رویدادنامه ثبت می‌شود.
- `accounts/views.py`: `UserViewSet` یک قفل اضافه دارد — نه حذف حساب خودتان
  ممکن است، نه حذف حساب مدیر پلتفرم از این مسیر.
- ۱۳ صفحهٔ فهرست (مشتریان، سرنخ‌ها، تماس‌ها، دسته‌بندی محصولات، محصولات،
  فروش، اسناد فروش داخلی، خدمات پس از فروش، انبارها، سفارش‌ها، فاکتورها،
  دریافت/پرداخت، کاربران) یک ستون چک‌باکس و نوار «X مورد انتخاب شد / حذف
  موارد انتخاب‌شده» گرفتند — فقط وقتی `can_hard_delete` باشد رندر می‌شود
  (یعنی فقط مدیر پلتفرم می‌بیندشان)، و فقط وقتی حداقل یک ردیف تیک بخورد
  نوار ظاهر می‌شود، دقیقاً همان رفتار قسمت مشتریان قالب مترونیک. `activity-logs`
  (رویدادنامه) عمداً از این قابلیت بیرون ماند — رویدادنامه یک ثبت غیرقابل‌تغییر
  است، حذف‌شدنی‌بودنش دقیقاً همان چیزی را نقض می‌کند که برایش وجود دارد.
- کلیک روی حذف یک `window.confirm` با متن صریح نشان می‌دهد: غیرقابل بازگشت
  است، رکورد با سابقهٔ وابسته حذف نمی‌شود، و به‌جای حذف بهتر است از
  غیرفعال‌سازی همان صفحه استفاده شود. بعد از حذف، پیام موفقیت/شکستِ جزئی
  (چند مورد حذف شد، چند مورد به دلیل سابقه حذف نشد) نمایش داده می‌شود.
- `common/static/common/dolphin-app.js`: یک `setupRowSelection` مشترک به
  `setupPagedList` (تابع فهرست‌بندی مشترک ۱۲ از این ۱۳ صفحه) اضافه شد —
  یعنی این قابلیت یک‌بار نوشته شد، نه ۱۳ بار.

### ۳) تقویم پیگیری در محیط واقعی: چرا همیشه «در حال دریافت…» می‌ماند

طبق آزمایش با دیتای خام: علت یک باگ در کد تقویم یا سرور نبود — علت این
بود که فایل `plugins/custom/fullcalendar/fullcalendar.bundle.js` (که تقویم
از ۱.۷.۱ به آن نیاز دارد) در **هیچ استقرار واقعی** به دست مرورگر نمی‌رسید،
پس `FullCalendar` تعریف‌نشده می‌ماند و صفحه بی‌صدا در همان حالت لودینگ
گیر می‌کرد. `runserver` (محیط توسعهٔ محلی) این مشکل را هرگز نشان نمی‌دهد،
چون مستقیم از پوشهٔ `assets/` سرو می‌کند، بدون گذر از `collectstatic` یا
`.dockerignore` — دقیقاً همان دو گذرگاهی که مشکل واقعی در آن‌ها بود:

- `config/settings.py`: `STATICFILES_COLLECT_IGNORE` یک ورودی ساده `"custom"`
  داشت که کل پوشهٔ `plugins/custom/` (شامل `fullcalendar`) را از خروجی
  `collectstatic` حذف می‌کرد — چون تطبیق روی نام پوشه است، نه مسیر کامل،
  و «فقط دمو» وقتی این قاعده نوشته شد درست بود، اما با اضافه‌شدن تقویم در
  ۱.۷.۱ دیگر درست نبود. به‌جای آن، هر ۱۵ باندل واقعاً بلااستفادهٔ زیر
  `plugins/custom/` (غیر از `fullcalendar`) تک‌به‌تک نام برده شدند.
- `.dockerignore`: به همین ترتیب، کل `assets` نادیده گرفته می‌شود و فقط
  چند مسیر مشخص با `!` دوباره اضافه می‌شوند — `fullcalendar` در آن فهرست
  نبود، پس حتی وارد ایمیج ساخته‌شده هم نمی‌شد. یک خط `!assets/plugins/custom/fullcalendar`
  اضافه شد.
- `scripts/validate_image_content.py`: همان اصلاح — فهرست ممنوعهٔ
  `assets/plugins/custom` تک‌به‌تک شد و دو فایل واقعی `fullcalendar` به
  فهرست الزامی اضافه شدند، تا این گیت هم مثل واقعیت باشد.
- `common/tests/test_static_assets.py`: تست‌های `REQUIRED`/`EXCLUDED` و
  گیت بستر ساخت ایمیج با همین تغییر هماهنگ شدند.

### شواهد اجراشده

`python manage.py collectstatic` قبل از رفع: `plugins/custom/fullcalendar`
در خروجی نبود. بعد از رفع: هست، و ۱۵ باندل دیگر همچنان نیستند.
`scripts/validate_image_content.py --context` → `IMAGE_CONTENT_PASS`.
تست زندهٔ مرورگر با دادهٔ واقعی (کاربر مدیر پلتفرم، ۵ سرنخِ زمان‌بندی‌شده):
تقویم بار می‌شود، رویدادها نمایش داده می‌شوند، ماهِ خالی هم درست کار
می‌کند. برای قابلیت حذف: تیک‌زدن ردیف → ظاهرشدن نوار → حذف با تأیید →
پیام موفقیت → ردیف از فهرست می‌رود — روی صفحهٔ مشتریان به‌صورت زنده تأیید
شد؛ نبودِ ستون چک‌باکس برای نقش غیرمدیر پلتفرم و ۴۰۳‌شدنِ تماس مستقیم API
هم تأیید شد. `python manage.py test --settings=config.test_settings` →
**۱۲۶۱ تست**، همان یک شکست محیطیِ از قبل شناخته‌شده (کلید امضای باقیمانده
در ریشهٔ مخزن)، بدون شکست تازه. `scripts/check_html_branding.py` →
`HTML_BRANDING_PASS files=245`.

---

## [1.7.4] — ۲۰۲۶-۰۹-۰۲

طبق درخواست مستقیم مالک محصول («دکمه‌ها و باکس‌های نوشته و نوشته‌ها باید
دور تا دورش margin مناسب داشته باشن و به هم نچسبند از هیچ طرف. طراحی باید
مدرن و زیبا باشه») — رفعِ ریشه‌ای به‌جای وصله زدن قالب به قالب.

### پیدا شد

بازبینی گسترده‌تر (نه فقط دکمه‌ها، بلکه هر جفت عنصر «جعبه‌مانند» مجاور —
دکمه، ورودی، کارت، نشان — در هر دو جهت افقی و عمودی، روی هر صفحهٔ پنل و
سه دیالوگ، شامل صفحه‌های جزئیات سفارش/فاکتور با داده واقعی و نمای موبایل)
تقریباً همه‌جا تمیز بود — به‌جز خودِ کلاس `gap-2` بوتسترپ، که پایه‌ای‌تر از
آن چیزی بود که رفع‌های `1.7.2`/`1.7.3` (قالب به قالب) پوشش داده بودند: **هر
جای دیگری در کد که این کلاس استفاده شود** (فعلی یا آینده) همان ۶٫۵px را
می‌گرفت.

### رفع شد

- `common/static/common/dolphin.css`: یک قاعدهٔ `.gap-2 { gap: 8px !important; }`
  اضافه شد — `!important` لازم بود چون خودِ باندل تم (`style.bundle.rtl.css`)
  تمام کلاس‌های فاصله‌گذاری بوتسترپ از جمله `.gap-2` را با `!important` صادر
  کرده؛ بدون آن هیچ قاعدهٔ عادی نمی‌توانست رویش بنویسد (فهمیده شد وقتی
  اولین تلاش بدون `!important` روی صفحهٔ زنده هیچ اثری نداشت).
- این یک قاعده جایگزین همهٔ رفع‌های تک‌قالبیِ قبلی نمی‌شود؛ رویشان اضافه
  می‌شود تا **هیچ استفادهٔ آیندهٔ `gap-2` هم** دوباره به این مشکل برنگردد.

### شواهد اجراشده

اسکن زندهٔ DOM با آشکارساز عمومی‌تر (جفت هر دو عنصر جعبه‌مانند مجاور، افقی
و عمودی) روی داشبورد، فهرست/جزئیات کاربران، مشتریان، سرنخ‌ها، تقویم پیگیری،
پروفایل بازاریاب، گزارش عملکرد، **سفارش و فاکتور با داده واقعی**، و سه
دیالوگ — در دسکتاپ و موبایل — پیش و پس از رفع اجرا شد؛ پس از رفع هیچ جفتی
زیر ۸px نماند. `python manage.py test --settings=config.test_settings` →
**۱۲۴۳ تست**، همان یک شکست محیطیِ از قبل شناخته‌شده، بدون شکست تازه.
`scripts/check_html_branding.py` → `HTML_BRANDING_PASS files=245`.

---

## [1.7.3] — ۲۰۲۶-۰۹-۰۲

طبق درخواست مستقیم مالک محصول («صفحه‌های دیگه رو هم بررسی کن، شاید جای
دیگه هم چسبیده باشه») — یک بررسی کامل بعد از `[1.7.2]`.

### بررسی شد

هر صفحهٔ سرو‌شدهٔ پنل (فهرست/جزئیات کاربران، مشتریان، سرنخ‌ها، تعاملات،
محصولات، دسته‌بندی‌ها، انبارها، موجودی، فروش، سفارش، فاکتور، دریافت/پرداخت،
چک، اقساط، اسناد فروش داخلی، خدمات پس از فروش، هر ۷ گزارش، رویدادهای
سامانه، تقویم پیگیری، پروفایل بازاریاب، و سه دیالوگ ساخت رکورد) با همان
اسکریپت اندازه‌گیری زندهٔ نوبت قبل، بعد از اعمال‌شدن `[1.7.2]`، دوباره
اسکن شد — **همه‌جا تمیز بود**، یعنی رفعِ نوبت قبل واقعاً کل پنل را پوشش
داده بود.

### رفع شد

- تنها یک نمونهٔ باقیمانده پیدا شد: ردیف «از تاریخ / تا تاریخ» در فیلتر
  تاریخ ثبت مشتریان (`customers/list.html`، پنهان تا وقتی «تاریخ ثبت»
  انتخاب شود) هنوز فقط `gap-2` (۶٫۵px) داشت. همان `gap-lg-3` نوبت قبل
  اضافه شد.

### شواهد اجراشده

`python manage.py test --settings=config.test_settings` → **۱۲۴۳ تست**، همان
یک شکست محیطیِ از قبل شناخته‌شده، بدون شکست تازه. `scripts/check_html_branding.py`
→ `HTML_BRANDING_PASS files=245`.

---

## [1.7.2] — ۲۰۲۶-۰۹-۰۲

طبق درخواست مستقیم مالک محصول («پنل رو تمیز کن... دکمه‌ها و نوشته‌ها نباید
به هم چسبیده باشن... تم اصلی و لاجیک پنل رو عوض نکن»)، یک پاس فاصله‌گذاری
با مهارت `ui-ux-pro-max` (استاندارد حداقل ۸px بین اهداف قابل‌کلیک، بخش Touch
& Interaction) اجرا شد — نه یک بازطراحی، فقط فاصله.

### رفع شد

- **علت واقعی پیدا شد، نه فقط علائم:** ریشهٔ فونت این تم `13px` است (نه
  پیش‌فرض ۱۶px بوتسترپ)، پس هر جا `gap-2` بوتسترپ به کار رفته بود عملاً
  `6.5px` می‌شد، نه `8px` مستندشده‌اش. با یک اسکریپت اندازه‌گیری واقعی روی
  صفحهٔ زندهٔ چند صفحهٔ نمونه (نه حدس بصری)، دقیقاً همین یک الگو در همهٔ
  نمونه‌های واقعاً چسبیده پیدا شد.
- ردیف دکمه‌های بالای صفحه («کاربر جدید» + «دریافت XLSX» و مشابهش) در ۹
  قالب — `customers/list.html`، `inventory/stock_levels.html`،
  `invoices/detail.html`، `products/list.html`، `product_categories/list.html`،
  `users/detail.html`، `users/list.html`، `users/profile.html`،
  `warehouses/detail.html`، `warehouses/list.html` — حالا `gap-lg-3` هم
  دارند، یعنی روی دسکتاپ همان فاصلهٔ سخاوتمندانه‌ای را می‌گیرند که خودِ
  `base.html` از قبل برای این ردیف در نظر گرفته بود (`gap-2 gap-lg-3`) و
  قالب‌های تک‌تک با پیچیدن دکمه‌هایشان در یک `div` با فقط `gap-2` گم کرده
  بودند.
- دکمه‌های «عملیات» هر ردیف جدول (مثل «جزئیات»/«مجوزها»/«پروفایل») —
  فاصله‌شان (`common/static/common/dolphin.css`) از `0.5rem` (که همان
  ۶٫۵px بود) به یک مقدار ثابت `8px` تغییر کرد تا فارغ از اندازهٔ فونت ریشه
  همیشه دقیقاً به حداقل استاندارد برسد.
- **هیچ رنگ، فونت، چیدمان، کلاس Metronic، یا منطق پنل عوض نشد** — فقط
  مقدار `gap`.

### شواهد اجراشده

اسکن زندهٔ صفحه (نه بازبینی چشمی) روی داشبورد، فهرست/جزئیات کاربران،
پروفایل بازاریاب، فهرست مشتریان، انبارها، تقویم پیگیری، و دیالوگ «ساخت
کاربر» — پیش از این تغییر ماژول‌های فوق کمترین فاصلهٔ واقعی `6.5px`
داشتند، پس از آن هیچ جفت دکمه/برچسبی زیر ۸px نماند (بجز گروه‌های دکمهٔ
segmented عمدی مثل `.btn-group`/`.fc-button-group` که قرار است بدون فاصله
باشند). `python manage.py test --settings=config.test_settings` → **۱۲۴۳
تست**، همان یک شکست محیطیِ از قبل شناخته‌شده (کلید خصوصی، نامرتبط)، بدون
شکست تازه. `scripts/check_html_branding.py` → `HTML_BRANDING_PASS
files=245`.

---

## [1.7.1] — ۲۰۲۶-۰۹-۰۲

طبق درخواست مستقیم مالک محصول («تقویم پیگیری سرنخ‌ها رو اضافه کن»)، از فهرست
«قابلیت‌های استفاده‌نشدهٔ قالب مترونیک» که همین امروز داده شد (بخش ۳.۲
`DOLPHIN_FEATURE_MAP_AND_ROADMAP.md`).

### اضافه شد

- **`/leads/calendar/`** («تقویم پیگیری» در منوی «مرکز تماس») — همان سرنخ‌های
  صفحهٔ فهرست، روی یک تقویم ماهانه به‌جای جدول، رسم‌شده با کتابخانهٔ
  FullCalendar خودِ تم (`assets/plugins/custom/fullcalendar/`، تا امروز
  استفاده‌نشده). هیچ مدل یا مهاجرت تازه‌ای لازم نبود — `Lead.next_follow_up_at`
  از قبل در دیتابیس بود؛ فقط یک راه تازه برای دیدن و تغییرش.
  - رنگ رویداد وضعیت سرنخ را نشان می‌دهد (آبی=در انتظار، سبز=تکمیل،
    قرمز=کنسل)؛ کلیک به جزئیات سرنخ می‌برد؛ کشیدن رویداد به روز دیگر همان
    `PATCH /api/v1/leads/<id>/` فهرست سرنخ‌ها را می‌زند و با همان مجوز کار
    می‌کند — بازاریاب فقط سرنخ خودش را جابه‌جا می‌کند، نقش‌های بالاتر هر سرنخ
    شرکت را.
  - `sales/views.py`: پارامتر تازهٔ `follow_up_from`/`follow_up_to` روی
    `GET /api/v1/leads/` (بازهٔ دقیق با منطقهٔ زمانی، نه یک روز کامل مثل
    فیلتر تاریخ ثبت مشتری) — دامنهٔ همان `leads_for` را دارد، هیچ سرنخی
    بیرون از دسترسی بیننده برنمی‌گردد.
  - **قالب میلادی FullCalendar، محتوای شمسی:** باندل خریداری‌شدهٔ تم هیچ
    لوکال فارسی ندارد؛ عنوان ماه/سال و شمارهٔ روزها در جاوااسکریپت
    (`common/static/common/dolphin-app.js`) به معادل شمسی بازنویسی می‌شوند.
    ناوبری داخلی خودِ کتابخانه میلادی می‌ماند — فقط چیزی که رویش نوشته شده،
    نه چیزی که رویش حساب می‌شود.

### رفع شد

- `globalMessage(message, true)` — پیام موفقیت — همیشه با رنگ `alert-danger`
  (قرمز) نمایش داده می‌شد، نه سبز؛ کد فقط یک کلاس `.success` بی‌اثر
  toggle می‌کرد که هیچ استایلی در کل CSS پروژه یا تم برایش تعریف نشده بود.
  حین تست همین تقویم (پیام «تاریخ پیگیری به‌روزرسانی شد.») پیدا شد؛ رفعش
  یعنی حالا کلاس واقعی تم (`alert-success`) جایگزین `alert-danger` می‌شود —
  اثرش روی **همهٔ** پیام‌های موفقیت پنل است، نه فقط این تقویم (ذخیرهٔ
  کاربر/مشتری/محصول/فاکتور/سفارش و ده‌ها مورد مشابه دیگر).

### شواهد اجراشده

`python manage.py test sales.tests.test_lead_calendar common.tests.test_lead_calendar_page --settings=config.test_settings`
→ ۱۵ تست، OK. `python manage.py test --settings=config.test_settings` →
همان یک شکست محیطیِ از قبل شناخته‌شده (فایل کلید خصوصی، نامرتبط)، بدون شکست
تازه. بازبینی مرورگر واقعی (سرور توسعهٔ محلی sqlite، چون Docker/PostgreSQL
روی این میزبان در دسترس نبود): تقویم با پنج سرنخ نمونه رسم شد؛ کشیدن دو
رویداد به روزهای دیگر هر دو با `PATCH 200` واقعی ثبت شد و بعد از رفرش صفحه
باقی ماند؛ کلیک رویداد به جزئیات سرنخ رفت؛ حالت تاریک/روشن و نمای موبایل هر
دو بدون مشکل بصری؛ کنسول و شبکه بدون خطای غیرمنتظره.
`scripts/check_html_branding.py` → `HTML_BRANDING_PASS files=245`.

---

## [1.7.0] — ۲۰۲۶-۰۹-۰۲

طبق درخواست مستقیم مالک محصول، صفحهٔ «پروفایل» جداگانه برای هر بازاریاب:
مدیر فروشگاه یا مدیر پلتفرم می‌تواند پروفایل هر بازاریاب را جدا ببیند
(اطلاعات فردی + فروش + عملکرد)؛ خودِ بازاریاب فقط پروفایل خودش را می‌بیند،
نه دیگران را. طرح‌بندی نزدیک به صفحهٔ «User Profile» قالب مترونیک خریداری‌شده
است (کارت هدر با آواتار حروف‌اول، برچسب‌های وضعیت/نقش/تماس، کارت‌های KPI
نقطه‌چین) و نمودار روند فروش با همان ApexCharts موجود در پنل رسم می‌شود.
یک قابلیت تازه است، نه رفع اشکال — به همین دلیل طبق قاعدهٔ خودِ این فایل
(«MINOR: قابلیت تازه») نسخه به‌جای پچ، مینور بالا رفت.

### اضافه شد

- `common/users/profile.html` + `DolphinUserProfileView`/`DolphinMyProfileView`
  (`common/ui_views.py`) پشت دو مسیر تازه: `/users/<id>/profile/` (پروفایل هر
  کاربر در محدودهٔ بیننده) و `/profile/` (میان‌بر «پروفایل خودم»، بدون نیاز به
  دانستن شناسهٔ خود).
- مجوز صفحه از `reports.own`/`reports.company` می‌آید — همان جفتی که خودِ
  گزارش عملکرد کاربران از قبل لازم دارد — نه از `users.manage_*` که امروز فقط
  مدیر پلتفرم دارد؛ همین انتخاب است که مدیر فروشگاه را هم به پروفایل تک‌تک
  بازاریابانش می‌رساند. محدودهٔ واقعیِ «کدام شناسه» از
  `reports.selectors.users_for_performance_report(actor)` می‌آید — همان تابعی
  که خودِ گزارش عملکرد از قبل به کار می‌برد؛ برای بازاریاب یعنی فقط خودش، برای
  نقش‌های بالاتر یعنی هر شناسهٔ CRM.
- اندپوینت تازهٔ `GET /api/v1/reports/user-performance/trend/`
  (`reports/sales_insights.py`، `reports/profile_views.py`) — فروش تأییدشدهٔ
  یک کاربر، هفتگی یا ماهانه، با همان قاعدهٔ «بازهٔ خالی صفر است نه حذف» که
  `build_customer_growth_report` از قبل دارد؛ `user_id` پیش‌فرضش خودِ فراخوان
  است و هرگز از محدودهٔ `users_for_performance_report` بیرون نمی‌رود.
- دکمهٔ «پروفایل» در فهرست کاربران (`/users/`) و در جدول گزارش عملکرد
  (`performance_panel.inc`، صفحهٔ داشبورد و گزارش شرکت هر دو) — مسیر کشف صفحهٔ
  تازه برای مدیر فروشگاه، که به‌خودیِ‌خود به `/users/` دسترسی ندارد.
- ورودی «عملکرد من» در منوی حساب کاربری هدر (کنار «پروفایل من» موجود، که همان
  دیالوگ ویرایش سریع باقی می‌ماند و دست‌نخورده است) — فقط برای کسی که
  `reports.own`/`reports.company` دارد.

### شواهد اجراشده

`python manage.py test common.tests.test_user_profile reports.tests.test_sales_insights --settings=config.test_settings`
→ ۲۴ تست، OK. بازبینی مرورگر واقعی (سرور توسعهٔ محلی روی sqlite، چون Docker/PostgreSQL
روی این میزبان در دسترس نبود): ورود مدیر فروشگاه → پروفایل بازاریاب دیگر باز
شد، KPI و نمودار روند (هفتگی/ماهانه) و ریزجزئیات درست پر شدند؛ ورود بازاریاب
→ پروفایل خودش باز شد، پروفایل بازاریاب دیگر ۴۰۴ داد؛ کنسول و شبکه بدون خطای
غیرمنتظره.

---

## [1.6.8] — ۲۰۲۶-۰۹-۰۱

ریپازیتوری و پوشهٔ محلی به `Dolphin` تغییر نام دادند
(`github.com/Ilia-Shakeri/Dolphin`)؛ ریموت `origin` به همان نشانی به‌روز
شد. هیچ نشانی قدیمی به‌صورت سخت‌کد در کد یا مستندات پیدا نشد.

### تغییر کرد

- `common/static/common/brand/android-chrome-192x192.png` و
  `android-chrome-512x512.png` حالا همان فایل‌های اصلیِ `assets/Dolphin
  Logos/` هستند، نه نسخهٔ بزرگ‌نمایی‌شده از `apple-touch-icon.png` که
  در ۱.۶.۶ ساخته شده بود — این دو فایل در آن نسخه در پوشهٔ منبع بودند اما
  هنوز اضافه نشده بودند و از قلم افتادند.

### شواهد اجراشده

`python manage.py test common.tests.test_static_assets --settings=config.test_settings`
→ OK. `scripts/check_html_branding.py` → `HTML_BRANDING_PASS files=243`.

---

## [1.6.12] — ۲۰۲۶-۰۹-۰۱

طبق تصمیم مستقیم مالک محصول («کلی داکیومنت هست که الکی طولانین و چرت‌وپرت
نوشتن... همشون رو یکی کن و بقیه رو پاک کن»)، مستندات پراکندهٔ `docs/` در دو
فایل موجود ادغام شدند، و نقشهٔ راه با وضعیت واقعی به‌روز شد.

### تغییر کرد

- `docs/backend/*.md` (۱۳ فایل) و `docs/frontend/*.md` (۲ فایل) در پیوست
  `BACKEND_SPEC.md` ادغام شدند (۸۸۲ → ۳۰۰۱ خط). آن دو پوشه دیگر وجود
  ندارند. `docs/backend/ERD.mmd` (نمودار Mermaid، نه Markdown — با `rm -rf`
  اول اشتباهی همراه بقیهٔ پوشه پاک شده بود، قبل از commit پیدا و به‌عنوان
  بلوک ```mermaid``` داخل همان بخش «Relationship catalog» بازیابی شد؛ هیچ
  محتوایی گم نشد).
- بقیهٔ `docs/ops/*.md` (۱۴ فایل، `DOLPHIN_DEPLOYMENT_RUNBOOK.md` نبود) در
  پیوست همان runbook ادغام شدند (۱۵۵۷ → ۴۹۱۰ خط). `docs/ops/` حالا فقط
  همان یک فایل را دارد.
- هر سرفصل ادغام‌شده تیتر اصلی خودش را حفظ کرد (فقط سطح heading یک پله
  پایین آمد، با احترام به بلوک‌های کد — یک باگ در اسکریپت ادغام اول باعث
  شد کامنت‌های PowerShell داخل بلوک کد به‌اشتباه heading تشخیص داده شوند؛
  پیش از commit پیدا و رفع شد) و یک یادداشت منبع `*(from `مسیر/فایل.md`)*`
  زیرش آمد.
- هر ارجاع بین‌فایلی (لینک مارک‌داون، اشارهٔ backtick) به یکی از این ۲۹ فایل
  حذف‌شده، در خودِ دو فایل ادغام‌شده به لنگر داخلی یا عبارت «بخش X این
  سند» تبدیل شد؛ در کد/تست‌ها به مسیر تازه یا توضیح متنی اصلاح شد.
- `DOLPHIN_CLIENT1_CODEX_ROADMAP.md`: جدول فازها اصلاح شد (`P1.7`/`P2` که
  واقعاً انجام شده بودند ولی ✅ نداشتند)؛ بخش تازهٔ «وضعیت واقعی در برابر
  نقشه» اضافه شد — می‌گوید استقرار واقعی Tiara (`1.6.9`) جلوتر از گیت‌های
  رسمی `P12`/`P13`/`P14` این نقشه اتفاق افتاد، اثر واقعی هرکدام را برمی‌شمرد،
  و یک اولویت پیشنهادی (نه تصویب‌شده) برای گام بعدی می‌دهد — بدون ابداع
  هیچ تصمیم کسب‌وکار/مالی/قانونی تازه.
- `README.md`، `DOLPHIN_PROJECT_HANDOFF.md` (فقط بند زندهٔ معماری مستندات،
  نه ورودی‌های تاریخی)، و `DOLPHIN_FEATURE_MAP_AND_ROADMAP.md` با مسیرهای
  تازه هماهنگ شدند.

### نُه تست که مستقیماً محتوای این فایل‌ها را می‌خواندند، اصلاح شدند

`common/tests/test_backup_scripts.py`، `test_load_readiness.py`،
`test_release_images.py`، `test_security_scan_runbook.py`،
`test_image_content.py`، `test_ui_connectivity.py`،
`accounts/tests/test_accounts.py`. سه تست اول از یک کمک‌تابع تازه
(`runbook_section()` / معادلش) استفاده می‌کنند که بخش مربوط به هر فایل
قدیمی را از فایل ادغام‌شدهٔ بزرگ جدا می‌کند — تا `assertLess`/`assertNotIn`ی
که ترتیب یا نبودِ یک عبارت را **در همان بخش خاص** اثبات می‌کرد، به‌جای
جست‌وجوی کل فایل ۵۰۰۰ خطی، همان معنای اصلی را نگه دارد.

### حذف نشد

`CHANGELOG.md` و ورودی‌های تاریخی `DOLPHIN_PROJECT_HANDOFF.md` — همان
تصمیم مستندشده در `[1.6.9]`: سابقهٔ چه‌چیزی-وقتی هستند، نه مستندات زنده،
و بازنویسی‌شان یعنی جعل تاریخ.

### شواهد اجراشده

`python manage.py test --settings=config.test_settings` → **۱۲۰۴ تست، OK**
(همان یک شکست محیطی از قبل — فایل کلید خصوصی واقعی در ریشهٔ چک‌اوت محلی —
بدون تغییر، پرچم‌شده جدا). `scripts/check_html_branding.py` →
`HTML_BRANDING_PASS files=243`. `python manage.py check` → OK.

---

## [1.6.11] — ۲۰۲۶-۰۹-۰۱

بازبینی دوم اسناد Markdown، این بار محتوای کامل هر فایل خوانده شد نه فقط
عنوانش — طبق درخواست مستقیم مالک محصول که بازبینی قبلی («چیزی اضافه نیست»)
را کافی نمی‌دانست.

### حذف شد

- `docs/backend/DISCOVERY.md` — یک عکس لحظه‌ای (snapshot) قدیمی از وضعیت
  کشف بک‌اند، که خودش صریحاً می‌گفت محتوای قبلی‌اش «منسوخ» است. با
  `DOLPHIN_PROJECT_HANDOFF.md` (مرجع زندهٔ وضعیت) کاملاً جایگزین شده بود، و
  **هیچ فایل دیگری در کل مخزن به آن ارجاع نمی‌داد** — تنها سندی از ۳۸ فایل
  Markdown پروژه با این ویژگی.

### بررسی شد، نگه داشته شد (با دلیل مستند)

باقی ۳۷ فایل تک‌تک خوانده شدند و هرکدام حداقل یک ارجاع واقعی از جای دیگر
مخزن دارند (شمارش دقیق در پاسخ به کاربر). نمونه‌هایی که ظاهراً هم‌پوشانی
دارند اما نیستند:

- `docs/ops/DEPLOYMENT.md`، `ROLLBACK.md`، `BACKUP_RESTORE.md`،
  `CLIENT1_LINUX_STAGING_GUIDE.md`، `MULTI_SERVER_DOCKER_DEPLOYMENT.md` —
  هرکدام صریحاً می‌گویند «رویه را تکرار نمی‌کنند» و فقط عمق موضوع خودشان را
  می‌پوشانند؛ `DOLPHIN_DEPLOYMENT_RUNBOOK.md` مرجع است.
- `docs/backend/DEPLOYMENT_PROFILE_OPTIONS.md` — «تاریخی، تصمیم‌گرفته‌شده»
  است، ولی خودِ `DOLPHIN_PROJECT_HANDOFF.md`ی زنده (بخش ۱۴) به مقایسهٔ
  ۱۴-معیاره‌اش ارجاع می‌دهد؛ حذفش آن ارجاع را بی‌معنی می‌کرد.
- `docs/backend/OPEN_BUSINESS_DECISIONS.md` — اکثر بندهای بخش ۱۴ (۱۳ تا ۲۰)
  هنوز واقعاً بازند؛ سند بازنویسی همان‌هاست، نه یک کپی.

### رفع شد

- پانویس `README.md` که می‌گفت «forooshbin/Kariz هیچ‌جا نیست» به‌روزرسانی
  شد تا استثنای عمدی `KARIZ_*` (متغیرهای محیطی استقرار، از نسخهٔ ۱.۶.۱۰) را
  هم بگوید — قبلاً خودش کمی نادرست شده بود.

### شواهد اجراشده

جست‌وجوی حساس‌به‌کوچک/بزرگ برای `kariz`/`forooshbin`/`frooshbin` روی کل
مخزن → ۴۰ فایل، همه با دلیل مستند (متغیرهای محیطی `KARIZ_*` عمداً
تغییرنام‌نیافته، مقادیر legacy در بوت‌استرپ Postgres، یا ورودی‌های تاریخی
CHANGELOG/handoff). `python manage.py check` → OK.
`scripts/check_html_branding.py` → `HTML_BRANDING_PASS files=243`.

---

## [1.6.10] — ۲۰۲۶-۰۹-۰۱

رفع مشکلات واقعی دیپلوی v1.6.9 روی سرور Tiara، از روی گزارش کامل خودِ نشست
دیپلوی (`tiara_dolphin_deployment_handoff.json`). این‌ها باگ نبودند که فقط در
کد دیده شوند؛ عملاً یک دیپلوی واقعی را کند و پر از گیر کردند.

### رفع شد

- **`scripts/new_deployment.py` حالا مستقیم در `secrets/.env` می‌نویسد**، نه
  در یک `.env` جدا در ریشهٔ پوشه — قبلاً این دو مسیر با هم فرق داشتند، و
  `deploy.sh` هیچ‌وقت به کپی‌ای که ابزار تولید می‌کرد نگاه نمی‌کرد؛ عملاً یک
  `cp .env secrets/.env` دستی لازم بود که هیچ‌جا مستند نشده بود.
- **`scripts/deploy.sh` حالا به‌صورت پیش‌فرض `secrets/.env` را می‌خواند**، با
  گزینهٔ `--env-file PATH` برای بازنویسی؛ مسیر تحلیل‌شده را همیشه چاپ می‌کند؛
  و اگر هم `.env` و هم `secrets/.env` باشند و یک هویت دیگر را نام ببرند (پروژه
  یا نام دیتابیس متفاوت)، به‌جای حدس زدن، رد می‌کند.
- **`deploy.sh` دیگر مرحلهٔ `db-finalize` را بعد از `migrate` جا نمی‌اندازد.**
  این باگ واقعی و جدی‌تر بود: `db-bootstrap` فقط به جدول‌هایی دسترسی می‌دهد که
  از قبل وجود دارند؛ `migrate` جدول تازه می‌سازد؛ و فقط `db-finalize` است که
  بعد از آن دوباره اجرا می‌شود و دسترسی نقش application را روی جدول تازه
  می‌گذارد. بدون آن، یک ریلیز که جدول تازه اضافه می‌کرد، همان جدول را برای
  اپلیکیشن بی‌دسترسی می‌گذاشت — بی‌سروصدا، تا وقتی یک درخواست واقعی به آن
  جدول می‌خورد.
- **`db-bootstrap` دیگر با `--no-backup` رد نمی‌شود.** قبلاً فقط داخل شاخهٔ
  گرفتن بکاپ اجرا می‌شد؛ نقش migration را هم همان‌جا آماده می‌کرد، پس
  `--no-backup` غیرمستقیم آن را هم رد می‌کرد و `migrate` با رمز عبور تازه‌ای
  که در `.env` چرخانده شده بود، لاگین نمی‌شد.
- **`sign_deployment_manifest.py` حالا `manifest.json` را با mode `0644`
  می‌نویسد.** قبلاً با هر umask ای که سیستم داشت نوشته می‌شد؛ اگر `0600` درمی‌آمد،
  کانتینر اپلیکیشن (که non-root اجرا می‌شود) با
  `PermissionError: [Errno 13] Permission denied` در همان اولین اجرا مواجه
  می‌شد — نه در لحظهٔ امضا یا کپی.
- الگوی sentinel بکاپ (`.dolphin-backup-root`) و الگوی نام آرشیو در
  `deploy.sh`، `backup-postgres.sh`، `verify-postgres-restore.{sh,ps1}` که در
  رفع نام‌ها (۱.۶.۹) به‌اشتباه با خودشان تکرار شده بودند (یک شرط تکراری
  بی‌اثر)، اصلاح شدند: حالا `dolphin` نسخهٔ فعلی است و `frooshbin`/`kariz`
  همچنان به‌عنوان مقدار قدیمیِ پذیرفته‌شده باقی می‌مانند — دقیقاً همان الگویی
  که کامنت‌های مدیریت نقش در `bootstrap-postgres.sh` از قبل استفاده می‌کردند.
- `.env.example`: `PYTHON_BASE_IMAGE` به‌وضوح build-only علامت‌گذاری شد —
  `compose.yml` هیچ‌وقت آن را نمی‌خواند، پس روی هاستی که فقط ایمیج را pull
  می‌کند بی‌معنی بود.

### اضافه شد

- `scripts/prepare-backup-volume.sh` — نسخهٔ idempotent همان one-liner آماده‌سازی
  بکاپ که در پیام خطای `deploy.sh` کپی می‌شد؛ روی حجمی که از قبل آماده است
  همین را می‌گوید و با موفقیت خارج می‌شود، نه اینکه همان خطای «حجم خالی
  نیست» را دوباره بدهد.
- `scripts/sign_deployment_manifest.py --generate-key` و `--print-public-key` —
  یک راه امن برای ساختن کلید امضای Ed25519 و گرفتن کلید عمومی‌اش با فرمت
  آماده برای `KARIZ_DEPLOYMENT_MANIFEST_KEYS`، به‌جای دستورهای دستی `openssl`
  که وقتی کلید هنوز وجود نداشت، یک مقدار خالی/ناقص را بی‌صدا در `.env`
  می‌نوشتند.
- `scripts/windows-employee-onboarding.ps1` — نسخهٔ بازبینی‌شده و عمومی
  (بدون نام مشتری خاص) از اسکریپت PowerShellِ نصب CA که قبلاً هر بار حین
  دیپلوی واقعی سرهم می‌شد؛ هش گواهی را چک می‌کند، هیچ‌وقت هشدار certificate
  را دور نمی‌زند.
- `docs/ops/DOLPHIN_DEPLOYMENT_RUNBOOK.md`: بخش تازهٔ «۶الف. نام داخلی با CA
  اختصاصی» — دقیقاً حالتی که دیپلوی Tiara بود (نه دامنهٔ عمومی، نه IP ثابت) و
  قبلاً هیچ بخشی برایش نبود؛ راهنمای `manage.py check` به‌جای import تنبل
  `django.conf.settings` برای اعتبارسنجی واقعی؛ و تصریح صریح دربارهٔ مجوز
  `chmod 644` روی manifest در بخش امنیت.

### شواهد اجراشده

`python manage.py test --settings=config.test_settings` → **۱۲۰۰+ تست، OK**
(یک تست از قبل موجود، `test_the_repository_carries_no_private_signing_key`،
رد شد — نه به‌خاطر این تغییرات، بلکه چون یک فایل کلید امضای واقعی این لحظه
در ریشهٔ چک‌اوت محلی است؛ در گزارش نهایی جدا پرچم شد، دست نخورد).

---

## [1.6.9] — ۲۰۲۶-۰۹-۰۱

جارو کامل نام‌های داخلی قدیمی. طبق تصمیم مستقیم مالک محصول: هیچ‌جا در کدبیس
نباید «کاریز» یا «فروش‌بین» بماند — نه فقط متن نمایشی (که در رفع‌برندسازیِ
پیشین انجام شده بود)، بلکه شناسه‌های فنیِ داخلی هم که تا امروز عمداً
دست‌نخورده مانده بودند (بخش Branding در `CLAUDE.md`).

### تغییر کرد — کاملاً بی‌خطر، بدون وابستگی به محیط واقعاً دیپلوی‌شده

- کلاس‌های ویو سمت سرور: `KarizXxxView` → `DolphinXxxView` (۸۰+ کلاس، هر ارجاع
  در `common/ui_urls.py` هم‌زمان به‌روز شد).
- فایل‌های استاتیک: `forooshbin.css` → `common/static/common/dolphin.css`،
  `forooshbin-app.js` → `dolphin-app.js` (با `git mv`، تاریخچه حفظ شد).
- ثابت نسخه: `FOROOSHBIN_VERSION` → `DOLPHIN_VERSION`؛ متغیر context قالب:
  `forooshbin_version` → `dolphin_version`.
- نام‌های logger: `forooshbin.request`، `forooshbin.server_fault` →
  `dolphin.*`.
- نام‌های پیش‌فرض دیتابیس/رول تازه‌نصب (`POSTGRES_DB`، `POSTGRES_*_USER` در
  `.env.example`) → `dolphin`/`dolphin_*`. یک نصب واقعیِ از قبل دیپلوی‌شده هرگز
  از این پیش‌فرض استفاده نمی‌کند — مقدار خودش را همیشه صریح در `.env` می‌گذارد.
- الگوهای CI-only دیتابیس/بازیابی/قرارداد ایزوله (`config/postgres_*_guard.py`،
  `KARIZ_PG_TEST_*`/`KARIZ_PG_RESTORE_*`/`KARIZ_PG_CONTRACT_*` →
  `DOLPHIN_PG_TEST_*` و مشابه، `KARIZ_BOOTSTRAP_*` → `DOLPHIN_BOOTSTRAP_*`) —
  این‌ها فقط لحظه‌ای و محلی‌اند، هیچ سروری آن‌ها را نگه نمی‌دارد.
- تمام مستندات (`docs/backend/`, `docs/ops/`, `BACKEND_SPEC.md`) و کل درخت
  دموی فروشنده (`assets/`, `apps/`, `dashboards/`, ...) — که هیچ‌کدام سرو
  نمی‌شوند، فقط مرجع بصری‌اند.
- نام‌گذاری مجدد فایل‌ها: `KARIZ_PROJECT_HANDOFF.md` →
  `DOLPHIN_PROJECT_HANDOFF.md`، `KARIZ_CLIENT1_CODEX_ROADMAP.md` →
  `DOLPHIN_CLIENT1_CODEX_ROADMAP.md`، `FOROOSHBIN_FEATURE_MAP_AND_ROADMAP.md` →
  `DOLPHIN_FEATURE_MAP_AND_ROADMAP.md`،
  `docs/ops/FOROOSHBIN_DEPLOYMENT_RUNBOOK.md` →
  `docs/ops/DOLPHIN_DEPLOYMENT_RUNBOOK.md`،
  `docs/FOROOSHBIN_CAPABILITIES_FOR_INVOICE_FA.txt` →
  `docs/DOLPHIN_CAPABILITIES_FOR_INVOICE_FA.txt`.

### عمداً حفظ شد — شناسه‌های فنی که یک محیط واقعاً دیپلوی‌شده به آن‌ها وابسته است

- **متغیرهای محیطیِ استقرار** (`KARIZ_PUBLIC_HOST`، `KARIZ_COMPOSE_PROJECT_NAME`،
  `KARIZ_*_IMAGE`، `KARIZ_HSTS_HEADER`، `KARIZ_TLS_*`، `KARIZ_DEPLOYMENT_MANIFEST_*`،
  `KARIZ_DATABASE_ROLE`، `KARIZ_BILLING_*`، `KARIZ_SELLER_*`، `KARIZ_PDF_*`،
  `KARIZ_NUMBER_FORMAT_*`، `KARIZ_INVENTORY_ALLOW_NEGATIVE_STOCK`) — نامشان
  **عوض نشد**. `compose.yml`، فیلتر envsubst در nginx، و
  `config/production_env.py` دقیقاً همین اسم‌ها را می‌خوانند، و یک `.env`
  واقعیِ روی استیجینگ (Nerkhbaan) یا پروداکشن (TIARA) همین اسم‌ها را دارد —
  خودِ `.env.example` هم از قبل این را «قرارداد سازگاری، نه برندسازی» خوانده
  بود. عوض‌کردن این‌ها یعنی هر استقرار واقعی، بعد از آپدیت به این نسخه، تا
  زمانی که `.env` آن دستی به همین اسم‌ها به‌روز نشود، بالا نمی‌آید.
- **کامنتِ «رول مدیریت‌شده» روی نقش‌های واقعیِ Postgres**
  (`scripts/bootstrap-postgres.sh`): چک اصلی حالا دنبال
  `'Dolphin managed … role v1'` می‌گردد، اما `'FrooshBin managed … role v1'` و
  `'Kariz managed … role v1'` به‌عنوان مقدار قدیمیِ پذیرفته‌شده باقی ماندند —
  دقیقاً همان الگویی که خودِ اسکریپت قبلاً برای گذار Kariz→FrooshBin داشت.
  بدون این، اسکریپت روی یک نقش از قبل دیپلوی‌شده «مدیریت‌نشده» تشخیص می‌داد و
  رد می‌کرد.
- **سنتینل و الگوی نام‌گذاریِ پشتیبان‌ها**
  (`scripts/backup-postgres.{sh,ps1}`, `scripts/verify-postgres-restore.{sh,ps1}`):
  فایل sentinel تازه (`.dolphin-backup-root`) اضافه شد، اما
  `.frooshbin-backup-root` و `.kariz-backup-root` هم پذیرفته می‌مانند؛ الگوی
  نام آرشیو هم هر سه پیشوند (`dolphin|frooshbin|kariz`) را می‌شناسد — یک پوشهٔ
  پشتیبانِ واقعی از قبل بدون اصلاح دستی کار می‌کند.

### شواهد اجراشده

`python manage.py test --settings=config.test_settings` → **۱۱۸۷ تست، OK،
۱۵ skip**. `scripts/check_html_branding.py` → `HTML_BRANDING_PASS files=243`.
بازبینی زندهٔ پنل در مرورگر (ورود، فهرست کاربران، بدون خطای کنسول/شبکه) با
`dolphin.css`/`dolphin-app.js` سرو شده روی نسخهٔ تازه.

### مستندسازی

- `README.md` تازه نوشته شد.
- `docs/backend/DEPLOYMENT_PROFILE_OPTIONS.md` وضعیتش اصلاح شد («تصمیم‌گیری
  شد»، نه «هنوز باز»)؛ خودش می‌گفت گزینهٔ C انتخاب شده اما بنر بالای فایل
  هنوز می‌گفت «هیچ گزینه‌ای تأیید نشده».
- بررسی مستندات برای پیدا کردن سند «اضافه»: هیچ سند واقعاً تکراری/متروک پیدا
  نشد — هر سند مرجع در `docs/ops/` صراحتاً به رانبوک اصلی ارجاع می‌دهد و
  می‌گوید خودش رویهٔ کلی را تکرار نمی‌کند؛ `AUDIT_FINDINGS.md` و
  `OPEN_FINANCIAL_QUESTIONS.md` هم صراحتاً به‌عنوان سابقهٔ بسته‌شده نگه داشته
  شده‌اند، نه سند فعال فراموش‌شده. چیزی حذف نشد.

---

## [1.6.7] — ۲۰۲۶-۰۹-۰۱

رفع همان یافتهٔ جانبیِ ثبت‌شده در ورودی `[1.6.6]`: صفحهٔ ارور مشترک
(۴۰۰/۴۰۳/۴۰۴/۵۰۰، از `common/error_views.py`) هیچ‌وقت باندل تم را لود
نمی‌کرد، پس کلاس‌های بوت‌استرپش (`card`, `card-body`, `btn btn-primary`)
اثری نداشتند — «کارت» بدون حاشیه/سایه بود و دکمهٔ «بازگشت به خانه» فقط یک
لینک زیرخط‌دار ساده نشان می‌داد.

### رفع شد

- `common/templates/common/error.html` حالا همان ترتیب `base.html` را
  دارد: `plugins.bundle.rtl.css`، بعد `style.bundle.rtl.css`، بعد
  `forooshbin.css` (با همان `?v=` که نسخهٔ تازه را به خواننده می‌رساند).
- تست تازه در `common/tests/test_auth_shell.py` این ترتیب را روی صفحهٔ
  ۴۰۴ پین می‌کند تا این خلأ دوباره بی‌صدا برنگردد.

### شواهد اجراشده

`python manage.py test --settings=config.test_settings` → **۱۱۸۶+ تست،
OK**. `scripts/check_html_branding.py` → `HTML_BRANDING_PASS files=243`.
بازبینی زندهٔ صفحهٔ ۴۰۴ در مرورگر با `DEBUG=False` (تنها حالتی که این
هندلرهای اختصاصی واقعاً اجرا می‌شوند)، هم در تم روشن هم تیره — کارت حالا
واقعاً حاشیه/سایه دارد و دکمه به‌شکل دکمهٔ آبی رندر می‌شود.

---

## [1.6.6] — ۲۰۲۶-۰۹-۰۱

جایگزینی همهٔ لوگوهای پنل با نشان تازهٔ Dolphin. آنچه تا امروز واقعاً سرو
می‌شد — `common/static/common/brand/Logo.png` و کل خانوادهٔ فاوآیکن — یک
آیکن ذره‌بین/نمودار بی‌ربط بود، نه نشان برند؛ این نسخه آن را با فایل‌های
واقعی پوشهٔ `assets/Dolphin Logos/` جایگزین می‌کند، هرکدام در جای درست خودش.

### تغییر کرد

- **نشان اصلی** (سایدبار، هدر موبایل، ریل جمع‌شده، پس‌زمینهٔ تیرهٔ صفحهٔ
  ورود) → نسخهٔ سفید/روشنِ باله، چون این چهار جا همه پس‌زمینهٔ تیره دارند.
- **صفحهٔ ورود** → به‌جای آیکن + دو خط عنوان جدا، حالا از نشان کامل
  «باله + Dolphin + دلفین» در یک تصویر استفاده می‌کند — بزرگ (تا ۳۴rem)،
  وسط‌چین، با سقف ارتفاعِ نسبت‌به‌صفحه تا در صفحه‌نمایش کوتاه هم جا بیفتد.
- **صفحهٔ ارور مشترک** (۴۰۰/۴۰۳/۴۰۴/۵۰۰) → چون این یکی تنها جایی است که
  نشان روی زمینهٔ روشن می‌نشیند، نسخهٔ سرمه‌ای (`Logo-light`) گرفت، نه
  نسخهٔ سفید که رویش دیده نمی‌شد.
- فاوآیکن، `apple-touch-icon`، و آیکن‌های PWA (`android-chrome-*`) همه از
  رَوی همان نشان تازه ساخته شدند؛ `site.webmanifest` از قبل نام درست
  («Dolphin») را داشت و دست نخورد.

### اضافه شد

- `common/static/common/brand/Logo-light.{png,webp}` — نسخهٔ زمینهٔ روشنِ
  نشان، فقط برای صفحهٔ ارور.
- `common/static/common/brand/Logo-With-Name.{png,webp}` — نشان کامل با
  نام، فقط برای صفحهٔ ورود.

### یافتهٔ جانبی، عمداً در این نسخه رفع نشد

بازبینی مرورگری صفحهٔ ارور را نمایش نداد — نه به‌خاطر لوگو، بلکه چون
`common/templates/common/error.html` از اول هیچ‌وقت باندل تم (`plugins.
bundle.rtl.css` / `style.bundle.rtl.css`) را لود نمی‌کرد، پس کلاس‌های
بوت‌استرپ آن (`card`, `btn`) اثری ندارند. این یک نقص از قبل موجود و
بی‌ربط به کار لوگو است؛ به‌عنوان کار جداگانه ثبت شد، نه این‌جا وصله شد.

### شواهد اجراشده

`python manage.py test --settings=config.test_settings` → **۱۱۸۶ تست، OK،
۱۵ skip**. `scripts/check_html_branding.py` → `HTML_BRANDING_PASS files=243`.
بازبینی کامل در مرورگر واقعی: صفحهٔ ورود (دسکتاپ/موبایل/ارتفاع کوتاه)،
سایدبار باز/جمع‌شده، هدر موبایل، صفحهٔ ۴۰۴ در حالت روشن و تیره، فاوآیکن و
`android-chrome-512x512.png` — بدون خطای کنسول یا شبکه روی مسیر واقعی.

---

## [1.6.5] — ۲۰۲۶-۰۸-۳۱

فاز ۳ (پایانی) از سامانهٔ مجوز در سطح کاربر: خودِ رابط کاربری. فازهای قبلی
لایهٔ داده و اجرای واقعی را ساختند؛ این نسخه همان‌ها را در دسترس یک مدیر
پلتفرم می‌گذارد.

### اضافه شد

- فرم «کاربر جدید» حالا فیلد «نقش» دارد (الزامی، از همان فهرستی که تغییرِ
  نقش هم استفاده می‌کند) — همان باگی که این کار را شروع کرد: تا امروز نقش
  اصلاً پرسیده نمی‌شد و هر کاربر تازه «بازاریاب» ساخته می‌شد.
- دکمهٔ «مجوزها» کنار «جزئیات» در فهرست کاربران. باز می‌کند: یک پنجرهٔ
  **نمایشی** (نام کاربر، نقش فعلی، ماتریس مجوز مؤثر با علامت ستاره برای
  ردیف‌های اختصاصی) — نه یک صفحهٔ ویرایش بی‌مقدمه.
- دکمهٔ «ویرایش مجوزها» چک‌باکس‌ها را فعال می‌کند. قاعدهٔ «ویرایش یعنی
  خواندن» همان لحظه در مرورگر هم اجرا می‌شود: تیک زدن «ویرایش» بلافاصله
  «خواندن» را هم می‌زند؛ برداشتن تیک «خواندن» ویرایش را هم خاموش می‌کند —
  دقیقاً همان قاعده‌ای که سرور مستقل از رابط کاربری اجرا می‌کند، پس هیچ‌وقت
  حالتی که سرور رد کند از این فرم بیرون نمی‌آید.
- «ذخیره تغییرات»، «بازنشانی به پیش‌فرض نقش» (با تأییدیه، چون مجوزهای
  دستی را دور می‌ریزد)، و «بستن».
- تغییر نقش دیگر بی‌صدا مجوز اختصاصی را پاک نمی‌کند: اگر کاربر مجوز
  اختصاصی داشته باشد، انتخاب نقش تازه یک پنجرهٔ تصمیم باز می‌کند — «حفظ
  مجوزهای فعلی» (پیشنهادی)، «بازنشانی به پیش‌فرض نقش تازه»، یا «انصراف از
  تغییر نقش». هیچ‌کدام به‌صورت پیش‌فرض یا بی‌اطلاع انتخاب نمی‌شود.

### رفع شد — یک ناسازگاری واقعی که همین بازبینی مرورگری پیدا کرد

منوی «مرکز تماس» از قبل با یک قابلیت (`customers.*`) کنترل می‌شد، برای **کل
گروه شامل مشتریان، سرنخ‌ها و تعامل‌ها**. تا امروز مشکلی نبود چون این سه همیشه
با هم به یک نقش داده می‌شدند. حالا که override می‌تواند فقط «مشتریان» را
جدا خاموش کند، همان قاعدهٔ قدیمی کل منو را — همراه سرنخ‌ها و تعامل‌هایی که
کاربر هنوز به آن‌ها دسترسی داشت — پنهان می‌کرد. با آزمایش واقعی در مرورگر
پیدا شد، نه با خواندن کد.

رفع شد با همان الگویی که «اسناد فروش» (سفارش/فاکتور) از قبل درست پیاده‌سازی
کرده بود: گروه، وقتی *هرکدام* از زیرمجموعه‌ها در دسترس باشد نمایش داده
می‌شود؛ هر زیرمجموعه هم مستقل با قابلیت خودش کنترل می‌شود. همین رفع برای
«کاتالوگ محصولات» (که با شکافتن `product_categories` از `products` در فاز ۱
همین مشکل را پیدا کرد) هم انجام شد. «انبار و موجودی»، «اسناد فروش داخلی»،
و «اسناد مالی» هرکدام از قبل فقط یک قابلیت داشتند — مشکلی نداشتند.

### شواهد اجراشده

- `python manage.py test --settings=config.test_settings` → **۱۱۸۶ تست، OK،
  ۱۵ skip** (بدون تغییر نسبت به فاز ۲؛ این فاز فقط قالب/جاوااسکریپت اضافه
  کرد).
- `scripts/check_html_branding.py` → `HTML_BRANDING_PASS files=243`.
- بازبینی کامل در مرورگر واقعی با دادهٔ QA تازه (یک مدیر پلتفرم، یک مدیر
  فروشگاه، دو بازاریاب): ساخت کاربر با نقش «مدیر فروشگاه» (تأیید شد از
  طریق API که نقش واقعاً همان شد، نه پیش‌فرض)، باز کردن مجوزهای یک بازاریاب
  (ماتریس دقیقاً با پیش‌فرض نقش مطابق)، ورود به حالت ویرایش، خاموش‌کردن
  Edit روی «مشتریان»، ذخیره، و تأیید مستقیم با درخواست API واقعی به‌عنوان
  همان کاربر: `GET /api/v1/customers/` → ۲۰۰، `POST` → ۴۰۳. خاموش‌کردن کامل
  Read روی «مشتریان» → هم API و هم آیتم نوار کناری «مشتریان» ناپدید شدند،
  «سرنخ‌ها»/«تعامل‌ها» (که override نشده بودند) باقی ماندند. بازنشانی به
  پیش‌فرض نقش، و هر دو مسیر تصمیمِ تغییر نقش (حفظ / بازنشانی) — هرکدام با
  بررسی مستقیم پاسخ API، نه فقط ظاهر صفحه. در ۳۷۵ پیکسل هم پنجرهٔ مجوزها
  بدون سرریز افقی و با اسکرول درست نمایش داده شد. صفحهٔ «دریافت‌ها» برای
  بازاریابِ بدون قابلیت پرداخت، صفحهٔ ۴۰۳ برندشدهٔ همیشگی را نشان داد، نه
  یک صفحهٔ نیمه‌بار.

---

## [1.6.4] — ۲۰۲۶-۰۸-۳۱

فاز ۲ از سامانهٔ مجوز در سطح کاربر: اعمال واقعی ماتریس Read/Write روی
درخواست‌های API — نه فقط ذخیره‌شدن در دیتابیس. تا این نسخه، override
ذخیره می‌شد ولی هیچ ویوستی آن را واقعاً چک نمی‌کرد.

### اضافه شد

- `common.permissions.HasCapabilityForMethod` — نسخهٔ آگاه‌به‌متد
  `has_any_capability`: متدهای خواندنی (`GET`/`HEAD`/`OPTIONS`) با
  `required_capabilities` سنجیده می‌شوند، متدهای نویسنده با
  `required_write_capabilities` (اگر ویوست آن را تعریف کرده باشد). ویوستی
  که این کلاس را تعریف نکند دقیقاً مثل قبل کار می‌کند — کلاس‌های
  `HasSalesCapability`/`HasBillingCapability`/`HasInventoryCapability`
  حالا همگی از همین یک کلاس ارث می‌برند.
- `aftersales.permissions.HasAfterSalesCapability` — این ماژول تا امروز
  هیچ گیت اعلامی نداشت (فقط `IsActiveAuthenticated` + خالی‌بودن queryset
  برای کاربر بی‌مجوز)؛ همان رفعی که `HasSalesCapability` قبلاً برای
  customers/leads و بقیه انجام داده بود، حالا برای after-sales هم انجام شد.
- ۱۴ ویوست (مشتریان، سرنخ‌ها، تعامل‌ها، فروش، دسته‌بندی، محصول، پیش‌فاکتور،
  سفارش، فاکتور، پرداخت، تخصیص پرداخت، چک، طرح اقساط، انبار، گردش انبار،
  اسناد فروش داخلی، خدمات پس از فروش) حالا `required_write_capabilities`
  دارند و واقعاً override را اجرا می‌کنند.

### رفع شد — قفل‌های نقش‌محورِ موازی که override را بی‌اثر می‌کردند

چند ویوست از قبل یک گیت نوشتنی جدا و **مبتنی بر نقش خام** داشتند
(`if self.request.user.role not in ELEVATED_OPERATORS`) — نه قابلیت. این
گیت‌ها override را نادیده می‌گرفتند: یک مدیر پلتفرم می‌توانست به یک
بازاریاب مجوز نوشتن روی «محصولات» بدهد، ولی همان بازاریاب باز هم ۴۰۳
می‌گرفت، چون قفل نقش هنوز آنجا بود. این‌ها به بررسی قابلیت تغییر کردند:
`sales/views.py` (دسته‌بندی و محصول)، `sales/services.py` (`create_product`,
`update_product`, دسته‌بندی، `register_sales_document`,
`transition_postal_status`, `deactivate_sales_document`)،
`inventory/services.py` (`_lock_inventory_manager`، یک تابع مشترک)،
`aftersales/services.py` (چهار قفل). رفتار پیش‌فرض هر نقش دقیقاً همان
مانده — این قفل‌ها همیشه دقیقاً همان نقش‌هایی را اجازه می‌دادند که حالا
قابلیت مشابه به آن‌ها داده شده.

**محدودیت شناخته‌شده، عمداً ثبت‌شده نه پنهان:** `quotations`/`orders`/
`invoices`/`payments` یک گیت عمیق‌تر و مشترک بین چند نوع سند دارند
(`billing._lock_document_writer`, `billing.payments._lock_payment_manager`)
که هنوز خام نقش‌محورند. **لغو** مجوز نوشتن روی این چهار ماژول کاملاً کار
می‌کند (گیت سطح ویو جلوی درخواست را می‌گیرد)؛ **اعطای** آن فراتر از
پیش‌فرض نقش هنوز به آن قفل عمیق‌تر نمی‌رسد. رفع کامل آن — عبور دادن
قابلیت مشخص هر نوع سند از میان ده‌ها نقطهٔ فراخوانی مشترک — کاری جداگانه
و پرخطرتر است، برای بعد ثبت شد. مستند در `accounts/module_permissions.py`.

### شواهد اجراشده

`python manage.py test --settings=config.test_settings` → **۱۱۸۶ تست، OK،
۱۵ skip** — شامل `accounts/tests/test_permission_overrides.py` تازه (۱۸
تست) که واقعاً از طریق `APIClient` درخواست می‌زند: لغو خواندن → ۴۰۳ به‌جای
صفحهٔ خالی، فقط-خواندن → نوشتن رد می‌شود، اعطای نوشتن فراتر از نقش پیش‌فرض
→ واقعاً کار می‌کند (برای ماژول‌های پوشش‌داده‌شده)، بازنشانی، تغییر نقش با
حفظ/پاک‌سازی مجوز، و امتناع کامل از اعطای `users`/`audit`.

---

## [1.6.3] — ۲۰۲۶-۰۸-۳۱

اولین مرحله از سامانهٔ مجوز انعطاف‌پذیر در سطح کاربر — لایهٔ داده و API، بدون
رابط کاربری هنوز (مرحلهٔ بعدی). نقش همچنان مجوزهای پیش‌فرض می‌دهد؛ حالا یک
مدیر پلتفرم می‌تواند مجوز **یک کاربر مشخص** را جدا از نقشش تنظیم کند، بدون
ساختن نقش تازه‌ای مثل «مدیر فروشگاه ۲».

### اضافه شد

- مدل تازهٔ `accounts.UserCapabilityOverride` — یک سطر برای هر قابلیتی که
  مجوز یک کاربر با پیش‌فرض نقشش فرق دارد؛ نبودِ سطر یعنی «همان پیش‌فرض نقش».
  مهاجرت افزایشی محض، بدون لمس جدول‌های موجود.
- `accounts/module_permissions.py` — فهرست ۱۶ ماژول واقعی پنل (مشتریان،
  سرنخ‌ها، فاکتورها، انبار، خدمات پس از فروش، کاتالوگ محصولات، گزارش‌ها و…)
  با نگاشت هرکدام به قابلیت‌های واقعی موجود در `ROLE_CAPABILITIES`، به‌اضافهٔ
  چند قابلیت «write» تازه (مثل `customers.manage`) برای ماژول‌هایی که تا امروز
  خواندن و نوشتن را جدا نمی‌کردند. `users` (مدیریت کاربران) و `audit` (رویدادها)
  از این ماتریس **عمداً بیرون‌اند** — همان مرز سخت امنیتی موجود در کد که
  می‌گوید این دو قابلیت را فقط `platform_admin` دارد، دست‌نخورده ماند.
- سه سرویس تازه در `accounts/services.py`: `user_permissions_for` (خواندن)،
  `set_user_permission_overrides` (فقط اختلاف با پیش‌فرض نقش را ذخیره می‌کند)،
  `reset_user_permissions` (حذف کامل مجوزهای اختصاصی، بازگشت به پیش‌فرض نقش).
  هر سه از همان `_locked_users` استفاده می‌کنند که مدیریت کاربر را از قبل به
  `platform_admin` محدود کرده بود.
- سه اکشن تازه روی `/api/v1/users/{id}/`: `GET permissions/`،
  `PUT permissions/`، `POST permissions/reset/`.
- «ادیت بدون خواندن» یک حالت نامعتبر است: `validate_matrix` هر بار که Edit
  روشن باشد Read را هم روشن می‌کند، هم در پاسخ به کلاینت و هم پیش از ذخیره.
- تغییر نقش دیگر مجوزهای اختصاصی را بی‌صدا پاک نمی‌کند: `change_user_role`
  پارامتر تازهٔ `keep_custom_permissions` گرفت (پیش‌فرض: نگه‌داشتن). فقط وقتی
  صریحاً `false` بیاید مجوزهای قبلی پاک و به پیش‌فرض نقش تازه بازنشانی می‌شوند.

### رفع شد — نقش، بخشی جدانشدنیِ ساخت کاربر

فرم «کاربر جدید» تا امروز اصلاً نقش نمی‌پرسید: `role` در `UserSerializer`
`read_only` بود، پس هر کاربر تازه همیشه «بازاریاب» ساخته می‌شد و نقش واقعی
فقط بعداً از صفحهٔ جزئیات و از مسیر جداگانهٔ «تغییر نقش» قابل تنظیم بود.

- `role` حالا یک فیلد الزامیِ قابل‌نوشتن در ساخت کاربر است (`create_crm_user`
  آن را به‌عنوان پارامتر جدا می‌گیرد، دقیقاً مثل `password`)، و همان قاعدهٔ
  `assignable_roles` که مسیر «تغییر نقش» را محدود می‌کند اینجا هم اجرا می‌شود
  — نقشی که یک مدیر پلتفرم در ساخت کاربر می‌بیند دقیقاً همان است که در تغییر
  نقش می‌بیند.
- در بروزرسانی کاربر (`PATCH`) نقش همچنان رد می‌شود — تغییر نقش فقط از مسیر
  اختصاصی `change-role` با قواعد و ثبت رویداد خودش انجام می‌شود؛ این رفتار
  عوض نشد.
- فرم «کاربر جدید» گزینهٔ نقش را اضافه کرد؛ فرم ویرایش کاربر همچنان نقش را
  نشان نمی‌دهد.

### یادداشت فنی — چرا «Read» گاهی دو قابلیت واقعی است

برخی ماژول‌ها (مشتریان، سرنخ‌ها، فاکتورها و…) یک قابلیت `.scoped` (مخصوص خودِ
کاربر) و یک `.company` (کل شرکت) دارند — این محور، **دامنهٔ داده** را هم حمل
می‌کند، کنترلی جدا که این ماتریس دست نمی‌زند. روشن‌کردن Read هرگز هر دو را
با هم نمی‌دهد؛ همیشه همان قابلیتی را تغییر می‌دهد که نقش آن کاربر از قبل با
خودش دارد، وگرنه یک بازاریاب می‌توانست از این صفحه به دید کل‌شرکتی برسد که
نقشش هرگز به او نمی‌داد.

### شواهد اجراشده

`python manage.py test --settings=config.test_settings` → **۱۱۶۸ تست، OK،
۱۵ skip**. مهاجرت جدید فقط یک جدول اضافه می‌کند؛ هیچ جدول یا رکورد موجودی
لمس نشد.

### باقیمانده برای مرحلهٔ بعد

این نسخه فقط لایهٔ داده/API است. رابط «مجوزها» در فهرست کاربران، حالت
نمایش/ویرایش، و اعمال واقعی Read/Write روی ویوست‌های sales/billing/inventory/
after_sales هنوز نیامده‌اند — نسخهٔ بعدی همین خط ۱.۶.x.

---

## [1.6.2] — ۲۰۲۶-۰۸-۳۱

### تغییر کرد — همهٔ ارورهای پنل، فارسی؛ بدون استثنا

تصمیم صریح مالک محصول: کاربر و مدیر پلتفرم هر دو از داخل پنل کار می‌کنند و باید
ارور را فارسی ببینند تا بفهمند مشکل از کجاست. **لاگ‌های سمت سرور همچنان
انگلیسی می‌مانند** — آن‌ها را فقط توسعه‌دهنده می‌خواند و تغییری در آن‌ها داده
نشد: نام تابع‌های لاگ، `operation=` و کلیدهای `changes=` در `log_activity`، و
تمام کامنت‌های کد دست‌نخورده باقی ماندند.

نکتهٔ «خارج از دامنه» ثبت‌شده در ۱.۶.۱ (بیش از ۱۶۵ پیام `BusinessRuleError` و
مشابه‌هایش به انگلیسی) دیگر خارج از دامنه نیست — همان کار است.

**دامنهٔ واقعی، پس از بررسی کامل کد، بزرگ‌تر از آن ۱۶۵ مورد بود.** هر پیامی که
از طریق `BusinessRuleError`، `BusinessConflictError`، `BusinessPermissionDenied`،
`IdempotencyConflict`، `ValidationError`، `PermissionDenied`، `NotFound` یا
`ParseError` با متن دستی انگلیسی به پاسخ API می‌رسید ترجمه شد — نزدیک به ۳۰۰
پیام، در ۳۶ فایل:

- `billing/services.py`, `billing/payments.py`, `billing/ledger.py`,
  `billing/money.py`, `billing/numbering.py`, `billing/serializers.py`,
  `billing/views.py`
- `sales/services.py`, `sales/serializers.py`, `sales/views.py`,
  `sales/imports.py`, `sales/customer_imports.py`
- `inventory/services.py`, `inventory/views.py`
- `aftersales/services.py`, `aftersales/views.py`
- `communications/services.py`, `communications/serializers.py`,
  `communications/views.py`
- `accounts/services.py`, `accounts/serializers.py`, `accounts/sessions.py`,
  `accounts/views.py`
- `reports/views.py`, `reports/serializers.py`, `reports/customer_views.py`,
  `reports/directory_views.py`, `reports/financial_views.py`,
  `reports/customer_insights.py`
- `common/phones.py`, `common/serializers.py`, `common/viewsets.py`,
  `common/parsers.py`, `common/exceptions.py`, `common/error_views.py`
- `common/static/common/forooshbin-app.js` (یک پیام خطای سمت کلاینت)

سه نکتهٔ فنی از دل این کار:

- **کلید فیلدهای ارور دست‌نخورده ماندند.** `{"amount": "..."}`، `{"status":
  "..."}` و مانند آن — این کلیدها همان نام فیلد HTML هستند که جاوااسکریپت
  پنل (`data-error-for`) برایشان پیام را زیر همان فیلد می‌نشاند؛ فقط متنِ
  پیام (سمت راست دیکشنری) فارسی شد، نه کلید.
- **اعتبارسنج رمز عبور جنگو تا حدی خودش فارسی است.** با `LANGUAGE_CODE="fa"`
  فعال، سه از چهار اعتبارسنج پیش‌فرض جنگو (رمز تکراری، رمز کاملاً عددی، رمز
  شبیه نام‌کاربری) پیام فارسیِ خودِ جنگو را برمی‌گردانند — این تغییر نکرد،
  چون سالم بود. تنها `MinimumLengthValidator` در فهرست ترجمهٔ خودِ جنگو برای
  `fa` این نسخه خالی بود؛ یک تابع کوچک (`persian_password_messages` در
  `accounts/services.py`) دقیقاً همان یک پیام را فارسی می‌کند و بقیه را
  دست‌نخورده رد می‌کند — نه بازنویسی اعتبارسنج، فقط پر کردن همان یک خلأ.
- **دو مسیر واقعاً پیام خام را به کاربر می‌رساندند، نه فقط دیکشنری ثابت:**
  `reports/customer_views.py` یک `except InvalidReportPeriod as exc: ...
  str(exc)` داشت که پیام را همان‌طور که در `reports/customer_insights.py`
  نوشته شده بود عبور می‌داد — آن سه پیام در مبدأ ترجمه شدند. `sales/imports.py`
  و `sales/customer_imports.py` هم برای هر ردیفِ ناموفقِ اکسل ورودی
  (`note_error`) و برای `_price()` پیام آزاد می‌ساختند، نه دیکشنری ثابت —
  همه ترجمه شدند.

چیزی که **دست نخورد**: پیام‌های پیش‌فرض DRF/جنگو که خودشان از قبل با `fa`
فارسی برمی‌گردند (۴۰۴، ۴۰۳، خطای پارس چندرسانه‌ای، و…) — این‌ها را قبلاً بررسی
کرده بودیم؛ فقط جایی که کد ما رشتهٔ انگلیسیِ دستی می‌فرستاد دست بردیم.

### شواهد

پنج آزمون رشتهٔ دقیق انگلیسی انتظار داشتند؛ هر پنج به‌روزرسانی شدند تا با متن
فارسی تازه مطابقت کنند (`accounts/tests/test_accounts.py`,
`common/tests/test_system_api.py` ×۴, `reports/tests/test_user_performance.py`
×۲). هیچ آزمون دیگری روی متن دقیق ارور فرض نکرده بود.

`python manage.py test --settings=config.test_settings` → **۱۱۶۷ تست، OK،
۱۵ skip**.

---

## [1.6.1] — ۲۰۲۶-۰۸-۳۱

بازبینی کامل و دستی پنل با دادهٔ واقعی، پیش از استقرار. **بدون تغییر کد. بدون
مهاجرت.**

### روش

یک دیتاست گسترده روی هر ماژول ساخته شد — ۵ مشتری (حقیقی و حقوقی)، ۳ محصول، یک
انبار با موجودی اولیه، سرنخ و تماس و فروش، سند فروش داخلی، خدمات پس از فروش،
پیامک ورودی، سفارش، پنج فاکتور با هر ترکیب وضعیت (پیش‌نویس، صادرشدهٔ رسمی
تسویه‌کامل، صادرشدهٔ غیررسمی تسویهٔ جزئی، صادرشده با قسط‌بندی، ابطال‌شده)، چهار
چک با هر وضعیت (در انتظار، وصول‌شده، خرج‌شده، برگشتی)، دو پرداختی. سپس هر ۲۷
صفحهٔ فهرست، هر نوع صفحهٔ جزئیات، و فرم‌های ساخت/ویرایش با کلیک واقعی در مرورگر
آزموده شدند — نه فقط از راه API.

### نتیجه

**هیچ باگ واقعی پیدا نشد.** موارد زیر مستقیماً با کلیک واقعی و بررسی مقدار
ذخیره‌شده تأیید شدند:

- محاسبهٔ فاکتور چندردیفی با نرخ مالیات و تخفیف درصدی (تأیید شد: جمع، تخفیف،
  مالیات و کل، رقم‌به‌رقم درست).
- صدور فاکتور، و پر شدن خودکار «تاریخ صدور» وقتی خالی مانده بود.
- اصلاح «یادداشت» روی فاکتور صادرشده، در حالی که بقیهٔ فیلدها قفل ماندند.
- فرم ثبت پرداخت روی هر سه حالت چک (مشتری/تازه) با فیلدهای درست‌شدهٔ هرکدام.
- خرج کردن یک چک دریافتی به‌عنوان پرداخت، با ثبت خودکار (`is_registered=true`)
  و ظاهر شدن روی میز پرداخت‌ها با وضعیت «خرج شده».
- هر چهار گذار وضعیت چک، شامل برگرداندن یک چک برگشتی به «در انتظار» — درست
  همان قابلیتی که در ۱.۴.۰ اضافه شد.
- تخصیص دریافت به فاکتور با انتخابگر جست‌وجوپذیر.
- دسترسی نقش‌محور: `qa.agent` سایدبار محدود دید و درخواست مستقیم به
  `/payments/` و `/users/` با ۴۰۳ واقعی (نه صرفاً پنهان در رابط) رد شد.
- چیدمان فیلترها و فاصلهٔ راهنمای نمودار، هم در دسکتاپ و هم در عرض ۳۷۵ پیکسل،
  بدون سرریز افقی.

### یک نکتهٔ قدیمی که در همین بازبینی دیده شد، خارج از دامنه

پیام‌های قاعدهٔ کسب‌وکار (`BusinessRuleError` و مشابه‌هایش) در سراسر کد —
بیش از ۱۶۵ مورد در `billing`، `sales`، `inventory`، `aftersales` — به انگلیسی
هستند، نه فارسی. این از قبل همین‌طور بوده (هیچ‌کدام در این جلسه یا نسخه‌های اخیر
اضافه نشده) و ترجمهٔ آن یک تصمیم و کار جداگانه است، نه یک باگ از این بازبینی.
ثبت شد تا فراموش نشود.

### شواهد

`python manage.py test --settings=config.test_settings` → **۱۱۶۷ تست، OK،
۱۵ skip**. `scripts/check_html_branding.py` → `HTML_BRANDING_PASS files=243`.
`scripts/validate_image_content.py --context` → `IMAGE_CONTENT_PASS`.
`manage.py check` سالم.

---

## [1.6.0] — ۲۰۲۶-۰۸-۳۱

**دربارهٔ شمارهٔ نسخه:** درخواست این مرحله «۱.۴.۲» بود؛ نسخهٔ واقعیِ در حال اجرا
از قبل ۱.۵.۰ بود (چهار commit جلوتر از هر «۱.۴.x»)، پس عقب بردنش به ۱.۴.۲
یعنی شکستن ترتیب نسخه‌ها. همان توالی واقعی ادامه یافت.

### تغییر کرد — ویرایش فاکتور صادرشده (یادداشت، و فقط یادداشت)

فاکتور صادرشده تا امروز مطلقاً تغییرناپذیر بود. حالا «یادداشت» را می‌شود بعد از
صدور هم اصلاح کرد؛ هر فیلد دیگر — تخفیف، نرخ مالیات، تاریخ سند، نوع فاکتور،
انبار — همچنان رد می‌شود، چون فاکتور صادرشده نسخه‌ای است که قبلاً به مشتری
داده شده و هیچ‌کدام از آنها نباید زیر پایش تغییر کند. `INVOICE_ISSUED_EDITABLE_FIELDS`
این مرز را در یک اسم می‌گذارد.

بازمحاسبهٔ مبالغ روی این مسیر عمداً اجرا نمی‌شود — یادداشت چیزی از جمع، تخفیف
یا مالیات را عوض نمی‌کند، پس دست نزدن به آنها امن‌تر از محاسبه‌ای بی‌اثر است.

### تغییر کرد — رویبرندینگ کامل به Dolphin / دلفین

نام تجاری فعال از ForooshBin / فروش‌بین به **Dolphin / دلفین** تغییر کرد: عنوان
صفحات، توضیح متا، مانیفست PWA، صفحهٔ ورود، پاورقی، سایدبار، پنل مدیریت جنگو،
عنوان OpenAPI، برگهٔ چاپ فاکتور، و نام فایل‌های خروجی اکسل (`dolphin-*.xlsx`
به‌جای `forooshbin-*.xlsx`).

**آنچه عمداً تغییر نکرد**، چون شناسهٔ فنی است نه متن نمایشی: نام دیتابیس/نقش‌های
PostgreSQL و الگوهای امنیتیِ جداکنندهٔ دیتابیس تست/بازیابی از تولید
(`config/postgres_*_guard.py`) — تغییرشان یعنی واگرا شدن از یک دیتابیس تولیدِ
از قبل مستقر یا از کار افتادن گیت جداسازی؛ کلاس‌های ویو سمت سرور
(`KarizXxxView`)؛ نام فایل‌های `forooshbin.css`/`forooshbin-app.js` و ثابت
`FOROOSHBIN_VERSION`؛ نام‌های logger؛ و مستندات مهندسیِ داخلی
(`KARIZ_PROJECT_HANDOFF.md` و مشابه‌هایش) که بخشی از محصول ارسالی نیستند.
دلیل هرکدام در `CLAUDE.md` بخش Branding ثبت شد تا دوباره کشف نشود.

**یک باگ واقعی پیدا و رفع شد در همین مسیر**: `scripts/check_html_branding.py`
یک دروازهٔ برندینگ قدیمی است (از رویبرندینگ قبلیِ Kariz→ForooshBin) که علاوه بر
درخت دموی فروشنده، سه قالب واقعیِ خودِ برنامه (`base.html`, `error.html`,
`print_base.html`) را هم — چون `{% extends %}` ندارند — به‌عنوان HTML دمو
اشتباه می‌گرفت. اولین اجرای `--fix` روی این نسخه، `{% block title %}` را از
هر سه‌شان **حذف کرد** و متن داخل `{% comment %}` را با کلمات فارسی/انگلیسی
مخلوط خراب کرد — یعنی عنوان هر صفحهٔ سایت یک رشتهٔ ثابت واحد می‌شد. کشف و برگردانده
شد، و اسکریپت با فهرست `FIRST_PARTY_TEMPLATE_PATHS` برای همیشه از این سه فایل
دور نگه داشته شد. درخت دموی فروشنده (۲۰۱ فایل) با همین ابزار به برند تازه
عادی‌سازی شد — دقیقاً همان کاری که ابزار برایش ساخته شده.

### اضافه شد — آیکون کنار عنوان صفحه

`{% block page_icon %}` در `base.html`، خالی به‌طور پیش‌فرض — صفحه‌ای که پر
نکندش دقیقاً مثل قبل می‌ماند. ۴۰ صفحهٔ فهرست/جزئیات یک آیکون keenicon متناسب با
معنای صفحه گرفتند (مشتریان → پروفایل، فاکتورها → قبض، چک‌ها → تیک‌مربع، …)، همه
از همان خانوادهٔ `ki-duotone` که قبلاً در سایدبار استفاده می‌شد. اندازه و رنگ در
یک کلاس (`page-heading-icon`) نگه داشته شد تا یکدست بماند.

### رفع شد — فاصلهٔ نشانگر رنگی از متن در راهنمای نمودار

اندازه‌گیری مستقیم روی یک نمودار واقعی: لبهٔ نشانگر رنگی دقیقاً روی لبهٔ متن
برچسب می‌نشست — فاصله صفر پیکسل، نه فقط کم. Apex فاصلهٔ بین آیتم‌های راهنما را
با `margin` درون‌خطی می‌دهد ولی هیچ فاصله‌ای بین نشانگر و برچسبِ همان آیتم
نمی‌گذارد. `itemMargin` در گزینه‌های نمودار و یک قاعدهٔ `margin-inline-end` روی
`.apexcharts-legend-marker` (با `!important`، چون Apex همین المان را با استایل
درون‌خطی می‌نویسد) هر دو فاصله را جدا از هم درست کردند.

### بررسی شد، بدون تغییر — چیدمان فیلترها

فیلترهای هر صفحهٔ فهرست از قبل روی `.list-filters` (شبکهٔ `auto-fit`، افقی در
دسکتاپ، تک‌ستونه زیر ۵۷۶ پیکسل) بودند؛ آخرین دو صفحهٔ باقی‌مانده (مشتریان،
محصولات) در یک commit قبلیِ همین جلسه به همین الگو منتقل شده بودند. چیزی برای
تغییر پیدا نشد.

### شواهد

`python manage.py test --settings=config.test_settings` → **۱۱۶۷ تست، OK،
۱۵ skip**. `scripts/check_html_branding.py` → `HTML_BRANDING_PASS files=243`.
در مرورگر با دادهٔ واقعی: سایدبار و صفحهٔ ورود «Dolphin / دلفین» نشان دادند،
فاصلهٔ نشانگر تا متن در راهنمای نمودار فاکتورها از ۰ به ~۷ پیکسل رسید، آیکون
کنار عنوان روی چند صفحه نمونه دیده شد.

---

## [1.5.0] — ۲۰۲۶-۰۸-۳۱

«تاریخ صدور» فیلد خودش را گرفت. **مهاجرت افزایشی: `billing.0013`.**

### اضافه شد — `Invoice.document_date`

تاریخی که اپراتور روی سند می‌نویسد. در ۱.۴.۰ خواسته شده بود و ساخته نشد، چون
نوشتنش در `issued_at` قید `invoice_draft_has_no_issue_time` را می‌شکست: فاکتور
پیش‌نویس نمی‌تواند `issued_at` داشته باشد، چون صدور همان چیزی است که آن را
می‌گذارد. مالک محصول «فیلد جدا» را انتخاب کرد، پس قید دست‌نخورده ماند.

دو فیلد، دو معنای متفاوت:

| فیلد | یعنی | چه کسی می‌نویسدش |
|---|---|---|
| `document_date` | تاریخی که روی کاغذ نوشته شده | اپراتور، تا وقتی فاکتور پیش‌نویس است |
| `issued_at` | لحظه‌ای که سامانه فاکتور را صادر کرد | فقط `issue_invoice` |

- **`DateField` است نه `DateTimeField`**: کسی روی فاکتور ساعت نمی‌نویسد.
- **هنگام صدور، اگر خالی باشد پر می‌شود** با روزِ صدور، تا ستونی که آن را
  می‌خواند برای سند صادرشده هرگز خالی نماند.
- **تا وقتی پیش‌نویس است قابل اصلاح** است؛ خالی کردنش هم پذیرفته می‌شود، چون
  برداشتن یک تاریخ اشتباه به اندازهٔ تصحیحش لازم است.

### تغییر کرد — کجاها دیده می‌شود

- **فاکتور تازه**: input «تاریخ صدور». خالی گذاشتنش یعنی روز صدور.
- **جزئیات فاکتور**: همان فیلد، قابل ویرایش روی پیش‌نویس. کنارش «زمان ثبت صدور»
  خواندنی است — تا دو معنا با هم اشتباه نشوند.
- **فهرست فاکتورها**: ستون «تاریخ صدور» حالا `document_date` را می‌خواند و برای
  ردیف‌های پیش از این فیلد، به `issued_at` برمی‌گردد تا ستون خالی نشود.

### رفع شد — یک روز جابه‌جا

`localDateValue` مقدار را از مسیر «لحظه» می‌خواند و ساعتش را دور می‌ریزد. یک
`DateField` هیچ‌وقت ساعت نداشته و به شکل `YYYY-MM-DD` می‌آید؛ فرستادنش از آن
مسیر آن را از یک منطقهٔ زمانی رد می‌کرد و می‌توانست روی روزِ قبل بنشیند.
`localDayValue` همان کار را از مسیر روز انجام می‌دهد.

---

## [1.4.0] — ۲۰۲۶-۰۸-۳۱

میزهای مالی، آن‌طور که واقعاً خوانده می‌شوند. **بدون مهاجرت.**

MINOR است نه PATCH: فیلتر تازه روی API، گذارهای تازه در چرخهٔ چک، و رفتار تازه
در «پرداخت شده» فاکتور — همه افزایشی، هیچ قراردادی نشکسته.

### فهرست‌ها — دریافت‌ها، پرداخت‌ها، چک‌ها، فاکتورها

- مقادیر هر ستون **وسط‌چین** شد. یک‌بار روی خودِ جدول، نه روی تک‌تک سلول‌ها، تا
  ستونی که بعداً اضافه شود هم بدون یادآوری وسط‌چین باشد.
- ستون «زمان» شد **«تاریخ ثبت»** و ساعت از مقدارش برداشته شد.
- **وضعیت چک، وضعیت خودِ چک است.** خرج کردن یک چک، سند دریافتی را که با آن آمده
  می‌بندد، پس چکِ زندهٔ خرج‌شده «ابطال‌شده» خوانده می‌شد. حالا روی روش چک همان
  مقداری دیده می‌شود که صفحهٔ چک‌ها نشان می‌دهد: «خرج شده».
- ستون «سررسید» در فاکتورها ستونی از خط تیره بود؛ جایش را **«نوع فاکتور»** گرفت
  و کنارش فیلتر رسمی/غیررسمی نشست.

### جزئیات دریافت و پرداخت

- **پدینگ**: کارت‌ها `card-body` نداشتند، برای همین input‌ها به لبهٔ کارت چسبیده
  بودند.
- **راست‌چین**: فیلدهای پولی `dir="ltr"` دارند تا ارقامشان جابه‌جا نشود، و همین
  آن‌ها را تنها فیلدهای چپ‌چینِ یک ستون راست‌چین کرده بود. حالا `text-align`
  جداگانه ست می‌شود.
- **یک فرم تخصیص به‌جای دو تا.** «تخصیص به فاکتور» و «تقسیم بین چند فاکتور» یک
  عملیات با تعداد سطر متفاوت بودند زیر دو عنوان. حالا یک فرم است، انتخاب فاکتور
  **قابل جستجو با شمارهٔ فاکتور** است، و با یک سطر باز می‌شود.
- «ابطال دریافت» حذف شد. در میز پرداخت‌ها کل بخش تخصیص حذف شد، چون فاکتوری برای
  تسویه وجود ندارد.
- روی روش چک، کنترل «وضعیت» سند پنهان است؛ وضعیت چک تنها وضعیت روی صفحه است.
- **پرداخت دیگر خودش را دریافت صدا نمی‌زند.** هر دو میز یک مسیر جزئیات دارند و
  سایدبار با پیشوند URL تطبیق می‌داد. حالا view جهت سند را می‌خواند و صفحه
  عنوان، مسیر بازگشت و `data-nav-match` را از آن می‌گیرد.

### فاکتور

- **«پرداخت شده» دیگر تایپ نمی‌شود.** عددی که اپراتور وارد می‌کرد فاکتور را
  تسویه‌شده نشان می‌داد بدون آنکه سندی، تخصیصی یا ردی در دفتر ساخته شود. حالا
  جمع تخصیص‌های میز دریافت‌هاست و «مانده» از آن پیروی می‌کند.
- **نرخ مالیات هرگز به فاکتور نمی‌رسید.** فرم آن را می‌گرفت، سرویس قبولش داشت، و
  درخواستِ میان این دو اصلاً حملش نمی‌کرد.
- چند کالا در یک فاکتور، مشتریِ قابل جستجو، و **تخفیف درصدی** که روی
  `discount_percent` خودِ سطرها می‌نشیند — بدون ساختن مفهوم تازه در سرور.
- «سفارش» و «سررسید» از کارت جزئیات حذف شدند.

### چک — عملیات قابل برگشت شد

«وصول» و «خرج کردن» پایانی بودند، پس یک دکمهٔ اشتباه برای همیشه می‌ماند. حالا هر
وضعیت می‌تواند به **«در انتظار»** برگردد، و فقط به آنجا.

**پول هم برمی‌گردد**، که مهم‌ترین بخش است: خروج از «وصول» اعتبارِ ثبت‌شده را پس
می‌گیرد و پرداخت به PENDING برمی‌گردد تا چک بتواند دوباره وصول شود — بدون این،
اصلاح اعمال می‌شد و هیچ پولی جابه‌جا نمی‌شد. دفتر append-only است، پس هر سه حرکت
خوانا می‌مانند.

### ثبت پرداخت — ترتیب سؤال‌ها

روی روش چک، **نوع چک** اول می‌آید چون شکل بقیه را تعیین می‌کند، و **شماره چک**
کنارش. زیر آن عنوان **«اطلاعات گیرنده»**:

- **چک مشتری**: گیرنده + تاریخ پرداخت. «مبلغ» حذف شد — مبلغ همان چک است و پرسیدن
  دوبارهٔ آن دعوت به عددی است که با سند خرج‌شده نمی‌خواند.
- **چک تازه**: گیرنده + مبلغ. «تاریخ پرداخت» حذف شد — هنوز چیزی پرداخت نشده؛ چک
  سررسید خودش را دارد.

میز دریافت‌ها دست‌نخورده ماند: چکِ دریافتی همچنان طرف حساب، مبلغ و تاریخ ورود
خودش را دارد.

**خرج کردن چک آن را ثبت‌شده هم می‌کند.** چکی که از دست ما خارج شده، به‌تعریف ثبت
شده است؛ پیش از این «ثبت نشده» می‌ماند و صفحهٔ چک‌ها سندی را که رفته بود، نافایل
نشان می‌داد.

### انجام نشد — «تاریخ صدور» به‌عنوان input

`Invoice` قید `invoice_draft_has_no_issue_time` را دارد: `issued_at` یک پیش‌نویس
باید NULL باشد، چون صدور همان چیزی است که آن را می‌گذارد. پذیرفتن این فیلد باعث
می‌شد هر ذخیره قید را بشکند و به شکل «شمارهٔ سند تکراری» گزارش شود.

نگه‌داشتن تاریخِ اعلامیِ اپراتور یا آن قید را می‌خواهد یا فیلدی جدا که تا لحظهٔ
صدور نگهش دارد. هر دو تصمیم کسب‌وکار است نه سیم‌کشی، پس ساخته نشد و یک تست دلیلش
را ثبت می‌کند.

---

## [1.3.13] — ۲۰۲۶-۰۸-۲۹

سایدبار جمع‌شده، همان‌طور که خودِ قالب دارد. **بدون مهاجرت.**

### اضافه شد — باز شدن موقت با هاور

با `data-kt-app-sidebar-hoverable="true"` روی `<body>`. این قابلیتِ خودِ قالب
است و تماماً در CSS آن پیاده شده؛ هیچ جاوااسکریپتی پشتش نیست. نشانگر که روی
نوار برود باز می‌شود و که برود بسته می‌شود، هر دو با انیمیشن.

### حذف شد — لوگویی که دکمه می‌شد

۱.۳.۱۱ نشان را دکمه کرده بود که با هاور به چِوران تبدیل می‌شد و سایدبار را باز
می‌کرد. پس گرفته شد: لوگویی که زیر نشانگر به کنترلِ دیگری تبدیل می‌شود به کاربر
نمی‌گوید کدام‌یک را قرار است بگیرد. حالا دوباره یک لینک ساده به خانه است، و باز
کردن بر عهدهٔ همان دکمه‌ای است که بسته بود — که در حالت جمع‌شده دیده می‌شود.

### رفع شد — علتِ واقعی، بعد از دو نسخه

سایدبار باریک نمی‌شد و ۱.۳.۱۱ و ۱.۳.۱۳ هر دو قاعده نوشتند تا کاری را به زور
انجام دهند که قالب از قبل بلد بود: عرض ثابت، `min-width: 0`، `overflow-x`،
وسط‌چین کردن آیکون‌ها، برچسب شناور برای هر عنوان، و رنگ و مکانِ دستیِ دکمه.

علت یک خط مارک‌آپ بود: لینکِ برند کلاس `d-flex` بوت‌استرپ را داشت که
`!important` است و بر `display: none` قالب غلبه می‌کرد؛ بلوکِ عریض در جریان
می‌ماند و — چون آیتم فلکس `min-width: auto` دارد — سایدبار را به اندازهٔ محتوای
خودش باز نگه می‌داشت. `d-flex` به یک `span` داخلی منتقل شد و همهٔ آن قاعده‌ها
غیرلازم شدند.

### باقی ماند — یک ایراد در خودِ قالب

قالب عرض را از یک اعلان می‌گیرد و با تغییر متغیر جمع می‌کند. **جمع شدن کار
می‌کند، باز شدن نه**: متغیر به ۲۶۵ برمی‌گردد و عرضِ استفاده‌شده روی ۷۵ می‌ماند،
یعنی سایدبارِ جمع‌شده هرگز باز نمی‌شود.

در دموی خودِ فروشنده — که به‌صورت محلی سرو شد و هیچ کدی از این پروژه در آن نبود —
همین اندازه‌گیری شد: بعد از toggle خودش، متغیر ۲۶۵px و عرض محاسبه‌شده ۷۵px.
پس برای هر حالت یک اعلان `width` جدا نوشته می‌شود تا چیزی که عوض می‌شود *قاعدهٔ
منطبق* باشد نه مقدار داخل یکی. در هر دو جهت انیمیشن دارد.

> **تصحیح.** در جریان همین کار یک بار نتیجه گرفتم این ادعا غلط است و رفعش را
> برداشتم؛ اشتباه بود و فقط جهتِ جمع شدن آزموده شده بود.

### رفع شد — جمع کردن که خودش را باطل می‌کرد

دکمهٔ toggle روی لبهٔ سایدبار است ولی فرزندِ آن است، پس نشانگری که «جمع کن» را
زد هنوز داخل سایدبار است و هاور بلافاصله بازش می‌کند. سایدبار هم چون باز می‌ماند،
دکمه هرگز از زیر نشانگر کنار نمی‌رود. در مرورگر واقعی دیده شد: کلیک ویژگی را
عوض کرد و عرض تا وقتی نشانگر تکان نخورد روی ۲۶۵ ماند. قالب برای همین ۳۰۰
میلی‌ثانیه `.animating` می‌گذارد، که برای انیمیشن کافی است و برای نشانگرِ ساکن نه.

حالا تا وقتی نشانگر واقعاً بیرون برود، `data-kt-app-sidebar-hoverable` از `body`
برداشته می‌شود. با ویژگی، نه با یک کلاسِ خودمان: همهٔ قاعده‌های peek در قالب به
همین ویژگی گره خورده‌اند، پس برداشتنش هم عرض و هم محتوای بازشده را با هم خاموش
می‌کند — کلاسی که با `:hover` بجنگد فقط عرض را می‌گرفت و برندِ عریض داخل جعبهٔ
۷۵ پیکسلی رندر می‌شد.

### تغییر کرد — دکمه، با کلاس‌های خودِ قالب

`position-absolute top-50 start-100 translate-middle rotate`. این یک
`inset-inline-start` دستی را بازنشسته کرد که دو بار باگ RTL ساخته بود، و چرخش
چِوران را از `[dir=rtl] .app-sidebar-toggle.active i` می‌گیرد نه از قاعدهٔ ما.

### شواهد

کنار هم با دموی فروشنده اندازه‌گیری شد: نوار ۷۵، wrapper ۲۶۵ که نوار برشش
می‌زند، آیکون‌ها داخل نوار، عنوان‌ها `opacity: 0`، برند عریض پنهان. peek تست
مرورگر واقعی دارد، چون رویداد ساختگی `:hover` تولید نمی‌کند و فقط `ActionChains`
نشانگر واقعی را حرکت می‌دهد.

---

## [1.3.12] — ۲۰۲۶-۰۸-۲۹

نمودارها با کتابخانهٔ خودِ قالب خریداری‌شده کشیده می‌شوند. **بدون مهاجرت.**

### تغییر کرد — ApexCharts جای SVG دست‌نویس را گرفت

تا ۱.۳.۱۱ نمودارها با SVG دست‌نویس کشیده می‌شدند تا باندل ۳٫۶ مگابایتی قالب وارد
نشود. مالک محصول پس از دیدن همین هزینه، صریح گفت نمودارهای خودِ قالب را می‌خواهد
و اندازهٔ پنل مهم نیست. این نسخه همان است.

- **ApexCharts فقط داخل `plugins/global/plugins.bundle.js` هست** — قالب نسخهٔ
  مستقلی از آن ندارد. پس کل باندل بار می‌شود: وزن دارایی‌های پنل از حدود ۵۷۶
  کیلوبایت به حدود ۴٫۲ مگابایت می‌رود و jQuery و select2 و dropzone هم می‌آیند
  که هیچ‌کدام در این پنل استفاده نمی‌شوند.
- هر سه شکل (`renderDonutChart`, `renderAreaChart`, `renderBarChart`) امضای
  قبلی‌شان را نگه داشتند، پس هیچ فراخوانی‌ای عوض نشد.
- نوار افقی شد، چون برچسب‌ها نام‌های فارسی با طول دلخواه‌اند.

### رفع شد — باندل از ایمیج بیرون می‌ماند

`plugins.bundle.js` در **چهار جا** به‌عنوان «بارگذاری‌نشده» علامت خورده بود:
`STATICFILES_COLLECT_IGNORE`، `.dockerignore`، `scripts/validate_image_content.py`
و `common/tests/test_static_assets.py`. اگر فقط تگ اسکریپت اضافه می‌شد، در
توسعه کار می‌کرد و **در ایمیج تولید، هر نمودار ۴۰۴ می‌گرفت** — همان شکستی که
`EXPECT_PRESENT` برای آن نوشته شده بود. هر چهار جا اصلاح شد و
`validate_image_content --context` سبز است.

### رفع شد — نمودار با تغییر پوستهٔ پنل عوض نمی‌شد

Apex رنگ‌ها را هنگام کشیدن داخل SVG می‌نشاند، از جمله رنگ متن. نموداری که در
حالت روشن کشیده شده بود، بعد از رفتن به تاریک همان جوهر روشن را نگه می‌داشت و
عدد وسط دونات با `--bs-gray-800` روی کارت تیره **تقریباً نامرئی** می‌شد.

حالا یک `MutationObserver` روی `data-bs-theme` هر نمودار زنده را دوباره می‌کشد.
اندازه‌گیری شد: روشن `#1B84FF/#17C653/#7239EA/#F6C000` ↔ تاریک
`#006AE6/#00A261/#883FFF/#C59A00`، و جوهر مرکز `#252f4a` ↔ `#b5b7c8`.

### رفع شد — قلم فارسی در نمودارها

`plugins.bundle.rtl.css` خودِ قالب روی `.apexcharts-text` قلم `Inter` را با
`!important` می‌گذارد و همان بر استایل inline که Apex از `chart.fontFamily`
می‌سازد غلبه می‌کند. **Inter هیچ گلیف فارسی ندارد**، پس هر برچسب محور به قلم
جایگزینِ سیستم می‌افتاد و با بقیهٔ کارت فرق می‌کرد. یک قاعده این را برمی‌گرداند.

ممنوعیت `font-family:` در شیوه‌نامهٔ پوششی **حذف نشد، باریک شد**: هر
`font-family` باید روی گره‌های `.apexcharts-*` باشد. شیوه‌نامه هنوز حق ندارد
قلم پنل را انتخاب کند؛ فقط حق دارد یک انتخابِ غلطِ فروشنده را پس بزند.

### تغییر کرد — انیمیشن ورود خاموش شد

Apex نمودار را با `requestAnimationFrame` از حالت خالی به هندسهٔ واقعی می‌برد.
نموداری که جایی سوار شود که فریم تولید نمی‌شود — تب پس‌زمینه، کارتِ هنوز پنهان،
مرورگر headless — **برای همیشه روی فریم خالی می‌ماند**. دقیقاً همین اندازه‌گیری
شد: مسیر هر میله `M0.101 … L0.101`، عرض صفر.

با خاموش شدن آن، همان میله ۷۱۶ پیکسل شد و چهار سطل صفر واقعاً صفر ماندند. ضمناً
چون تغییر پوسته حالا هر نمودار را از نو می‌کشد، نگه‌داشتنش یعنی تکرار نیم‌ثانیه
انیمیشن روی همهٔ نمودارها در هر بار جابه‌جایی روشن/تاریک.

### تغییر کرد — رنگ‌ها معنا دارند

- **سنی مطالبات**: پنج رنگ که بالا می‌روند — سبز، آبی، زرد، نارنجی، قرمز. پیش‌تر
  دو سطل وسط هر دو زرد بودند، یعنی «۳۱ تا ۶۰» و «۶۱ تا ۹۰» به یک اندازه بد
  به نظر می‌رسیدند. نارنجی از `--bs-orange` می‌آید چون قالب رنگی در آن فاصله
  ندارد.
- **ترکیب سود**: درآمد آبی، بهای تمام‌شده زرد، سود ناخالص سبز.
- Apex میله‌ها را با شفافیت ۰٫۸۵ پر می‌کند؛ روی کارت روشن هر رنگ رنگ‌پریده
  می‌شد. حالا کدر است، وگرنه رمپ معنایی خوانده نمی‌شود.

### جهت محورها — و اینکه چرا برنگشت

نمودار دست‌نویسِ قبلی محور زمان را برعکس می‌کرد تا قدیمی‌ترین نقطه سمت راست
بنشیند. معادل Apex یعنی `xaxis.reversed: true` آزموده شد:

- روی نمودار ناحیه‌ای **بی‌اثر** است؛ اندازه‌گیری شد که قدیمی‌ترین دسته با وجود
  آن همچنان در `x=66` سمت چپ ماند.
- روی میلهٔ افقی **بدتر از بی‌اثر** است و هر میله را به عرض صفر جمع می‌کند.

خودِ قالب هم نه `reversed` می‌گذارد نه `opposite`، و نمودارهایش در بیلد RTL
چپ‌به‌راست خوانده می‌شوند. پس همهٔ اینها هم همان‌طورند. **این یک تغییر رفتار
واقعی نسبت به نسخهٔ دست‌نویس است، نه فراموشی.**

### تست‌هایی که عمداً به‌روز شدند

- `test_billing_shell_browser` سنجه‌هایش را از `div`های قدیمی به SVG خودِ Apex
  برد. برچسب‌ها از `tspan` خوانده می‌شوند نه از گروه، چون Apex کنار هر برچسب یک
  `<title>` می‌گذارد و متنِ گروه هر نام را **دوبار** دارد.
- `test_auth_shell_browser` کمکی `chart_names()` گرفت که منتظر کشیده شدن Apex
  می‌ماند؛ خواندن مستقیم `.text` با رسم ناهمگام مسابقه می‌داد.
- استثنای «تنها URL مجاز در اسکریپت» **بازنشسته شد**: آن استثنا برای فضای‌نام
  XML بود که `createElementNS` لازم داشت، و با رفتن رسمِ دست‌نویس دیگر چیزی برای
  پوشاندن ندارد. حالا اسکریپت **هیچ** رشتهٔ URL‌شکل ندارد.
- چهار تست تازه: باندل بار می‌شود، **پیش از** `scripts.bundle.js` بار می‌شود،
  رسم با `new ApexCharts(` است نه `createElementNS`، و هر نمودار پیش از
  استفادهٔ دوبارهٔ ظرفش `destroy()` می‌شود.

### رفع شد — نشتی نمودار روی هر فیلتر

Apex بیرون از فرزندانِ ظرف هم DOM و شنونده نگه می‌دارد، پس `replaceChildren()`
تنها پاکش نمی‌کند. حالا هر رسمِ دوباره نمونهٔ قبلی را `destroy()` می‌کند —
وگرنه هر بار اعمالِ فیلتر یک نمودار زندهٔ رهاشده جا می‌گذاشت.

---

## [1.3.11] — ۲۰۲۶-۰۸-۲۹

سایدبار جمع‌شده که واقعاً خوانا باشد، و پروفایل که از داشبورد به منوی کاربر
منتقل شد. **بدون مهاجرت.**

### رفع شد — سایدبار جمع‌شده خالی به نظر می‌رسید

آیکون‌ها **گم نشده بودند؛ بیرون از نوار کشیده می‌شدند.**

قالب عمداً `wrapper` را در حالت جمع‌شده روی عرض کامل نگه می‌دارد و می‌گذارد
سایدبارِ ۷۵ پیکسلی برشش بزند — این‌طوری حالت hover-to-peek خودش کار می‌کند، که
این پنل از آن استفاده نمی‌کند. نتیجه: لینک‌ها ۲۳۴ پیکسل می‌ماندند و
`justify-content: center` هر آیکون را وسط همان ۲۳۴ پیکسل می‌گذاشت.

اندازه‌گیری شد: آیکون روی **x=1447** بود در حالی که نوار قابل‌دیدن **۱۵۱۰ تا
۱۵۸۵** بود. با باریک کردن منو به عرض نوار، هر ۱۱ آیکون داخل آمدند.

### تغییر کرد — نشان، خودش دکمهٔ باز کردن است

در حالت جمع‌شده تنها چیزِ بالای نوار همان نشان است، پس کلیکِ بدیهی باید کار
مفید را بکند. حالا:

- **`<button>` است، نه لینک.** «رفتن به داشبورد» از قبل اولین گزینهٔ خودِ منوی
  زیرش است.
- **با هاور، یک چِوران رویش می‌آید** تا پیش از فشرده شدن بگوید چه می‌کند.
- **دکمهٔ شناور جمع/باز قبلی در حالت جمع‌شده پنهان می‌شود** — دو راه برای یک کار،
  روی هم، گیج‌کننده است.
- باز کردن از طریق همان toggle واقعی انجام می‌شود، پس `KTToggle` همچنان تنها
  صاحب وضعیت است و همان کوکی را می‌نویسد. **دو کنترل، یک منبع حقیقت.**

### اضافه شد — برچسب کنار آیکون‌ها

یک نوار ۷۵ پیکسلی با ۱۱ آیکون بی‌برچسب، از خواننده می‌خواهد ۱۱ شکل را حفظ کند.

حالا با هاور، **نام همان بخش کنار نشانگر شناور می‌شود** و آیکون هم روشن می‌شود.
نام از خودِ `.menu-title` می‌آید که از قبل در DOM است — نه یک `title` تازه — پس
یک منبع برای کلمه وجود دارد و تأخیر tooltip بومی هم در کار نیست.

> **یک اشتباه که دوباره تکرار شد.** برچسب اول با `inset-inline-end` نوشته شد و
> روی **x=1595** افتاد، بیرون از پنجرهٔ ۱۶۰۰ پیکسلی. زیر RTL، `inline-start`
> لبهٔ **راست** است؛ برای فرستادن برچسب به چپ باید `inset-inline-start` داد.
> همین اشتباه در ۱.۳.۸ روی پاپ‌آپ پوسته هم رخ داده بود.

### تغییر کرد — آدمک روی دکمهٔ کاربر

۱.۳.۸ خورشید/ماه پوسته را آنجا گذاشته بود، که کنترلِ حسابِ کاربر را شبیه یک کلید
پوسته می‌کرد. پوسته از قبل خودش را روی ردیف «حالت» داخل همان منو گزارش می‌کند،
که همان‌جایی است که خواننده دنبالش می‌گردد.

### تغییر کرد — «پروفایل من» یک مودال شد

از کارت داخل داشبورد به یک dialog با تم پنل منتقل شد که از منوی کاربر باز
می‌شود:

- **از هر صفحه‌ای در دسترس است.** قبلاً ویرایش پروفایل یعنی رفتن به داشبورد و از
  دست دادن کاری که در جریان بود.
- **در داشبورد جای درستی نداشت**: کنار اعداد شرکت می‌نشست، در حالی که فرمِ
  خودِ صاحب حساب است.
- **منتقل شد، نه کپی.** دو نسخه از یک فرم از هم جدا می‌افتند، و `setupProfile()`
  با id پیدایشان می‌کند — پس نسخهٔ دوم یعنی «کدام‌یک پر می‌شود» به ترتیب سند
  بستگی پیدا می‌کرد. یک تست همین را می‌بندد.
- **در اولین باز شدن بارگذاری می‌شود، نه در بارگذاری صفحه.** حالا در پوستهٔ همهٔ
  صفحه‌هاست؛ گرفتن زودهنگامش یعنی یک درخواست اضافه روی هر صفحه، برای فرمی که
  بیشتر بازدیدها اصلاً بازش نمی‌کنند.

### تست‌هایی که عمداً به‌روز شدند

پنج فایل تست مرورگر، «دیده شدن `#profile-form` روی داشبورد» را نشانهٔ پایان
ورود گرفته بودند. این همیشه یک نماینده بود — اتفاقاً آخرین چیزی که داشبورد رسم
می‌کرد — و با رفتن پروفایل به dialog دیگر درست نبود.

حالا روی **دکمهٔ حساب کاربری** صبر می‌کنند: در هر صفحه‌ای هست، و به جای هیچ فرمی
وابسته نیست. نامِ کاربر هم با `textContent` **خوانده** می‌شود نه دیده — چون آن
عنصر `d-none d-md-flex` دارد و انتظارِ «دیده شدن» روی موبایل هرگز برآورده
نمی‌شد.

`test_desktop_login_profile_and_logout_flow` حالا خودِ مودال را باز و بسته
می‌کند، چون این تست دربارهٔ پروفایل است.

### حذف شد — سقف تعداد اعلان‌های شیوه‌نامه

این عدد در چهار نسخه ۱۲۰ → ۱۴۰ → ۱۶۰ → ۱۸۰ رفته بود و هر بار هم با دلیل موجه.
در ۱.۳.۸ داخل خودِ تست نوشته شد: «پیش از بالا بردن دوباره، جایش را عوض کنید.»
این همان لحظه است.

سقفی که فقط بالا می‌رود، **رشد پنل را اندازه می‌گیرد نه انضباطش را** — و هر بار
یک بحث بازبینی می‌خواست که هیچ یافته‌ای تولید نمی‌کرد. قاعدهٔ واقعی
(«کامپوننت‌های قالب را از نو نساز») تست مستقل خودش را دارد و به عدد نیاز ندارد.
پیش از حذف، سه اعلان زائد هم برداشته شد — از جمله یک قاعدهٔ عرض که اندازه‌گیری
نشان داد **هیچ کاری نمی‌کرد**.

---

## [1.3.10] — ۲۰۲۶-۰۸-۲۹

رفع اشکالات ممیزی ۲۰۲۶/۰۸/۲۹. **بدون مهاجرت، بدون فیلد تازه، بدون تغییر رفتار
درست.** طبق جدول شماره‌گذاری همین فایل این یک **PATCH** است.

### رفع شد — صدور فاکتور روی PostgreSQL کار نمی‌کرد

`issue_invoice()` دکوراتور `@transaction.atomic` نداشت
(`billing/services.py`). روی PostgreSQL خطای
`TransactionManagementError: select_for_update cannot be used outside of a
transaction` می‌داد؛ یعنی **صدور هر فاکتوری خطای ۵۰۰ بود.**

یک **رگرسیون** بود: کامیت اولیهٔ `d391b9e` دکوراتور را داشت و `41411e0`
(نسخهٔ `1.2.0`) حذفش کرده بود. docstring خودِ تابع در تمام این مدت می‌گفت
«All three effects share this transaction with the status change» — یعنی مستند،
تضمینی را توصیف می‌کرد که کد نداشت.

### رفع شد — تغییر وضعیت چک روی PostgreSQL کار نمی‌کرد

`transition_cheque()` (`billing/payments.py`) همان مشکل را داشت. یعنی **هر چهار
دکمهٔ وضعیت چک** که در `1.3.7` اضافه شدند روی تولید خطا می‌دادند.

جدا از خطا، این تابع چهار چیز می‌نویسد — وضعیت چک، سطر تاریخچه، وضعیت پرداخت، و
سطر دفتر. بدون تراکنش، شکست وسط کار حالت ناسازگار به جا می‌گذاشت.

### رفع شد — ثبت پرداخت بدون `customer` خطای ۵۰۰ می‌داد

`register_payment()` آرگومان `customer` را **اجباری** می‌خواست، ولی از `1.2.1`
که این فیلد برای پرداختی nullable شد، DRF دیگر اجباری‌اش نمی‌دانست و وقتی کلید در
درخواست نبود اصلاً پاسش نمی‌داد → `TypeError` → ۵۰۰.

حالا پیش‌فرضش `None` است و قاعدهٔ «دریافتی مشتری می‌خواهد» — که چند خط پایین‌تر
بود و هرگز اجرا نمی‌شد — مثل هر قاعدهٔ دیگری ۴۰۰ برمی‌گرداند.

نکتهٔ ظریف: ارسال صریح `{"customer": null}` همیشه درست کار می‌کرد. فقط **نبودِ
کلید** می‌شکست، که دقیقاً همان شکلی است که یک فراخوان معمولی می‌فرستد.

### اضافه شد — گیتی که این دسته را می‌بیند

**مهم‌تر از هر سه رفع بالا.**

هر سه اشکال با ۱۰۸۱ تست سبز زنده بودند، چون تست‌ها روی SQLite‌اند و تولید روی
PostgreSQL. جنگو `select_for_update()` بیرون از تراکنش را فقط روی بک‌اندی رد
می‌کند که قفل ردیف دارد؛ SQLite عبارت را **بی‌صدا حذف می‌کند**.

`common/tests/test_row_locks_are_transactional.py` دو بررسی دارد:

- **ایستا:** کل درخت را با AST می‌خواند و هر تابع عمومی‌ای را که
  `select_for_update()` صدا می‌زند و تراکنش خودش را ندارد، با نام و شماره خط رد
  می‌کند. چون خواندنِ منبع است، توابعی را هم می‌گیرد که **هیچ تستی صدایشان
  نمی‌زند** — و همین بود که رگرسیون را زنده نگه داشت.
- **زمان اجرا:** مقدار `has_select_for_update` را روی اتصال `True` می‌کند تا
  جنگو دقیقاً همان بررسی PostgreSQL را اجرا کند، بدون نیاز به سرور PostgreSQL.

اثبات شد که کار می‌کند: با برگرداندن موقتِ رگرسیون ۱.۲.۰، تست دقیقاً
`billing/services.py:1068 issue_invoice()` را نام برد.

### رفع شد — نشت سطل throttle بین تست‌ها (باز هم)

سه کلاس پرترافیک `cache.clear()` در `setUp` نداشتند و بسته به ترتیب اجرا ۴۲۹
می‌گرفتند. همان الگوی ده کلاس دیگر این مجموعه.

### آنچه اشتباه گزارش شده بود و اصلاح می‌شود

ممیزی «N+1 روی فهرست کاربران» را گزارش کرده بود. **اشتباه بود.** با شمارش واقعی
کوئری سنجیده شد: **۶ کوئری برای ۲۵ ردیف** — ثابت، نه N+1. `crm_identities`
از `Exists()` استفاده می‌کند و serializer هیچ ForeignKey‌ای را دنبال نمی‌کند، پس
`select_related` چیزی برای انجام دادن ندارد. تغییری داده نشد.

مورد «۴۰۹ به‌جای ۴۰۰ روی تلفن تکراری» هم دست‌نخورده ماند: ۴۰۹ برای تعارض از نظر
معنایی درست است و عوض کردنش یک تصمیم محصولی است، نه رفع اشکال.

---

## [1.3.9] — ۲۰۲۶-۰۸-۲۸

سه پاسخ مالک محصول، پیاده‌شده. **بدون مهاجرت.**

### اضافه شد — یک سند، دیده‌شده از هر دو میز

چکی که از مشتری گرفته شده و بعد به دیگری داده شده، **یک سند است** و باید در هر دو
فهرست دیده شود: هم همان دریافتی است که ثبت شد، هم پولی که از مجموعه خارج شده.

پس میز پرداخت‌ها حالا `desk=disbursement` می‌پرسد، نه `direction=disbursement`، و
سرور آن را این‌طور می‌فهمد: پرداختی‌ها **به‌علاوهٔ** دریافتی‌هایی که چکشان خرج
شده.

> **این یک نمای دوم از همان ردیف است، نه ردیف دوم.** ثبت یک سند پرداخت برای خرج
> کردن چک، همان پول را دو بار می‌شمرد و حساب مشتری را بابت چکی بدهکار می‌کرد که
> اصلاً پول ما نبوده. برای همین `spend_received_cheque` هیچ‌چیز نمی‌سازد و این
> کار یک **کوئری** است، نه یک سند.

`direction` سرجایش ماند و کار خودش را می‌کند؛ آن دو، دو سؤال متفاوت‌اند. تغییر
وضعیت چک همچنان فقط از تب چک‌ها انجام می‌شود.

چکی که هنوز در انتظار است یا وصول شده، در میز پرداخت‌ها **دیده نمی‌شود** — اولی
هنوز دست ماست و دومی پولی است که آمده، نه رفته.

### تغییر کرد — ابطال یک‌طرفه شد

سند ابطال‌شده دیگر دوباره تأیید نمی‌شود؛ باید **از نو ثبت** شود.

۱.۳.۷ عکسش را اجازه می‌داد و یک بستانکاری تازه ثبت می‌کرد. از نظر حساب درست بود و
باز هم چیز درستی برای پیشنهاد دادن نبود: اپراتوری که ردیف اشتباه را ابطال کرده،
می‌توانست برش گرداند و جز دو سطر دفتر ردی نماند — در حالی که ثبت دوباره، سندی
می‌سازد که شماره‌اش می‌گوید واقعاً کِی وارد شده. گزینهٔ «تأییدشده» روی سند
ابطال‌شده حالا غیرفعال است، نه اینکه پیشنهاد شود و بعد رد.

### تغییر کرد — کاهش مبلغ دیگر رد نمی‌شود

تخصیص اجباری نیست: پول می‌تواند روی حساب مشتری بنشیند بی‌آنکه به فاکتوری بخورد.

پس مبلغ کمتر دیگر رد نمی‌شود. تخصیص‌هایی که جا نمی‌شوند **آزاد می‌شوند** تا آنچه
می‌ماند جا شود، و بقیه سرجایشان می‌مانند — آزادسازی از همان مسیر `release_allocation`
می‌گذرد، پس مبلغ پرداختی و وضعیت تسویهٔ هر فاکتور مثل آزادسازی دستی برمی‌گردد و
خودِ آزادسازی هم ثبت می‌شود.

**از جدیدترین شروع می‌کند**، چون تخصیص‌های قدیمی‌تر آن‌هایی هستند که اپراتور
به‌احتمال بیشتر هنوز منظورش است.

---

## [1.3.8] — ۲۰۲۶-۰۸-۲۸

سایدبار جمع‌شونده که واقعاً جمع می‌شد اما تغییری نشان نمی‌داد، و انتخاب پوسته که
حالا یک ردیف با پاپ‌آپ کوچک است. **بدون مهاجرت.**

### رفع شد — سایدبار با کلیک جمع نمی‌شد

کلیک روی دکمه، فقط برچسب‌های منو را پنهان می‌کرد؛ **عرض سایدبار تا بارگذاری بعدی
صفحه تکان نمی‌خورد.**

علتش دقیق است: عرض سایدبار `var(--bs-app-sidebar-width)` است و دکمه همان **متغیر
سفارشی** را عوض می‌کند، نه خودِ اعلان `width` را. کرومیوم عرضی را که از `var()`
حل می‌شود دوباره اعمال نمی‌کند وقتی یک `transition` روی `width` مسلح باشد. در
صفحهٔ زنده اندازه گرفته شد: **با هر کلیک متغیر از ۲۶۵ به ۷۵ می‌رفت و جعبه روی ۲۶۵
می‌ماند**؛ با خاموش کردن transition، همان کلیک‌ها بلافاصله کار کردند.

پس transition عرض حذف شد. جمع شدن حالا فوری است — که معاملهٔ درستی است: سایدباری
که فوری حرکت کند بهتر از انیمیشنی است که هرگز اجرا نمی‌شود. قالب همین transition
را داخل قاعدهٔ جمع‌شدهٔ خودش تعریف می‌کند، پس همان انتخابگر اینجا تکرار شده تا
وزنش بیشتر باشد. زیر نقطهٔ شکست دست‌نخورده است؛ آنجا سایدبار کشویی است و با
`transform` حرکت می‌کند.

### رفع شد — لوگوی ریز، «نمایش داده نمی‌شد»

نمایش داده **می‌شد**: به عرض یک ستون پیکسل و بلندتر از صفحه.

`<img>` صفت‌های `width="1278" height="1230"` را دارد تا مرورگر پیش از رسیدن فایل،
جای درست را رزرو کند. CSS فقط عرض را می‌داد، پس **صفت ارتفاع سرجایش می‌ماند** و
نشان ۲۹×۱۲۳۰ رندر می‌شد. یک `height: auto` روی `.brand-mark` کافی بود؛ حالا
۲۹×۲۸ است.

### تغییر کرد — انتخاب پوسته، یک ردیف با پاپ‌آپ

سه گزینه از حالت تخت داخل منوی کاربر درآمدند. حالا **یک ردیف با عنوان «حالت»**
هست که آیکون پوستهٔ فعلی را در لبه‌اش نشان می‌دهد، و با **هاور یا کلیک** یک
پاپ‌آپ کوچک کنارش باز می‌شود: روشن، تاریک، سیستم.

- **هاور و کلیک، هر دو.** فقط هاور، صفحهٔ لمسی و کاربر کیبورد را جا می‌گذاشت.
- **سمت باز شدن اندازه‌گیری می‌شود، نه فرض.** منوی کاربر به یک لبهٔ پنجره چسبیده،
  پس سمتِ ترجیحی همیشه سمتی نیست که جا دارد. اگر پاپ‌آپ از صفحه بزند بیرون، به
  سمت دیگر می‌چرخد.
- **آیکون ردیف به هیچ اسکریپتی نیاز ندارد**: هر دو آیکون در DOM هستند و قاعدهٔ
  `theme-light-show`/`theme-dark-show` خود قالب از روی `data-bs-theme` تصمیم
  می‌گیرد کدام دیده شود — پس از همان اولین فریم درست است.

> **یک نکته که وقت گرفت.** پاپ‌آپ کلاس `.menu-sub` قالب را دارد که تا وقتی KTMenu
> کلاس `.show` را اضافه نکند `display: none` است. اینجا هیچ‌کس `.show` اضافه
> نمی‌کند — اسکریپت خود پروژه بازش می‌کند — پس پاپ‌آپ **۰×۰** می‌ماند هر کاری که
> اسکریپت می‌کرد. حالا `display` صریحاً گفته شده و `[hidden]` هم کنارش.

### اضافه شد — نسخه روی آدرس CSS و JS

`forooshbin.css` و `forooshbin-app.js` حالا با `?v={{ forooshbin_version }}`
لینک می‌شوند.

بدون این، مرورگری که نسخهٔ دیروز را نگه داشته بعد از هر دیپلوی همان را استفاده
می‌کند و پنل، **مارک‌آپ تازه را روی CSS و اسکریپت کهنه** اجرا می‌کند. همین موضوع
در جریان همین کار سه بار وقت گرفت — تغییرها روی سرور بودند و در مرورگر نبودند.
باندل‌های فروشنده دست‌نخورده‌اند؛ آن‌ها یکجا و به‌ندرت عوض می‌شوند.

### سقف شیوه‌نامه از ۱۶۰ به ۱۸۰ رفت — و این بار یک تست دیگر هم اضافه شد

اول ۶ اعلان حذف شد: پاپ‌آپ دیگر فاصله‌ای از منو ندارد، پس آن `::before` که فقط
برای پل زدن روی همان فاصله وجود داشت، چیزی نمی‌خرید.

ولی این **سومین بار پیاپی** است که این سقف بالا می‌رود (۱۲۰ → ۱۴۰ → ۱۶۰ → ۱۸۰) و
هر بار هم با دلیل موجه. این یعنی **عدد، ابزار درستی نیست** — نه اینکه قاعده غلط
باشد. پس قاعدهٔ واقعی حالا **تست مستقل خودش** را دارد: «کامپوننت‌های قالب را
بازنساز». پیش از بالا بردن دوبارهٔ عدد، باید جایش را با همان تست عوض کرد؛ عددی که
فقط بالا می‌رود، رشد پروژه را اندازه می‌گیرد نه انضباطش را.

---

## [1.3.7] — ۲۰۲۶-۰۸-۲۸

صفحهٔ جزئیات سند مالی قابل اصلاح شد — برای مدیر پلتفرم و فقط او. **بدون مهاجرت.**

### تغییر کرد — کارت جزئیات

- **«تخصیص‌یافته» و «تخصیص‌نیافته» رفتند.** تخصیص بخش خودش را پایین همین صفحه
  دارد، با نام فاکتورها. دو عدد در بالای کارت همان را تکرار می‌کردند بی‌آنکه
  بگویند کدام فاکتور، و خواننده باید پایین می‌رفت تا بفهمد.
- **«شماره پیگیری» فقط روی حواله بانکی است.** روی نقدی و چک ردیفش خالی نیست،
  اصلاً نیست.
- **«وضعیت» دو مقدار دارد**: تأییدشده و ابطال‌شده. «در انتظار وصول» جزوشان نیست —
  آن حالتی است که پرداختِ چکی خودش از آن عبور می‌کند، نه پاسخی که اپراتور بدهد.
- **روی چک، «حالت چک» اضافه شد** کنار «وضعیت چک». **هر دو خواندنی‌اند**، حتی وقتی
  بقیهٔ کارت قابل ویرایش است: این دو فیلد توصیفی نیستند و جابه‌جا شدنشان برای پول
  معنا دارد، پس از صفحهٔ چک‌ها و از مسیر سرویس‌هایی می‌گذرند که می‌دانند هر حرکت
  چه هزینه‌ای دارد.

### اضافه شد — اصلاح سند

هر فیلد **جز «شماره» و «ثبت‌کننده»** برای مدیر پلتفرم قابل ویرایش است، با دکمهٔ
«ذخیره تغییرات». آن دو هرگز اضافه نمی‌شوند: اولی هویت سند است و دومی کسی که ثبتش
کرده، و رکوردی که بشود نویسنده‌اش را بازنویسی کرد، رکورد نیست.

**`POST /api/v1/payments/<id>/correct/`** تازه است. نقش **هم در endpoint و هم در
سرویس** بررسی می‌شود: قابل‌ویرایش بودن یک input روی صفحه یک راحتی است، هیچ‌وقت
مجوز نیست. تستی از روی HTTP این را می‌بندد — مدیر فروش ۴۰۳ می‌گیرد.

### دفتر بازگو می‌شود، نه بازنویسی

بخش سختِ این کار فیلدها نبود. پرداخت تأییدشده از قبل یک بستانکاری در دفتر ثبت
کرده؛ عوض کردن مبلغ یا مشتریِ آن، همان سطر را غلط می‌کند.

دفتر append-only است، پس **هیچ سطری بازنویسی نمی‌شود**: سطر قدیمی برگردانده
می‌شود و سطر تازه ثبت می‌شود — که در حسابداری دوطرفه، تعریفِ «اصلاح» است. و
معناى تازه‌ای اختراع نشد: `cancel_payment` و `_post_payment_credit` از قبل همین
دو حرکت را بلد بودند و اینجا فقط به ترتیب صدا زده می‌شوند.

    مبلغ ۱٬۰۰۰٬۰۰۰ → ۲٬۵۰۰٬۰۰۰ روی یک دریافت تأییدشده:
      payment_received   بستانکار ۱٬۰۰۰٬۰۰۰
      payment_cancelled  بدهکار   ۱٬۰۰۰٬۰۰۰
      payment_received   بستانکار ۲٬۵۰۰٬۰۰۰

اثر خالص دقیقاً ۲٬۵۰۰٬۰۰۰ است و **هر سه حرکت دیده می‌شوند**. جابه‌جا کردن سند به
مشتری دیگر هم همین‌طور: پول از حساب اولی برمی‌گردد و روی دومی می‌نشیند.

ابطال از این مسیر، از `cancel_payment` می‌گذرد — پس تخصیص‌ها آزاد می‌شوند.
برگرداندن یک سند ابطال‌شده، قرینهٔ همان است: یک بستانکاری تازه، **افزوده** می‌شود
نه اینکه برگشتِ قبلی پاک شود.

### رفع شد — دو اشکال که حین همین کار پیدا شد

- **مبلغ کمتر از آنچه تخصیص یافته، حالا با پیام رد می‌شود.** پایگاه داده هم
  جلویش را می‌گرفت، ولی به شکل `IntegrityError` — یعنی ۵۰۰ که به اپراتور هیچ
  نمی‌گفت. همان قاعده، این بار با کلمه، پیش از آنکه چیزی نوشته شود.
- **`update_payment` در `views.py` وارد نشده بود** و endpoint با `NameError`
  می‌شکست. با اولین فراخوانی واقعی در مرورگر پیدا شد.

### تست‌هایی که عمداً به‌روز شدند

`test_manager_receipts_stock_then_orders_invoices_and_takes_payment` مقدار
«تأییدشده» را از یک input می‌خواند؛ حالا وضعیت یک select است، پس **هر دو** سنجیده
می‌شود: مقدارِ ذخیره‌شده (`confirmed`) و برچسبی که خوانده می‌شود (تأییدشده). و
چون «تخصیص‌یافته» از کارت رفته، شاهدِ تخصیص حالا خود ردیف تخصیص است.

---

## [1.3.6] — ۲۰۲۶-۰۸-۲۸

دو میز جدا برای دریافت و پرداخت، و چکی که هر دو محورش دستی جابه‌جا می‌شود.
**بدون مهاجرت.**

### تغییر نام

- «اسناد بازرگانی» → **«اسناد فروش»**
- «صندوق و دریافت» → **«اسناد مالی»**

### اضافه شد — میز پرداخت‌ها

`/disbursements/` کنار `/payments/`. **یک قالب و یک هندلر، دو میز**: یک دریافت و
یک پرداخت همان سندند که از دو جهت خوانده می‌شوند، پس واژه‌ها از
`payment_direction` می‌آید و بقیه مشترک است. کپی دوم دویست خط مارک‌آپ، فردا از
اصلش جدا می‌افتاد.

- ستون طرف حساب روی میز پرداخت **«گیرنده»** است و روی میز دریافت «مشتری».
- هر میز فقط جهت خودش را فهرست می‌کند (`?direction=`)، وگرنه هر دو صفحه یک
  ردیف‌ها را زیر دو عنوان نشان می‌دادند.

### تغییر کرد — فهرست دریافت‌ها و پرداخت‌ها

- ستون‌های **«جهت»** و **«تخصیص‌یافته»** حذف شدند. هر میز یک جهت دارد، پس ستونی
  که آن را در هر ردیف تکرار می‌کرد چیزی نمی‌گفت.
- **فیلتر وضعیت حذف شد.**
- دکمه‌های «اقساط» و «چک» از این صفحه رفتند؛ هرکدام ورودی خودشان را در سایدبار
  دارند.

### تغییر کرد — فرم ثبت

- **دکمه‌های «دریافتی/پرداختی» حذف شدند.** جهت را صفحه تعیین می‌کند، نه فرم.
- **«کارت» از روش‌ها حذف شد.** مدل هنوز می‌پذیردش و ردیف‌های قدیمی دست‌نخورده‌اند —
  چیزی بازتفسیر یا پاک نشد — فقط دیگر قابل انتخاب نیست.
- **نقدی:** «شماره رسید» رفت. **حواله بانکی:** «شماره حساب/شبا» رفت و نام بانک
  شد «بانک مقصد» (روی پرداخت: «بانک مبدأ»). **چک:** «شماره مرجع» و «صاحب حساب»
  رفتند و بانک شد «بانک مبدأ» (روی پرداخت: «بانک مقصد»).
- **طرف حساب قابل جستجو شد**، چون دفتر مشتریان قرار است از حد یک dropdown ساده
  بگذرد. پس از انتخاب، فقط نام نشان داده می‌شود.

> **چرا `<select>` بومی زیرش ماند.** جعبهٔ جستجو روی select واقعی سوار است؛ همان
> select مقدار را نگه می‌دارد و همان چیزی است که ثبت می‌شود. ویجتی که select را
> **جایگزین** کند باید یک **کپی** از مقدار نگه دارد، و کپی‌ای که جدا بیفتد نامی
> نشان می‌دهد که ثبت نخواهد شد.
>
> و تعویض در **جاوااسکریپت** انجام می‌شود، نه در مارک‌آپ: تا وقتی اسکریپت اجرا
> نشده، صفحه یک `<select>` سالم دارد. اول با `hidden` در قالب نوشته شده بود و
> **تست مرورگر گرفتش** — سلنیوم حاضر نشد عنصر نامرئی را بچرخاند، و درست هم بود:
> ادعای «اگر اسکریپت نیامد باز هم کار می‌کند» با `hidden` در قالب، حرف نادرستی
> بود.

### اضافه شد — پرداخت با چکِ مشتری

روی میز پرداخت، «نوع چک» دو مقدار دارد: **چک مشتری** یا **چک تازه**. با «چک
مشتری» فقط «شماره چک» پرسیده می‌شود و میان چک‌های دریافتیِ در انتظار جستجو
می‌شود.

این کار **هیچ سند و هیچ چک تازه‌ای نمی‌سازد** — همان ابزار است که دست عوض می‌کند،
و به `spend/` می‌رود. ساختن ردیف دوم یعنی همان مبلغ دو بار شمرده شود.

### اضافه شد — هر دو محور چک از خود ردیف

- ستون **عملیات**: چهار دکمه — برگشت، خرج کردن، وصول، در انتظار. مقصدی که گراف
  وضعیت اجازه نمی‌دهد **غیرفعال** نشان داده می‌شود، نه پنهان: دکمه‌ای که با عوض
  شدن وضعیتِ ردیف‌ها می‌آید و می‌رود، شبیه خرابی رندر است.
- ستون **عملیات دوم**: «ثبت شده» / «ثبت نشده». هر دو همیشه هستند و حالت فعلی
  به‌صورت فشرده نشان داده می‌شود، پس عرض ستون با تغییر حالت تکان نمی‌خورد.
- **`POST /api/v1/cheques/<id>/registration/`** تازه است. عمداً روی پرداختِ زیرش
  **هیچ اثری ندارد**: ثبت‌شدن می‌گوید کاغذ کجاست، نه اینکه پول رسیده — و اگر
  اثر داشت، «ثبت‌شده» کردن یک دسته چک، یک دسته بستانکاری ثبت می‌کرد که کسی
  دریافت نکرده.
- دکمهٔ «دریافت‌ها» از این صفحه رفت.

### تغییر کرد — چک همیشه خام ثبت می‌شود

هر چکی که از میز دریافت یا پرداخت ثبت شود، **همیشه** «در انتظار» و «ثبت نشده»
شروع می‌کند. **در سرویس** اعمال می‌شود نه در فرم، تا درخواستی که دستکاری شده
نتواند ابزاری را در حالتی بگذارد که کسی انتخابش نکرده. `is_registered` هنوز
پذیرفته و بعد نادیده گرفته می‌شود، تا فراخوان قدیمی خطا ندهد.

### تغییر کرد — فیلترها کشیده شدند

فیلتر هر فهرست حالا یک grid است که کل عرض کارت را می‌گیرد: **۲۲ قالب**.
`flex-wrap` با `w-auto` باعث می‌شد هر کنترل فقط اندازهٔ محتوایش باشد و سه select
کوتاه وسط یک کارت پهن جمع شوند. دکمهٔ «اعمال» به اندازهٔ متن خودش می‌ماند —
کشیدنش روی یک‌سوم ردیف، شبیه فیلتر چهارم می‌شد.

### سقف شیوه‌نامه از ۱۴۰ به ۱۶۰ رفت

باز هم تصمیم، نه دور زدن: اول ۴ اعلان که قالب از قبل داشت حذف شد. آنچه ماند یک
grid فیلتر و یک combobox جستجوشونده است — و combobox یک **کامپوننت** است، که این
تست معمولاً درست می‌کند جلویش را بگیرد. اینجا هست چون combobox خود قالب،
select2 است که در همان bundle ۳.۵ مگابایتی بارگذاری‌نشده زندگی می‌کند.

---

## [1.3.5] — ۲۰۲۶-۰۸-۲۷

حالت روشن، تاریک و سیستم برای کل پنل. **بدون مهاجرت.**

### اضافه شد

- **سه حالت** در منوی کاربر (بالا، سمت چپ): **روشن**، **تاریک**، و **سیستم** که
  از تنظیم خود سیستم‌عامل پیروی می‌کند.
- **آیکون‌های خود قالب**: `ki-night-day`، `ki-moon` و `ki-screen`.
- **دکمهٔ کاربر خودش می‌گوید الان کدام حالت روشن است** — دو آیکون در DOM هستند و
  قاعدهٔ `theme-light-show`/`theme-dark-show` خود قالب تصمیم می‌گیرد کدام دیده
  شود. یعنی از همان **اولین فریم** درست است، بدون هیچ اسکریپتی از ما.

خودِ سازوکار مال قالب خریداری‌شده است: `KTThemeMode` منو را می‌بندد، «سیستم» را
با `prefers-color-scheme` حل می‌کند، `data-bs-theme` را روی `<html>` می‌نویسد و
انتخاب را نگه می‌دارد. رنگ‌ها هم از قبل آنجا بودند — بوت‌استرپ ۵.۳ و متغیرهای
قالب، **۱۵۷ متغیر** برای خاکستری‌ها، پس‌زمینهٔ صفحه و سایدبار. **هیچ پالتی در این
پروژه تعریف نشد.**

### آنچه این پروژه اضافه کرد — اسکریپتی که از اولین نقاشی جلو می‌زند

`KTThemeMode` روی `DOMContentLoaded` اجرا می‌شود، و تا آن لحظه کسی که حالت تاریک
دارد **یک صفحهٔ سفید دیده است**. یک اسکریپت درون‌خطی در `<head>`، پیش از همهٔ
شیوه‌نامه‌ها، این را حل می‌کند.

دو کلید و دو معنی: `data-bs-theme-mode` همان چیزی است که کاربر **انتخاب** کرده
(روشن/تاریک/سیستم) و `data-bs-theme` چیزی است که همان الان **به آن حل می‌شود**.
فقط انتخاب ذخیره می‌شود؛ «سیستم» باید در هر بار بارگذاری دوباره حل شود، وگرنه
کسی که «سیستم» را انتخاب کرده، بعد از عوض کردن تنظیم سیستم‌عاملش جواب دیروز را
می‌گیرد.

اگر `localStorage` در حالت ناشناس دسترسی ندهد، پنل در حالت پیش‌فرض بالا می‌آید:
پنلی با پوستهٔ اشتباه یک ایراد است، پنلی که بالا نیاید یک قطعی است.

> **از فایل قالب کپی نشد، و دلیلش مهم است.** قالب همین اسکریپت را دارد، ولی
> `layouts/dark-sidebar.html` در این مخزن از یک **ترجمهٔ ماشینی آسیب دیده**:
> داخل شناسه‌ها `mode` با «حالت» جایگزین شده (`defaultThemeحالت`)، `getItem`
> کوچک‌نویس شده (`getitem`)، و یک `?` از ternary افتاده. کپی‌کردنش یک اسکریپت
> خراب به پنل می‌آورد. همان ایده اینجا **درست** نوشته شده.

### چرا منو داخل منوی کاربر است، نه پشت دکمهٔ جدا

قالب این سه گزینه را در یک dropdown تودرتو می‌گذارد که `KTMenu` با Popper جای‌گذاری
می‌کند، و Popper در همان bundle ۳.۵ مگابایتی است که این دیپلوی بارگذاری نمی‌کند.
`KTThemeMode` به هیچ‌کدام نیاز ندارد: `[data-kt-element="theme-mode-menu"]` را
هرجا باشد پیدا می‌کند و کلیک‌ها را خودش می‌بندد. پس گزینه‌ها ردیف‌های ساده‌ای در
منویی هستند که از قبل باز است — همان‌جایی که خواسته شده بود.

### سند چاپی تاریک نمی‌شود

`print_base.html` قالب پنل را extend نمی‌کند، پس هیچ‌چیز آن را پوسته‌دار نمی‌کند و
فاکتور در هر حالتی سیاه روی سفید می‌ماند. این ساختاری است، نه شانسی — و یک تست
نگهش می‌دارد، چون extend کردنش شبیه یک مرتب‌سازی بی‌ضرر به نظر می‌رسد و بی‌صدا
شروع می‌کند فاکتور تاریک چاپ کردن.

### آزموده شد

هر سه حالت در مرورگر واقعی: «سیستم» با سیستم‌عامل تاریک → تاریک، همان «سیستم» با
سیستم‌عامل روشن → روشن، و «تاریک» صریح روی سیستم‌عامل روشن → تاریک و ماندگار بعد
از جابه‌جایی صفحه. روی صفحهٔ جزئیات دریافت در حالت تاریک، **هیچ عنصری نسبت کنتراست
زیر ۳:۱ نداشت**.

---

## [1.3.4] — ۲۰۲۶-۰۸-۲۷

سایدبار جمع‌شونده، و ظاهر مدرن برای فهرست‌های انتخاب. **بدون مهاجرت.**

### اضافه شد — سایدبار جمع می‌شود و یادش می‌ماند

- **دکمهٔ جمع/باز کردن** روی لبهٔ داخلی سایدبار، وسط نوار لوگو: همان‌جا در هر
  صفحه، با منو اسکرول نمی‌شود، و برای پیدا کردنش لازم نیست کسی بگردد.
- **جمع‌شده یعنی ۷۵ پیکسل**: فقط نشان کوچک و آیکون‌ها می‌مانند. عنوان‌ها، فلش‌ها
  و زیرمنوهای آکاردئونی می‌روند — در ۷۵ پیکسل جایی برای باز شدنشان نیست.
- **حالت روی سرور خوانده می‌شود، نه با اسکریپت بعد از بارگذاری.** این پنل بین
  صفحه‌ها بارگذاری کامل دارد؛ اگر حالت را جاوااسکریپت برمی‌گرداند، سایدبارِ
  جمع‌شده در **هر جابه‌جایی** اول باز نقاشی می‌شد و بعد بسته می‌شد.

خودِ جمع شدن کار قالب خریداری‌شده است: CSS آن به `data-kt-app-sidebar-minimize`
روی `<body>` واکنش نشان می‌دهد و `KTToggle` همان را جابه‌جا می‌کند و کوکی‌اش را
می‌نویسد. آنچه این پروژه اضافه کرد، سه چیزی است که قالب نمی‌تواند بدهد: خواندن
کوکی سمت سرور، یک لوگوی دوم برای ریل باریک، و جای درست دکمه.

> **سه قاعده‌ای که باید وزن بیشتری از ابزارهای خود قالب داشته باشند.** این‌ها
> `!important` یا انتخابگر سنگین‌تر دارند و هیچ‌کدام سلیقه‌ای نیست:
>
> - `min-width: 0` روی سایدبارِ جمع‌شده. یک flex item به‌طور پیش‌فرض
>   `min-width: auto` دارد یعنی «هرگز کوچک‌تر از محتوایم» — بدون این، متغیر عرض
>   ۷۵ پیکسل می‌گفت، قاعده هم می‌خواندش، و جعبه همچنان ۲۶۵ پیکسل اندازه می‌گرفت
>   چون متن برند تو‌یش جا نمی‌شد. **همین یک اعلان بود که کل قابلیت را از کار
>   انداخته بود.**
> - `display: none !important` روی برند بازشده، چون `d-flex` بوت‌استرپ خودش
>   `!important` است.
> - انتخابگر `.app-sidebar .app-sidebar-toggle` برای پنهان‌کردن دکمه در موبایل،
>   چون `.btn.btn-icon` قالب دو کلاس است و یک کلاس به آن می‌بازد.

زیر نقطهٔ شکست، سایدبار کشویی است و جمع کردنش معنایی ندارد، پس دکمه پنهان می‌شود.

### تغییر کرد — ظاهر فهرست‌های انتخاب

هر انتخاب در این پنل یک `<select>` واقعی است و **همان می‌ماند**. فهرستش از API
با `fillSelect` پر می‌شود، مقدارش در ده‌ها جا با `.value` خوانده می‌شود و تست‌ها
رویش ادعا دارند؛ جایگزین کردنش با یک listbox اسکریپتی یعنی نگه‌داشتن یک **کپی**
از مقدار، و کپی‌ای که از اصل جدا بیفتد به اپراتور برچسبی نشان می‌دهد که همان چیزی
نیست که ثبت خواهد شد.

پس کنترل بومی استایل شد، نه اینکه پشت یکی دیگر پنهان شود:

- جعبهٔ بسته کاملاً از آنِ ماست: حالت hover، حلقهٔ focus، و فلشی که هنگام focus
  رنگ می‌گیرد.
- **`color-scheme: light dark`** روی هر `select` — تنها دستگیره‌ای که یک صفحه
  روی فهرستی دارد که خودِ مرورگر می‌کشد. بدون آن، فهرست باز در حالت تاریک سفید
  باز می‌شود. (این همان چیزی است که ۱.۳.۵ رویش سوار می‌شود.)
- `option` و `optgroup` رنگ و فاصلهٔ خودِ پنل را می‌گیرند، و ردیف راهنما (همانی
  که `fillSelect` با مقدار خالی می‌نویسد) کم‌رنگ‌تر است چون یک انتخاب نیست.

جایی که مرورگری این‌ها را نپذیرد، `color-scheme` همچنان فهرست را خوانا نگه
می‌دارد؛ یعنی افت به فهرست پلتفرم است، نه به فهرست ناخوانا.

---

## [1.3.3] — ۲۰۲۶-۰۸-۲۷

یک دور دیباگ روی کل پنل: هر صفحه با داده واقعی باز شد، فرم‌ها پر و ثبت شدند، و
آنچه شکست اینجا آمده. **بدون مهاجرت.**

### رفع شد — تقسیم دریافت از طریق API اصلاً کار نمی‌کرد

`POST /api/v1/payments/<id>/allocate-across/` که در ۱.۳.۱ اضافه شد، **هر شناسهٔ
فاکتوری را «pk نامعتبر» رد می‌کرد**. یعنی قابلیت بند ۳.۲ از روز اول در عمل
غیرقابل‌استفاده بود.

علتش دقیقاً همان چیزی است که تست‌های آن نسخه نمی‌توانستند بگیرند:
`AllocatePaymentSerializer` دامنهٔ فاکتورها را در `__init__` تنظیم می‌کرد، و یک
serializer که به‌عنوان فیلد تودرتو استفاده می‌شود (`many=True`) **یک‌بار و در
زمان تعریف کلاس** ساخته می‌شود — جایی که هیچ request و هیچ context‌ای وجود ندارد.
پس نسخهٔ تودرتو برای همیشه `Invoice.objects.none()` می‌ماند.

حالا دامنه در `ScopedIssuedInvoiceField.get_queryset()` حل می‌شود که DRF آن را
**در زمان اعتبارسنجی هر درخواست** صدا می‌زند، و `self.context` روی یک فیلد
bind‌شده از والدینش بالا می‌رود؛ پس همان فیلد هم مستقل و هم تودرتو درست کار
می‌کند.

> **چرا تست‌های ۱.۳.۱ این را نگرفتند.** آن‌ها `allocate_payment_across()` را
> مستقیم صدا می‌زدند و یک فراخوانی مستقیم سرویس هرگز به serializer نمی‌رسد.
> `billing/tests/test_payment_split_api.py` تازه اضافه شد و از روی HTTP می‌رود.
> تست سرویس و تست API یک تست نیستند.

### رفع شد — مبلغ خام در گزارش عملکرد

جدول و کارت‌های «عملکرد کاربران» تنها جایی در پنل بودند که مبلغ را از مسیر
`money` رد نمی‌کردند: `sales_amount` و `average_sale_amount` عیناً همان چیزی چاپ
می‌شدند که API می‌فرستد (`12500000.00`) — بدون گروه‌بندی و با اعشاری که هیچ‌جای
دیگر پنل نشان داده نمی‌شود. مقدار پیش‌فرض روی خود قالب هم `۰.۰۰` بود.

### رفع شد — ستون‌های فاکتور چاپی با پاورقی خودش نمی‌خواند

روی فاکتور واقعی، ستون تخفیف `۵٬۰۰۰٬۰۰۱` جمع می‌زد زیر پاورقی‌ای که
`۵٬۰۰۰٬۰۰۰` می‌گفت. همین برای مالیات و جمع کل.

`printed_line_breakdown` مقادیر **اعشاری ذخیره‌شده** را پخش می‌کرد و جمعشان دقیق
بود — بعد صفحه هر سطر را برای نمایش رو به بالا رند می‌کرد. **رند رو به بالا جمع‌پذیر
نیست**: دو سطر که هرکدام کسر داشتند، هرکدام یک ریال گرفتند و ستون یک ریال از
جمع کل جلو زد. حالا کل محاسبه در **ریال صحیح** انجام می‌شود — همان واحدی که خواننده
می‌بیند — با تقسیم به روش «جمع دونده» که تضمین می‌کند اجزا دقیقاً به کل برسند.
پاورقی هم از همان محاسبه برمی‌گردد، پس ستون و جمع دیگر نمی‌توانند از هم جدا شوند.

### رفع شد — `payment_made` هیچ نام فارسی‌ای نداشت

این مقدار در ۱.۲.۱ به `CustomerLedgerEntry.EntryType` اضافه شد و نه نقشهٔ
برچسب‌ها یادش گرفت و نه فیلتر صفحهٔ دفتر. نتیجه: یک ردیف واقعی دفتر رشتهٔ انگلیسی
`payment_made` را در جدولی فارسی چاپ می‌کرد و اصلاً قابل فیلتر کردن نبود.
`adjustment_debit` و `adjustment_credit` هم در فیلتر نبودند.

`common/tests/test_label_coverage.py` تازه اضافه شد و **هر هشت نقشهٔ برچسب و دو
فیلتر را برابر enum خودشان می‌سنجد**. همین نوع خطا دو بار در سطح پایگاه داده به
این پروژه ضربه زده (فهرست دستی داخل `CheckConstraint`)؛ این بار در سطح نمایش.

### رفع شد — `[object Object]` به‌جای پیام خطا

خطای یک serializer تودرتو به شکل `[{invoice: [...]}]` برمی‌گردد و
`showError` آن را مستقیم `join` می‌کرد. حالا ساختار پیمایش می‌شود، پس هر شکلی که
DRF بدهد متن خوانا می‌شود.

### اضافه شد

- **فیلتر `direction` روی `GET /api/v1/payments/`.** جهت از ۱.۲.۱ یک فیلد
  درجه‌یک است و همان فهرست نمودار جهت دارد، ولی endpoint حاضر نبود روی آن فیلتر
  کند — «فقط پولی که خارج شده را نشان بده» سؤالی بود که API دربارهٔ ستونی که خودش
  ذخیره می‌کند نمی‌توانست جواب دهد.
- **ستون «جهت» روی فهرست دریافت‌ها**، و ستون «مشتری / گیرنده» که برای پرداختی به
  `payee` برمی‌گردد. پیش از این یک پرداختی بدون مشتری فقط «—» نشان می‌داد، در حالی
  که نام گیرنده روی همان ردیف ذخیره بود؛ و هیچ چیز در ردیف نمی‌گفت پول به کدام
  سمت رفته.
- **`mark_sale` حالا `sold_at` را پیش‌فرض می‌گذارد.** ستون NOT NULL بود و بدون
  مقدار `IntegrityError` می‌داد (یعنی ۵۰۰) به‌جای خطای اعتبارسنجی که هر فیلد
  دیگری در همان تابع می‌دهد. serializer از قبل همین پیش‌فرض را داشت، پس رفتار
  پنل عوض نمی‌شود.

### رفع شد — نشت سطل throttle بین تست‌ها، دوباره

دو تست در اجرای کامل ۴۲۹ می‌گرفتند (یکی از آن‌ها به شکل `KeyError: 'id'` ظاهر
می‌شد، چون بدنهٔ پاسخِ throttle‌شده اصلاً `id` ندارد). سطل با شناسهٔ کاربر کلید
می‌خورد و تست‌های rollback‌شده همان شناسه‌ها را دوباره می‌سازند، پس یک کلاس خرج
کلاس قبلی را به ارث می‌برد. تست‌های تازهٔ همین نسخه ترتیب اجرا را عوض کردند و این
دو را از آستانه رد کردند.

`cache.clear()` به `setUp` هر دو اضافه شد — همان کاری که ده کلاس پرترافیک دیگر در
این مجموعه می‌کنند.

> **این الگو تکرارشونده است.** هر کلاس تستی که درخواست زیاد می‌فرستد باید خودش
> سطل را پاک کند، و یادش می‌رود. یک راه‌حل سامانه‌ای (پاک کردن خودکار پیش از هر
> تست) ارزش بررسی دارد؛ اینجا الگوی موجود دنبال شد تا تغییر کم‌خطر بماند.

### یک نکته که ثبت می‌شود — همین اشکال قبلاً یک بار رفع شده بود

docstring کلاس `DocumentLineScopeTests` دقیقاً همین خطا را توصیف می‌کند:
«serializer تودرتو پیش از bind شدن ساخته می‌شود، پس `__init__` خودش context خالی
می‌بیند». برای فیلد `product` با `ScopedLineItemsMixin` حل شده بود — و در ۱.۳.۱
همان اشتباه روی `invoice` تکرار شد.

دلیلش خودِ راه‌حل قبلی است: آن mixin از **والد** می‌خواهد که `scope_line_products()`
را صدا بزند، و صدا زدنش یادِ کسی می‌رود. `ScopedIssuedInvoiceField` چیزی از والد
نمی‌خواهد؛ دامنه را خودش در `get_queryset()` حل می‌کند، پس فراموش‌شدنی نیست.

`DocumentLineInputSerializer` عمداً دست‌نخورده ماند — کار می‌کند و تست رگرسیون
دارد — ولی همان شکنندگی را دارد و اگر روزی serializer تودرتوی تازه‌ای اضافه شود،
جای اول برای نگاه کردن همین است.

### تست‌هایی که عمداً به‌روز شدند

- `test_manager_to_agent_daily_workflow_and_company_report` رشتهٔ خام `20.00` را
  انتظار داشت — همان اشکالی که بالا رفع شد. حالا `۲۰ ریال` را می‌خواهد.
- تست‌های `printed_line_breakdown` برای قرارداد تازه (ریال صحیح + جمع دقیق)
  بازنویسی شدند.

### آنچه بررسی شد و سالم بود

هر ۴۹ صفحهٔ پنل و ۳۴ endpoint با داده واقعی باز شدند: بدون ۵۰۰، بدون خطای
کنسول، بدون asset‌ـی که ۴۰۴ بدهد. RTL، `lang="fa"`، فونت IRANSansWeb و هر سه
شیوه‌نامه بار می‌شوند و صفحه در عرض ۳۴۶ پیکسل هم سرریز افقی ندارد. نمودارهای
هر فهرست، نمودار عملکرد، و نمودار شهر مشتریان همه رسم می‌شوند.

---

## [1.3.2] — ۲۰۲۶-۰۸-۲۷

بقیهٔ پاسخ‌های مالک محصول: بندهای ۵، ۶، ۷، ۸ و ۹، و فارسی‌شدن رویدادهای سامانه.
با این نسخه، **همهٔ پرسش‌های `OPEN_FINANCIAL_QUESTIONS.md` بسته شده‌اند** جز
بند ۶.۲ که خودِ مالک محصول پرسش برگشتی داده است.

### بند ۹ — چاپ، شبیه نمونهٔ `kala-1.pdf`

- **دو بلوک هویت** کنار هم: «مشخصات فروشنده» و «مشخصات خریدار»، هرکدام با نام،
  شماره ثبت/شماره ملی، شماره اقتصادی، شهر، نشانی کامل، کدپستی و تلفن — همان
  ترتیب نمونه. مقادیر از snapshot بند ۲ می‌آیند؛ اگر فاکتور پیش از وجود snapshot
  صادر شده باشد از پروندهٔ جاری خوانده می‌شود و **پانویس می‌گوید که چنین شده**.
- **جدول یازده‌ستونی** با **مالیات روی هر سطر**، دقیقاً مثل نمونه.
- **مبلغ به حروف، به ریال** (بند ۹.۲) — `billing/words.py`.
- **جای مهر و امضا** برای فروشنده و خریدار (بند ۹.۴). **بارکد و QR ندارد**
  (بند ۹.۳).

> **چرا ستون مالیات محاسبه می‌شود و ذخیره نمی‌شود.** سند ذخیره‌شده یک عدد مالیات
> در سرصفحه دارد که روی جمعِ پس از تخفیف حساب شده — تنها شکلی که می‌شود آن را با
> `total_amount` سنجید. نمونهٔ چاپی مالیات را سطر‌به‌سطر می‌خواهد، پس ستون‌ها
> ساخته می‌شوند و **یک قاعده مهم است: جمعشان باید دقیقاً همان چیزی باشد که ذخیره
> شده.** تخفیف سرصفحه و مالیات به نسبت سهم هر سطر پخش می‌شوند و **باقی‌ماندهٔ
> گِرد کردن به سطر آخر** می‌رود. سندی که جمع ستون‌هایش با پاورقی خودش نخواند،
> از سندی بدون آن ستون‌ها بدتر است. هشت تست همین را می‌بندند.

### بند ۸ — ابطال و صدور مجدد

- **`reissue_invoice()`** و `POST /api/v1/invoices/<id>/reissue/`: فاکتور صادرشده
  باطل می‌شود و **یک پیش‌نویس تازه** با همان اقلام و سرصفحه ساخته می‌شود، در یک
  تراکنش. پیش‌نویس و نه فاکتور صادرشده — چون معمولاً همان چیزی که باعث صدور مجدد
  شده باید اصلاح شود، و شمارهٔ رسمی هم باید در لحظهٔ صدور گرفته شود.
- **دلیل ابطال در توضیحات خود فاکتور نوشته می‌شود** (بند ۸.۲)، با پیشوند
  `[ابطال]` و **افزوده به** یادداشت قبلی، نه جایگزین آن. رویدادنگار همین را ثبت
  می‌کند، ولی رویدادنگار چاپ و تحویل نمی‌شود؛ توضیحات می‌شود.
- صدور مجدد **هیچ قاعده‌ای را سست نمی‌کند**: فاکتوری که پول رویش تخصیص خورده
  همچنان تا آزادسازی تخصیص‌ها رد می‌شود.
- ۸.۱ (بدون ریست سالانه) و ۸.۳ (بدون محدودیت زمانی) از قبل درست بودند.

### بند ۷ — گزارش‌ها روی **مبنای نقدی**

پاسخ «نقدی» یک پاسخ است ولی سه چیز را هم‌زمان عوض می‌کند، پس هر سه تثبیت شده‌اند:

- **دوره روی تاریخ دریافت پول فیلتر می‌شود، نه تاریخ صدور فاکتور.** فاکتوری که
  فروردین صادر و اردیبهشت پرداخت شده، مال اردیبهشت است.
- **فاکتور صادرشدهٔ پرداخت‌نشده هیچ سهمی ندارد.** آن یک مطالبه است و جایش گزارش
  مطالبات است.
- **فاکتور نیمه‌پرداخت به اندازهٔ پرداخت‌شده‌اش سهم دارد، و بهای تمام‌شده به همان
  نسبت شناسایی می‌شود** — وگرنه فروشی که نصفش وصول شده، تمام هزینه‌اش را مقابل
  نصف درآمدش نشان می‌داد و زیانی گزارش می‌شد که رخ نداده.

`basis="accrual"` رفتار قبلی را نگه می‌دارد. انتخاب مالک محصول نیست و هیچ‌جای پنل
آن را صدا نمی‌زند؛ هست تا **تفاوتی که این پاسخ ایجاد کرده دیدنی و آزمودنی باشد**،
نه فقط ادعاشده.

- **۷.۶** بدون قاعدهٔ دوم حل می‌شود: فاکتور ابطالی تخصیص‌هایش آزاد شده، پس چیزی
  از آن وصول نشده؛ و تخفیف هم از قبل بیرون است چون وصولی مقابل مبلغ پس از تخفیف
  سنجیده می‌شود.
- **۷.۴** با بند ۴.۲ حل شد: چک وصول‌نشده حالا دریافت‌شده حساب می‌شود و وضعیتش
  «در انتظار» است.
- **۷.۲، ۷.۳، ۷.۷** از قبل درست بودند — گزارش سود همین حالا **بازهٔ دلخواه با
  تاریخ شمسی** می‌گیرد. ۷.۵ (نبود ماژول هزینه) کاری لازم نداشت.

### بند ۶ — دفتر برای بازاریاب

- **قابلیت تازهٔ `ledger.own`** به نقش بازاریاب داده شد و `ledger_entries_for`
  او را به **مشتریان خودش** محدود می‌کند، با استفادهٔ دوباره از `customers_for`
  نه با نوشتن قاعدهٔ دوم.
- **عمداً `ledger.company` گشاد نشد.** اجازه و دامنهٔ دسترسی دو کنترل جدا هستند:
  این می‌گوید بازاریاب اصلاً می‌تواند دفتری ببیند، و selector می‌گوید دفترِ چه
  کسی. گشاد کردن `ledger.company` یعنی دادن دید سراسری و بعد پس گرفتنش با یک
  queryset — همان شکلی که یک فیلتر جاافتاده را به مانده‌ی همهٔ مشتریان تبدیل
  می‌کند.
- ۶.۱ (مانده مثبت = بدهکاری مشتری) از قبل در خود مدل نوشته شده بود.

### بند ۵ — کاری لازم نداشت

- **۵.۱** «برگشت نداریم؛ بازپرداخت را پرداختی ثبت کن» — از ۱.۲.۱ همین است:
  پرداختی وجود دارد و `allocate_payment` هر چیزی جز دریافتی را رد می‌کند.
- **۵.۲** «مدیر اصلی ثبت و تایید می‌کند، فعلا» — از قبل همین است: ثبت سند مالی
  فقط برای نقش‌های ارشد است و مرحلهٔ تایید جدا وجود ندارد.

### رویدادهای سامانه فارسی شدند

«رویداد های سامانه باید فارسی باشند چون سایت و پنل فارسی است.»

- `auditlog/labels.py` — نام فارسی برای **هر ۶۶ عملیاتی که کد واقعاً ثبت می‌کند**.
- **مقدار ذخیره‌شده عوض نشده.** `ActivityLog.operation` همان شکل ASCII‌ـش را نگه
  داشته، چون قرارداد ماشین است: رویدادنگار با آن فیلتر می‌شود، API آن را می‌دهد،
  و ردیف‌های نوشته‌شده آن را دارند. ترجمه هنگام نوشتن یعنی هر ردیف تاریخی با نامی
  که ردیف‌های تازه دارند پیدا نشود، و یعنی متن نمایشی داخل رکورد حسابرسی — همان
  جدولی که باید از همه سخت‌تر بازنویسی شود.
- ترجمه در `operation_display` سریالایزر می‌نشیند، **کنار** مقدار ذخیره‌شده نه
  به‌جایش.
- عملیات بدون ترجمه به شکل ذخیره‌شده‌اش نمایش داده می‌شود — زشت، ولی هرگز خالی.
- **یک تست کد را می‌خواند** و هر `operation=` را با این جدول می‌سنجد، پس افزودن
  رویداد بدون ترجمه همین‌جا گرفته می‌شود نه در پنل. تستی هم هست که مطمئن شود
  برچسب‌ها واقعاً فارسی‌اند و دو رویداد یک نام ندارند.

### آنچه هنوز باز است

**فقط بند ۶.۲.** مالک محصول در پاسخ به «کدام رویدادها به دفتر پست شوند؟» پرسیده
بود «دفتر چیست؟». توضیح داده شد؛ تا پاسخ ندهد این یک بند باز می‌ماند. بقیهٔ
`OPEN_FINANCIAL_QUESTIONS.md` بسته است.

---

## [1.3.1] — ۲۰۲۶-۰۸-۲۷

پاسخ‌های مالک محصول به پرسش‌های باز مالی: بندهای ۱، ۲، ۳ و ۴ پیاده شدند.

### یادداشت شماره‌گذاری — این نسخه از جدول بالا پیروی نمی‌کند

**این نسخه به تصمیم صریح مالک محصول `1.3.1` شماره خورده است** («این تغییرات ورژن
۱.۳.۰ میشن و فعلا سراغ ۲.۰.۰ نرو»، و سپس «put the version 1.3.1»). تحلیل زیر
نگه داشته شده تا بعداً کسی این را نقض ناخواستهٔ جدول نخواند — این دومین باری است
که شمارهٔ نسخه به تصمیم مالک محصول از تحلیل جدا می‌شود؛ بار قبل `1.2.1` بود.

طبق جدول همین فایل، MAJOR یعنی «یک قاعدهٔ حسابداری تغییر معنا می‌دهد» یا «مهاجرتی
لازم است که برگشت‌پذیر نیست». با آن تعریف، این تغییر **MAJOR است، نه PATCH**، و
دو دلیل دارد:

- **چک حالا پیش از وصول، حساب مشتری را بستانکار می‌کند.** معنای «مانده مشتری» عوض
  شده است. همان مشتری با همان چک، امروز عدد دیگری نشان می‌دهد. این تنها یک قابلیت
  تازه نیست؛ تفسیر هر مانده‌ای در محصول را جابه‌جا می‌کند.
- **مقادیر وضعیت چک در API عوض شده‌اند.** `registered`/`deposited`/`returned`/
  `cancelled` دیگر وجود ندارند. هر مصرف‌کننده‌ای که این رشته‌ها را می‌شناخت
  می‌شکند.

آنچه این خطر را کوچک می‌کند و احتمالاً پشتوانهٔ تصمیم مالک محصول است: تنها
مصرف‌کنندهٔ API همین پنل است و همراه با آن به‌روز شده، و مهاجرت — برخلاف تعریف
MAJOR — **برگشت‌پذیر است و هیچ داده‌ای را پاک نمی‌کند** (پایین‌تر).

### تغییر کرد

- **رند کردن مبلغ حالا رو به بالا است، نه نیم‌به‌بالا.** (بند ۱.۴)
  `۹۹۹٫۴۹` که پیش از این `۹۹۹` چاپ می‌شد، حالا `۱٬۰۰۰` است. قاعده این است که عددی
  که به مشتری نشان داده می‌شود نباید از آنچه بدهکار است کمتر باشد؛ بهایش حداکثر
  یک ریال بیش‌نمایی است. هر دو مسیر نمایش — `money()` در جاوااسکریپت و فیلتر
  `money` در جنگو — یک قاعده دارند و تست، رقم‌به‌رقم یکی بودنشان را نگه می‌دارد.
  **مبلغ ذخیره‌شده دست‌نخورده است**؛ این فقط نمایش است. هیچ اعشاری در پنل نمایش
  داده نمی‌شود.
- **چک دو محور شد: حالت و وضعیت.** (بند ۴.۱)
  کارکنان هیچ‌وقت چک را «یکی از هفت حالت» نمی‌دیدند؛ دو پرسش جدا داشتند. حالا:
  - **حالت** (`is_registered`): ثبت شده / ثبت نشده
  - **وضعیت** (`status`): در انتظار / وصول شده / برگشت / خرج شده
  دو محور مستقل‌اند — چکی می‌تواند ثبت‌شده و هنوز در انتظار باشد.
- **چک هنگام دریافت، حساب مشتری را بستانکار می‌کند.** (بند ۴.۲)
  پیش‌فرض `BILLING_CHEQUE_CREDITS_ON` از `cleared` به `registration` رفت. دیپلویی
  که طور دیگری حساب می‌کند، همچنان می‌تواند با تنظیمات بگوید.
- **چک برگشتی، مبلغ را به مانده فاکتور برمی‌گرداند.** (بند ۴.۳)
  چون اعتبار هنگام دریافت داده شده، برگشت باید آن را پس بگیرد. از مسیر
  `cancel_payment` انجام می‌شود، پس تخصیص‌ها آزاد می‌شوند و **هر دو حرکت در دفتر
  می‌مانند** — برگشت قابل حسابرسی است، نه چکی که انگار هرگز نبوده.

### اضافه شد

- **`Cheque.registered_on`** — تاریخ ثبت چک، که اپراتور می‌گوید و معمولاً پیش از
  روزی است که به دست ما رسیده. (بند ۴.۴) تاریخ روز همان `created_at` است که سامانه
  مهر می‌زند و کسی ویرایشش نمی‌کند؛ تاریخ سررسید همان `due_date` است.
- **`Cheque.is_registered`** و فیلتر `is_registered=true|false` روی
  `GET /api/v1/cheques/`.
- **`Cheque.legacy_status`** — وضعیت هفت‌مقداری پیش از ۱.۳.۱، عیناً نگه‌داشته.

### مهاجرت

`billing/migrations/0011` **برگشت‌پذیر است و هیچ چیز را پاک نمی‌کند.** مقدار
اصلی هر ردیف پیش از بازنویسی در `legacy_status` کپی می‌شود و مهاجرت معکوس دقیقاً
همان را برمی‌گرداند.

> **یک فرض که باید تأیید شود.** `returned` (عودت به مشتری) و `cancelled`
> (لغوشده) در میان چهار وضعیت تازه جایی ندارند. هر دو به `pending` نگاشت شده‌اند،
> نه به `bounced` — چون «برگشت» یعنی بانک پرداخت نکرده، و دربارهٔ این ردیف‌ها
> چنین چیزی رخ نداده. این نگاشت **پولی را زنده نمی‌کند**: پرداخت زیر هر کدام
> پیش‌تر لغو شده بود و همهٔ اعداد محصول از وضعیت پرداخت می‌آیند، نه از وضعیت چک.
> اثرش فقط برچسبی است که در فهرست چک‌ها دیده می‌شود. اگر برچسب جدا لازم است،
> `legacy_status` حقیقت را دارد و یک مهاجرت بعدی بدون هیچ از دست رفتنی می‌تواند
> اصلاحش کند.

### رفع شد

- فهرست مقادیر مجاز در قید `cheque_status_valid` دیگر دستی نگه‌داشته نمی‌شود؛ از
  `CHEQUE_STATUS_VALUES` می‌آید که کنار خود enum است. این فهرست دو بار از enum عقب
  افتاده بود و هر دو بار در تولید شکست. یک تست حالا یکی بودنشان را می‌بندد.

### بند ۲ — هویت و نشانی، هنگام صدور روی سند قفل می‌شود

فاکتور صادرشده سند چیزی است که **قبلاً اتفاق افتاده**، پس نباید بعداً عوض شود.
تا پیش از این، هویت خریدار زنده خوانده می‌شد؛ یعنی اصلاح یک غلط تایپی در شمارهٔ
اقتصادی یک مشتری، **بی‌صدا هر سند مالیاتی صادرشده به او را بازنویسی می‌کرد** —
دقیقاً آن یک کاری که یک سند مالیاتی هرگز نباید بکند.

- **۱۵ ستون تازه روی `Invoice`**: نام، شماره ملی/شناسه ملی، شماره اقتصادی، نشانی،
  کدپستی، شهر و تلفنِ **خریدار**؛ و همان‌ها به‌علاوهٔ شمارهٔ ثبت برای **فروشنده**.
- **فقط یک بار نوشته می‌شوند، توسط `issue_invoice`**، در همان تراکنش تغییر وضعیت.
  پس فاکتور صادرشده هرگز بدون snapshot وجود ندارد.
- **نشانی از پروندهٔ مشتری می‌آید و به هیچ عنوان دستی وارد نمی‌شود** (خواستهٔ صریح
  مالک محصول). این با «نبودِ راه نوشتن» تضمین شده، نه با یک اعتبارسنجی: هیچ فیلد
  serializer، هیچ ورودی فرم و هیچ مسیر API‌ای این ستون‌ها را نمی‌نویسد. **یک تست
  همین را می‌بندد** و اگر روزی یکی از این ستون‌ها نوشتنی شود، شکست می‌خورد.
- **فاکتور غیررسمی هم snapshot می‌گیرد.** آن هم چاپ و تحویل می‌شود؛ دلیلی ندارد
  نسخهٔ آن جابه‌جا شود در حالی که نسخهٔ رسمی ثابت می‌ماند.
- هویت فروشندهٔ دیپلوی از محیط می‌آید: `KARIZ_SELLER_REGISTRATION_NUMBER`،
  `KARIZ_SELLER_ADDRESS`، `KARIZ_SELLER_POSTAL_CODE`، `KARIZ_SELLER_CITY`،
  `KARIZ_SELLER_PHONE`. نبودشان مانع صدور نمی‌شود؛ فقط نام و شناسه و شماره
  اقتصادی لازم‌اند.

ستون‌ها روی ردیف‌های قدیمی خالی‌اند، و خالی یعنی «snapshot گرفته نشده».

### بند ۳ — یک دریافت بین چند فاکتور، و حذف اضافه‌پرداخت

- **`allocate_payment_across()`** و `POST /api/v1/payments/<id>/allocate-across/`:
  یک دریافت، چند فاکتور، **یک ثبت**. (بند ۳.۱ و ۳.۲) روی صفحهٔ دریافت، بخش تازهٔ
  «تقسیم بین چند فاکتور» جایی است که اپراتور می‌گوید سهم هر فاکتور چقدر است؛ خالی
  گذاشتن یعنی تا سقف مانده همان فاکتور.
- **همه‌اش یا هیچ‌کدام.** تقسیم یک تصمیم است که در یک صفحه گرفته می‌شود؛ اجرایی که
  دو فاکتور را تسویه کند و روی سومی بشکند، اپراتور را با وضعیتی رها می‌کند که
  هرگز انتخابش نکرده. تراکنش مشترک یعنی تنها چیزی که اتفاق افتاده، همان خطایی است
  که گزارش شده.
- **هیچ قاعده‌ای برای دسته‌ای بودن سست نمی‌شود**: همان مشتری، فقط فاکتور صادرشده،
  هرگز بیش از مانده فاکتور، هرگز بیش از باقی‌ماندهٔ دریافت. هر سطر از خود
  `allocate_payment` می‌گذرد.
- **پس از هر تخصیص، از مانده هر فاکتور کم و به `paid_amount` آن اضافه می‌شود** —
  همان چیزی که خواسته شد.
- **پرداخت جزئی همچنان مجاز است** (بند ۳.۳)، داخل تقسیم هم.
- **اضافه‌پرداخت حذف شد** (بند ۳.۴). دریافتی که از بدهی مشتری بیشتر باشد **رد
  می‌شود**، پیش از آنکه چیزی نوشته شود.

> **یک قید عمدی روی بند ۳.۴.** این قاعده فقط وقتی اعمال می‌شود که مشتری دست‌کم یک
> فاکتور صادرشده داشته باشد. دریافت از مشتری‌ای که هیچ فاکتوری ندارد، «پول روی
> حساب» است نه اضافه‌پرداخت — و بند ۳.۱ صریحاً می‌گوید دریافت می‌تواند
> تخصیص‌نیافته بماند و بعداً تخصیص یابد، که دقیقاً همین است. سنجیده شد: خواندن
> سخت‌گیرانه‌تر **۵۰ تست** را می‌شکست، یعنی گرفتن بیعانه پیش از فاکتور در این
> محصول جریان عادی است. با این قید، ۱ تست تغییر کرد — همانی که عمداً
> اضافه‌پرداخت می‌ساخت تا رد شدنش را بسنجد.

بازپرداخت از این مسیر نمی‌گذرد: طبق پاسخ بند ۵.۱، بازپرداخت به‌صورت **پرداختی**
ثبت می‌شود.

### آنچه هنوز انجام نشده

بندهای ۵، ۶ (از جمله «دفتر چیست؟» که باید توضیح داده و دوباره پرسیده شود)، ۷،
۸ (ابطال و صدور مجدد) و ۹ (چاپ شبیه نمونهٔ `kala-1.pdf`، مبلغ به حروف، مالیات
ستونی روی هر سطر). و فارسی‌کردن رویدادهای سامانه، که آخرین کار است.

---

## [1.2.1] — 2026-08-26

اسناد مالی دوجهته: دریافتی و پرداختی، برای هر سه نوع سند.

### یادداشت شماره‌گذاری — این نسخه از جدول بالا پیروی نمی‌کند

**این نسخه به تصمیم صریح مالک محصول `1.2.1` شماره خورده است.** طبق جدول ابتدای
همین فایل، PATCH یعنی «نه فیلدی اضافه می‌شود نه رفتاری عوض»، و این تغییر پنج
فیلد و دو مقدار enum تازه دارد و رفتار تازه می‌آورد — یعنی با آن تعریف **MINOR
است، نه PATCH**. تحلیل کامل پایین آمده تا بعدا کسی این را نقض ناخواستهٔ جدول
نخواند.

چرا MINOR بود و نه MAJOR: **هیچ `RunPython`‌ای در مهاجرت‌ها نیست.** هیچ ردیف
موجودی بازنویسی یا بازتفسیر نمی‌شود؛ سه `AlterField` فقط فهرست مقادیر مجاز را
گشاد می‌کنند یا یک NOT NULL را شل می‌کنند. اگر enum چک واقعا به چهار حالت جمع
می‌شد — یعنی REGISTERED و DEPOSITED در یک مقدار ادغام می‌شدند — آن مهاجرت
برگشت‌ناپذیر بود و طبق همان جدول MAJOR می‌شد. مالک محصول تصمیم گرفت فقط `spent`
اضافه شود، پس آن اتفاق نیفتاد.

**پیامد عملی:** کسی که فقط شماره‌ها را می‌بیند، از `1.2.0` به `1.2.1` انتظار یک
رفع اشکال دارد، نه چهار مهاجرت و پنج ستون تازه. هنگام استقرار باید همان دقت یک
نسخهٔ MINOR به کار برود.

### اضافه شد

- **`Payment.direction`** با دو مقدار «دریافتی» و «پرداختی»، پیش‌فرض دریافتی.
  هر ردیف پیش از این فیلد دریافتی بوده، پس پیش‌فرض تاریخ را بازتفسیر نمی‌کند.
- **`Payment.payee`** — گیرندهٔ پول در پرداختی. متن آزاد، چون گیرنده اغلب مشتری
  نیست و ساختن مدل تأمین‌کننده برای نگه داشتن یک نام، تصمیم بزرگ‌تری از این کار
  است.
- **`Payment.customer` حالا می‌تواند خالی باشد** — ولی فقط در پرداختی. دو قید
  پایگاه داده این را نگه می‌دارند: دریافتی همچنان مشتری می‌خواهد، و پرداختی
  گیرنده می‌خواهد.
- **`Cheque.bank_account`** (شماره جاری)، **`Cheque.source`** (چک خودمان / چک
  مشتری)، و **`Cheque.paid_to`** (به چه کسی خرج شد).
- **`Cheque.Status.SPENT`** — حالت هفتم، نه جایگزین شش تای قبلی.
- **`CustomerLedgerEntry.EntryType.PAYMENT_MADE`**.
- **`spend_received_cheque()`** و `POST /api/v1/cheques/<id>/spend/`.
- **نمودار «مبلغ به تفکیک جهت»** کنار نمودار روش.
- ۲۱ تست تازه در `billing/tests/test_bidirectional_payments.py`.

### قاعده‌های رفتاری

- **فقط دریافتی به فاکتور تخصیص می‌خورد.** پرداختی هرگز مانده فاکتور را کم
  نمی‌کند؛ اگر می‌کرد، پولی که از شرکت خارج شده بدهی مشتری را کم می‌کرد. این در
  سرویس اعمال می‌شود نه با قید پایگاه داده، چون یک CheckConstraint روی
  PaymentAllocation نمی‌تواند ستون direction را که روی جدول دیگری است بخواند —
  و همین در تست تثبیت شده.
- **دریافتی حساب مشتری را بستانکار می‌کند، پرداختی بدهکار.** جهت از قاعده‌ای
  می‌آید که خود `CustomerLedgerEntry` دربارهٔ خودش نوشته است («بدهکار یعنی
  بدهی مشتری بیشتر می‌شود»)، نه از قاعده‌ای که اینجا اختراع شده باشد.
- **پرداختی بدون مشتری هیچ سطری در دفتر نمی‌سازد** و ساختارا نمی‌تواند: مشتری
  روی `CustomerLedgerEntry` فیلد اجباری است.
- **خرج کردن چک هیچ چک یا سند تازه‌ای نمی‌سازد.** همان چک ثبت‌شده است که به
  دیگری داده می‌شود؛ ساختن ردیف دوم یعنی همان مبلغ دوبار شمرده شود. سند دریافتِ
  زیر آن بسته می‌شود، چون پول دیگر از آن مسیر نمی‌آید.
- **چک سپرده‌شده به بانک قابل خرج کردن نیست** — دست ما نیست.

### رفع شد

- **نمودار «دریافتی به تفکیک روش» حالا `direction=receipt` را فیلتر می‌کند.**
  بدون آن، از این پس مبالغ پرداختی هم رویش جمع می‌شد و عددی گزارش می‌شد که
  کاملا موجه به نظر می‌رسید و غلط بود. هیچ عدد موجودی عوض نمی‌شود، چون هر ردیف
  امروز دریافتی است.
- دو قید `CHECK` موجود فهرست مقادیر مجازشان را دستی نگه می‌داشتند و مقادیر تازه
  را نمی‌شناختند (`cheque_status_valid`، `ledger_entry_type_valid`). تست‌ها
  گرفتندشان؛ بدون آن، اولین چک خرج‌شده و اولین پرداختی به مشتری در تولید شکست
  می‌خورد.
- **نشت سطل throttle بین تست‌ها.** سطل با شناسهٔ کاربر کلید می‌خورد و تست‌های
  rollback‌شده همان شناسه‌ها را دوباره می‌سازند، پس یک تست خرجِ تست قبلی را به
  ارث می‌برد و به‌جای چیزی که بررسی می‌کرد ۴۲۹ می‌گرفت. `cache.clear()` به
  `setUp` اضافه شد.

### آنچه عمداً حدس زده نشد

- **بخش E همچنان باز است** جز آنچه صریحا خواسته شد: تخصیص اجباری یا اختیاری،
  اضافه‌پرداخت، برگشت پرداخت، و اینکه چه کسی ثبت و چه کسی تایید می‌کند.
- **`source=customer_endorsed` امروز روی هیچ ردیفی نوشته نمی‌شود.** طبق طرح،
  خرج کردن چک مشتری ردیف تازه‌ای نمی‌سازد، پس این مقدار جایی برای نشستن ندارد.
  در enum ماند چون خواسته شده بود، ولی نوشته نمی‌شود.
- **بند D.10 (snapshot هنگام صدور) همچنان باز است** و به این کار ربطی پیدا
  نکرد.
- مسیر `manual_paid_entry` دست‌نخورده ماند؛ یک تست تثبیت می‌کند که هیچ‌کدام از
  این تغییرها آن را تکان نداده است.

---

## [1.2.0] — 2026-08-26

فاز مالی و نمودار روی صفحات لیست، در چهار مرحله که هرکدام جدا کامل و تست شد.

**MINOR است نه PATCH:** سه فیلد تازه روی مدل، چهار مهاجرت افزایشی، و سه نقطهٔ
انتهایی تازه. هیچ قاعدهٔ کسب‌وکاری موجود عوض نشد و اختیار هیچ نقشی تغییر نکرد؛
ولی قراردادی که یک استقرار روی آن حساب می‌کند بزرگ‌تر شده است.

**آنچه عمداً حدس زده نشد:** `OPEN_BUSINESS_DECISIONS.md` بخش D بندهای ۳ تا ۶
(نرخ مالیات، شمول قیمت، ترتیب تخفیف/مالیات، رند کردن)، بند ۱۰ (snapshot هنگام
صدور)، و کل بخش E جز آنچه صریحا خواسته شد. بند ۷ (شماره‌گذاری) با تصمیم مالک
محصول بسته و پیاده شد.

---

### فاز ۱: فاکتور رسمی / غیررسمی

#### اضافه شد

- **`Invoice.invoice_type`** با دو مقدار «رسمی» و «غیررسمی»، پیش‌فرض غیررسمی.
  این پاسخ به پرسش D.2 در `OPEN_BUSINESS_DECISIONS.md` است: این استقرار هر دو
  را صادر می‌کند و تمایز در سطح سند ثبت می‌شود، نه در سطح استقرار.
- **`Customer.economic_code`** — ایران دو شناسهٔ متفاوت می‌دهد: شخص حقیقی کد
  ملی دارد (همان `national_id` موجود) و شخص حقوقی شناسهٔ ملی **به‌علاوهٔ** شمارهٔ
  اقتصادی. این دو عدد از هم مشتق نمی‌شوند، پس دو ستون جدا دارند.
- **هویت فروشنده در سطح استقرار** — `KARIZ_SELLER_LEGAL_NAME`،
  `KARIZ_SELLER_NATIONAL_ID`، `KARIZ_SELLER_ECONOMIC_CODE`. خالی بودنشان معنادار
  است: استقراری که پرشان نکرده نمی‌تواند فاکتور رسمی صادر کند و در لحظهٔ صدور رد
  می‌شود، به‌جای چاپ سند مالیاتی با فروشندهٔ خالی.
- **دو مهاجرت افزایشی**: `billing/0006_invoice_invoice_type.py` و
  `sales/0019_customer_economic_code.py`.
- ۱۳ تست تازه در `billing/tests/test_official_invoice.py`.

#### تغییر کرد

- صدور فاکتور رسمی وقتی هویتی کم باشد **رد می‌شود** و همهٔ موارد کم را یک‌جا
  گزارش می‌کند. بررسی در لحظهٔ صدور است نه پیش‌نویس، تا اپراتور بتواند سند را
  بسازد و بعد هویت‌ها را کامل کند.
- فرم فاکتور انتخابگر نوع دارد و زیر آن فهرست چیزهایی که هنوز کم است دیده
  می‌شود. آن فهرست فقط آینهٔ سرور است؛ تصمیم با سرویس است.

- **سری شمارهٔ رسمی، جدا و بدون‌شکاف** (`Invoice.official_number`) — تصمیم مالک
  محصول در پاسخ به D.7. سری `OINV-` مستقل از `INV-` است.
  - شماره **در لحظهٔ صدور** گرفته می‌شود، نه هنگام ساخت. اگر هنگام ساخت گرفته
    می‌شد، پیش‌نویسی که رها شود یا رسمی ساخته و بعد غیررسمی شود، شماره‌ای از این
    سری را مصرف کرده بود که روی هیچ سند مالیاتی نمی‌نشیند — یعنی دقیقاً همان
    شکافی که این سری نباید داشته باشد.
  - **لغو، شماره را آزاد نمی‌کند.** معنای بدون‌شکاف همین است.
  - دو قید پایگاه داده: یکتایی مشروط (خالی‌ها مستثنا) و اینکه فقط فاکتور رسمی
    می‌تواند شمارهٔ رسمی داشته باشد.

#### آنچه عمداً پیاده **نشد**

`OPEN_BUSINESS_DECISIONS.md` بخش D بندهای ۳ تا ۶ هنوز باز است: نرخ مالیات،
شمول یا عدم شمول قیمت، ترتیب تخفیف نسبت به مالیات، و قاعدهٔ رند کردن. هیچ‌کدام
حدس زده نشد. (بند ۷ — شماره‌گذاری — با تصمیم مالک محصول بسته شد و بالا پیاده
شده است.) یک تست صریح تثبیت می‌کند که فاکتور رسمی و
غیررسمی با همان سطرها **همان ارقام** را می‌دهند؛ اگر روزی فرق کنند آن تست
می‌شکند، و باید بشکند.

---

---

### فاز ۲: اسناد مالی / دریافت‌ها

#### تغییر کرد

- **فرم ثبت دریافت به حالت (mode) تبدیل شد**، به‌جای یک فرم با فهرست کشویی. هر
  روش فقط فیلدهای خودش را نشان می‌دهد:
  - **نقدی** — شماره رسید، تاریخ دریافت، مبلغ.
  - **حواله بانکی** — نام بانک، شماره حساب/شبا، شماره پیگیری، تاریخ، مبلغ.
  - **چک** — از همان مدل `Cheque` موجود؛ **در این دور دست نخورد** و اصلاحش به
    فاز بعد موکول شد.
  - **کارت** — حذف **نشد**؛ داده‌های قبلی روی آن هست. آخرین حالت است و کم‌رنگ‌تر،
    ولی کاملا کار می‌کند.
- برچسب فیلد مرجع با روش عوض می‌شود («شماره رسید» در برابر «شماره پیگیری»). روی
  رکورد یک ستون است، چون یک مفهوم است؛ ولی دو کلمه برای کسی که تایپش می‌کند
  یکی نیستند.
- مبلغ حالا از همان اینپوت ریالی گروه‌بندی‌شوندهٔ بقیهٔ پنل استفاده می‌کند، و
  تاریخ دریافت اینپوت جلالی دارد (خالی یعنی «حالا»).

#### اضافه شد

- **`Payment.bank_name` و `Payment.bank_account`** — حواله بانکی تا امروز جایی
  برای بانک مبدأ و شماره حساب نداشت. هر دو اختیاری‌اند: اپراتوری که حواله را از
  روی صورتحساب ثبت می‌کند ممکن است شمارهٔ حساب را نداشته باشد، و رد کردن رسید به
  آن دلیل یعنی پول اصلا ثبت نشود.
- **یک قید پایگاه داده**: این دو ستون فقط روی `bank_transfer` می‌نشینند. رسید
  نقدی‌ای که شماره حساب داشته باشد یعنی فرم در رکورد اشتباه نوشته است.
- مهاجرت افزایشی `billing/0008_payment_bank_account_payment_bank_name_and_more.py`.
- ۹ تست تازه در `billing/tests/test_payment_methods.py`، به‌علاوهٔ تست مرورگر که
  خود حالت‌ها را کلیک می‌کند.

#### آنچه عمداً دست نخورد

`Cheque` و چرخهٔ وضعیتش، تخصیص پرداخت به فاکتور، اضافه‌پرداخت، و برگشت پرداخت —
همگی در `OPEN_BUSINESS_DECISIONS.md` بخش E باز هستند و هیچ‌کدام حدس زده نشد. چک
همچنان تا وصول، حساب مشتری را بستانکار نمی‌کند.

---

---

### فاز ۳: زیرساخت نمودار + صفحهٔ مشتریان

#### تصمیم

گزینهٔ **الف** انتخاب شد: `renderLineChart` دست‌نویس با SVG، کنار
`renderBarChart`. دلیلش در `docs/frontend/CHARTS_GROUNDWORK.md` ثبت شد — سری
زمانی اینجا ده‌ها نقطه دارد نه هزاران (اندپوینت بیش از ۱۲۰ سطل را رد می‌کند)،
ApexCharts همچنان فقط داخل باندل ۳٫۵ مگابایتی در دسترس است، و RTL و تاریخ جلالی
و رقم فارسی باید برای هر نمودار جداگانه پیکربندی می‌شد به‌جای اینکه یک‌بار نوشته
شود. گزینهٔ ب برای روزی باز ماند که واقعا نقطهٔ زیاد یا zoom لازم شود.

#### اضافه شد

- **`renderLineChart`** — سری زمانی با SVG دست‌نویس. مقیاس با `viewBox`، پس روی
  موبایل بدون کد اندازه‌گیری خوانا می‌ماند. محور راست‌به‌چپ است. هر نقطه یک
  `<title>` دارد، یعنی تولتیپ بومی و قابل خواندن برای صفحه‌خوان، بدون یک خط کد
  موقعیت‌دهی.
- **دو اندپوینت تازه** در `reports/customer_views.py`:
  - `GET /api/v1/reports/customer-cities/` — پراکندگی شهری با درصد.
  - `GET /api/v1/reports/customer-growth/` — تعداد ثبت‌شده در هر هفته/ماه به
    همراه جمع تجمعی.
- **دو نمودار زیر جدول مشتریان**: پراکندگی شهری (میله‌ای) و روند رشد (خطی، با
  انتخاب هفتگی/ماهانه/بازهٔ دلخواه).
- ۱۸ تست در `reports/tests/test_customer_insights.py` و ۲۰ بررسی رفتاری روی خود
  رندرکننده.

#### جزئیاتی که عمدی‌اند

- **سطل خالی صفر گزارش می‌شود، نه حذف.** خطی که میان دو نقطه با فاصلهٔ سه‌ماهه
  کشیده شود در حالی که بقیهٔ فاصله‌ها یک‌ماهه است، دربارهٔ شیب دروغ می‌گوید.
- **خط تجمعی از جایی شروع می‌شود که دفتر واقعا ایستاده بود** — مشتریان قدیمی‌تر
  از بازه، ارتفاع شروع خط‌اند، نه صفر.
- **مشتری بدون شهر با استانش شمرده می‌شود؛ بدون هیچ‌کدام در ردیف «ثبت‌نشده».**
  حذفشان یعنی همهٔ درصدها سهمشان را بیش از واقع نشان دهند.
- **هر دو نمودار روی `customers_for` ساخته شده‌اند**، نه روی `Customer.objects`.
  بازاریاب فقط دفتر خودش را می‌شمارد — و فقط دفتر حقیقی را، چون همان selector
  این را هم اعمال می‌کند. تست‌ها هر دو را تثبیت می‌کنند.

#### رفع شد

- تست `test_the_script_carries_no_placeholder_or_third_party_reference` هر
  `http://` را رد می‌کرد و فضای‌نام XML لازم برای `createElementNS` را هم گرفت.
  آن رشته آدرس نیست، شناسه است و هیچ مرورگری واکشی‌اش نمی‌کند. به‌جای ضعیف کردن
  قاعده، دقیقا همان یک رشته استثنا شد و **یک تست دوم اضافه شد** که تضمین می‌کند
  استثنا همان یکی بماند و به در باز تبدیل نشود.

---

---

### فاز ۴: نمودار برای بقیهٔ صفحات لیست

#### اضافه شد

- **یازده نمودار، یک جدول اعلانی** — `reports/list_charts.py`. هر ورودی سه چیز را
  صریح نام می‌برد: قابلیت استقرار، اختیارهای نقش، و سازنده‌ای که از selector همان
  ماژول شروع می‌کند. یک view به‌جای یازده تا، ولی هیچ‌کدام از آن سه کنترل مشترک
  نیست — چیزی که مشترک است فقط لوله‌کشی است.
- `GET /api/v1/reports/list-chart/<key>/` — قابلیت لازم برای هر کلید در لحظهٔ
  درخواست حل می‌شود، پس استقراری که ماژولی ندارد برای نمودارش هم ۴۰۴ می‌دهد،
  دقیقا مثل صفحه‌هایش.
- کارت نمودار روی: فاکتورها، سفارش‌ها، دریافت‌ها، محصولات، دسته‌بندی‌ها، سرنخ‌ها،
  خدمات پس از فروش، اسناد پستی، موجودی انبار، فروش‌ها، تماس‌ها.
- بارگذاری با `data-list-chart` در قالب انجام می‌شود، نه با تابعی برای هر صفحه.
  صفحهٔ دوازدهم فقط یک کارت در قالب و یک ورودی در جدول لازم دارد، بدون هیچ
  جاوااسکریپت تازه‌ای.

#### دو انحراف از فهرست درخواستی، هر دو به‌خاطر خود مدل

- **فاکتورها بر اساس وضعیت تسویه.** `settlement_status` ستون نیست؛ یک property
  پایتونی روی `is_manually_settled`، `status`، `paid_amount` و `total_amount`
  است. با **خواندن همان property** شمرده می‌شود، نه با بازنویسی قاعده در SQL —
  چون آن روز که دو نسخه از هم فاصله بگیرند، نمودار بی‌صدا با فاکتوری که از آن
  آمده تناقض پیدا می‌کند.
- **ارزش موجودی** هم ستون نیست؛ `quantity * average_cost` است و در همان کوئری
  جمع زده می‌شود.

#### جزئیاتی که عمدی‌اند

- **دریافت لغوشده شمرده نمی‌شود.** نمودار پول دریافتی نباید پولی را بشمارد که
  پس گرفته شده است.
- **فروش لغوشده در نمودار بازاریاب نیست.**
- **دسته‌بندی بدون محصول فعال، با صفر دیده می‌شود** نه اینکه از صفحهٔ خودش غیب
  شود (`filter=` روی aggregate، نه روی queryset).
- **دنبالهٔ بلند در ردیف «سایر» جمع می‌شود، نه حذف** — تا جمعی که خواننده
  می‌بندد با فهرست بالای همان صفحه بخواند.
- **خطای نمودار فقط همان کارت را می‌گیرد.** نقشی که اختیارش را ندارد یا استقراری
  که ماژول را ندارد، فهرست بالای صفحه‌اش دست‌نخورده کار می‌کند.

#### تست

۱۴ تست تازه که **هر یازده سازنده را اجرا می‌کنند** — روی پایگاه دادهٔ خالی، با
دامنهٔ مدیر، و با دامنهٔ بازاریاب. هیچ‌کدام نمودارها را تک‌تک نام نمی‌برند، پس
نمودار دوازدهم از همان لحظهٔ اعلان پوشش داده می‌شود. خطای املایی در نام یک فیلد
یا اختیاری که هیچ نقشی ندارد، اینجا می‌شکند نه روزی که کسی آن یک صفحه را باز کند.

---

---

## [1.1.3] — 2026-08-26

نسخهٔ نمودارها و پاک‌سازی. هیچ مهاجرتی ندارد، هیچ قاعدهٔ کسب‌وکاری عوض نشده و
اختیار هیچ نقشی تغییر نکرده است.

### اضافه شد

- **یک رندرکنندهٔ واحد نمودار میله‌ای** — `renderBarChart` در
  `forooshbin-app.js`. ورودی‌اش `{label, value, display}` است: `value` طول میله
  را تعیین می‌کند و `display` همان متنی است که خواننده می‌بیند و از قبل قالب‌بندی
  شده. جدا نگه داشتن این دو کل طراحی است — با این کار یک نمودار نمی‌تواند
  به‌اشتباه همان عددی را چاپ کند که با آن اندازه گرفته.
  - گزینه‌ها: `ariaLabel`، `limit` (چند تای برتر)، `sort` (برای دنبالهٔ ثابت
    خاموش)، `keepZero` (دستهٔ خالی را حذف نکن، خالی بکش).
  - **هیچ کتابخانه‌ای اضافه نشد.** ApexCharts داخل باندل ۳٫۵ مگابایتی پوستهٔ
    خریداری‌شده است که `collectstatic` آن را کنار می‌گذارد، و هر نموداری که این
    گزارش‌ها لازم دارند مقایسه‌ای میان چند سطر است. مبنای تصمیم در
    `docs/frontend/CHARTS_GROUNDWORK.md` نوشته شده است.
- **سه نمودار تازه:**
  - **سنی مطالبات** — پنج بازهٔ سررسید به ترتیب ثابت. بازهٔ خالی حذف نمی‌شود؛
    «هیچ مطالبهٔ بیش از ۹۰ روز نداریم» خبر خوبِ خواننده است، نه دادهٔ غایب.
  - **ترکیب درآمد** — درآمد، بهای تمام‌شده و سود ناخالص کنار هم. سود منفی میله
    ندارد ولی رقمش چاپ می‌شود، پس زیان با «چیزی برای کشیدن نبود» پنهان نمی‌ماند.
  - **ارزش موجودی** — ده کالای با بیشترین ارزش؛ بقیه در جدول پایین می‌مانند.
- **`scripts/build-image.sh`** — تگ را از `VERSION` می‌خواند، پس نمی‌تواند نسخه‌ای
  را نام ببرد که داخل ایمیج نیست، و هر دو build-arg لازم Dockerfile را می‌دهد.
- **`docs/frontend/CHARTS_GROUNDWORK.md`**.

### تغییر کرد

- **دو رندرکنندهٔ نمودار به یکی تبدیل شدند.** پیش از این دو کپیِ تقریبا یکسان
  وجود داشت که از هم فاصله گرفته بودند — و همین دو نقص زیر را ساخته بود.
- **مستندات: پنج سند حذف شد** — `RELEASE_NOTES.md` و `SOURCE_MANIFEST.md` (هر دو
  در سطر اول خودشان را «historical, not live status» اعلام کرده بودند)،
  `DEPLOYMENT_BOOTSTRAP.md` (داخل بخش ۱.۱۵ رانبوک ادغام شد)، و دو گزارش
  پاک‌سازیِ تمام‌شده. تاریخچهٔ Git همه را نگه داشته است.

### رفع شد

- **هر استقرار تازه در همان بوت اول شکست می‌خورد.** `new_deployment.py` رازها را
  با `token_urlsafe(32)` می‌ساخت که ۴۳ کاراکتر است، در حالی که
  `config/production_env.py` هر `DJANGO_SECRET_KEY` زیر ۵۰ کاراکتر را رد می‌کند.
  هنوز هیچ استقراری با آن ابزار ساخته نشده بود، پس کسی متوجه نشده بود. تست
  کنارش حداقل ۱۶ را بررسی می‌کرد — قاعدهٔ اسکریپت bootstrap برای رمز پایگاه
  داده — و راضی بود. حالا فایل تولیدشده به خودِ `validate_production_environment`
  داده می‌شود؛ رازها ۶۴ کاراکترند.
- **`displayDay` دو بار تعریف شده بود** و نسخهٔ دوم برنده می‌شد، پس نگهبانِ
  `YYYY-MM-DD` خام — که روز تقویمی است و نباید از منطقهٔ زمانی رد شود — هرگز اجرا
  نمی‌شد.
- **نمودار عملکرد مبلغ خام چاپ می‌کرد** (`12500000.00`) کنار جدول‌هایی که
  `۱۲،۵۰۰،۰۰۰ ریال` می‌خواندند.
- **نمودار پیامک ورودی تاریخ میلادی و رقم لاتین نشان می‌داد** در پنلی که همه‌جایش
  جلالی است. `local_date` یک `DateField` است و خام می‌آید؛ همان چیزی که نگهبان
  بالا برایش نوشته شده بود.
- **رانبوک در حالت قفل‌شدگی گمراه می‌کرد.** نوشته بود
  `bootstrap_platform_admin` وقتی یک مدیر پلتفرم **فعال** وجود دارد اجرا نمی‌شود.
  کد `filter(role=PLATFORM_ADMIN).exists()` است، بدون هیچ شرط `is_active` — پس
  غیرفعال کردن تنها مدیر، این مسیر را باز نمی‌کند. دقیقا همان وضعیتی که این بخش
  در آن خوانده می‌شود.
- **دستور build رانبوک هرگز کار نمی‌کرد**: `docker build .` خالی با
  `base name (${PYTHON_BASE_IMAGE}) should not be blank` متوقف می‌شود.

### پس گرفته شد

- **توضیح TTY در نسخهٔ قبل اشتباه بود.** ادعا شده بود psql هنگام وجود TTY رمزها
  را از ترمینال می‌خواند و مقادیر `.env` را دور می‌ریزد. `-T` دو انتشار است که
  اعمال شده و پرسش‌ها همچنان چاپ می‌شوند، در حالی که پشتیبان‌گیریِ بلافاصله بعدش
  با همان نقش موفق است — یعنی مقادیر لوله‌شده خوانده می‌شوند. پرسش‌ها انعکاس psql
  هنگام خواندن stdin است و مسیر غیرتعاملی کنارش عمدا فقط برای پایگاه دادهٔ
  یک‌بارمصرف باز است.

---

## [1.1.2] — 2026-08-25

نسخهٔ ابزار استقرار و پرداخت‌های ظاهری. هیچ منطق کسب‌وکاری، هیچ مهاجرتی و هیچ
تغییری در اختیارات نقش‌ها ندارد؛ به همین دلیل PATCH است و نه MINOR — با اینکه
یک ابزار تازه به `scripts/` اضافه شده، آن ابزار بیرون از برنامهٔ سرویس‌شده است.

### اضافه شد

- **`scripts/new_deployment.py`** — یک استقرار تازه را از یک مجموعه پاسخ
  می‌سازد: نام پروژه، نام پایگاه داده، هر چهار نقش، هر دو والیوم و پنج راز
  تولیدشده، همه از یک `--slug` مشتق می‌شوند تا نتوانند با هم اختلاف پیدا کنند.
  فهرست قابلیت‌ها و قاعده‌های وابستگی از `common/deployment/registry.py`
  **ایمپورت** می‌شود، نه رونویسی، پس ابزار و برنامهٔ در حال اجرا هرگز از هم دور
  نمی‌افتند. چیزی نمی‌سازد و چیزی را بالا نمی‌آورد؛ یک فایل با دسترسی `0600`
  می‌نویسد و قدم‌های بعدی را چاپ می‌کند. `.env` موجود را هرگز بازنویسی نمی‌کند.
- **`scripts/deploy.sh`** — دستور یک‌مرحله‌ای انتشار، به‌همراه `--status`،
  `--rollback` و `--no-backup`. پیش از شروع سه چیز را بررسی می‌کند که سکوتشان
  گران تمام می‌شود: کهنه بودن `nginx/default.conf` در checkout، اشغال بودن پورت
  ۸۰ به‌دست پروژهٔ دیگر، و اشتراک والیوم پایگاه داده با پروژهٔ دیگر.
- **بررسی تازه:** اگر تگ ایمیج شکل `vX.Y.Z` داشته باشد و با فایل `VERSION`
  داخل همان ایمیج یکی نباشد، انتشار **انجام نمی‌شود**. دقیقاً همان اشتباهی که
  این نسخه را لازم کرد: ایمیجی با تگ `v1.1.2` ساخته شد در حالی که `VERSION`
  داخلش هنوز `1.1.1` بود، و پاورقی پنل — که راست می‌گفت — `۱.۱.۱` نشان داد.

### تغییر کرد

- **جزئیات ظاهری جدول‌ها و دیالوگ‌ها**، همه داخل پالت و قالب خریداری‌شده و بدون
  دست زدن به رنگ، فونت یا ساختار صفحه:
  - **فاصلهٔ دکمه‌های عملیات در سطرهای جدول.** این دکمه‌ها در جاوااسکریپت پشت هم
    به سلول اضافه می‌شوند، پس هیچ فاصله‌ای بینشان نبود و روی موبایل دو هدف لمسی
    به هم چسبیده بودند. روی خودِ دکمه‌ها اعمال شد نه روی کلاس سلول، چون فقط سه
    سلول از ده سلول عملیاتِ برنامه آن کلاس را دارند.
  - **ارقام هم‌عرض (tabular) برای ستون‌های عددی.** ستون‌های `dir="ltr"` — قیمت،
    شمارهٔ فاکتور، شمارهٔ تلفن — عمودی خوانده می‌شوند و رقم‌های با عرض متفاوت
    مقایسهٔ نگاهی را سخت می‌کنند.
  - **برجسته شدن سطر زیر نشانگر.** جدول مشتریان نه ستون دارد و سخت‌ترین کارِ
    خواندنش، نگه داشتن چشم روی یک سطر است. روی لمس غیرفعال است، چون مرورگر
    موبایل حالت hover را شبیه‌سازی می‌کند و روی آخرین سطر لمس‌شده گیر می‌کند.
  - **ارتفاع ورودی‌ها روی نشانگر لمسی** به ۴۴ پیکسل رسید، هم‌اندازهٔ دکمه‌های
    کنارشان؛ تراکم دسکتاپ دست‌نخورده ماند.
  - **`dvh` به‌جای `vh` برای ارتفاع دیالوگ.** `100vh` روی موبایل یعنی صفحه با
    نوارهای جمع‌شده، پس پاورقی دیالوگ — همان‌جا که دکمه‌ها هستند — زیر لبهٔ صفحه
    می‌افتاد. روی گوشی، فاصلهٔ امن (`env(safe-area-inset-bottom)`) هم رعایت شد.
  - ورود کوتاه دیالوگ (۱۶۰ms، فقط مقیاس و شفافیت تا چیزی جابه‌جا نشود)، پشت
    `prefers-reduced-motion`.
  - `.sr-only` بازنشانی `padding` و `border` را کم داشت.

## [1.1.1] — 2026-08-25 — روی صحنه‌آزمایی، آزموده و در حال اجرا

دو دفتر مشتری، و فاکتورهای مشتری در صفحهٔ جزئیات او.

> **یادداشت شماره‌گذاری.** این نسخه به درخواست مالک محصول `1.1.1` شماره خورده
> است. طبق جدول بالای همین فایل، افزوده شدن فیلد `Customer.kind`، یک مهاجرت
> افزایشی و دو نقطهٔ انتهایی تازه با تعریف **MINOR** می‌خواند، نه PATCH. شماره
> عمداً و با تصمیم مالک محصول این‌طور گذاشته شد؛ اینجا ثبت می‌شود تا بعداً کسی
> تصور نکند جدول نقض شده است.

### اضافه شد

- **دو دفتر مشتری — حقیقی و حقوقی (`Customer.kind`).** یک جدول، دو فهرست: همان
  ستون‌ها و همان فیلترها یک دفتر دیگر را می‌خوانند، به‌جای صفحهٔ دومی که همه‌چیز
  را تکرار کند. پیش‌فرض **حقیقی** است و عمداً خالی نیست — برخلاف `Product.unit`
  که خالی‌اش یعنی «هنوز ثبت نشده». اگر خالی می‌ماند، هر مشتریِ پیش از این فیلد در
  **هیچ‌کدام** از دو فهرست دیده نمی‌شد و از دفتری که بازاریاب با آن کار می‌کند
  ناپدید می‌شد.
  - مهاجرت افزایشی `sales/migrations/0018_customer_kind_customer_customer_kind_valid.py`
    (یک فیلد + یک قید بررسی).
  - دو دکمهٔ **حقیقی** و **حقوقی** زیر دکمهٔ «مشتری جدید».
  - ورودی **نوع مشتری** در فرم ساخت مشتری.
  - پارامتر `kind` روی `GET /api/v1/customers/` و روی خروجی اکسل.
- **ورودی اکسل مشتریان** — `POST /api/v1/customers/import-xlsx/`.
  - همان گردش رفت‌وبرگشت محصولات: سرستون‌های خروجی و ورودی یکی‌اند و ستون‌ها
    **بر اساس نام** تطبیق داده می‌شوند.
  - **تشخیص تکراری با شمارهٔ تلفن** انجام می‌شود — همان چیزی که در بقیهٔ این کد
    هویت مشتری است و در پایگاه داده هم قید یکتایی دارد. کد ملی هم وقتی نوشته
    شده باشد بررسی می‌شود. شماره پیش از مقایسه نرمال می‌شود، پس `09121110005` و
    `+989121110005` یک نفرند.
  - **محدودیت آگاهانه:** ردیفی که نه تلفن دارد و نه کد ملی، با هیچ‌چیز قابل
    مقایسه نیست و ساخته می‌شود.
  - لیستی که در پنجره انتخاب شده بر ستون `kind` داخل فایل **مقدم است**؛ کاربر یک
    لیست را برای ورود انتخاب کرده و پخش‌کردن ردیف‌هایش بین دو دفتر آن چیزی نیست
    که خواسته.

### تغییر کرد

- **دامنهٔ دید بازاریاب به دفتر حقیقی محدود شد** (`customers_for`). این یک قاعدهٔ
  دامنهٔ داده است، نه پنهان‌سازی در قالب: بازاریاب نه دکمه‌ها را می‌بیند، نه با
  `?kind=legal` چیزی می‌گیرد، نه می‌تواند مشتری حقوقی را با شناسه باز کند، نه
  می‌تواند یکی بسازد یا مشتری خودش را به آن دفتر منتقل کند. چون پیش‌فرض فیلد
  «حقیقی» است، هیچ مشتریِ موجودی از دید هیچ بازاریابی خارج نمی‌شود.
- **دکمهٔ «دریافت XLSX» به «خروجی لیست» تغییر نام داد** و کنارش **«ورودی لیست»**
  اضافه شد. هر دو یک پنجره باز می‌کنند که اول می‌پرسد کدام لیست؛ خروجی بعد از آن
  دکمهٔ **دانلود** می‌دهد و ورودی، **بارگذاری فایل** و دکمهٔ **بارگذاری**.
- **چیدمان فیلترهای صفحهٔ مشتریان:** جست‌وجو و مرتب‌سازی سمت راست، دکمهٔ «اعمال»
  سمت چپ، هر دو با فاصله از لبه‌های کارت.
- **ستون‌های خروجی مشتریان** حالا `kind` و `address` هم دارند، و ستون تلفن شمارهٔ
  خوانا (`raw_phone`) را می‌دهد نه شکل نرمال‌شده را — چون همان است که کاربر
  می‌خواند و دوباره می‌نویسد.
- سرویس `create_customer_with_phone` و `update_customer` حالا `kind` را
  اعتبارسنجی می‌کنند و نام فیلد درست را در خطا می‌گویند؛ قید پایگاه داده به‌تنهایی
  خطایی می‌داد که هیچ فیلدی را نام نمی‌برد.
- `nginx/default.conf` و `FILE_UPLOAD_PATHS` حالا هر دو مسیر بارگذاری را پوشش
  می‌دهند.

### حذف شد

- **باکس «سفارشات مرتبط» از صفحهٔ جزئیات مشتری**، و جایش **«فاکتورهای مرتبط»**
  نشست. هر سطر یک سند است: شماره، وضعیت، تسویه، مبلغ کل، مانده، تاریخ صدور. هر
  دو ستون تسویه و مانده نمایش داده می‌شوند چون می‌توانند با هم نخوانند — فاکتوری
  که دستی تسویه شده «پرداخت‌شده» خوانده می‌شود در حالی که ماندهٔ متعارفش
  دست‌نخورده است، و نشان‌ندادن یکی از این دو یعنی صفحه دروغ بگوید.

---

## [1.1.0] — 2026-08-24 — روی صحنه‌آزمایی؛ جداگانه تحویل نشد

نسخهٔ کاتالوگ محصولات و واحد پول. همهٔ تغییرها افزایشی‌اند: هیچ دادهٔ موجودی
دست‌کاری نمی‌شود و هیچ اختیاری از هیچ نقشی گرفته نمی‌شود.

### اضافه شد

- **واحد محصول (`Product.unit`).** فهرست ثابتِ «جعبه، عدد، کارتن، کیلوگرم، گرم»؛
  متن آزاد نیست، چون دو املای مختلف از یک واحد روی دو سند مختلف می‌نشیند و آن دو
  سند با هم اختلاف پیدا می‌کنند. مقدار خالی مجاز است و معنایش «هنوز ثبت نشده»
  است، نه یک پیش‌فرض خاموش — پیش‌فرض گذاشتن یعنی نوشتن واحد اشتباه روی فاکتور
  مشتری.
  - مهاجرت افزایشی `sales/migrations/0017_product_unit_product_product_unit_valid.py`
    (یک فیلد + یک قید بررسی). هیچ مهاجرت قدیمی بازنویسی نشده است.
  - ستون **واحد** در فهرست محصولات.
  - `unit` و `unit_display` در `ProductSerializer`.
- **خروجی اکسل کاتالوگ محصولات** — `GET /api/v1/exports/products.xlsx`.
  دامنهٔ دید دقیقاً همان دامنهٔ فهرست محصولات است.
- **ورودی اکسل محصولات** — `POST /api/v1/products/import-xlsx/`.
  - ستون‌ها **بر اساس نام سرستون** تطبیق داده می‌شوند، نه جای ستون؛ پس فایلی که
    ستون‌هایش جابه‌جا شده باشد هم درست خوانده می‌شود، و فایلی که از این سامانه
    نیامده باشد به‌کل رد می‌شود به‌جای آنکه ستون‌به‌ستون در فیلد اشتباه بنشیند.
  - سرستون‌های خروجی و ورودی **یکی‌اند**: کاربر اول خروجی می‌گیرد، رویش
    می‌نویسد و همان فایل را برمی‌گرداند.
  - **محصول تکراری اضافه نمی‌شود.** تکراری بودن از روی کد محصول (SKU) و بدون
    حساسیت به بزرگی و کوچکی حروف تشخیص داده می‌شود. ورود اطلاعات هرگز محصول
    موجود را بازنویسی نمی‌کند.
  - پاسخ سه عدد جدا می‌دهد — ثبت‌شده، تکراری، نامعتبر — و پنل هر سه را به کاربر
    می‌گوید: تعداد ثبت موفق، تعداد تکراریِ اضافه‌نشده، و تعداد ردیف نامعتبر.
  - هر ردیف از همان سرویس `create_product` می‌گذرد که فرم دستی از آن می‌گذرد، پس
    اعتبارسنجی، بررسی دسترسی و ردّ ممیزی یکسان است.
  - سقف‌ها: ۵۰۰۰ ردیف در هر بار، و ۵ مگابایت حجم فایل.
- **دو دکمهٔ «خروجی اکسل» و «ورودی اکسل»** در صفحهٔ محصولات.
- **فایل `VERSION`**، نمایش نسخه در پاورقی پنل، و همین فایل `CHANGELOG.md`.

### تغییر کرد

- **همهٔ قیمت‌های پنل به ریال و با جداکنندهٔ سه‌رقمی.** اعشار (`٫۰۰`) از نمایش
  برداشته شد — ریال در عمل واحد خردتر ندارد و `٫۰۰` دائمی فقط عدد را طولانی‌تر
  می‌کند. گرد کردن روی رشتهٔ رقم‌ها انجام می‌شود نه از راه `float`، چون مبلغ
  همان‌طور که ذخیره شده معتبر است و رفت‌وبرگشت اعشاری می‌تواند رقم آخرش را جابه‌جا
  کند. **مقدار ذخیره‌شده در پایگاه داده دست‌نخورده و با همان دو رقم اعشار باقی
  می‌ماند؛ این تغییر فقط نمایشی است.**
- **قیمت‌ها همان‌طور که تایپ می‌شوند گروه‌بندی می‌شوند**، پس لازم نیست کسی «،» را
  دستی بزند. این روی همهٔ فیلدهای مبلغ پنل اعمال شده است، نه فقط محصولات: قیمت
  محصول، قیمت واحد سطر سند، بهای واحد حرکت انبار، مبلغ دریافت، مبلغ تخصیص، مانده
  اول دوره، و کادر «پرداخت‌شده»ی فاکتور.
- **صفحهٔ محصول تازه:** ورودی «بارکد داخلی» جای خود را به **واحد** داد.
- **صفحهٔ جزئیات محصول:** همان جایگزینی، به‌علاوهٔ نمایش واحد.
- **فیلترهای صفحهٔ محصولات** تمام عرض کارت را می‌گیرند و جست‌وجو بزرگ‌ترین سهم را
  دارد؛ پیش از این وسط‌چین به نظر می‌رسیدند.
- **حد حجم بدنهٔ درخواست** حالا دو مقدار دارد: مقدار عمومی (۲۵۶ کیلوبایت،
  اندازه‌گیری‌شده از بزرگ‌ترین سندی که API می‌پذیرد) و یک مقدار جدا و همچنان
  کراندار برای مسیر بارگذاری فایل (۵ مگابایت). بدون این تفکیک، برای اینکه یک
  مسیر فایل بگیرد باید حد همهٔ مسیرها بالا می‌رفت. `nginx/default.conf` هم همین
  تفکیک را دارد و باید با تنظیمات هم‌گام بماند.
- سرویس `create_product` حالا خودش `unit` را اعتبارسنجی می‌کند. پیش از این قید
  پایگاه داده آن را می‌گرفت، اما به شکل `IntegrityError` که این ماژول با پیام
  «کد محصول تکراری است» گزارشش می‌کرد — یعنی نام فیلد اشتباه به کاربر می‌گفت.

### حذف شد

- **فیلتر مرتب‌سازی صفحهٔ محصولات، به‌طور کامل** — همان کنترلی که پنج گزینهٔ
  «نام، کد، کمترین قیمت، بیشترین قیمت، تازه‌ترین» را داشت. فهرست حالا به ترتیب
  نام مدل می‌ماند و پارامتر `ordering` دیگر از این صفحه فرستاده نمی‌شود.
  (نقطهٔ انتهایی API همچنان مرتب‌سازی را می‌پذیرد؛ فقط این کنترل از رابط برداشته
  شده است.)

### رفع شد

- **پدینگ فرم جزئیات محصول.** محتوای فرم داخل `card-body` قرار گرفت؛ پیش از این
  ورودی‌ها به لبهٔ چپ و راست کارت می‌چسبیدند و با بقیهٔ فرم‌های پنل هم‌شکل نبودند.
- قیمت در فهرست محصولات خام چاپ می‌شد (`12500000.00`) در حالی که هر جدول دیگری
  از قالب‌بند مشترک استفاده می‌کرد.
- دو کادر خواندنیِ «قیمت واحد» و «مبلغ کل» در صفحهٔ جزئیات فروش هم خام بودند.

---

## [1.0.0] — 2026-08-20 — **نسخهٔ تحویل‌شده**

نسخه‌ای که به شرکت **TIARA** تحویل داده شد.

### اضافه شد

- دارایی‌های هویتی برند: favicon، مانیفست سایت، لوگو.
- [`docs/ops/DOLPHIN_DEPLOYMENT_RUNBOOK.md`](docs/ops/DOLPHIN_DEPLOYMENT_RUNBOOK.md)
  (named `FOROOSHBIN_DEPLOYMENT_RUNBOOK.md` at the time of this entry, renamed 2026-09-01)
  — تنها سند مرجع نصب، راه‌اندازی، به‌روزرسانی، پشتیبان‌گیری، بازیابی و
  عیب‌یابی. هر دستور آن در برابر خود مخزن بررسی شده است.

### رفع شد

- **استقرار به‌خاطر نام پایگاه داده رد می‌شد.** یک بررسی هویتی که از پاک‌سازی نام
  برند به‌جا مانده بود، استقرار سالمِ صحنه‌آزمایی را با پیام
  `POSTGRES_INIT_USER must use the frooshbin role identity` متوقف می‌کرد. آن
  بررسی از هیچ چیزی محافظت نمی‌کرد و از هر سه لایه برداشته شد.
- ارتفاع لوگوی صفحهٔ ورود که باعث اسکرول شدن صفحه می‌شد.
- یک مسیر دارایی ایستای منسوخ در مستندات (`/static/common/favicon.ico` که به
  `common/brand/` منتقل شده بود).

---

## [0.7.0] — 2026-08-18 — تصمیم‌های محصول و ممیزی

### اضافه شد

- **جامعهٔ هدف کمپین** (`TargetAudienceMember`) با وضعیت مشتق‌شده؛ شمارهٔ تلفن
  هویت عضو است.
- **سرنخ‌ها** واژگان وضعیت گرفتند و مشتری‌شان اختیاری شد.
- **تسویهٔ دستی فاکتور**: یک بازنویسیِ نمایشیِ سطح‌فاکتور، **یک‌طرفه و
  برگشت‌ناپذیر**، بدون هیچ اثر حسابداری. هیچ رکورد پرداخت، تخصیص یا دفتر معینی
  ساخته نمی‌شود و ماندهٔ متعارف مشتری تکان نمی‌خورد.
- **فعال/غیرفعال کردن برگشت‌پذیر** مشتری و محصول به‌دست مدیر پلتفرم؛ هیچ دادهٔ
  تاریخی حذف نمی‌شود.
- همهٔ صفحه‌های سفارش، فاکتور، کمپین و کال‌سنتر روی اثاثیهٔ خود قالب Metronic
  بازنویسی شدند.

### تغییر کرد

- **سفارش مالک چرخهٔ موجودی است**؛ فاکتور هرگز موجودی را جابه‌جا نمی‌کند
  (`BILLING_INVOICE_AFFECTS_STOCK` به‌طور پیش‌فرض خاموش).
- خنثی بودن نسبت به تکرار: پرچم `stock_applied` به‌همراه شمارندهٔ `stock_revision`.

### حذف شد

- **پیش‌فاکتور (Quotation) از پنل برداشته شد** — از سایدبار هم دیده نمی‌شود.
  قابلیت حذف نشد، خاموش شد (`DEFAULT_OFF_FEATURES`)؛ پس داده‌های تاریخی سر جایشان
  می‌مانند و هر استقرار دیگری می‌تواند روشنش کند.

### رفع شد

پنج نقص با شدت بالا از ممیزی کد:

- **نشت کلید خنثی‌سازی تکرار در پرداخت** — کلید به‌تنهایی تطبیق داده می‌شد و
  پرداخت مشتری دیگری را برمی‌گرداند، و در ضمن یک پرداخت واقعی دوم را بی‌صدا
  می‌بلعید.
- **رقابت روی موجودی در PostgreSQL** — `IntegrityError` بدون savepoint گرفته
  می‌شد.
- **ماندهٔ دفتر معین** از `balance_after` جدیدترین سطر خوانده می‌شد، پس سطرهای با
  تاریخ عقب‌افتاده ناپدید می‌شدند. حالا از جمع بدهکار منهای بستانکار حساب می‌شود.
- **کلید نشست از API بیرون می‌رفت** — با ارجاع HMAC جایگزین شد.
- صفحهٔ «دسترسی مجاز نیست» ماژول خودش را اجرا می‌کرد و خطای اجرانشده می‌داد.

---

## [0.6.0] — 2026-08-17 — رابط کاربری روی قالب خریداری‌شده

### تغییر کرد

- کل رابط سرویس‌شده روی قالب **Metronic RTL** (نسخهٔ سایدبار تیره) بازسازی شد.
- فهرست نادیده‌گیریِ `collectstatic` واقعی شد: دارایی‌های جمع‌آوری‌شده از ۱۰۵
  مگابایت به ۱۱ مگابایت رسید.

### رفع شد

- اجرای قالب در ایمیج Docker جا افتاد.
- استقرار بدون دامنه (فقط IP) پشتیبانی شد.
- همهٔ اختیارات (capabilities) از کانتینر برنامه گرفته شد.
- فاکتور چاپی روی کاغذ و روی گوشی خوانا شد.

---

## [0.5.0] — 2026-08-16 — انبار، مالی و هویت ForooshBin

### اضافه شد

- ماژول‌های **انبار** و **مالی** با معنای کراندار؛ **دفتر معین فقط-افزودنی**
  (`CustomerLedgerEntry`).
- گزارش‌های مالی در رابط فارسی.
- چاپ فاکتور و پیش‌فاکتور به PDF روی سرور.
- **تاریخ جلالی** در سراسر رابط؛ ذخیره‌سازی متعارف همچنان میلادی ISO است.

### تغییر کرد

- نام محصولِ رو به مشتری به **ForooshBin / فروش‌بین** تغییر کرد.
- نقش `company_it` مهار شد؛ شکاف‌های نشست و خروجی بسته شد.

---

## [0.4.0] — 2026-08-15 — امنیت و اثبات اجرا

### اضافه شد

- **مانیفست استقرار امضاشده (Ed25519)** به‌عنوان تنها منبع درستیِ فهرست
  قابلیت‌ها؛ در صورت نبود یا نامعتبر بودن، **بسته** می‌شود نه باز.
- اثبات بازیابی نسخهٔ پشتیبان روی PostgreSQL واقعی.

### تغییر کرد

- مدیریت کاربران به مدیر پلتفرم محدود شد.
- در معرض بودن Django Admin مهار شد.
- گیت‌های امنیتی پایگاه داده واقعاً بسته می‌شوند.

---

## [0.3.0] — 2026-08-13 … 2026-08-14

### اضافه شد

- **اسناد فروش** و تاریخچهٔ وضعیت پستی.
- **خدمات پس از فروش** با CRUD، تاریخچه و کنترل دسترسی نقش‌محور.
- **پیامک ورودی** با گزارش و ریزنمایی.
- **دسته‌بندی محصولات**.
- گزارش جزئیات عملکرد کاربر.

---

## [0.2.0] — 2026-08-11 … 2026-08-12

### اضافه شد

- سلسله‌مراتب سایدبار و جای‌گیرِ ماژول‌های بعدی.
- کد پستی و دسته‌بندی در پروندهٔ مشتری.
- فیلتر وضعیت فعال محصول.

### تغییر کرد

- واژگان «مشتری» ↔ «بازاریاب (کال سنتر)» دو بار در کل قالب‌ها و آزمون‌ها اصلاح
  شد تا با معنای مورد نظر مدیر محصول یکی شود.

---

## [0.1.0] — 2026-08-09 … 2026-08-10 — پایه

### اضافه شد

- ساختار پروژهٔ Django/DRF، احراز هویت و مدیریت کاربران.
- مدیریت محصول و فروش (نما، قالب، سریالایزر).
- اسکریپت‌های آمادگی بار و اعتبارسنجی ایمیج انتشار.
