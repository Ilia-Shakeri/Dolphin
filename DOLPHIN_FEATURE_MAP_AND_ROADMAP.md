# نقشهٔ کامل فیچرها و نقشهٔ راه Dolphin (دلفین)

> ## ✅ اشکالات بحرانی ممیزی — بسته شد در `1.3.10`
>
> ممیزی ۲۰۲۶/۰۸/۲۹ دو اشکال پیدا کرد که روی PostgreSQL تولید را می‌شکستند و روی
> SQLite تست سبز می‌ماندند — `issue_invoice()` و `transition_cheque()`، هر دو
> بدون `@transaction.atomic`. هر دو رفع شدند.
>
> **آنچه باید بماند:** گیت
> [`common/tests/test_row_locks_are_transactional.py`](common/tests/test_row_locks_are_transactional.py)
> که این دسته را می‌گیرد. بدون آن، همین اشکال بار سوم برمی‌گردد — یک‌بار در
> `1.2.0` برگشته بود و ۹ نسخه زنده ماند.
>
> سابقه و روش در بخش یافته‌های ممیزی کدبیس، داخل `BACKEND_SPEC.md`.


**تاریخ نگارش:** ۲۰۲۶/۰۸/۲۰ — بر اساس خواندن کامل کد، مدل‌های دیتابیس، مسیرهای API، قالب‌های سرو‌شده، و تمام مستندات پروژه (`BACKEND_SPEC.md` به‌همراه پیوستش، `DOLPHIN_PROJECT_HANDOFF.md`، `docs/ops/DOLPHIN_DEPLOYMENT_RUNBOOK.md`). به‌روزرسانی ۲۰۲۶/۰۹/۰۱ (۱.۶.۱۲): مستندات پراکندهٔ `docs/backend/`، `docs/frontend/` و بقیهٔ `docs/ops/` همه در همان دو فایل یکی شدند؛ ارجاع‌های زیر با مسیر جدید به‌روز شدند.

**هدف این سند:** یک مرجع واحد فارسی که نشان دهد الان دقیقاً چه چیزی داریم، چه چیزی در قالب خریداری‌شدهٔ Metronic آماده است ولی استفاده نکرده‌ایم، برای برندسازی و مشتری بعدی (مثل TIARA) چه کاری لازم است، افزودن حسابداری چقدر کار دارد، و ایدهٔ «مینی‌اپ ساخت پنل با تیک‌زدن فیچر» چقدر واقعی و قابل‌اجراست.

هیچ قاعدهٔ مالی/مالیاتی/حسابداری جدیدی در این سند ابداع نشده؛ هرجا دربارهٔ حسابداری صحبت شده، فقط **شکاف فعلی** توصیف شده، نه یک راه‌حل پیشنهادی با جزئیات قانونی.

---

## فهرست

1. [تصویر کلی: این پنل چیست](#۱-تصویر-کلی-این-پنل-چیست)
2. [فیچرهای پیاده‌شده — ماژول به ماژول](#۲-فیچرهای-پیاده‌شده--ماژول-به-ماژول)
3. [آنچه در قالب Metronic هست ولی در پنل ما نیست](#۳-آنچه-در-قالب-metronic-هست-ولی-در-پنل-ما-نیست)
4. [برندسازی برای TIARA یا هر مشتری بعدی](#۴-برندسازی-برای-tiara-یا-هر-مشتری-بعدی)
5. [برنامهٔ حسابداری — تحلیل صادقانهٔ شکاف](#۵-برنامهٔ-حسابداری--تحلیل-صادقانهٔ-شکاف)
6. [ایدهٔ همکار: مینی‌اپ ساخت پنل با تیک‌زدن فیچر](#۶-ایدهٔ-همکار-مینی‌اپ-ساخت-پنل-با-تیک‌زدن-فیچر)
7. [نقشهٔ راه پیشنهادی برای ادامه](#۷-نقشهٔ-راه-پیشنهادی-برای-ادامه)
8. [جمع‌بندی و اولویت پیشنهادی](#۸-جمع‌بندی-و-اولویت-پیشنهادی)

---

## ۱. تصویر کلی: این پنل چیست

### معماری

- **Django + Django REST Framework + PostgreSQL + Docker Compose + Nginx** — یک مونولیت ماژولار، نه میکروسرویس.
- **مدل استقرار:** «تک‌مستأجر با دیتابیس جدا برای هر مشتری، روی یک کدبیس مشترک.» یعنی هر مشتری (مثل TIARA) یک نصب Docker جدا و یک دیتابیس PostgreSQL جدا روی سرور خودش می‌گیرد؛ کد یکی است. هیچ‌جای کد اسم مشتری خاصی هارد‌کد نشده.
- **رابط کاربری:** روی تم خریداری‌شدهٔ **Metronic (نسخهٔ dark-sidebar، RTL)** ساخته شده. هیچ فریم‌ورک فرانت‌اند جدیدی (React/Vue) اضافه نشده؛ فقط HTML سرو‌شده از Django + یک فایل جاوااسکریپت خام (`common/static/common/dolphin-app.js`, حدود ۴۰۰۰ خط) که هر صفحه را با `fetch` به API وصل می‌کند.
- **کنترل دسترسی سه‌لایه، مستقل از هم:**
  1. **فعال‌بودن ماژول** (آیا این استقرار اصلاً این بخش را دارد) — از یک **manifest امضاشده با Ed25519** خوانده می‌شود، نه از تنظیمات ساده. توضیح کامل در بخش ۴ و ۶.
  2. **نقش کاربر** (`accounts/access.py`) — این نقش اجازهٔ این کار را دارد یا نه.
  3. **دامنهٔ داده** (`selectors.py` هر ماژول) — این کاربر فقط ردیف‌های خودش را می‌بیند یا همهٔ شرکت را.
- غیرفعال‌کردن یک ماژول **هیچ داده‌ای را پاک نمی‌کند** — فقط دسترسی به آن را می‌بندد. این طراحی از اول برای «همان کد، چند مشتری با نیاز متفاوت» ساخته شده.

### نقش‌ها (الان چهار نقش وجود دارد)

| نقش | نام فارسی در پنل | خلاصه |
|---|---|---|
| `sales_agent` | بازاریاب (کال سنتر) | فقط کار خودش: مشتری‌هایی که خودش ساخته، سرنخ‌های تخصیص‌یافته، اسناد بازرگانی خودش |
| `sales_manager` | مدیر فروشگاه | همان دسترسی کارکردی مدیر پلتفرم روی کل داده‌های کسب‌وکار شرکت (مشتری، سرنخ، سفارش، فاکتور، انبار، گزارش)، به‌جز عملیات امنیتی/اداری |
| `company_it` | مدیر فنی مشتری | مشابه مدیر فروشگاه؛ **در حال حاضر برای Client-1 غیرفعال است** (`internal_it_role` در manifest نیست) |
| `platform_admin` | مدیر پلتفرم | تنها نقشی که کاربر می‌سازد/غیرفعال می‌کند، نقش تغییر می‌دهد، و وضعیت فعال/غیرفعال مشتری و محصول را کنترل می‌کند |

هیچ سلسله‌مراتب پویا یا سازندهٔ نقش (role builder) وجود ندارد؛ این چهار نقش، ثابت و در کد تعریف شده‌اند.

---

## ۲. فیچرهای پیاده‌شده — ماژول به ماژول

هر ماژول هم API کامل REST دارد (با `drf-spectacular`، مستندات OpenAPI خودکار) و هم صفحهٔ سرو‌شدهٔ HTML متناظر در پنل. مسیرهای پنل از `common/ui_urls.py` می‌آیند.

### ۲.۱ مدیریت کاربران و نشست‌ها
- ساخت/ویرایش/غیرفعال‌سازی کاربر (فقط مدیر پلتفرم؛ حذف واقعی هرگز امکان‌پذیر نیست).
- **بدون تغییر گذرواژه از هیچ رابطی** — عمداً حذف شده. بازیابی گذرواژهٔ فراموش‌شده فقط با دستور روی سرور (`manage.py changepassword`) ممکن است.
- منوی کاربر در سربرگ (نه سایدبار): نام و نقش، پروفایل، **مدیریت نشست‌های فعال** (دیدن دستگاه‌ها با شناسهٔ مبهم غیرقابل‌بازگشت، پایان‌دادن به یک نشست یا همهٔ نشست‌های دیگر)، خروج.
- کلید نشست هرگز از سرور خارج نمی‌شود؛ فقط یک ارجاع رمزنگاری‌شده نمایش داده می‌شود.

### ۲.۲ مشتریان (CRM)
- ثبت مشتری با نام، کد ملی، ایمیل، استان/شهر/کد پستی، دسته‌بندی متنی، آدرس، یادداشت.
- چند شمارهٔ تلفن به ازای هر مشتری، با نرمال‌سازی خودکار اعداد فارسی/عربی به شکل `+98...` و یکتایی سراسری شمارهٔ فعال.
- فیلتر و جست‌وجو بر اساس نام/تلفن/کدپستی/دسته‌بندی/آدرس/**بازهٔ تاریخ ثبت**.
- وضعیت فعال/غیرفعال — برگشت‌پذیر، فقط مدیر فروشگاه/پلتفرم، هیچ حذف سختی در کار نیست.
- باکس «سفارشات مرتبط» در جزئیات مشتری (ردیف به ردیف: محصول، تعداد، مبلغ).
- بازاریاب فقط مشتریانی را می‌بیند که خودش ساخته و **حق ویرایش ندارد**.

### ۲.۳ سرنخ و کمپین + «جامعهٔ هدف»
- «سرنخ» = یک کمپین بازاریابی، نه لزوماً یک مشتری مشخص (`Lead.customer` اختیاری است).
- **جامعهٔ هدف (`TargetAudienceMember`)** — فهرست افراد یک کمپین، با نام و شماره؛ شماره به‌عنوان هویت با یکتایی سراسری.
- وضعیت هر فرد در جامعهٔ هدف **کاملاً خودکار و مشتق** است، هیچ‌جا با دست تایپ نمی‌شود:
  - ورود → «سرنخ»
  - ثبت تماس در مرکز تماس → «در تعامل»
  - ساخته‌شدن مشتری با همان شماره → «مشتری» (بالاترین اولویت)
- وضعیت خود سرنخ: «تکمیل / در انتظار تکمیل / کنسل شده».
- بازاریاب فقط سرنخ‌های تخصیص‌یافته به خودش را می‌بیند، **نه ساخت نه ویرایش**.
- تاریخچهٔ کامل واگذاری سرنخ (append-only).

### ۲.۴ مرکز تماس (تعاملات)
- ثبت تماس روی یک هویت از جامعهٔ هدف (نه مستقیم روی سرنخ) با نوع تماس (ورودی/خروجی)، نتیجه، زمان، پیگیری بعدی.
- انتخاب هویت با جست‌وجو (`datalist`)، محدود به کمپین‌های خود کاربر برای بازاریاب.
- append-only — تعامل ثبت‌شده ویرایش/حذف نمی‌شود.

### ۲.۵ کاتالوگ محصول
- محصول: SKU یکتا، نام، دسته‌بندی، برند، بارکد یکتا (اختیاری)، قیمت فعلی، توضیحات.
- دسته‌بندی محصول: کد، نام، نام نرمال‌شده (فارسی/عربی یکسان‌سازی می‌شود)، ترتیب نمایش.
- وضعیت فعال/غیرفعال محصول — برگشت‌پذیر، فقط مدیر فروشگاه/پلتفرم؛ محصول غیرفعال دیگر قابل انتخاب در سند تازه نیست ولی خط‌های قدیمی دست‌نخورده می‌مانند.

### ۲.۶ انبار و موجودی
- چند انبار (`Warehouse`)، موجودی هر کالا در هر انبار (`StockItem`) با میانگین موزون بهای تمام‌شده.
- حرکت انبار (`StockMovement`) با انواع مختلف در مدل (موجودی اول دوره، خرید، فروش، برگشت، تعدیل، انتقال بین انبار)؛ **فرم دستی اپراتور فقط سه نوع را ارائه می‌دهد** (موجودی اول دوره، برگشت از مشتری، ارسال به مشتری) — بقیه را خودِ سیستم هنگام تایید سفارش می‌سازد.
- **موجودی منفی هرگز مجاز نیست** — اگر تایید سفارش بیش از موجودی باشد، سفارش خودکار کنسل می‌شود و «موجودی کافی نبود» به یادداشتش اضافه می‌شود.
- **چرخهٔ موجودی، مالِ سفارش است نه فاکتور:** ساخت سفارش (پیش‌نویس) هیچ اثری ندارد؛ تایید سفارش دقیقاً یک‌بار کم می‌کند؛ کنسلی سفارشِ تاییدشده دقیقاً یک‌بار برمی‌گرداند؛ ویرایش سفارشِ تاییدشده فقط تفاضل تعداد را جابه‌جا می‌کند. همهٔ این‌ها با کلید idempotency و شمارندهٔ نسخه، در برابر ارسال دوباره یا مسابقهٔ همزمانی محافظت شده‌اند.

### ۲.۷ اسناد بازرگانی: سفارش و فاکتور
- **پیش‌فاکتور (Quotation) در پنل خاموش است** — تصمیم محصولی: مشتری اول ابتدا فاکتور می‌زند، نه پیش‌فاکتور. مدل، سرویس، API و تست‌ها در کد باقی مانده‌اند و با یک manifest دیگر بدون بازنویسی کد فعال می‌شوند.
- **سفارش:** مشتری، انبار، روش ارسال (پست/پیک)، تاریخ ارسال، اقلام. تاریخ ثبت سرورساز و غیرقابل‌تغییر است. وضعیت (پیش‌نویس/تاییدشده/تحویل‌شده/لغوشده) فقط با نقش‌های مدیریتی تغییر می‌کند.
- **فاکتور:** مستقل از سفارش قابل صدور است (فاکتور اول). پیوند فاکتور↔سفارش یک رابطهٔ واقعی است که **بعد از وجود هر دو سند** برقرار می‌شود؛ یک سفارش می‌تواند چند فاکتور داشته باشد.
- **صدور فاکتور موجودی را کم نمی‌کند** — این تصمیم روشن جلوگیری می‌کند از کسر دوبارهٔ همان کالا (چون سفارش قبلاً کم کرده).
- بهای تمام‌شدهٔ هر ردیف در لحظهٔ صدور اسنپ‌شات می‌شود (برای گزارش سود ناخالص).
- **تسویهٔ دستی فاکتور:** یک باکس «پرداخت شده» که اپراتور تایپ می‌کند؛ اگر دقیقاً برابر مانده باشد، فاکتور **برای همیشه** تسویه‌شده علامت می‌خورد (حتی اگر بعداً عدد کوچک‌تری تایپ شود). این یک override نمایشی است — **هیچ Payment، هیچ سطر دفتر حساب، هیچ تغییری در مبلغ واقعی پرداخت‌شده نمی‌سازد**؛ مانده واقعی همچنان از رکوردهای پرداخت واقعی محاسبه و منتشر می‌شود.

### ۲.۸ صندوق و دریافت
- **پرداخت (`Payment`):** نقد، کارت، حواله، چک. کلید idempotency مشتری‌محور (کلید تکراری با مبلغ/مشتری متفاوت رد می‌شود، هرگز پرداخت کس دیگری برنمی‌گردد).
- **تخصیص پرداخت (`PaymentAllocation`)** به یک یا چند فاکتور.
- **چک (`Cheque`)** با تاریخچهٔ کامل تغییر وضعیت (`ChequeStatusHistory`).
- **قسط‌بندی (`InstallmentPlan` / `Installment`)** — تقسیم یک فاکتور به چند قسط با تاریخ سررسید.
- **دفتر حساب مشتری (`CustomerLedgerEntry`)** — append-only، هیچ ردیفی هرگز ویرایش/حذف نمی‌شود؛ مانده از **جمع همهٔ ردیف‌ها** محاسبه می‌شود (نه فقط آخرین ردیف)، پس پرداخت با تاریخ گذشته یا مانده اول دوره هرگز گم نمی‌شود.

### ۲.۹ اسناد فروش داخلی (پیگیری پستی)
- ثبت مرسولهٔ داخلی مرتبط با مشتری/فروش، با اسنپ‌شات آدرس در لحظهٔ ثبت.
- وضعیت پستی و تاریخچهٔ تغییر وضعیت (append-only) — **بدون اتصال به هیچ ارائه‌دهندهٔ پستی واقعی**؛ این یک ثبت داخلی دستی است، نه رهگیری زندهٔ پستی.

### ۲.۱۰ خدمات پس از فروش
- ثبت درخواست خدمات مرتبط با مشتری/فروش/سند، با موضوع، شرح، وضعیت، اپراتور تخصیص‌یافته.
- تاریخچهٔ append-only رویدادها (ایجاد/تخصیص/تغییر وضعیت/بستن).
- کارشناس با workstream مخصوص `after_sales` فقط پرونده‌های تخصیص‌یافتهٔ خودش را می‌بیند.

### ۲.۱۱ گزارش‌ها
- **عملکرد کاربر** — تعداد مشتری ساخته‌شده، تعداد و مبلغ فروش، به تفکیک کاربر و بازهٔ زمانی؛ خروجی JSON و XLSX.
- **گزارش اسناد فروش داخلی** — گروه‌بندی جغرافیایی و وضعیت پستی.
- **گزارش پیامک ورودی.**
- **مطالبات (Receivables)** — سن‌بندی بدهی مشتریان.
- **سود ناخالص (Profit)** — از روی اسنپ‌شات بهای تمام‌شده؛ فاکتور بدون انبار «اندازه‌گیری‌نشده» گزارش می‌شود، نه رایگان.
- **ارزش‌گذاری موجودی انبار.**
- همهٔ گزارش‌ها **جدولی** هستند؛ نمودار بصری فقط یک نمودار میله‌ای دستی‌ساز در داشبورد وجود دارد (چون amCharts قالب نیاز به CDN خارجی دارد که ممنوع است). جزئیات کامل در بخش ۳.

### ۲.۱۲ رویدادهای سیستم (Audit Log)
- هر عملیات حساس (ساخت/ویرایش/تغییر وضعیت/تسویه/واگذاری و...) با actor، عملیات، شیء هدف، و یک payload امن (بدون گذرواژه، بدون کلید نشست، بدون دادهٔ حساس خام) ثبت می‌شود.
- IP واقعی کاربر فقط وقتی از nginx پذیرفته می‌شود که `AUDIT_TRUSTED_PROXY_CIDRS` درست تنظیم شده باشد.
- مدیر پلتفرم همهٔ رویدادها را می‌بیند؛ مدیر فنی فقط رویدادهای غیر-پلتفرمی.

### ۲.۱۳ پیامک ورودی
- ساختار داده و گزارش آماده است، ولی **هیچ اتصال واقعی به هیچ ارائه‌دهندهٔ پیامک وجود ندارد** (`BLOCKED_EXTERNAL`). فعال‌سازی نیازمند مستندات رسمی provider، اعتبارنامهٔ sandbox، و امضای قرارداد فنی است — نه یک تصمیم کدنویسی داخلی.

### ۲.۱۴ چاپ و PDF
- صفحهٔ چاپ فاکتور مستقل از تم Metronic (بدون سایدبار/دارک‌مود) با اسنپ‌شات ذخیره‌شدهٔ سند.
- تولید PDF سمت سرور **اختیاری** است و فقط با نصب Chromium داخل ایمیج فعال می‌شود؛ در غیر این صورت دکمهٔ «چاپ/ذخیرهٔ PDF» مرورگر جایگزین آن است. با محدودیت همزمانی (یک اسلات رندر) تا یک دانلود همزمان کل سرور را قفل نکند.

### ۲.۱۵ زیرساخت عملیاتی (این هم بخشی از «فیچر» تجاری محصول است)
- استقرار کامل Docker Compose: PostgreSQL + Django/Gunicorn + Nginx، با نقش‌های جدا در دیتابیس (init/migration/app/backup) و مجوز دقیق هر جدول.
- پشتیبان‌گیری خودکار با چک‌سام، بازیابی آزمایشی در دیتابیس یک‌بارمصرف (هرگز مستقیم روی دیتابیس زنده).
- HTTPS اجباری، HSTS، rate-limit روی لاگین، محدودیت اندازهٔ درخواست، حالت write-stop برای آپدیت امن.
- راهنمای کامل نصب/راه‌اندازی/آپدیت/rollback/troubleshooting در `docs/ops/DOLPHIN_DEPLOYMENT_RUNBOOK.md`.

---

## ۳. آنچه در قالب Metronic هست ولی در پنل ما نیست

قالب خریداری‌شده (پوشه‌های `dashboards/`, `apps/`, `widgets/`, `pages/`, `layouts/`) خیلی بیشتر از آنچه استفاده کرده‌ایم دارد. این‌ها **قبلاً خریداری و پرداخت شده‌اند** — افزودنشان فقط کار طراحی/توسعه است، نه هزینهٔ لایسنس تازه.

### ۳.۱ نمودار (بیشترین اولویت، چون خودتان اشاره کردید)

**خبر فنی مهم:** کتابخانهٔ **ApexCharts از قبل داخل `assets/plugins/global/plugins.bundle.js` باندل شده** — نیازی به دانلود یا CDN خارجی نیست. دلیل استفاده‌نکردن از آن این بوده که این باندل ۳٫۵ مگابایتی همراه با کد jQuery/Bootstrap است که پنل فعلی به آن نیاز ندارد (فقط از `KTMenu`/`KTDrawer` در `scripts.bundle.js` سبک‌تر استفاده شده).

سه راه برای افزودن نمودار واقعی:
1. **لود کامل `plugins.bundle.js`** — ساده‌ترین راه، هزینه‌اش ۳٫۵MB اضافه‌بار روی هر صفحه.
2. **استخراج فقط بخش ApexCharts** از آن باندل به یک فایل جدا (کار فنی متوسط، بدون افزایش سنگین حجم).
3. نصب مستقیم بستهٔ ApexCharts به‌صورت جدا (نیاز به بررسی لایسنس دوباره ندارد چون خودِ نسخهٔ باندل‌شده در تم موجود است).

نمودارهای آمادهٔ نمونه در تم: `dashboards/finance-performance.html`, `dashboards/store-analytics.html`, `dashboards/call-center.html`, `widgets/charts.html` — می‌توانند الگوی مستقیم نمودار فروش ماهانه، مقایسهٔ عملکرد بازاریاب‌ها، روند مطالبات، و ترکیب فروش بر اساس دسته‌بندی باشند.

### ۳.۲ تقویم / کلندر
`apps/calendar.html` — تقویم کامل با drag/drop رویداد. کاربرد بالقوه: **تقویم پیگیری سرنخ‌ها و قرارهای خدمات پس از فروش** به‌جای فقط یک فیلد تاریخ در جدول.

### ۳.۳ چت داخلی
`apps/chat/` (private, group, drawer) — گفت‌وگوی داخلی بین کارمندان. کاربرد بالقوه: هماهنگی بین بازاریاب و مدیر روی یک سرنخ خاص، بدون نیاز به ابزار پیام‌رسان جدا.

### ۳.۴ فایل منیجر / پیوست فایل
`apps/file-manager/` — امکان آپلود و مدیریت فایل. **این دقیقاً همان چیزی است که در بخش «Open business decisions» I داخل `BACKEND_SPEC.md` به‌عنوان تصمیم باز ثبت شده:** آیا مشتری/سرنخ/فاکتور باید بتواند فایل پیوست داشته باشد؟ اگر بله، چه نوع فایل، چه سقف حجمی، چه مدت نگهداری. **هیچ مدل دیتابیسی برای پیوست فایل هنوز وجود ندارد.**

### ۳.۵ مرکز پشتیبانی (Helpdesk) کامل‌تر
`apps/support-center/tickets/` — تیکتینگ با اولویت، SLA، و تاریخچهٔ گفت‌وگو. ماژول «خدمات پس از فروش» فعلی ما ساده‌تر است (فقط موضوع/شرح/وضعیت/تخصیص). این می‌تواند الگوی توسعهٔ آیندهٔ آن باشد.

### ۳.۶ Kanban / مدیریت پروژه
`apps/projects/` — می‌تواند الگوی یک **بورد کانبان برای پیگیری سرنخ‌ها بر اساس مرحله** باشد (مثلاً «تماس اول → علاقه‌مند → مذاکره → بسته‌شده») به‌جای فقط جدول لیستی فعلی.

### ۳.۷ صفحات عمومی / بازاریابی
`pages/pricing`, `pages/about.html`, `pages/faq/` — اگر روزی بخواهید یک **صفحهٔ فرود عمومی** برای معرفی محصول (نه پنل داخلی) بسازید، این‌ها الگوی آماده‌اند. این‌ها کاملاً جدا از پنل CRM هستند و ربطی به مشتری‌های نصب‌شده ندارند.

### ۳.۸ موارد کم‌کاربرد برای این محصول
`apps/subscriptions`, `dashboards/crypto.html`, `dashboards/school.html`, `dashboards/podcast.html`, `dashboards/online-courses.html` — این‌ها دموی صنایع دیگر هستند و برای یک CRM فروش/انبار کاربرد مستقیم ندارند؛ صرفاً به‌عنوان الگوی بصری (کارت، جدول، فرم) ممکن است الهام‌بخش باشند.

---

## ۴. برندسازی برای TIARA یا هر مشتری بعدی

### چیزی که از قبل حل شده (چون از اول برای همین طراحی شد)

- **کد یک ذره هم به «Dolphin» یا «Client-1» وابسته نیست** جز در متن نمایشی/برندی؛ منطق تجاری کاملاً عمومی است.
- هر مشتری از قبل **دیتابیس، رمزها، و manifest جدای خودش** را می‌گیرد (`docs/ops/DOLPHIN_DEPLOYMENT_RUNBOOK.md`، بخش ۱).
- فعال/غیرفعال‌بودن هر ماژول (سفارش، فاکتور، انبار، پیامک، پس از فروش، ...) از قبل **بدون تغییر کد** با یک manifest امضاشدهٔ جدا برای TIARA قابل‌تعیین است.
- نام دیتابیس/نقش‌ها هم از قبل انتخابی است؛ چیزی به نام «kariz» یا «forooshbin» یا «dolphin» در دیتابیس اجباری نیست (تازه در ۲۰۲۶/۰۸/۱۹ این محدودیت عمداً برداشته شد).

### چیزی که واقعاً باید عوض شود

| مورد | فایل/محل | کار لازم |
|---|---|---|
| لوگو و نام برند در پنل | `common/static/common/brand/*`, `common/templates/common/base.html`, `login.html` | جایگزینی فایل‌های تصویر + یکی‌دو رشتهٔ متن قالب |
| رنگ‌بندی/تم بصری | `common/static/common/dolphin.css` (لایهٔ نازک روی Metronic) | تغییر متغیرهای رنگ برند در همین فایل کوچک، بدون دست‌زدن به خودِ Metronic |
| عنوان صفحه/متادیتا/favicon | `base.html`, `common/static/common/brand/site.webmanifest` | جایگزینی فایل‌ها |
| نام دامنه/گواهی TLS | `.env` مشتری جدید | مطابق بخش ۶ و ۷ راهنمای استقرار |
| متن‌های فارسی ثابت پنل (اگر TIARA اسم متفاوتی برای «سفارش»، «فاکتور» و… بخواهد) | قالب‌های `common/templates/common/**` | این یک **تصمیم محصولی جدید** است، نه صرفاً برندینگ — باید دقیقاً مثل تصمیم‌های Client-1 مستند شود |

### چیزی که نباید عوض شود

- کد Django/سرویس‌ها/مدل‌ها — تغییرشان برای برند جدید نه لازم است، نه توصیه می‌شود.
- ساختار Metronic (`KTMenu`, `KTDrawer`, کلاس‌های `data-kt-*`) — این‌ها بخشی از قرارداد لایسنس تم هستند، نه چیزی که با برند عوض شود.

### دربارهٔ «برند کردن به اسم خودمان»

اگر منظور این است که Dolphin دیگر برند «مشتری» نباشد بلکه **محصول شرکت شما** باشد که به مشتری‌های مختلف (TIARA و بعدی‌ها) فروخته می‌شود — این دقیقاً همان معماری «یک کد، چند استقرار» است که از قبل وجود دارد. تنها کاری که لازم است: هر مشتری جدید = یک پوشهٔ `secrets/.env` جدید + یک manifest امضاشدهٔ جدید + رنگ/لوگوی متفاوت. **این نیازمند بازنویسی معماری نیست.**

---

## ۵. برنامهٔ حسابداری — تحلیل صادقانهٔ شکاف

**آنچه داریم:** فقط یک **دفتر معین مشتری (Accounts Receivable sub-ledger)** — `CustomerLedgerEntry`. این append-only است، بدهکار/بستانکار دارد، و مانده از جمع تمام ردیف‌ها محاسبه می‌شود. این **فقط طرف «مشتری به ما بدهکار است چقدر»** را پوشش می‌دهد.

**آنچه نداریم (و باید صادقانه گفته شود قبل از هر وعده به مشتری):**

- **دفتر کل (General Ledger)** و **کدینگ حساب‌ها (Chart of Accounts)** — هیچ مدلی برای حساب‌های دارایی/بدهی/درآمد/هزینه/سرمایه وجود ندارد.
- **سند حسابداری دوطرفه (Double-entry Journal Entry)** — سیستم فعلی فقط یک ردیف بدهکار/بستانکار در دفتر مشتری می‌نویسد، نه یک آرتیکل کامل حسابداری.
- **ماژول هزینه‌ها (Expenses)** — چیزی برای ثبت هزینه‌های شرکت (اجاره، حقوق، ...) نیست؛ گزارش سود ناخالص فقط «فروش منهای بهای تمام‌شدهٔ کالا» است، نه سود خالص واقعی.
- **مالیات/فاکتور رسمی مالیاتی** — نرخ مالیات در تنظیمات هست ولی **هیچ ادعای انطباق قانونی مالیاتی ایران وجود ندارد**؛ این یک تصمیم قانونی/حسابداری جداست که در بخش «Open business decisions» D داخل `BACKEND_SPEC.md` هنوز باز مانده.
- **ترازنامه، صورت سود و زیان، صورت جریان وجه نقد** — هیچ‌کدام وجود ندارند.
- **تطبیق بانکی (Bank Reconciliation)** — وجود ندارد.

**نتیجه:** «افزودن حسابداری» یک فیچر کوچک نیست؛ عملاً یک **ماژول مستقل بزرگ** است که باید مثل یک پروژهٔ جدا با تصمیم‌های دقیق حسابداری/مالیاتی (نه حدسی) طراحی شود — دقیقاً به همان روشی که مدل فعلی این پروژه با «سؤال دقیق از مالک محصول قبل از کدنویسی» پیش رفته. پیشنهاد: وقتی به این مرحله رسیدید، یک سند مجزای «الزامات حسابداری» مثل `OPEN_BUSINESS_DECISIONS.md` باز شود.

---

## ۶. ایدهٔ همکار: مینی‌اپ ساخت پنل با تیک‌زدن فیچر

### ارزیابی کوتاه

**ایده معتبر و قابل‌اجراست، و نکتهٔ خوب این است که نصف زیرساختش از قبل ساخته شده.** سیستم فعلی «Deployment Profile (Option C)» دقیقاً همین مسئله را حل می‌کند: کدام ماژول‌ها برای این استقرار روشن باشند.

### چیزی که از قبل داریم

- فهرست دقیق فیچرهای قابل‌روشن/خاموش‌کردن، با وابستگی‌های واقعی بین آن‌ها (`common/deployment/registry.py`):
  `customers`, `products`, `leads` (شامل تعاملات), `sales`, `sales_documents`, `after_sales`, `inbound_sms`, `inventory`, `quotations`, `orders`, `invoices`, `payments`, `customer_ledger`, `reports`, `audit_log`, `internal_it_role`.
- یک اسکریپت خط‌فرمان که همین حالا manifest امضاشده می‌سازد: `scripts/sign_deployment_manifest.py --profile-id <id> --feature <name> ... --output manifest.json`.
- Django این manifest امضاشده را وقتی بالا می‌آید تایید می‌کند (Ed25519) و اگر جعلی/دستکاری‌شده باشد، **اصلاً بالا نمی‌آید** — یعنی امنیت این تنظیمات از قبل جدی گرفته شده.
- سایدبار و مسیرهای API خودشان از روی همین فهرست، صفحات غیرفعال را کاملاً پنهان می‌کنند (نه فقط مخفی بصری — واقعاً ۴۰۴ می‌دهند).

### چیزی که کم داریم برای ساختن خودِ «مینی‌اپ»

1. **یک رابط گرافیکی برای تیک‌زدن** — الان این کار با آرگومان خط‌فرمان (`--feature`) انجام می‌شود، نه با کلیک.
2. **تولید خودکار `.env` و راه‌اندازی Docker** — الان طبق راهنمای `docs/ops/DOLPHIN_DEPLOYMENT_RUNBOOK.md` دستی انجام می‌شود.
3. **رنگ/لوگوی سفارشی به‌صورت پارامتری** — الان یک فایل CSS ثابت است، نه یک تنظیم قابل‌انتخاب از فرم.
4. **مدیریت چند مشتری از یک پنل مرکزی** (مثلاً دیدن وضعیت سلامت همهٔ استقرارهای TIARA/Client-2/... از یک‌جا) — این اصلاً وجود ندارد و می‌تواند خودش یک محصول جدا باشد.

### سه سطح پیشنهادی برای این ایده (از ساده به پیشرفته)

**سطح ۱ — فرم ساخت manifest (کم‌ریسک، سریع):**
یک فرم وب کوچک (حتی جدا از پنل اصلی) که تیک‌های فیچر را می‌گیرد و به‌جای اجرای دستی اسکریپت، همان `sign_deployment_manifest.py` را در پس‌زمینه صدا می‌زند و فایل manifest نهایی را برای دانلود می‌دهد. **کلید خصوصی امضا هرگز نباید وارد این فرم شود مگر روی ماشین مالک پلتفرم** — این خط قرمز امنیتی همان چیزی است که همین الان هم در `docs/ops/DOLPHIN_DEPLOYMENT_RUNBOOK.md` روی آن تاکید شده.

**سطح ۲ — تولید `.env` هم اضافه شود:** فرم بالا به‌همراه گرفتن نام دامنه/IP، رنگ برند، و تولید خودکار پیش‌نویس `.env` بر پایهٔ `.env.example`.

**سطح ۳ — راه‌اندازی خودکار روی سرور (پرریسک‌تر):** این فرم مستقیماً روی سرور مشتری SSH بزند و مراحل راهنمای استقرار را اجرا کند. **این سطح نیازمند بررسی امنیتی جدی است** (دسترسی SSH خودکار به سرور مشتری یک ریسک واقعی است) و پیشنهاد می‌شود فقط بعد از اینکه سطح ۱ و ۲ چند بار دستی امتحان و پایدار شدند دنبال شود.

### جمع‌بندی این بخش

این ایده را رد نکنید — **پایه‌اش را از قبل دارید**. شروع منطقی، سطح ۱ است: یک ابزار داخلی کوچک (نه حتی لزوماً وب) که رابط کاربرپسندی روی همان اسکریپت امضای manifest بسازد.

---

## ۷. نقشهٔ راه پیشنهادی برای ادامه

این فازبندی بر اساس **ارزش کسب‌وکاری و ریسک**، نه ترتیب اجباری. هرکدام مستقل قابل‌شروع است.

### فاز الف — پولیش و برندسازی TIARA (کوتاه‌مدت)
- جایگزینی لوگو/رنگ/نام برند طبق بخش ۴.
- تصمیم دربارهٔ اینکه آیا واژه‌های فارسی ثابت پنل (سفارش، فاکتور، ...) برای TIARA همان می‌مانند یا تغییر می‌کنند.
- یک بار کامل راهنمای استقرار (`DOLPHIN_DEPLOYMENT_RUNBOOK.md`) روی سرور واقعی TIARA اجرا و تأیید شود.

### فاز ب — نمودار و گزارش بصری (ارزش دیداری بالا، ریسک کم)
- افزودن ApexCharts (بخش ۳.۱) به داشبورد و گزارش عملکرد/مطالبات/سود.
- این بیشترین «حس محصول تجاری» را با کمترین ریسک منطق کسب‌وکار می‌دهد.

### فاز ج — پیوست فایل (نیاز به تصمیم محصولی، سپس کد)
- ابتدا سؤالات بخش I در `OPEN_BUSINESS_DECISIONS.md` پاسخ داده شود (چه نوع فایل، سقف حجم، نگهداری).
- سپس مدل و API پیوست برای مشتری/سرنخ/فاکتور/پس‌ازفروش.

### فاز د — پیامک واقعی (نیازمند قرارداد provider)
- فقط با مدارک رسمی provider، اعتبارنامهٔ sandbox، و تصمیم قانونی نگهداری داده قابل‌شروع (بخش J در `OPEN_BUSINESS_DECISIONS.md`).

### فاز ه — تقویم پیگیری (بخش ۳.۲)
- تبدیل فیلدهای «پیگیری بعدی» پراکنده به یک نمای تقویمی واحد برای سرنخ و خدمات پس از فروش.

### فاز و — مینی‌اپ ساخت پنل (بخش ۶، سطح ۱)
- فرم داخلی ساخت manifest، مستقل از پنل مشتری.

### فاز ز — حسابداری (بزرگ‌ترین پروژه، جداگانه برنامه‌ریزی شود)
- ابتدا یک سند «الزامات حسابداری» مثل بخش ۵ نوشته و با مشاور مالی/حسابدار تأیید شود.
- این فاز را با فازهای بالا قاطی نکنید؛ حجم تصمیم‌گیری آن به‌تنهایی به‌اندازهٔ کل پروژهٔ فعلی است.

---

## ۸. جمع‌بندی و اولویت پیشنهادی

اگر بخواهم یک ترتیب عملی و کم‌ریسک پیشنهاد بدهم:

1. **اول:** برندسازی TIARA (فاز الف) — چون تحویل شده و باید حرفه‌ای دیده شود.
2. **دوم:** نمودار (فاز ب) — چون قالب از قبل دارد و فقط باید متصل شود؛ سریع‌ترین راه برای «محصول جدی‌تر به‌نظر رسیدن».
3. **سوم:** بررسی سطح ۱ مینی‌اپ ساخت پنل (فاز و) — چون سرمایه‌گذاری کمی می‌خواهد و مستقیماً روی مقیاس‌پذیری فروش به مشتری‌های بعدی اثر می‌گذارد.
4. **بقیه (پیوست فایل، پیامک، تقویم، حسابداری):** هرکدام وقتی لازم شد و بعد از پاسخ‌دادن به سؤالات محصولی مربوطه‌شان، جداگانه شروع شوند.

هیچ‌کدام از این‌ها امروز پیاده‌سازی نشده‌اند؛ این سند فقط نقشه و تحلیل است.
